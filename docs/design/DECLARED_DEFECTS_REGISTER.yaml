# DECLARED DEFECTS REGISTER -- the machine-readable rung 4 of the WORK-SOURCE HIERARCHY
# (director ruling 2026-07-23, DIRECTOR_RULING_WORK_IS_THE_DEFAULT + NIGHT_ENFORCEMENT).
#
# WHY THIS FILE EXISTS: a declared fidelity defect that is NOT in the drawable set is a
# contradiction (ruling rung 4). Before this register the spike-tail defect was "declared top
# priority, untouched five days" -- declared only in FRAME prose the draw could not see, so
# `authorized_set_enumeration` read all-empty -> REST-LEGITIMATE while a top-priority defect sat
# open. This file makes every declared defect DRAWABLE by construction: the draw
# (`supervisor.py::_declared_defect_backlog_draw`) walks it as rung 4, above the PRIORITIES-prose
# backlog / propose-half / forward-discovery / HARDEN floor.
#
# INVARIANT (R15-enforced, tests/background/test_defect_backlog_draw.py):
#   - any defect with status != `closed` is DRAWABLE -> rest is illegitimate while one exists.
#   - a defect is `closed` ONLY when its fidelity gap is measured-closed (the ledger figure moves),
#     NOT when a plan (propose-doc) is written. A plan is `plan_doc`; the defect stays open until the
#     sim/company fix lands and the gap is re-measured. (Otherwise the loop would go idle beside a
#     still-open gap the instant a plan existed -- the exact failure this rung fixes.)
#
# SCHEMA per defect:
#   id            stable slug
#   title         one line
#   declared_at   ISO date it was declared a defect
#   source        where it was declared (doc / ledger / director ruling)
#   priority      1 = highest (drawn first); ties broken by declared_at (oldest first)
#   status        open | closed   (open = drawable; closed = gap measured-closed, left the set)
#   evidence      the grounded figure(s) -- belief vs truth, so the gap is real not asserted
#   plan_doc      the minted propose-doc (rung 4 mint), or null if not yet planned
#   closes_when   the measurable condition that moves it to `closed`

defects:
  - id: SPIKE_TAIL_SSP_RESIDUAL
    title: 'SSP spike tail: the residual position settled at System Sell Price is not shaped like reality'
    declared_at: '2026-07-18'
    source: >-
      docs/design/SCENARIO_SPINE_AND_TRADING_FRICTION_FRAME.md (per G4 fidelity ledger);
      re-declared top-priority + "untouched five days" in
      DIRECTOR_RULING_WORK_IS_THE_DEFAULT_2026-07-23 and NIGHT_ENFORCEMENT_2026-07-23.
    priority: 1
    status: closed
    closed_at: '2026-07-24'
    closed_by: >-
      Intraday SP-level SSP shape landed on the forward settlement path (sim/scenario/intraday_shape.py,
      wired into simulation/run_scenario.py::_expand_daily_to_hh). A MEAN-PRESERVING within-day profile
      (diurnal + tightness-keyed scarcity spike + oversupply trough) replaces the flat 48-period expansion.
      BOTH closes_when conditions proven this commit:
      (T1) tests/sim/test_forward_intraday_shape.py::test_forward_intraday_reaches_scarcity_regime_and_preserves_daily_mean
      -- population HH exceedance now reaches the scarcity regime calibrated to spike_tail_real_target.json
      (frac_gt_1000 ~0.96x, frac_gt_2000 ~1.01x, frac_gt_3000 ~1.10x of real; max ~GBP4,100 vs the old
      structural ceiling ~GBP683) WHILE the daily mean is preserved (max abs err < 1e-3, so the daily-mean
      calibration vs spike_tail_real_target_daily.json is untouched -- R12 sibling-trap avoided).
      (T3) tests/sim/test_residual_bites_intraday.py -- with a fixed block hedge + peak-weighted consumption,
      the company's residual-at-SSP cost tail moves >5x at the worst period and extends past the entire flat
      world; mutation guard (neutralise the shape) shows the move vanishes (R15 both ways). The old flat-today
      tripwire is FLIPPED to a within-day-shape tolerance check in the SAME commit (register discipline).
      R13 seam honoured: the shape is calibrated to REAL SSP (fidelity, blind to P&L); NO per-scenario
      severity dial was added (severity is expressed via the director-owned daily generator price level).
    evidence: >-
      Imbalance-price (SSP) distribution understated in the tail: model max GBP 574/MWh vs real
      GBP 4,038/MWh; negative-price fraction 0.013% (model) vs 2.241% (real), per G4's ledger.
      SSP peak confirmed against Elexon Insights API: real max GBP 4,000.00/MWh exactly, 8 Jan 2021
      SP39. Consequence: the residual position settled at SSP -- where the spike tail becomes
      commercially real (hedged blocks vs consumed half-hourly shape) -- cannot bite the company as
      it bit real suppliers in 2021-22.
      STEP-2 CORRECTION (2026-07-23, code-traced): the GBP574 figure was measured from
      sim/price_engine.py::synthetic_price, which is GATED OFF and never settles the residual (it is a
      conditional-MEAN merit-order model; its tail is thin by construction and reshaping its A0/A1/A2
      would be an R12 number-tune). The residual actually settles against REAL Elexon SSP historically
      (tail already real) and against sim/scenario/bimodal_generator.py in forward worlds -- which tops
      out ~GBP683 and never reaches the >GBP1,000 scarcity regime because its documented crisis-spike
      overlay is UNIMPLEMENTED. The fix is a physics-shaped crisis-spike overlay on the FORWARD scenario
      generator, calibrated to spike_tail_real_target.json (machinery = fidelity; per-scenario severity =
      director-reserved curriculum, R13). Live tripwire on the real path: tests/sim/test_scenario_forward_tail.py.
      STEP-3 DIAGNOSIS (2026-07-23, code-traced -- redirects the fix, gap unchanged): run_scenario.py::
      _expand_daily_to_hh writes each daily generator price to all 48 half-hours identically, so the forward
      residual settles at DAILY granularity with NO intraday shape. The real DAILY-MEAN SSP max is GBP960 (never
      >GBP1,000) with 0.18% negative days; the GBP4,038 / 2.24% figures are HALF-HOURLY (a single SP), so grading
      the daily generator against them is a category error and a daily price tuned toward GBP4,038 would be LESS
      real (an R12 sibling-trap). The real bite is INTRADAY (flat block hedge vs spiky HH SSP), structurally
      absent while every HH of a forward day is equal. Corrected fix = intraday SP-level shape in the forward
      path (bigger structural change, supervised build), calibrated to the new daily target. Emitted this tick:
      docs/design/spike_tail_real_target_daily.json (daily_mean + daily_max), R15-proven in test_ssp_tail_target.py.
      STEP-3 CONTROL (2026-07-23 tick): the failable tripwire now exists on the CORRECT (intraday) locus --
      tests/sim/test_forward_intraday_shape.py asserts run_scenario.py::_expand_daily_to_hh currently flat-
      expands one daily price across all 48 SPs (zero intraday spread), so the forward residual carries NO
      within-day shape. GREEN now; FAILS when the intraday-shape overlay lands. R15-proven both ways
      (measurement fires on a spiky day; FAIL-CLOSED on empty) + a structural assertion on the expansion
      itself. The physics change (intraday SP-level shape) stays a supervised build; this tick landed only
      the tests-first control the redirected fix needs. Register stays open -- the gap is not measured-closed.
      STEP-4 CLOSURE (2026-07-24 tick, physics landed per director ruling WORK_IS_THE_DEFAULT "ADVANCE the
      fix per that plan"): the intraday SP-level shape overlay is implemented and both closes_when conditions
      are measured-shut -- see closed_by above. The redirected fix is complete: forward worlds now carry the
      block-hedge-vs-spiky-half-hourly-SSP mismatch that bit real suppliers in 2021-22, calibrated to the real
      HH exceedance curve, with the daily mean untouched. Four control-only ticks preceded this; the physics
      change itself landed here.
    plan_doc: docs/design/proposals/PROPOSE_SPIKE_TAIL_ATTACK_PLAN.md
    closes_when: >-
      the FORWARD residual carries a real INTRADAY (SP-level) SSP shape -- run_scenario.py::_expand_daily_to_hh
      (or the generator feeding it) produces a within-day profile whose worst periods reach the scarcity regime
      calibrated to the half-hourly exceedance curve in docs/design/spike_tail_real_target.json while the daily
      mean stays near docs/design/spike_tail_real_target_daily.json -- i.e. tests/sim/test_forward_intraday_shape.py
      flips from flat-is-real-today to a within-day-shape tolerance check -- AND the company's residual-at-SSP
      exposure is shown to move under the corrected intraday tail (T3). (NOTE: the earlier daily-generator
      closes_when via test_scenario_forward_tail.py is superseded -- the step-3 diagnosis established the daily
      path is the wrong comparand; the daily-mean max is GBP960 and should NOT reach the real HH tail.)
