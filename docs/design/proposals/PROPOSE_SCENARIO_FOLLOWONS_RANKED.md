# [PROPOSE-THEN-PROCEED] Scenario-spine follow-ons — the five registered candidates as ONE ranked queue

**Minted:** 2026-07-23, seed ordered by DIRECTOR_RULING_WORK_IS_THE_DEFAULT §"seed the backlog NOW"
+ NIGHT_ENFORCEMENT §2. This is the **rung 5 data source** (registered follow-ons as a drawable queue,
not a memorial). Provenance: `docs/design/SCENARIO_SPINE_AND_TRADING_FRICTION_FRAME.md` §C.
**Window:** propose-then-proceed; SIM machinery reversible, curriculum VALUES director-gated (R13). Levels/deps are proposals.

## Why this doc exists

The scenario steer registered five follow-ons and the FRAME's own words were "*follow-ons registered not built*."
A registered follow-on is a **queue, not a memorial** (ruling rung 5). This doc ranks them into one drawable list
so the draw (once rung 5 is wired) — and a human reading it tonight — can see the next real build without a doorbell.
Ranking is a **diagnostic and tie-breaker, never a target** (LAW A); exit tests remain the only gate.

## The ranked queue (highest build-value first — compounding return, R "compounding work first")

| rank | id (proposed) | title | closes | depends_on | why here |
|---|---|---|---|---|---|
| 1 | `SPINE_1_scenario_world_state` | `ScenarioSpine` object + curriculum registry + wall + byte-identical baseline | integration seam for world rotation | `sim/scenario/` | The BUILD of §A — the seam everything else rides. Unblocks 2–5. Reversible; W1/W2 walls proven first. |
| 2 | `SPINE_3_gas_storage_crisis_regime` | Formed European storage stock-and-flow + gas-balance crisis regime (level+vol+corr jointly, persistent); formed seasonal spread + 2022 inversion | **F2 + F4** | `SPINE_1`, BC-1 | The load-bearing Spec-004 gap; the world that makes the spike tail *structural*, not sampled. Highest fidelity delta. |
| 3 | `SPINE_4_living_curve_and_spread_surface` | Forward curve as a living object with stored history; risk-premium dynamics (widen-after-crisis / compress-in-calm); liquidity horizon; full regime-widening spread SURFACE | **F3** | `SPINE_3`, `WVC_2` | The object the cover fan accrues against; supersedes the static spread table. Needs SPINE_3's regime first. |
| 4 | `SPINE_5_tail_machinery` | Stochastic outage process; interconnector/French channel; renewables-surplus negative-price floor | **F5** | `SPINE_3` | Ranked below 2–3: **in-window the real Elexon record already supplies tails** — this matters once rotation LEAVES history. Directly serves the `SPIKE_TAIL_SSP_RESIDUAL` defect. |
| 5 | `SPINE_2_launch_worlds` | The three proposed worlds' ratified parameter VALUES | — | `SPINE_1`, **director ratification (R13)** | Values are director-gated, not agent-set — cannot be *built*, only *proposed then ratified*. Sits last because it is a values-call, not a build. |

**Breadcrumbs (registered, not atoms):** carbon limb of the spark spread (engine R10, may be wired earlier if
trivially safe); collateral/margin calls (already in the working-capital layer; bites in `crisis_2021_22`);
network/ancillary/locational costs (PARKED, one breadcrumb); the governing-rationale map-hygiene sentence.

## Coupling notes

- **Rank 4 (`SPINE_5`) and the `SPIKE_TAIL_SSP_RESIDUAL` defect are the same physics** from two directions:
  the defect fixes the in-window tail against the real record; SPINE_5 forms the tail once rotation leaves
  history. The defect (rung 4) is drawn first — it is a *declared* gap with a live figure; SPINE_5 follows.
- **Rank 5 (`SPINE_2`) is director-reserved** (curriculum VALUES, R13) — it is proposed here, never agent-set.

**Risk & proportionality:** a ranking of already-registered candidates; no build, no map write (orchestrator is
sole map writer per THREE_LANES until H9). Tag: **proceed as the rung-5 queue; each build faces its own tests.**
