# [ADVISOR-NOTE] — CCM provenance + the ghost-pusher incident (v4, FINAL, 2026-08-07)

**Type:** [PROVENANCE + INCIDENT RECORD]. Supersedes v1–v3.1 of this note entirely, including their partial corrections. Written once, after the mechanism was reproduced, fixed, and re-verified. Everything below traces to a commit, a run, or the director's word.

## Who CCM was
Eleven director-launched Claude Code cloud sessions over 2026-08-05→07, each advisor-briefed with a fenced, self-contained task, delivering by PR (or issue, once, under fire). Provenance rides on the `[CCM]` commit prefix and PR/issue numbers. CCM is not the worker seat and never consumed docs/staging.

## What shipped (all advisor-verified, all merged by the director)
PR #4 registry validator · #5 simplifications store (map 1.7MB→249KB, first Birth Certificate) · #6 epistemic-wall ratchet, perimeter-widened ({company,saas}↔{sim,simulation}, 2 forbidden-direction edges named) · #7 hook seat-guard · #8 ruff+mypy ratchets (resident-platform-pinned after two amendments) · #9 + issue #11 money-core characterization ×2 (findings note holds both passes) · #10 invoice/SQLite characterization · #12 daemon seat-guard · #13 the ghost-pusher kill. Plus, same week, sibling-advisor addendum `ARCHITECTED_OUT_EDGE_INTEGRITY` (06:44 chat, **director-confirmed**).

## THE INCIDENT — final mechanism
`tests/background/test_process_run_complete.py` contained imperfectly-isolated tests that invoked `_refresh_published_liveness_on_skip("abc")` against the REAL tree. That function commits the liveness files by pathspec and runs a hardcoded **`git push origin HEAD:main`** (`process_run_complete.py:1484`). The push succeeded wherever the host lent git credentials — every cloud session's proxied git — and failed only in credential-less clones. Every unexplained main push of the week sits inside a session's test-suite window: Tue 17:38, Wed 20:51 (which fast-forwarded main past the pass-2 branch, producing issue #11's 422 — its "resident worker's liveness daemon sharing this working tree" was this test-spawned subprocess), Thu 06:47, Thu 07:53. Advisor reproduction: local ghost commit `05ec0eb` manufactured at HEAD in a credential-less clone, then the same tree produced nothing under the fix. Skynet was dark throughout; the poesys environment has no init script; the July Routine never ran.

## Corrections log, consolidated (rule: no attribution ahead of its decisive check)
Wrongly accused in sequence, each retracted by a named check: repo hooks as pushers (hooks did fire in sandboxes and are now guarded, but pushes never came that way) · GitHub workflows, twice (none scheduled; author strings configurable) · "Skynet is alive" (per-agent heartbeats frozen at 08-03; PC on ≠ stack up) · environment-boot (`has_init_script:false`) · the Routine (zero artifacts in four weeks) · an ntfy replay ghost (no responder was running to ack anything). Two advisor tooling artifacts also bit: a shallow clone hid the true branch list; a `grep|head&&echo` pipeline reported a fence breach that wasn't. All standing text in v1–v3.1 asserting any of the above is void; this section is the record.

## Guard architecture now — three layers plus a tripwire
(1) Hooks: `.claude/hooks/_seat.py` (PR #7). (2) Daemon entrypoints: `background/_seat.py`, 44 guarded + 4 launchers, structural-lock test (PR #12). (3) The side-effect itself: the liveness function's first act is the seat check — importing callers no longer bypass anything (PR #13). Plus a per-test and per-session tripwire in `tests/background/conftest.py` that names any test manufacturing a real-repo commit. `SE_SEAT=resident|foreign` overrides everywhere. Optional belt for the wake: set `SE_SEAT=foreign` in the poesys cloud environment's variables.

## Standing items for the wake (details live in the rewritten runbook; this is the register)
mypy census red on main by design — 44 modules drifted exactly +1 from guard insertions (551→595): preferred fix = annotate `background/_seat.py` so the baseline returns to 551; alternative = re-freeze at 595. · 3 `stamp_human_presence` tests fail off-seat as PR #7 collateral (make seat-independent or mark). · `block_reason` prose on 27 atoms vs 4 new `blocked_reason` tokens — reconcile inside the ruled map rework. · 16 `worktree-agent-*` orphan branches on origin await salvage doctrine. · build-executor BLOCKED (`map_unreconciled`) since 07-29 — this week's validator/store likely unblocks it. · ghost `from_rich` replay class + `in_progress/` backlog to triage. · `company/data/*.db` needs a gitignore line. · 8 env-dependent background-test failures (HMAC/secrets/stamping), pre-existing, catalogued in PR #12/#13.

— v4, final. Advisor, 2026-08-07. The system's own artifacts — issue #11's confession, the tripwire, three guard layers — now tell this story without this note; the note exists so the wake reads it in one place.
