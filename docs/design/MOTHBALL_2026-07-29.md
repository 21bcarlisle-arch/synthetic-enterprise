# MOTHBALL RECORD — the apparatus audited against THE STANDARD (2026-07-29)

Companion to `THE_STANDARD.md`. Actions the director's ruling
`DIRECTOR_RULING_MOTHBALL_THE_APPARATUS_2026-07-29.md` authorised: for every gate, queue,
window, approval path, blocked-state tracker, draw-ordering rule and reporting ritual, ask
**"under the new standard — no one-way doors, silence is agreement, never wait, show the
working — what is this actually for?"** Serves as-is → keep (say what it does). Could serve
adapted → adapt (say what changed). Cannot say plainly what it is for → **mothball** (disable,
keep the code, record it). Burden of proof is on KEEPING. A wrong removal costs minutes;
dead machinery costs a working day each time it fires.

**Status of this record:** LIVING. This is the first-pass audit written by the worker tick that
found the ruling. Each row carries a **verdict** and, honestly, whether it is **already
disabled** or **verdict-recorded-execution-pending**. Nothing here is irreversible; reinstating
is `git revert` + a flag flip. Rows are grouped by the director's own test: *after this, nothing
should be able to block the machine except real money, real people, irreversible public
publication under Poesys's name, or a safety control.*

---

## MOTHBALL — waiting/approving/blocking machinery the standard withdraws (§2)

| Component | What it was for | Verdict | Executed? |
|---|---|---|---|
| **Blocked-mint tracker** (27 `PLANNER_MINTED_*` held on `director_level_up`/`build_open`) | Held internal level-ups and BUILD-opens pending a director console act | **MOTHBALL** — these are internal simulation levels/opens, NOT §2-reserved (no real money/people/public/safety). Under §2 the block is withdrawn: propose the level, record the undo, proceed. | Verdict recorded; releasing the 27 is the execution follow-up (touches the R16 level-gate — do carefully, see note) |
| **`_rule0_harden_draw` + HARDEN cooldown rotation** (`supervisor.py`) | Kept the tick "never empty" by re-drawing at-target atoms to re-verify | **ADAPT → then MOTHBALL** — §6: "re-verifying at-target work is the lowest-value thing available and must never be what fills a day." Demote BELOW real backlog work now; mothball entirely once the Timeframe-2 backlog is wired as atoms (removing it before the backlog exists would starve the loop → violates CONTINUOUS). | Verdict recorded; blocked on backlog-atom wiring |
| **Propose-then-proceed windows used as blockers** | Waiting-room for reversible acts | **MOTHBALL** — §3: silence is validation; notify then proceed. A window that *delays* a reversible act is exactly what's withdrawn. Keep only as an async notify (fire-and-continue). | Verdict recorded |
| **Reserved-category checks outside §2** (`one_way_door.py` categories beyond real money/people/public-publication/safety) | Escalated a broad list of "one-way doors" | **ADAPT** — narrow the reserved set to §2's four exactly. Values/fitness-function stays director-reserved by his separate standing word (do not silently drop it); the rest of the list is withdrawn. | Verdict recorded; needs care (safety-adjacent file) |

## KEEP — earns its place under the new standard (say what it does, one line)

| Component | Why it stays |
|---|---|
| **Incident-forged rules R1–R17 / the epistemic wall** | The standard itself says failures are "diagnosed to cause, not patched"; these ARE that. STABLE depends on them. |
| **Gap & simplification registers** | The director names them explicitly: "the backlog's raw material." Kept, and now feed §7.3. |
| **Coherence checks** (published-story-vs-truth) | Directly serve §4 "show the working" — they stop the public explanation drifting from the truth. |
| **Red-teaming / mutation tests (R15)** | Keep finding genuine defects; serve STABLE. Kept — but pointed at *live/new* work, not at re-verifying at-target atoms. |
| **tree_lock / push-verify / IaC reconstructability** | Keep the machine STABLE and its state readable; not a wait, a correctness primitive. |
| **Safety controls, secrets-out-of-tree, egress allowlist, profile-change = director-only** | §2-reserved by definition (protect real people / real-world posture). Untouched. |

---

## Honest scope note (this tick)

This record was seeded by a **bounded background worker tick** that woke to do a HARDEN atom and
instead found the ruling. It has: adopted `THE_STANDARD.md`, written this audit, and notified the
director. It has **deliberately not** unilaterally ripped out the level-gate, `one_way_door.py`, or
the draw logic in one uncareful pass — those are wide-blast-radius changes (the R16 level-gate has
wedged publishing before) and belong in careful execution, not a 5-minute micro-tick. The verdicts
above are the director's to reverse at will (silence = agreement). Execution of the MOTHBALL rows
proceeds from here — it is underway, not "proposed and parked."
