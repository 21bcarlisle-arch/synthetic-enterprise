atom_id: W1_6b_merit_order_reconstruction
simplifications:
- '2026-07-27 MINTED into the map as a DRAWABLE ATOM from DIRECTOR_RULING_HARNESS_INVESTMENT_AND_ITS_EVIDENCE_2026-07-27 (Problem One: the merit-order reconstruction was named in ratified rulings + the PLANNER_MINTED_merit_order_reconstruction_discover_2026-07-25 mint-doc but existed as NO atom in maturity_map.yaml -- a grep-confirmed gap; the requirement is a drawable atom with real exit criteria, decomposition mine). DISCOVER+FRAME are DISCHARGED (evidence above: live-vs-target form + exact diff FRAME, DUKES 5.10.C/5.14 per-type efficiencies + EF_GAS 0.184 + UK Carbon Price Support ~£18/tCO2 in the market_research doc). This atom is the held BUILD. EXIT CRITERIA (falsifiable, R15): (1) the ordinary-day RECONSTRUCTIBILITY test passes -- on ordinary GB days the marginal plant is a gas CCGT and SSP is substantially reconstructible from gas-over-efficiency + carbon + residual demand + wind, a scarcity term earning structure only in TIGHT hours (Board Spec 004); (2) the SAME W1_6_physics_price_signal per-cell lift table, re-measured against the SAME UNCHANGED naive baseline family, holds UNMOVED -- that unmoved measurement is the proof pricing became RIGHT not TUNED (the ruling''s own right-vs-tuned test); (3) R15 mutation -- a non-monotone / mis-ordered merit stack FIRES the reconstructibility check. R13 BINDING (not a wall against building): the baseline changes for fidelity-to-reality reasons decided BLIND to company P&L; NO interim tuning -- no per-cell fits, no regime-partition coefficient passes, no temporary recalibration while the structural work waits (R12 unchanged). depends_on W1_6_physics_price_signal (the fidelity row this repairs) + is the named unblock for PLANNER_MINTED_ssp_negative_lift_cells part (a). BUILD held behind the propose-then-proceed window (closes 2026-07-28); a tick after with no director revision draws it (level moves stay blocked_on director_level_up, R16 -- no self-bump). May split into DISCOVER/FRAME (done) + engine-BUILD sub-atoms at build-draw; the mint-doc is the decomposition reference.'
- '2026-07-29 blocked_on CLEARED (DIRECTOR_RULING_RIP_OUT_PERMISSION_MACHINERY_2026-07-29.md items 1+2): director_build_open/director_level_up are abolished as BLOCK TYPES -- there is no such thing as needing a build opened, and levels are proposed+recorded (ledger-backed, self-certification via background.gate_authorization.record_level_up_self_certified now suffices, R16), never gated. Prior blocked_on/block_reason preserved above/below for the record; this atom re-enters the normal draw for whichever lane/loop_stage it already carries.'
