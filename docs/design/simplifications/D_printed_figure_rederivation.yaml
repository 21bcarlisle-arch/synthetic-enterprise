atom_id: D_printed_figure_rederivation
simplifications:
- '2026-08-03 BUILT L0->L2, self-certified (worker tick). THE MINT''S PRESCRIBED FIX WAS REFUTED BY MEASUREMENT, AND THAT IS THE MAIN RESULT. The mint (below) specified a DECLARED display precision of 2dp of a penny with the printed amount DERIVED from the printed rate. Measured against the real book before building: the precision a line needs is a function of its MAGNITUDE, because the rounding error is quantity x delta -- required precision spans 1 to 6 decimal places, and the same 2dp rate that reproduces a 50 kWh line to the penny is out by 7.86 pounds on a 157,128.8 kWh one. So the prescribed fix would have CHANGED THE CHARGE on 86% of bills (median ~7p) purely to tidy the printout. Changing what a customer is charged so the bill''s arithmetic looks right is the wrong way round, and it is only tempting because a unit rate LOOKS contractual. Here it is not: these rates are DERIVED (commodity_amount/consumption, standing_charge/days) from a half-hourly settled charge that no single unit rate generated. MECHANISM BUILT IS THE INVERSE: the amount is primary and the RATE is fitted to it -- saas/money.py::minimal_display_rate returns the COARSEST precision in [2,6] that reproduces the printed amount exactly, floored at 2dp so a domestic bill still reads like a bill, and None when no printable precision does (the portal already renders a line without a rate, claiming nothing). Zero money moved.'
- '2026-08-03 R10 CLASS CLOSURE, not an instance fix. PRINTED_LINE_REDERIVES registered in the invariants library + check_printed_line_rederives(), covering the invoice summary line, the standing-charge line AND the per-register rows -- the portal renders one row per register, so those are printed lines in their own right. Deliberately a SEPARATE invariant from PRINTED_BILL_FOOTS_EXACTLY rather than a widening of it: that one asks whether the COLUMN adds to the TOTAL, this one whether each LINE reproduces itself, and a bill can foot perfectly while its usage line multiplies out wrong. One check must not answer two questions. THE PRECISION DECISION LIVES IN ONE PLACE (saas/money.py) and is applied at three call sites (ledger summary, register row, portal mapper fallback for records predating the field); the browser no longer computes a rate at all.'
- '2026-08-03 EVIDENCE (R11, verified to the RENDERED value, not the JSON; R14 basis: billed). THE DEFECT WAS A RENDER DEFECT, which is why measuring the JSON alone would have missed it: site/customers/index.html called toFixed(2) on the rate and threw away the precision before the customer saw it. BEFORE, measured on the rendered artefact: 86.1% of usage lines (1441/1674) and 243 standing-charge lines showed a multiplication that does not hold -- e.g. ''317.9 kWh x 11.90p = 37.82 pounds'' when the product is 37.83; plus 324 invoices printing raw binary-float residue (0.23983870967741938 pounds/day). AFTER, on the regenerated ledger: check_printed_line_rederives 0/1557 failures, 0 failures as rendered, 0 lines carrying float residue, and 1557/1557 lines still print a rate (nothing was fixed by suppressing the arithmetic). Printed-rate precision distribution 1dp:39 2dp:226 3dp:675 4dp:237 5dp:211 6dp:169 -- the spread the single declared constant could not have covered. RENDER VERIFIED BY DRIVING THE PAGE''S OWN FUNCTIONS: tools/verify_printed_bill_render.mjs lifts rateStr() and billUsageLinesHtml() verbatim out of index.html by source extraction and parses the arithmetic back out of the emitted HTML -- reimplementing them would have drifted from the page and passed while the page failed, the same tautology shape R15 names. R11 BOUND STATED HONESTLY: this is the rendered output of the LOCAL regenerated data; the live poesys.net re-fetch lands with the next publish cycle, and per the sibling atom''s own hard-won lesson that step is NOT claimed until it is done.'
- '2026-08-03 R15 MUTATION-PROVEN BOTH WAYS. tools/mutate_printed_figure_rederivation.py applies 9 REAL SOURCE mutations (never to the tests) and asserts each fires ITS OWN named test with the baseline restored green -- 4 on the money boundary (display floor dropped below 2dp; rate accepted without checking it reproduces the amount; non-finite quantity guard removed; precision cap failing open) and 5 on the invariant (the comparison always passing; a rate with nothing to multiply passing; the printed-rate precision guard removed; non-finite figures reading as clean; per-register rows no longer checked). The render control is proven both ways separately: restoring toFixed(2) makes it report 1292/1557 rendered lines failing, restored to 0. 36 tests. Killer patterns attacked by name -- TAUTOLOGY: the invariant module must not import saas.money (the module that PRINTS these figures), asserted by AST over its imports rather than a substring search, since the file''s own prose necessarily names saas.money to explain why it does not call it and a grep-shaped test would fail on the explanation while missing a real import added later. FAIL-OPEN: a printed rate with a missing quantity or amount FAILS rather than passing for want of evidence; two explicit vacuity guards assert the population tests are not passing on an empty or rate-less book. NaN-BLINDNESS: non-finite checked FIRST everywhere, since every comparison here is NaN-blind.'
- '2026-08-03 NAMED RESIDUALS, not built this pass. (a) L3 would need the Expert-Hour walk of the deployed bill surface plus the post-publish live re-fetch; neither is claimed. (b) FINDING, queued not fixed (SELF-INTERRUPT DISCIPLINE, machine not blocked): site/data/customers/C1_2.json, C2_2.json and C5_2.json are ORPHANS -- absent from the current run''s per_customer_lifetime and from the ledger, so no generator ever rewrites them, and they still carry 103 stale pre-fix lines that FAIL this invariant. This is the same orphan-staleness class the sibling atom recorded for C2_2/C5_2, now confirmed to persist and to have grown by one file; the durable fix is for the publish path to reconcile or prune customer files absent from the run, not for each atom to note them again. Their surviving the fix is also the evidence that this control is not a fail-open: it fires on 103 real published lines. (c) The full float->Decimal core migration remains separate and open, unchanged by this atom.'
- '2026-08-03 MINTED (worker tick, sole-map-writer) from the HARDEN walk of D_money_boundary_reconciliation, MEASURED ON THE DEPLOYED SURFACE, not hypothesised. The footing fix closed "the column adds up". It did not close "each line can be re-derived", which is the same customer test one level down and is currently false on the majority of bills: (a) commodity_amount_gbp != consumption_kwh x unit_rate_p_per_kwh as printed on 1382/1603 indexed invoices, because the printed unit rate is rounded to 1dp while the charge used full precision -- proportional, ~0.05%, but on an industrial bill that is 20.86 pounds the customer cannot account for; (b) standing_charge_gbp_per_day prints raw binary-float residue (0.24000000000000002, 0.23983870967741938) on 396/1603; (c) standing_charge_gbp != per_day x days_in_period on 27/1603.'
- '2026-08-03 MINT -- THE DESIGN QUESTION IS PRECISION, NOT ROUNDING, and that is why this is a separate atom rather than a widening of PRINTED_BILL_FOOTS_EXACTLY. A p/day rate and a p/kWh rate are NOT penny figures; quantizing them to 0.01 pounds would destroy the rate (0.2398 -> 0.24) and is the wrong fix. Real UK bills print unit rates to 2-3dp of a PENNY and the line reconciles at that precision, so the fix is a DECLARED display precision per figure KIND (money=2dp of a pound, rate=2dp of a penny), with the printed amount derived from the printed rate -- the same "derive the displayed whole from the displayed parts" move saas/money.py already makes for the total, applied one level down. R15: mutation must prove the control FIRES on a printed rate whose product does not reproduce the printed line, and PASSES a legitimately re-derivable line; NON-FINITE FIRST (comparison guards are NaN-blind here, a known repeat class); FAIL-CLOSED on a missing rate or a missing consumption figure -- a line item with no derivation shown is exactly the defect. Watch the tautology: the check must re-derive with its own arithmetic, never by calling the generator that printed the figure.'
map_notes:
  name: 'Printed-figure re-derivation: every figure on the face of a bill must be reproducible by the customer holding it -- the adjacent half of the money-boundary class the footing fix did not close'
map_records:
  evidence:
  - saas/money.py
  - company/compliance/domain_invariants.py
  - tools/generate_billing_ledger.py
  - tools/verify_printed_bill_render.mjs
  - tools/mutate_printed_figure_rederivation.py
  - tests/company/compliance/test_printed_figure_rederivation.py
  - site/state/billing_ledger.json
