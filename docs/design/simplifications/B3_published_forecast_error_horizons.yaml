atom_id: B3_published_forecast_error_horizons
simplifications:
- '2026-07-29 REGISTERED into the map (worker tick, sole map writer per THREE_LANES) from docs/design/BACKLOG.md item B3, on the director''s direct instruction (docs/staging/from_rich_20260729_173731.md, verbatim: "Your backlog is in a document but the thing that picks your next job reads the map. That''s why you keep saying there''s no work. Put all eleven backlog items into the map as real unfinished work items now"). REGISTERED AS DRAWABLE, NOT BLOCKED -- deliberately carries NO blocked_on: the earlier Wave-A/B registrations (SPINE_1/SPINE_3/B6) used blocked_on: director_build_open, and _is_externally_blocked drops a blocked atom from EVERY lane including the idle DISCOVER/FRAME draw, so they were invisible to the picker -- which IS the defect the director just named. The BUILD wall is preserved by the CORRECT mechanism instead: loop_stage: idle + the fronts.yaml stage_advance gate (idle -> build is a director console act), so DISCOVER/FRAME is drawable now and BUILD still is not (CLAUDE.md "Epoch gating gates BUILD, never thought"). DoD (BACKLOG B3): a typed inbound forecast feed exposes >=2 horizons; forecast error is non-zero and monotonically shrinks toward delivery by a calibrated schedule; the company''s naive 120-day belief is measured against it and the gap is reported per the coupled triad. WALL: the company reads the PUBLISHED forecast and its own error history only -- it may never read the realised outturn the forecast was generated from, and no forecast-generation internal crosses sim_interface.py. R13: the error-shrink schedule is calibrated to external published forecast-accuracy sources (NESO/Elexon), decided blind to company P&L, never tuned because company results look wrong. No BUILD code, level_current 0 (R16: no self-bump).'
- '2026-07-29 DISCOVER+FRAME pass (Lane-3 fork, doc-only, no BUILD code): verified NO published multi-horizon forecast feed exists anywhere in sim/ or company/ today -- every *forecast*.py file (load_forecast.py, consumption_forecast.py, cash_flow_forecast.py, ev_demand_forecast.py, ncc_forecast_register.py) is an INTERNAL company projection of its OWN book, not a received external market forecast; this atom''''s own file_scope (sim/forecast_publication.py) does not exist on disk. The named naive-120-day-belief IS company/pricing/tariff_engine.py:36 COMPANY_LOOKBACK_DAYS -- CompanyTariffEngine.get_forward_price() (tariff_engine.py:269-322), a BACKWARD-looking rolling mean of trailing observed spot (never a forward forecast), point-in-time-clean (only reads records before delivery_date, tariff_engine.py:308-314) but carrying no genuine forward information -- wired to the wall at company/interfaces/sim_interface.py:296,320-322. Real-world calibration anchor PARTIALLY exists on disk: docs/market_research/WEATHER_FORECAST_ERROR_BY_HORIZON.md (2026-07-24 live Elexon pull) gives day-ahead demand MAE~1.7-4.2pct (confidence M, 2-week sample, own doc flags too short for a smooth curve) and a CONFOUNDED wind series (metering-scope mismatch, doc''''s own words: "do not feed the raw biased MAE curve into a simulation variable") -- week-ahead, season-ahead and ANY price-forecast horizon are UNSOURCED, no fabricated number recommended. Coupled-triad status: all three legs (SIM/COMPANY/HARNESS) are currently EMPTY for this atom -- background/coupled_triad.py::_AUTHORITATIVE_COUPLING has no B3 entry and no company-side twin atom is registered (checked all C1-C13 ids), so even once built this atom cannot reach L3 until a twin (recommended C14_forecast_belief_convergence) is registered alongside it, mirroring the working W1_6<->C13 pattern (background/weather_price_triad.py + background/gap_metric.py::prediction_gap). Full detail, proposed seam (new SimInterface.get_published_forecast method beside the existing get_forward_price), horizon set, R15 mutation tests and L3 exit test: docs/design/B3_PUBLISHED_FORECAST_ERROR_HORIZONS_FRAME.md. No BUILD code, level_current held at 0 (R16: no self-bump).'
map_notes:
  name: 'Published forecast feed at multiple horizons with realistic, delivery-converging error: the company receives forecasts (demand/wind/price) at >=2 horizons whose error is non-zero and shrinks by a calibrated schedule as delivery approaches, so its own naive 120-day belief can be measured against a real wall on the future. BACKLOG.md B3 (Wave B).'
map_records:
  evidence:
  - docs/design/BACKLOG.md
  - docs/design/THE_MODEL_ON_A_PAGE.md
  - docs/design/B3_PUBLISHED_FORECAST_ERROR_HORIZONS_FRAME.md
