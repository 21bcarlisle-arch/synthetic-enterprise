atom_id: G1_fidelity_grid_scorer
simplifications:
- '2026-07-20 LIVE COMPANY CONSUMER WIRED (blocked_on coupled_triad_l3_needs_live_company_consumer -> MET). background/live_fidelity_evidence.py bridges the L3 payment triad belief-vs-truth gap (W2_11 truth -> W4_4 seam -> D5 consumer) into this grid scorer LIVE in simulation/run_phase2b.py; R1 consumer-verified in a real run (cell A1_G2 gap 0.2, grid worst A1_G1 fail-open floor, gate PASS, 14 cells untested; 20 run_phase2b tests green, no regression). 10 R15 tests on the bridge (FIRES/QUIET/WALL/GATE both directions/import-freedom), epistemic PASS. L2->L3 PROPOSED: level_current NOT moved (MATURITY_MAP section 0, levels are director-ratified); blocked_on set to director_level_up to stop the build re-draw. Commits 3f36e3776 (offline bridge) + this run-loop wire.'
- '2026-07-19 BUILT + director-RATIFIED L1 (console FRONT_OPEN EPOCH2_CAMPAIGN + LEVEL_UP ts=1784462364): 3 fidelity measures on the improved constructs (EPOCH2_G_CONSTRUCT_CHALLENGE_RESPONSE); 24 R15 tests, epistemic PASS. L1->L2 = a live physics-atom emits into the evidence ledger + a real gap is measured.'
