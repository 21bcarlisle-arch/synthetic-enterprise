atom_id: EP6_wall_protocol_typing
chunk: 22
simplifications:
- "2026-08-20 pass 25 BUILD, level HELD at 2, loop_stage stays build. THE ANSWER TO THE QUESTION PASSES 22, 23 AND 24 EACH RECORDED AS THE NEXT MOVE IS: A DENYLIST, AND IT WAS FAIL-OPEN ON 9 OF THE 11 FIELDS OF THIS MODULE'S OWN TRUTH RECORD. The question, unchanged for three passes, was \"both codecs refuse `FORBIDDEN_TRUTH_FIELDS` by name, so what happens when the world adds an observable that is not on that list but is nonetheless ground truth the company should not have -- is the list a wall or a denylist?\" It is a denylist. This pass measures that, demonstrates the leak, and replaces the mechanism with a closed set on all THREE of the wall's codecs. Everything below is observed-with-evidence unless labelled inferred (R9). MEASURED AT: HEAD b8e4f26be, worktree.\n\n  MEASURED FIRST, BEFORE WRITING ANYTHING, the discipline pass 16 installed and pass 24 kept. The measurement is a straight enumeration of the SIM's own truth-holding dataclasses against the seam's forbidden list. `FlexDispatchTruth` carries 11 fields; the list names 2 of them (`residual_mw`, `true_baseline_mwh`). The 9 it does not name include `true_delivered_mwh`, `true_delivery_ratio` and `true_utilised_revenue` -- fields whose own names begin with the word the list was built around. `StackedFlexTruth` carries 16 fields and the list names 1. So the list enumerates INSTANCES of a class it does not describe, and the three `true_*` misses are not exotic cases: they are the SIM's answer to exactly the question the company is supposed to be unable to answer.\n\n  THE LEAK WAS DEMONSTRATED, NOT INFERRED. Registered a `FlexSettlementLine` carrying one extra field, `true_delivered_mwh` -- the codec's own docstring names \"adding one more useful field to a settlement line\" as the natural edit it protects against -- and called `encode_observable_payload`. It returned a wire dict with 9 fields including `'true_delivered_mwh': 4.2`. The company reads `metered_delivery_mwh` off that same payload; the GAP between the two is the entire flex-inference problem the triad harness exists to measure, and it was one field addition away from being handed over.\n\n  THE DOCSTRING ASSERTED THE OPPOSITE, IN THE R10 VOCABULARY, AND THAT IS THE PART WORTH KEEPING. `sim/flex_dispatch.py::encode_observable_payload` said, verbatim: \"R10: the CLASS is refused by name, so a future L2 field addition fails here rather than passing because nobody thought of that instance.\" Refusing a class BY NAME is a contradiction in terms, and the phrase survived four passes and an Expert-Hour-shaped question because it uses the right vocabulary for the wrong mechanism. A control that describes itself in the language of the doctrine it violates is harder to catch than one that says nothing.\n\n  THE OTHER TWO CODECS WERE WORSE, WHICH IS WHY THIS IS NOT AN INSTANCE FIX. `simulation/conversation_response.py::encode_observable_payload` had NO field-level check at all -- `FORBIDDEN_TRUTH_FIELDS` is imported there by tests only, so a latent trait added to `ConversationResponse` under any unpredicted name (`propensity_scalar`, `segment_score`) crossed unrefused. `simulation/payment_seam_adapter.py::encode_observable_payload` had neither a field check NOR a denylist, across SIX payload types, and the other two seams were built citing its pattern. Fixing flex alone would have been an instance fix of a class defect, which R10 forbids by name.\n\n  WHAT REPLACED IT: THE CLOSED SET, `OBSERVABLE_PAYLOAD_FIELDS`, one per seam contract. The codec now asks \"is every field on this payload one the contract has DECLARED observable\" instead of \"is this field one of the leaks we already thought of\". Adding a field to a payload dataclass and nothing else is REFUSED at the point of emission. Widening the wall requires editing the declaration, and that edit is the epistemic act -- the place a reviewer has to answer the contract header's own question out loud. Refusal is symmetric: a payload that LOSES a certified field is refused too, so the declaration cannot silently go stale in either direction.\n\n  DECLARED, NOT DERIVED, AND THIS IS THE R15 POINT. The obvious implementation is `get_type_hints(PayloadClass)`, and it is worthless here: a set computed from its own subject widens the instant the subject widens, which is R15 TAUTOLOGY for precisely the question being asked. This seam already contains that mistake in a place where it is defensible and one where it is not -- `conversation_response.py`'s DECODE leg builds its allowlist with `get_type_hints(ConversationMessage)`, which is correct against a malformed WIRE (its actual subject) and blind to a careless CONTRACT edit. So the closed set is written out by hand, the duplication is load-bearing, and an AST test on each contract asserts the declaration is a literal and refuses a computed one.\n\n  THE DENYLIST WAS KEPT AS A SECOND BELT AND ORDERED FIRST, deliberately. It still fires on the one case the closed set cannot see -- a truth field added to the dataclass AND declared observable in the same edit -- and running it first keeps the two belts SEPARATELY OBSERVABLE: `test_mutation_a_known_forbidden_trait_still_trips_the_denylist_belt` can only pass if the denylist check fired, so the older control cannot be silently subsumed by the newer one and then be said to still exist.\n\n  R15 PROVEN BOTH WAYS ON ALL THREE SEAMS, 13 new tests (4 flex, 5 conversation, 4 payment). THIS RECORD FIRST SAID 11 AND THE COMMIT MESSAGE STILL DOES: the count was written from the plan rather than counted off the tree, which is the same defect in miniature as the one this pass fixes. Corrected in place -- the text of one entry changed, no entry added, so the store count stays 24. Each seam has a NULL CONTROL (the real declared payload still encodes, so what the refusals detect is the field and not the fixture), an UNDECLARED-FIELD mutation, and a STALE-DECLARATION mutation; the conversation seam additionally proves the denylist belt fires on its own, and the payment seam proves the declaration covers all six payload types and agrees with each. The mutations are registered under the REAL payload type names because the defect modelled is someone EDITING the real class, not smuggling a new one past the type gate.\n\n  ONE PRE-EXISTING TEST CHANGED ITS FIXTURE AND THE REASON IS RECORDED HERE, not buried. `test_a_FORBIDDEN_TRUTH_FIELD_is_refused_BY_NAME_at_the_point_of_emission` used two fabricated payload types, `Leaky` and `Clean`. Its null control `Clean` is now refused ON ITS OWN ACCOUNT -- a fabricated type has no declaration -- which is a STRONGER wall than that test's subject and would have masked it. The null control was moved onto the real `FlexSettlementLine`; the leaky half is untouched and still asserts the denylist message.\n\n  SUITE: 2,163 passed across `tests/interface`, `tests/sim/test_flex_dispatch.py`, `tests/simulation/test_conversation_response.py`, `tests/company/comms`, `tests/company/market`; 639 passed across `tests/tools/test_wall_channel_census.py` + `tests/company/interfaces` including the channel-C wire-conformance gate armed by pass 23; 29 passed on `tests/simulation/test_payment_seam_adapter.py`. `python3 -m tools.epistemic_verifier`: PASS, 577 company/ + saas/ files, no barrier violations.\n\n  WHAT THIS PASS DOES NOT ESTABLISH.\n    * IT IS NOT THE HARDEN PASS / EXPERT HOUR. It answers the question that pass was to put, and answering it turned out to be a build move rather than a review one. The cold-eyes walk against the codecs is still unrun and is still the first blocker on L3, now one pass older again.\n    * THE CLOSED SET GUARDS THE ENCODE LEG ONLY. A field the far side invents on decode is a different question with a different subject, and the decode allowlists remain derived (`get_type_hints`), which is right for their subject and wrong for this one.\n    * FOUR OF SIX CHANNELS STILL HAVE NO CONFORMANCE QUESTION -- A, B, E and F are enumerated only. Unchanged from pass 22.\n    * NOTHING HERE MAKES THE (SEAM, LEG) UNIT FINER. A fourth crossing of an already-wired leg handed over as a Python object is still invisible to the channel-C gate.\n\n  L3 IS NOT CLAIMED. This pass discharges the first of pass 22's four blockers in the sense that the question it was to ask is now answered and mechanised; the HARDEN/Expert-Hour review of the codecs themselves is not done.\n\n  NEXT MOVE, recorded not asked (NEVER_ASK_WITHOUT_RECOMMENDING): THE COLD-EYES WALK AGAINST THE THREE CODECS, now that all three carry the same mechanism and a stranger can be asked one question about all of them rather than three questions about three shapes. The question to put to it: the closed set moves the epistemic decision to the moment someone edits `OBSERVABLE_PAYLOAD_FIELDS` -- so is there anything at all, other than a reviewer's attention, standing between \"I added a field and the test went red\" and \"I added the field name to the tuple and it went green\"? That is the honest residual risk of this design and it should be written down by someone who did not build it."
