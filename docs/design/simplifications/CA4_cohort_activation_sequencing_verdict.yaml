atom_id: CA4_cohort_activation_sequencing_verdict
simplifications:
- '2026-07-27 MINTED as a DRAWABLE ATOM from DIRECTOR_RULING_COHORT_ASSIGNMENT_ACTIVATED_2026-07-27 (WORK THIS CREATES item 4; ruling section 3 "you are invited to disagree with the activation itself"). LANE W2_customer_generator = SIM_ACTORS front (OPEN, opened_by director console ts=1784351583) -> drawable now; doc-only decision, DISCOVER/FRAME never gated. DELIVERABLE: a written verdict, evidence-first (R9), on whether cohort activation should proceed NOW. Default is PROCEED (ruling tag proceed). A counter-proposal is legitimate ONLY with evidence that activation (a) corrupts a baseline a later comparison depends on, (b) invalidates an in-flight comparison, or (c) collides with in-flight work -- e.g. the population-existence wiring (PLANNER_MINTED_generator_draw_wiring_2026-07-24, in_progress) or any unresolved N/lambda escalation on the pool draw. This verdict soft-GATES CA1 (stated in prose, NOT a machine depends_on -- CA4 target L1 would target-level-deadlock CA1; CA1 scope step 1 reads this verdict). EXIT CRITERIA: (1) verdict written naming each of the three counter-proposal triggers checked with evidence; (2) if PROCEED -> CA1 is authorized to wire; if COUNTER-PROPOSE -> the preferred sequencing is stated with evidence and NTFY-escalated to the director (curriculum re-sequencing is director-owned, R13). R12: the verdict must not be tuned to any company-P&L outcome. Level move blocked_on director_level_up (R16).'
- '2026-07-27 BUILT (worker tick). VERDICT written: docs/design/CA4_COHORT_ACTIVATION_SEQUENCING_VERDICT.md -> PROCEED (matches ruling tag proceed). All three counter-proposal triggers checked with disk/git evidence (R9): (a) NO baseline corruption -- assign_cohort() own-substream isolation + to_customer_dict() omits cohort => observable stream byte-identical (population_draw.py:251-256,216-230; tests green); (b) NO comparison invalidation -- default-off control kept, no P&L anchored to the drawn book; (c) NO in-flight collision -- CA1 cohort-VARIETY is additive over PLANNER_MINTED_generator_draw_wiring EXISTENCE-wiring, disjoint. HONEST CAVEAT recorded: live_population sits behind the still-director-reserved SE_DRAW_POPULATION (default off, unwired) so the CA1 flip is presently LATENT -- it arms the release rung, changes nothing observable today; consistent with ruling section 3 (a curriculum act on a mechanism, not a wiring instruction). R12: verdict not tuned to any P&L. Level move blocked_on director_level_up (R16).'
map_records:
  evidence:
  - docs/staging/done/DIRECTOR_RULING_COHORT_ASSIGNMENT_ACTIVATED_2026-07-27.md
