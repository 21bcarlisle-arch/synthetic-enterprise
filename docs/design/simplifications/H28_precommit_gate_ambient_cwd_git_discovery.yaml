atom_id: H28_precommit_gate_ambient_cwd_git_discovery
simplifications:
- '2026-07-28 MINTED as a drawable proposal atom from an H24 HARDEN red-team pass (SELF_INTERRUPT_DISCIPLINE: register-not-fix). The residual was already an executable boundary marker in tests/tools/test_pre_commit_test_gate.py (test_scrub_does_NOT_close_ambient_cwd_upward_walk__known_residual_boundary, 2026-07-27) but had been twice-queued-but-never-captured as a drawable map item, so the FIX would never be drawn. WORK THIS CREATES: (1) a which-tests analysis over the gate''s selected test set to find which control tests legitimately read the real .git by upward-walk vs which could be made cwd-blind; (2) the scoped hardening (candidate: set GIT_CEILING_DIRECTORIES or an explicit discovery boundary on the pytest subprocess env in tools/pre_commit_test_gate.py so a gate-run test from cwd=ROOT cannot silently discover+mutate the real repo) WITHOUT breaking tests that need the real repo; (3) an R15 mutation proof both-ways. DISCOVER-workable now; BUILD awaits epoch/twin open (loop_stage idle). NOT the same vector as the shipped GIT_* scrub (that closes leaked-env; this closes discovery-by-location) -- defense-in-depth for the same commit/publish chain.'
- '2026-07-28 FRAME + WHICH-TESTS ANALYSIS (Lane-3 DISCOVER, orchestrator, doc-only, no code, no level move per §0; scheduled self-refill draw, agenda+staging empty). Delivers work-item (1) of the mint (which-tests analysis) and REDIRECTS work-item (2) with an evidence-backed refutation of the proposed candidate. WHICH-TESTS RESULT: over the gate''s selectable set -- the fixed CONTROL_TESTS + LEVEL_SENSITIVE_TESTS AND the git-touching per-file tests the _gitless_env docstring names (build_executor, retro_cadence_check, worker_seat) -- EVERY real git subprocess passes an EXPLICIT cwd= (fork_reconciler _git_run cwd=repo/wt_path; build_executor _git cwd=work/remote), an explicit `git -C <repo>` (retro_cadence_check), or an explicit path arg (git init <tmp>), or is MOCKED (deadmans_switch monkeypatches subprocess.run; gate_authorization/fronts_reconciler patch evaluate/git away; worker_seat shells no git). NONE relies on ambient cwd=ROOT discovery for a real (esp. mutating) operation. The SOLE ambient-real-repo reader is the residual-marker test itself (test_scrub_does_NOT_close_ambient_cwd_upward_walk__known_residual_boundary), which reads READ-ONLY (rev-parse --show-toplevel / --is-bare-repository) purely to ASSERT the residual still exists, and self-documents (its own comment) that it flips when the fix lands. So the atom name''s worry "some control tests legitimately read the real repo, so closing it blindly risks regressing them" is UNSUBSTANTIATED for the CURRENT gate test set -- the risk is FORWARD-LOOKING: the per-file selection is open-ended, so a FUTURE test could shell git from ambient cwd to read the REAL project history and would then be regressed (that regression is arguably DESIRABLE -- a gate-run test should never depend on real-repo state -- so it should be a documented invariant, not a silent surprise). CANDIDATE REFUTED (empirical, read-only rev-parse probes, non-mutating, run this session): GIT_CEILING_DIRECTORIES -- the atom''s proposed fix -- DOES NOT close this vector. Confirmed on disk: scrub+cwd=ROOT resolves the real repo (rc=0); ceiling=ROOT STILL resolves it (rc=0); ceiling=parent(ROOT) STILL resolves it (rc=0). Reason (git docs, verified behaviour): GIT_CEILING_DIRECTORIES stops the UPWARD walk but "will not exclude the current working directory" -- and ROOT/.git lives in the cwd itself, so the ceiling is never consulted for it. CORRECT FIX (for BUILD, NOT written here): run the gate''s pytest subprocess from a NEUTRAL non-repo cwd instead of cwd=str(ROOT) (PROVEN: scrub+cwd=neutral -> rc=128 "not a git repository", vector closed), which leaves every explicit-cwd / `-C` test unaffected (PROVEN: same scrubbed env + `git -C <tmprepo>` still rc=0). COST/RISK the BUILD must handle (this is WHY it is non-trivial, and vindicates the mint''s "needs a which-tests analysis first"): pytest today receives target paths RELATIVE to cwd=ROOT ("tests/background/test_x.py") and leans on ROOT for rootdir/conftest/PYTHONPATH discovery -- moving cwd forces ABSOLUTE target paths + an explicit --rootdir=ROOT, plus a check that no gate-run test reads project files via a Path.cwd()-relative-to-ROOT path. R15 both-ways at BUILD: MUTATION = a gate-run test that MUTATES the real repo from ambient cwd must FAIL under the fix and PASS-while-corrupting without it; the existing residual-marker test is INVERTED to assert cwd-blindness (its own comment pre-authorises exactly this). BUILD gated (loop_stage idle); DISCOVER/FRAME is now SATURATED (analysis + fix-direction + candidate-refutation complete; further DISCOVER adds nothing -- frame_saturated:true set). Defense-in-depth on the same commit chain as the shipped GIT_* scrub, distinct vector (discovery-by-location, not leaked-env).'
