atom_id: C14_thermal_parameter_inference
simplifications:
- '2026-08-03 MINTED (worker tick) from docs/staging/ADVISOR_DISCOVERY_PREMISE_FABRIC_PHYSICS_2026-08-03.md appendix item B (ADOPTED, sequenced second). DISCOVER doc: docs/design/PREMISE_FABRIC_PHYSICS_DISCOVER.md section 5. loop_stage: idle because that doc authorises DISCOVER+FRAME only. THIS IS WHY THE WORK IS MORE THAN A FIDELITY FIX: it creates a genuine belief-vs-truth mechanism of the same class as the payment triad. SIM knows actual (R, C); the company observes EPC-class data -- itself an imperfect MODELLED estimate of real fabric, covering only ~60pct of stock, biased toward transacting properties, and up to 10 years stale -- plus meter reads at the customer''s actual read frequency plus published weather. THREE DISTINCT ERROR SOURCES, each faced by a real supplier: EPC modelling error, EPC staleness, EPC ABSENCE. A real supplier''s fabric knowledge is an estimate of an estimate; modelling all three is cheap here and gives the company something honest to be wrong about.'
- '2026-08-03 FRAME -- WALL DISCIPLINE IS A GATE ON THIS ATOM, NOT A NICETY: the company must NEVER be handed the SIM''s (R, C). Inference runs strictly on observables through company/interfaces/sim_interface.py, and an epistemic-verifier pass is a required exit condition. ESTIMATOR DELIBERATELY LEFT OPEN (SIMPLICITY GUARD): the UKF is justified by the non-linearities (temperature-dependent COP, deadband switching), but if a first cut can be done with regularised least squares on the heating-response gradient, DO THAT FIRST and escalate only if the gap metric demands it. Adopt the MECHANISM; leave the estimator open.'
- '2026-08-03 BUILT L0->L2 (worker tick). company/pricing/thermal_inference.py + tests/company/pricing/test_thermal_inference.py, 44 pass + 1 strict-xfail in 3.0s; epistemic_verifier PASS (530 files); 596 tests/company/pricing green. THE ESTIMATOR THE FRAME LEFT OPEN WAS RESOLVED DOWNWARD, as instructed: regularised least squares on the heating-response gradient (weighted OLS with a SEARCHED balance point, shrunk toward the EPC prior by inverse-variance weighting in LOG space) recovers the truth well enough that the UKF is not justified -- no filter, no state-space machinery. MEASURED against W1_12 traces on real weather (sim/weather_data/C1.csv, 2022): posterior within 0.2-4pct of each premise''s actual HLC on daily gas reads, against 2-24pct for the EPC prior alone. Log space is not decoration: HLC is strictly positive with multiplicative errors, and a symmetric band on a well-insulated flat crosses zero. THE THREE ERROR SOURCES ARE COPED WITH BY WIDENING UNCERTAINTY, NEVER BY CORRECTING TOWARD TRUTH -- a bias correction would require knowing the answer, which is what the wall withholds: EPC modelling error is a floor that is never removed however new the certificate; staleness inflates the prior per year since lodgement (capped, so age does not imply false precision); absence falls back to a labelled stock-class prior that is_actionable REFUSES no matter how tight its band. TWO FAIL-OPENS IN THE UNCERTAINTY MODEL WERE FOUND BY MEASURING, NOT BY REVIEW, AND BOTH ARE NOW CONTROLS: (1) CADENCE -- the same premise read daily/weekly/monthly came out 0.2/8/18pct from truth while the reported sd barely moved (0.136->0.148), i.e. the company was equally confident about a quarterly-read premise and a smart-metered one; a per-doubling structural term fixed it, and stating the uncertainty honestly IMPROVED the estimates (monthly 18.4->7.5pct) because the shrinkage weights became right. (2) FLAT SCOP -- an ASHP premise read 55pct above its true HLC (ten times the gas error) because a flat assumed SCOP reads temperature-dependent COP collapse as fabric loss; the company cannot correct a bias it cannot see, so the band widens and the posterior shrinks toward the certificate (41->16pct). (3) A summer-only read history PASSED the degree-day span test, because the searched balance point drifts to 18C and a UK July still spreads a few degree days -- so winter fabric loss was being extrapolated from summer hot water; MIN_PEAK_HDD_K now requires the premise to have actually been OBSERVED IN THE COLD. R15 BOTH WAYS, five REAL SOURCE MUTATIONS each firing its own named test and baseline restored green: drop MIN_PEAK_HDD_K (summer test fires), invert the blend precision weights (5 tests fire), repair-instead-of-raise a backwards register (meter-history test fires), toothless wall regex (3 wall tests fire), drop _require_finite (the NaN test fires -- and it demonstrates WHY the finite check runs first: nan < x is False, so a NaN register slides straight past the backwards comparison). MEASURED NEGATIVE RESULT, RECORDED AND DELIBERATELY NOT TUNED AWAY: the daily-lag memory fraction does NOT recover fabric-mass ordering -- the pre-1919 solid-wall detached came out at 13.3h against the post-2000 flat''s 21.8h, i.e. backwards -- because demand memory is dominated by the occupant''s setback schedule and the heat source''s recovery power, not by structure. It is pinned as a STRICT xfail so that an estimator improvement FAILS and forces the docstring claim to be corrected rather than silently outgrown; separating controller from fabric is the UKF escalation this atom has not earned. THE WALL IS THE GATE AND IT HOLDS: the module imports nothing from simulation/sim (sim_imports_in is a standing control over source text, proven to fire on four import spellings and proven NOT to fire on simplejson/simulated look-alikes), and the test suite -- which IS the harness and may see both sides -- feeds the company nothing but cumulative register reads, published mean temperatures and an EPC record in the register''s own string vocabulary. DELIBERATELY L2, NOT THE L3 TARGET: the gap is measured on a 5-premise PANEL, not the population, and H_GAP_fabric_belief_truth_gap (the money consequence of a fabric-targeted intervention decided on belief vs on truth) is unbuilt. Nothing is wired into a live pricing or retrofit-recommendation path, so no company DECISION has yet been made wrong by this belief. That wiring + the population-level gap IS the L2->L3 step, and W1_12 stays at L2 until it happens.'
