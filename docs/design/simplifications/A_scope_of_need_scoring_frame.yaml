atom_id: A_scope_of_need_scoring_frame
simplifications:
- '2026-07-19 AUTHORED into the map (orchestrator landing act; the DISCOVER doc is doc-only and explicitly defers the maturity_map edit to the orchestrator/BUILD) from EPOCH2_A_SCOPE_OF_NEED_SCORING_FRAME_DISCOVER.md (2c3d7c03f), per DIRECTOR_DIRECTIVE_SEAT_WORK_AND_CONTINUITY_PROOF_2026-07-19. In-region of the OPEN EPOCH2_CAMPAIGN front (lane G_data_learning). L0 -> BUILD: the 5x3 {A1..A5} x {G1,G2,G3} grid + worst-cell MAX/CVaR scoring over G1_fidelity_grid_scorer, R15-failable (a book green ONLY on the comfortable middle A5 must NOT pass the worst-cell gate). Director VALUES-CALLS OPEN (equal-cell vs harm-weighted worst-cell; grid-completeness) -- build on the asserted equal-cell default, leave the call flagged (EPOCH2_VALUES_CALLS_BATCH_1). NO level claimed (levels are director-ratified, MATURITY_MAP.md section 0).'
- '2026-07-19 BUILT (background/scope_of_need_scoring_frame.py + 13 R15 tests, epistemic PASS): the 15-cell grid (5 archetypes x 3 regimes), worst-cell headline (default q=0 pure MAX per the director written rule; CVaR top-k smoother available), middle-to-edge spread, fail-open on untested cells, wall-leak guard. R15 killer mutation proven (14 good / 1 blind scores ~blind not the ~0.16 mean; middle-only-green never reports clean; untested cell cannot exit the MAX). VALUES-CALL defaults asserted+marked: WORST_CELL_Q=0.0, EQUAL_CELL_WEIGHTING=True (harm-weights are the director cat-6 call). L1 PROPOSED (batched for director LEVEL_UP; level_current NOT moved per section 0; blocked_on director_level_up stops the re-draw). L1->L2 = feed real per-cell coupled gaps from a live run.'
