atom_id: OPS7_provenance_stamps_on_live_pages
map_notes:
  origin_note: 'MINTED FROM deliverable 2 of the WORK THIS CREATES block in DIRECTOR_RULING_PUBLISH_DECOUPLING_2026-08-10. Ruling property 1, verbatim: ''the site serves the most recent snapshot that passed its scoped verification, stamped with run id, verifying commit, and verification time -- visible on the page, not in a log.'' EXIT: (1) the three fields render on the live public pages; (2) R11-verified by FETCHING the live surface and quoting the rendered values -- never the code, the file on origin, or the deploy log; (3) R14 holds independently: a financial figure beside the stamp still carries its own settled/billed/banked basis, which a freshness stamp does not supply; (4) the stamp is DERIVED from the verifying run and never hand-set -- an R15 mutation proves a re-stamped stale run FAILS, because ''fake-fresh (re-stamping stale runs) remains the cardinal sin'' and the weekend''s four-times-republished figure is the ruling''s named counterexample.'
map_records:
  evidence:
  - docs/staging/done/DIRECTOR_RULING_PUBLISH_DECOUPLING_2026-08-10.md
