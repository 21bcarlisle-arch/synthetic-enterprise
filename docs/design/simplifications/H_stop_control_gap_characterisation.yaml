atom_id: H_stop_control_gap_characterisation
simplifications:
- '2026-08-03 MINTED into the map (worker tick, sole-map-writer) from docs/staging/in_progress/PLANNER_MINTED_stop_control_gap_characterisation_2026-07-28.md. Parked behind director_build_open (abolished 2026-07-29, swept 2026-08-03); registered here per R17. loop_stage is DISCOVER, not build, and that is not a park: the deliverable IS the characterisation -- an enumeration of every stop affordance (Stop hook, TaskStop, run/hold flags, tree_lock, systemd units, the publish gate) against what it actually halts and what keeps running behind it. NO MODEL CHANGE; this is operational-safety LEGIBILITY.'
- '2026-08-03 MINT -- WHY IT IS REAL WORK AND NOT PAPERWORK: this machine has already demonstrated that stopping is harder than it looks -- pkill -f patterns that killed the worker''s own gate, bounded workers holding a lock for their lifetime, a wedge alert that clears only on a successful publish. Each of those is a stop affordance whose actual reach was discovered by accident, in production, at cost. R15 APPLIES EVEN TO A DISCOVER ARTEFACT (this is the OUTCOME-test form): a characterisation that lists only the stops that WORK has demonstrated nothing -- the artefact must name at least the known gaps above and be checkable against the live process set, so a later reader can falsify it by finding a stop it claims exists that does not. The moment it becomes a mechanism rather than a document, it must be able to FAIL.'
- '2026-08-03 BUILT L0->L2 (worker tick). The DISCOVER doc existed since 2026-07-28 but the level never moved, and the mint note said why it should not have: an inventory that only LISTS stops has demonstrated nothing. L2 is the falsifier -- background/stop_control_audit.py turns all 8 rows of STOP_CONTROL_GAP.md into machine-readable claims, each resolved against a source the registry does NOT control (real module source for symbols, the literal flag name in each claimed reader, `def <name>(` for every cited test, process_manifest.yaml''s declared state for what a control can actually reach). R15 both ways in tests/background/test_stop_control_audit.py, 30 tests: FAIL-OPEN killed (empty registry, or every row reclassified inert, is VACUOUS -- not a pass); FAIL-SILENT killed (missing/malformed doc or manifest, or a doc with no parseable verdict, RAISES -- an unavailable check is a failed check); TAUTOLOGY killed (test_dead_target_reads_the_manifest_not_the_registry flips the MANIFEST while holding the registry fixed and the verdict changes, which registry-vs-registry could not do).'
- '2026-08-03 IT FIRED ON REAL STATE, first run -- this is the evidence the level rests on, not a synthetic mutation. Inventory row 6 claimed `.comment_intake_locked` in background/director_comments.py as an existing director-only halt flag (cited from user memory, and explicitly marked "not audited in this pass"). All three oracles refute it: the channel was RETIRED PERMANENTLY 2026-07-24, that module''s intake path is deleted and main() is a safe no-op, NO source in the repo references .comment_intake_locked at all, and process_manifest.yaml declares director-comments state=retired. A lock over a process that must never run halts nothing = DEAD_TARGET. Row reclassified in the doc (kept, not deleted) and the original claim pinned by test_control_over_a_retired_process_is_a_dead_target so it cannot be re-asserted silently. This is the "live mechanism with a DEAD input" class, caught by mechanism instead of by accident.'
- '2026-08-03 RELEASE SIDE (R11, no orphan transition): the residual is now MEASURED, not asserted. The doc''s headline "Coverage verdict: PARTIAL" is re-derived every run from every live control''s reach + mid_flight, so if anyone ever builds the section-4 control (window-reachable + mid-flight), the derived verdict becomes MET and the doc FAILS its own audit as VERDICT_STALE. The characterisation cannot outlive its own gap being closed. Residual TODAY, unchanged and still open: 2 live stop controls (#1 .build_executor_enabled, #2 .sim_runner_hold), both console-only, both cycle-boundary -- no director-window-reachable and no mid-flight stop exists anywhere.'
- '2026-08-03 BLOCK PREMISE DEAD, recorded in the doc rather than silently dropped. Both the mint and section 5 of the doc escalate the section-4 BUILD as "category-5 safety-control work, director_build_open, console-only, never self-authorised". Both halves are gone: director_build_open was ABOLISHED 2026-07-29 and swept 2026-08-03 (PROPOSE, RECORD, ACT), and the reserved list is now four real-world classes of which this is none -- CLAUDE.md states that a "safety control" stopping a SIMULATION is not one of them, and the only reserved real-world control is the agent''s own sandbox profile, which a stop control does not touch (it NARROWS what this machine can do). Section 5 is kept and marked SUPERSEDED with the reasoning, so a later reader cannot re-derive the dead gate from it. The section-4 build is drawable now; it is not built here because THIS atom is the characterisation -- unblocked, not reserved.'
map_records:
  evidence:
  - docs/staging/in_progress/PLANNER_MINTED_stop_control_gap_characterisation_2026-07-28.md
  - docs/design/STOP_CONTROL_GAP.md
  - background/stop_control_audit.py
  - tests/background/test_stop_control_audit.py
