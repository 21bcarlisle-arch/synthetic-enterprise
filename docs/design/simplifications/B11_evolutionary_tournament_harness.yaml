atom_id: B11_evolutionary_tournament_harness
simplifications:
- '2026-07-29 REGISTERED into the map (worker tick) from docs/design/BACKLOG.md item B11 on the director''s from_rich_20260729_173731 instruction (full quote + no-blocked_on rationale in B3''s note). THE VALUES/MECHANISM SPLIT IS THE WHOLE REASON THIS CAN BE REGISTERED AT ALL, and it is a WALL, not a dial: A5_tournament_fitness_mortality (idle, L0/1) already holds the fitness function + mortality RULES, which are category-6 director-reserved ("values decisions defining what the company is FOR" -- the fronts.yaml values_decisions gate names A5 explicitly), and A4_sim_approver holds the policy-agent approver. This atom is ONLY the harness that RUNS a tournament given a fitness function it does not author: rerun, score, rank, remove, publish. It must therefore READ the fitness definition from A5''s artefact and must never define or adjust one -- the R15-failable control is a test proving the harness REFUSES to run when no ratified fitness definition exists (fail-closed), so it can never quietly supply its own. depends_on SPINE_1: a tournament needs a scenario set to rerun across, per BACKLOG''s own "only worth running once A-D give it worlds to die in". Epoch 4 (fitness/mortality) means BUILD stays epoch-gated by the fronts epoch_ceiling of 3 -- correctly, and that gate is what holds it, not an over-broad blocked_on that would also hide the DISCOVER/FRAME work. A8_experiment_loop_speed (idle, L2/3) is the inner-loop-speed prerequisite that makes a tournament arithmetically feasible -- named here rather than made a hard dependency, since DISCOVER/FRAME on the harness shape does not need it. No BUILD code, level_current 0.'
- '2026-08-03 FRAME SATURATED (worker tick, L3 DISCOVER/FRAME draw; NO build code written -- epoch-4 gated). THE RESIDUAL WAS BOOKKEEPING, NOT THINKING: docs/design/B11_EVOLUTIONARY_TOURNAMENT_HARNESS_FRAME.md (24.8KB, 2026-07-29) already holds a complete DISCOVER+FRAME, and its own 2.10 says saturation comes from citing it in this atom''s evidence list -- but evidence still read [docs/design/BACKLOG.md] alone and frame_saturated was unset, so the atom kept re-drawing for FRAME work that was already done. Cited + flagged now. RE-VERIFIED, NOT RE-STAMPED (all four load-bearing claims checked against disk this tick): (1) background/tournament_harness.py and tests/background/test_tournament_harness.py still absent -- confirmed; (2) tools/tournament_runner.py exists and its score_field default is still the agent-chosen "total_net_gbp" at line 511 -- the exact seam B11 must wrap deliberately rather than call through, WALL risk #1 stands; (3) sim/scenario/spine.py IS on main (commits e0a7ba151 + 90cd95039) exposing ScenarioSpine/load_world/available_worlds/rotation_set/select_for_rotation, available_worlds() = [crisis_2021_22, history_replay, neso_central, supply_glut], but rotation_set() returns EMPTY -- every curriculum world is provenance:proposal, ratified:false, in_rotation:false, true_probability:null; (4) no ratified A5 fitness artefact exists (company/regulatory/supplier_fitness_register.py is Ofgem SUPPLIER fitness, a different thing; A5_TOURNAMENT_FITNESS_MORTALITY_FRAME.md is a FRAME, not a ratified artefact). ONE DISCREPANCY FOUND AND RECORDED rather than passed over: the FRAME describes tools/tournament_runner.py as "2,796 lines fetched in full"; the file is 796 lines. Not load-bearing (every cited line number and the score_field default check out) but the doc''s own figure is wrong. SO THE REAL BLOCKER IS NAMED AND IT IS NOT A PERMISSION: a tournament with an empty rotation set and no ratified fitness definition has nothing to run and nothing to score by. Both inputs are director-reserved VALUES (R13 curriculum; A5 fitness/mortality) -- genuinely absent inputs, not a gate anyone needs to open. Two-way-door filter therefore holds BUILD, plus the open question the FRAME leaves honestly unresolved: what a "company configuration" actually IS has no typed representation anywhere yet. SIDE FINDING, FLAGGED NOT FIXED (belongs to its own draw, and re-stamping another atom''s cell from outside it is the exact defect the re-verify rule names): SPINE_1_scenario_world_state reads level_current 0 / loop_stage idle while its substrate is demonstrably built and on main -- the abolished-block stale-cell class.'
map_records:
  evidence:
  - docs/design/BACKLOG.md
  - docs/design/B11_EVOLUTIONARY_TOURNAMENT_HARNESS_FRAME.md
