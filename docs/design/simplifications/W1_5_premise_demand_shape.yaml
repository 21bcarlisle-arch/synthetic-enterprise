atom_id: W1_5_premise_demand_shape
simplifications:
- '2026-07-13 REGISTERED (WEATHER_PHYSICS_HIERARCHY.md, director P1, QUEUE -- DISCOVER/FRAME workable now, BUILD gated to Epoch 3 with W1_2_generate_futures). L3, conditional on L2. Layered randomness (NOT flat): national signal + correlated regional deviation + premise-specific weather response + idiosyncratic noise. AGGREGATION-CONSISTENCY invariant #2: summed+scaled premise demand must reconcile to national demand -- if the premises do not add up to the country the physics is wrong. Mutation-tested (R15) same as L2. Couples to the customer archetypes (W2 cluster) for thermal/occupancy characteristics. loop_stage idle: FRAME design authored this session (docs/design/WEATHER_PHYSICS_HIERARCHY_DESIGN.md); BUILD opens in Epoch 3. Anchoring rule (anti-marking-own-homework): GENERATOR anchors (real historical weather/demand) and VALIDATOR anchors (independent published sub-national consumption stats / regional degree-days / published system demand) MUST be different sources; the company never validates against SIM ground truth.'
- '2026-07-15 DISCOVER pass (self-refill draw, DISCOVER/FRAME only, no BUILD code). Checked for existing premise-level demand: simulation/household.py is the real per-premise unit today, but the L3 target here -- each property''s half-hourly demand as f(LOCAL weather, thermal performance, heating type, occupancy, archetype) reconciling to national by an aggregation-consistency invariant -- is NOT yet built as a reconciled shape (the household model is not driven off a W1_4 regional-local weather field, and no summed+scaled-premise == national invariant exists, mirroring the W1_4 gap one level down). HARD DEPENDENCY confirmed real: this atom genuinely CANNOT be built before W1_4''s reconciliation invariant exists (depends_on: [W1_4_regional_weather_field] is correct), because premise reconciliation is defined relative to the regional field W1_4 must first make aggregation-consistent. Unlike W1_3/W1_4, this atom is genuinely closer to greenfield at the reconciled-shape level -- level_current=0 is DEFENSIBLE here, NOT mis-registered. No level change, no BUILD code.'
- '2026-07-16 FRAME (H17 Lane-3): canonical per-atom FRAME terminus authored (premise-demand decomposition + national-reconciliation invariant + coupled-triad gap to C13); BUILD deferred (idle/epoch-gated, awaits W1_4).'
- '2026-07-20 VERIFY (evidence-grounded, like the W1_3/W1_4 re-levels): the premise-demand mechanism ALREADY EXISTS, under-registered at L0. simulation/demand_model.py::build_demand_shape computes each property half-hourly demand = f(weather via heating/cooling degree-days, heating_system per-degree-day coefficients, occupancy_pattern via occupancy_multiplier, archetype assets EV + solar self-gen) over a 48-period base shape -- exactly the W1_5 DoD decomposition; wired into the demand/run path, 20 green tests (tests/simulation/test_demand_model.py). L0->L1 PROPOSED (built in any form; level_current NOT moved). HONEST REMAINING GAPS: (1) build_demand_shape uses NATIONAL mean_temp_c, NOT the LOCAL per-property weather the W1_4 regional field now provides -- the W1_4->W1_5 coupling is UNWIRED (a real build with blast radius on the demand path); (2) the national-reconciliation invariant is not mechanised; (3) idiosyncratic per-property noise; (4) the coupled-triad gap to C13. No code changed (VERIFY only).'
- '2026-07-21 L1->L2 BUILD LANDED (worker tick, level HELD at 1 -> L2 PROPOSED; not self-promoted, levels are proposals). Closed the two load-bearing L2 gaps the 2026-07-20 VERIFY named: (1) premise demand now responds to LOCAL weather -- new simulation/premise_demand.py::premise_demand_shape drives each premise off national_temp + its W1_4 regional deviation (local_mean_temp_c), a PURE ADDITIVE forward extension (deviation=0 is byte-identical to the shipped build_demand_shape, zero blast radius; the 20 existing demand-model tests stay green); (2) the national-reconciliation invariant is mechanised and R15-FAILABLE -- aggregate_reconciles compares the demand-weighted premise aggregate to an INDEPENDENT national reference (each region priced at national temp, NOT the premise sum = TAUTOLOGY guard), passing for the real W1_4-reconciling field and FIRING (mutation-proven) when a region is perturbed off-manifold or the reference is wrong; empty/zero inputs raise (FAIL-OPEN guard). Shows the real physics: dispersion across the convex HDD kink makes the aggregate exceed at-mean demand (Jensen) -- a small within-tol residual = the winter-risk under-pricing the invariant now makes visible; reconcile_to_national supplies the summed+SCALED enforcement step. 13 new tests (tests/simulation/test_premise_demand.py), epistemic PASS. R10 SIMPLIFICATION (deferred, not faked): idiosyncratic per-property noise (gap 3) NOT added -- needs its own C-S2 RNG substream, registered as remaining L2->L3 work alongside the coupled-triad gap. L2->L3 remainder (director-gated, batched): the coupled-triad company-vs-truth gap to C13 (C13 idle/L0, and _AUTHORITATIVE_COUPLING still lacks W1_5<->C13 -- register when C13 opens for BUILD) + Expert-Hour + director level-up ratification. blocked_on: director_level_up SET to stop the build-redraw of landed L1->L2 work (same dial-defect fix as W1_7).'
- '2026-07-21 L2->L3 BUILD LANDED (worker tick, SIM_ACTORS front OPEN + epoch 3 == ceiling + loop_stage already build; level HELD at 2 -> L3 PROPOSED, NOT self-promoted -- levels are proposals). The L3 WALL was already cleared: the coupled-triad company-vs-truth gap is MEASURED -- _AUTHORITATIVE_COUPLING carries W1_5<->C13 and coupled_gap_ledger#W1_5_premise_demand_shape holds the L1-vs-L2 comparison, C13 having been built to L2 RATIFIED (c22bed73d). So this pass builds the ONE remaining agent-buildable L2->L3 DoD term the 2026-07-21 L1->L2 entry deferred (R10, not faked): idiosyncratic per-property noise. Mechanism (simulation/premise_demand.py, +~110 lines, PURELY ADDITIVE): idiosyncratic_factor(premise_id, seed, cv) draws a MEAN-1, strictly-positive lognormal (log-mean = -sigma^2/2 so E[factor]==1 exactly) from this atom''s OWN named C-S2 substream (STREAM_NAME=W1_5_premise_demand_shape, salt ''idiosyncratic'', _substream = sha256(name::salt::base_seed), _base_seed_for mixes premise_id with any explicit book seed) -- deterministic per (premise_id, seed) on replay, isolated so a draw here can never shift another subsystem''s sequence (the RNG-substream discipline, idiom copied from household_budget). Wired into premise_demand_shape via optional idiosyncratic_factor=1.0, which is BYTE-IDENTICAL to the L2 path (x*1.0==x; the 13 L2 + 20 demand_model tests stay green, zero blast radius). AGGREGATION-CONSISTENCY PRESERVED BY CONSTRUCTION (the whole reason the noise is mean-1): the demand-weighted mean factor of a book converges to 1.0 as the book grows (LLN), so the noised aggregate reconciles to national exactly as the noise-free one -- proven by test_mean_factor_converges_to_one_over_a_large_book (5000-premise book, mean factor 1.0 +/- 0.01). R15 FAILABLE control noise_is_unbiased: True iff the demand-weighted mean factor is within NOISE_BIAS_TOL(=0.02) of 1.0; PASSES the real mean-1 draw, FIRES (mutation-proven, test_noise_is_unbiased_FIRES_on_biased_factors monkeypatches a +10% systematic bias -> False) because a biased factor would silently inflate/deflate national demand, and RAISES on an empty book (FAIL-OPEN guard) -- covers all three R15 killer patterns. 12 new tests (32 total tests/simulation/test_premise_demand.py), demand_model 20 green, coupled-triad 24 green (tests/test_weather_demand_triad.py + tests/company/test_weather_normalisation_belief.py), epistemic verifier PASS (521 files). R10/R12 SIMPLIFICATION (declared, not fabricated): the noise CV=0.15 is a conventional diagnostic band, NOT a fitted truth -- real premise-level metered dispersion needs half-hourly smart-meter/settlement panel data unavailable this session; flagged for calibration. The mean-preservation (and thus the invariant) is independent of the CV value chosen. L2->L3 remainder now WALLED only: director Expert-Hour + director L3 level-up ratification. blocked_on: director_level_up SET to stop the dial-weighted build-redraw of landed L2->L3 work (same dial-defect fix as W1_4/W1_7). No level self-promotion, file_scope unchanged.'
