atom_id: EP6_wall_protocol_typing
simplifications:
- "2026-08-21 pass 53 LAND, level HELD at 2, loop_stage stays build. PASS 52 WROTE 675 LINES ACROSS BOTH SIDES OF THE WALL, TESTED NONE OF IT, AND COMMITTED NONE OF IT -- THE FOURTH PASS RUNNING. The tick drew EP6 and found five files dirty against HEAD carrying an entire enrolment exchange -- the company's `FlexEnrolmentBook`, the venue's `VenueRegistrations`/`answer_enrolment`, a seam bumped to v2 with a new payload type, and the census re-pinned to match -- with NOT ONE test file touched. Commits 131b86df7, 21c286c15 and 4e8253db9 wrote this same sentence about passes 43, 44-45 and 46. This pass is the landing. Everything below is observed-with-evidence unless labelled inferred (R9). MEASURED AT: HEAD 6f667b545.\n\n  WHAT THE UNLANDED WORK ACTUALLY WAS, verified at the tree rather than read off a row -- pass 52 left NO record at all, which is the inverse of this atom's usual failure and is worse in one specific way: there was no claim to check, so nothing but reading 675 lines could say what it did. It is the repair pass 50 recommended in its own closing line: FRAME THE FLEX CROSSING. `CompanyVenueOffer` has always been the company's own participation decision and `WallRequest[FlexEnrolment]` has been declared since the seam was written, and NOTHING IN THIS BUILD EVER JOINED THEM -- so every dispatch instruction and settlement line arrived as an answer to a question no company had asked, and the venue dispatched a unit nobody had registered.\n\n  THE INDEPENDENT INSTRUMENT CONFIRMS IT, and this is the difference between this pass and pass 43, where the census structurally could not see the leg. `tools/wall_channel_census.py`'s LEG MODEL IS UNEDITED HERE -- the only census change in this commit is the SURFACE_PINS re-pin -- so its reading is an outside verdict rather than my own. Measured in a DETACHED GIT WORKTREE at HEAD (a tar extract fails the census closed: it needs a real repo, and it says so rather than passing): HEAD reads `[request=dormant, response=wire]`, `declares a request leg this build never sends, so it is not scored`, and a standing RED -- `UNSOLICITED INBOUND: the response role is live and NOTHING IN THIS BUILD EVER ASKS`. With this commit it reads `[request=wire, response=wire]`, `both roles live`, and THE RED IS GONE. One standing census red cleared, on the seam pass 50 named.\n\n  A MEASUREMENT I RETRACTED BEFORE RECORDING IT, because it is this project's own two-trees defect and I nearly published it. Running the census from the WORKTREE against HEAD's sources reported the flex seam `UNPINNED: declares version 1, which SURFACE_PINS has no entry for` -- which would have read as a second red cleared. It is an ARTEFACT: the tool half came from my worktree (pin table already at v2) and the subject half from HEAD (seam still v1), so the two halves read two different trees. In the detached worktree HEAD pins correctly at `v1 = ea1b70a309a2a3e4`. Nothing was wrong at HEAD and no second red was cleared. Recorded because the false version of this sentence is the more flattering one.\n\n  WHAT THIS PASS ADDED, and it is the whole reason the pass exists: 27 TESTS ACROSS THREE FILES, where pass 52 wrote none. Placed in the files whose STEMS match the three changed production modules, because this project's gate selects tests by filename stem -- a new test file named for the exchange would not have been selected by a commit to `flex_participation.py`. The round trip is driven END TO END, company encoder to VENUE codec to company reader, never with a stubbed reply: a company-side test that asserted this module agrees with a fixture I wrote is the R15 TAUTOLOGY for the one question a seam is asked.\n\n  LANDED: `FlexEnrolmentBook` in `company/market/flex_participation.py`\n\n  LANDED: `observe_outcome` in `company/market/flex_participation.py`\n\n  LANDED: `encode_enrolment_payload` in `company/market/flex_participation.py`\n\n  LANDED: `MisroutedOutcome` in `company/market/flex_participation.py`\n\n  LANDED: `FlexEnrolmentRefused` in `company/market/flex_participation.py`\n\n  LANDED: `answer_enrolment` in `sim/flex_dispatch.py`\n\n  LANDED: `VenueRegistrations` in `sim/flex_dispatch.py`\n\n  LANDED: `decode_request` in `sim/flex_dispatch.py`\n\n  LANDED: `EnrolmentRefused` in `sim/flex_dispatch.py`\n\n  LANDED: `FlexEnrolmentOutcome` in `interface/contracts/flex_observable_seam.py`\n\n  LANDED: `ENROLMENT_REFUSAL_CODES` in `interface/contracts/flex_observable_seam.py`\n\n  LANDED: `REQUEST_PAYLOAD_FIELDS` in `interface/contracts/flex_observable_seam.py`\n\n  LANDED: `test_the_full_enrolment_ROUND_TRIP_crosses_the_wall_and_comes_back_registered` in `tests/company/test_flex_participation.py`\n\n  LANDED: `test_MUTATION_an_outcome_on_a_correlation_id_this_company_NEVER_SUBMITTED_is_refused` in `tests/company/test_flex_participation.py`\n\n  LANDED: `test_C_S2_a_RESENT_enrolment_returns_the_SAME_reference_and_does_not_mint_a_second` in `tests/sim/test_flex_dispatch.py`\n\n  LANDED: `test_MUTATION_the_venue_judges_the_window_on_ITS_OWN_clock_not_the_senders` in `tests/sim/test_flex_dispatch.py`\n\n  LANDED: `test_MUTATION_a_field_added_to_the_payload_and_not_DECLARED_is_caught` in `tests/interface/test_flex_observable_seam.py`\n\n  R15, THREE PRODUCTION MUTATIONS RUN AT THIS TICK, each against a tree materialised by `git archive` OUTSIDE the repo with only the mutated file swapped -- never on the shared worktree, which carried 190 other-lane modifications and a 352-test suite in flight throughout. Each redded EXACTLY its named test against a 56-passing null control, so none is always-red. (1) `submitted is None` -> `False` in `observe_outcome`, i.e. the register is not consulted: reds `test_MUTATION_an_outcome_on_a_correlation_id_this_company_NEVER_SUBMITTED_is_refused` and nothing else -- the refusal is keyed on the company's OWN book and not on anything about the message. (2) The venue's idempotency short-circuit removed so a resend mints a second reference: reds the C-S2 test alone. (3) The overlap predicate collapsed from half-open to closed (`<` -> `<=`): reds the boundary test alone, which is the point of asserting BOTH directions there -- a registration ending exactly when another starts is two consecutive availability periods, not a double sale, and that boundary is the only value this rule is easy to be wrong about.\n\n  A MUTATION THAT WAS CAUGHT BY THE WRONG BELT, fixed in the writing and recorded because it is this project's stacked-belts class met live. The misrouted-acceptance test first mutated `answer['payload']['unit_id']`, and it passed -- on the PAYLOAD KEY-SET belt one layer earlier (`UNKNOWN_FIELD`), never reaching the `MisroutedOutcome` refusal it names. A bare `raises` would have called that a pass. The mutation now goes inside `fields`, and the refusals in this battery are asserted by REASON CODE rather than by exception type wherever two belts could both fire.\n\n  THE NULL CONTROLS, said explicitly because refusal tests without them prove only that a seam is broken: the full round trip returns the venue's own minted reference (`DFS_TURN_DOWN-REG-000001`); the forged-id answer is ACCEPTED byte-for-byte by the book that really submitted it; a butt-jointed window is accepted where an overlapping one is refused; the same unit MAY enrol at a different venue over the same window (overlap is scoped by venue deliberately, because a multi-venue book is the legitimate stacking case); and the clock mutation's third assertion is an OPEN window accepted by the same venue at the same clock -- without which the two refusal assertions would also hold of a venue that refused everything.\n\n  WHAT DID NOT CHANGE, said plainly. NO PUBLISHED FIGURE MOVED and none was recomputed; nothing in this pass is wired into a live run. `FlexEnrolmentBook` has NO PRODUCTION CALLER -- it is a primitive with a proven round trip and no scenario driving it, which is the same shape as pass 43's `missing_sequences` and is stated the same way rather than counted as traffic. Wiring it into the live flex path is the next item and is NOT claimed here.\n\n  EVIDENCE: tests/company/test_flex_participation.py 14 passed (7 at HEAD); tests/sim/test_flex_dispatch.py 32 passed (26 at HEAD); tests/interface/test_flex_observable_seam.py 11 passed (5 at HEAD); 352 passed across the downstream consumers (tests/interface, the stacked-dispatch, passthrough, phase41a, flex-revenue and crossing-conversation suites); the three mutations above with the figures given; `python3 -m tools.epistemic_verifier` PASS on the one changed company file; `python3 -m tools.wall_crossing_dispositions` OK -- 6 live crossings, 91 ruled, and NO new wall crossing created (the company module imports `company.interfaces.crossing_conversation`, which is company-side; the venue codec imports no company module, asserted by a test); `python3 -m tools.wall_channel_census --worktree` every frozen channel matches its list exactly.\n\n  WHY THE LEVEL STILL DOES NOT MOVE, unchanged from passes 38-52 and not re-opinionated here: Q9 and Q15 need an act in a RESERVED class (contacting real organisations, spending real money), so level_target 3 is unreachable in this epoch whatever is built. `tools.discovery_pass_ceiling` says so and says explicitly NOT to record `infeasible_here`, because that verdict would retire the payable half.\n\n  MAP EDIT: `file_scope` gains the four files this pass's work lives in that the atom did not declare -- interface/contracts/flex_observable_seam.py, tests/company/test_flex_participation.py, tests/sim/test_flex_dispatch.py, tests/interface/test_flex_observable_seam.py. The seam contract is the clearest instance of this atom's own recorded 'landable set is wider than the file_scope' class: the flex seam's own contract was owned by no atom while two atoms edited it. Same reading passes 35, 36, 42, 43, 44 and 45 used: file_scope declares which files an atom owns, changes no difficulty, sets no target and tunes no output.\n\n  WHAT REMAINS. The ordering is read off the rows rather than invented here. (1) Wire `FlexEnrolmentBook` into the live flex path, so the enrolment exchange has a scenario and not only a unit proof -- the smallest item with a measurable consequence. (2) Q3's BRANCHING conversation (a switch with objection is an alternative continuation, not a later position on one path), the larger item pass 44 and 47 both left. (3) The census's LEG_ENVELOPE_NAMES third/fourth-leg blindness, filed by pass 48 and 50 and still filed. THE LESSON THIS PASS IS FOR, and it is now four passes deep: this atom keeps producing work that is sound and invisible. Pass 45 found three real defects in the gap between pass 44's record and its commit; pass 47 found two false measurements in pass 46's; this pass found 675 lines with no control of any kind in pass 52's. The code was good each time. What is missing each time is the part that makes it checkable by someone who was not there."
- "2026-08-21 pass 53 ADDENDUM, written after the gate refused the landing above and appended rather than folded into it, because this store is append-only and a row that quietly absorbs its own correction teaches nothing. THE GATE CAUGHT THREE THINGS MY OWN SUITE RUN COULD NOT, and one of them was mine.\n\n  REFUSAL 1, AND IT WAS MY OWN TEST POISONING TWENTY-TWO OTHERS. `test_the_declared_request_key_set_is_what_the_venue_refuses_against` proved the venue decodes against the CONTRACT by calling `importlib.reload(sim.flex_dispatch)` under a widened declaration. Reload REDEFINES every class in the module, so the 22 tests in `tests/sim/test_flex_dispatch_stacked.py` that had already imported the old classes failed `isinstance` against the new ones. IT PASSED WHEN THE FILE RAN ALONE and redded the gate -- which is precisely this project's own recorded rule, never mutate a shared module while a suite is in flight, produced by me inside the very battery I was writing to prove the work was controlled. Repaired by mutating the module's own binding via `monkeypatch` instead: same claim, same both-direction proof, no reload. The null control (a well-formed wire accepted against the real declaration) is unchanged, and both files now pass in ONE session -- 60 tests -- which is the only run that could ever have shown this.\n\n  REFUSAL 2, A CONTROL REDDING ON ITS OWN SUCCESS CASE, and its author predicted it in writing. `test_THE_LIVE_WALL_IS_UNSOLICITED_ON_EXACTLY_THE_TWO_SEAMS_THE_WALK_NAMED` pinned the conversant set as EQUAL to two named seams. Clearing flex made it three and redded the test -- this project's recorded COUNT-PINNED class, where the number was never the law but the size of the backlog the day it was written. Its docstring says exactly what to do: \"WHEN THIS GOES GREEN the assertion below fails, and that is the correct moment to move the check into the CLI's return and this test into `assert verdict.ok`.\"\n\n  SO THE CHECK IS NOW ARMED, and this is the substantive thing the record above does not contain. Measured first, not assumed: `seam_conversation_conformance_at(worktree=True)` reports ok=True, conversant = all three scored seams, and unsolicited/unanswered/silent all EMPTY. Three separate comments in `tools/wall_channel_census.py` each promised the check \"becomes part of this return the day `.ok` is True on HEAD\", and the flex request leg was the last thing standing between the wall and that day. `conversations.ok` is now in the CLI's return, so a seam that asks into nothing REDS rather than printing a paragraph nobody must read. The sibling test that asserted the check must NOT be in the exit code is FLIPPED rather than deleted -- its useful half was never \"it is unarmed\" but \"the reader and the exit code agree about whether this control counts\" -- and it now fails if the check is ever disarmed. This is R11's no-orphan-transitions rule paid: the repair had a release, and the release does something.\n\n  LANDED: `conversations.ok` in `tools/wall_channel_census.py`\n\n  LANDED: `test_the_conversation_check_is_REPORTED_by_the_CLI_and_deliberately_NOT_in_its_exit_code` in `tests/tools/test_wall_channel_census.py`\n\n  REFUSAL 3, THE STORE COUNT. The append above rolled the store (a two-file atomic write, live file plus `archive/EP6_wall_protocol_typing.038.yaml`) and the map's `simplifications_count` still declared 47. The count invariant is TREE-WIDE, so this one atom's stale number refuses every lane's commit, not just mine. Corrected to 48, and to 49 by this entry.\n\n  EVIDENCE FOR THIS ADDENDUM: tests/sim/test_flex_dispatch.py + tests/sim/test_flex_dispatch_stacked.py 60 passed IN ONE SESSION (the run that exposes the reload defect; separately they were 32 and 22-failing); tests/tools/test_wall_channel_census.py 207 passed; tests/design/test_simplifications_store.py 41 passed; ruff clean on every changed file.\n\n  THE LESSON, and it is the same one the record above draws about pass 52 turned on myself. I wrote a battery to prove that untested work was now controlled, and the battery contained a defect that only a run I had not done could see. Pass 52's failure was writing code with no controls; mine was trusting a green file-scoped run as if it were a green suite. The gate is what closed both, which is the argument for never landing around it."
- "2026-08-21 pass 54 LAND, level HELD at 2, loop_stage stays build. THE EXCHANGE PASS 53 LANDED HAD NO PRODUCTION CALLER, AND THE DOOR THAT SHOULD HAVE BEEN IT WAS A NEAR-COPY THAT CROSSED NOTHING. Pass 53's own closing line named this as the next item and did not claim it. Everything below is observed-with-evidence unless labelled inferred (R9). MEASURED AT: HEAD 72d628afd.\n\n  WHAT WAS ACTUALLY WRONG, and it is one level up from where pass 53 left it. `SimInterface.enrol_flex` is the ONE declared COMPANY -> WORLD crossing for flex, and it crossed nothing: `StubSimInterface` built a request envelope with its OWN private correlation-id grammar, appended it to `self._flex_enrolments` -- a list nothing in this build reads -- and handed the company back its own question; `LiveSimInterface` did not implement the method AT ALL, so a live company could not enrol into a flex venue. `sim/flex_dispatch.py`'s own registration-desk comment says this in as many words. NO TEST IN THIS REPOSITORY CALLED `enrol_flex`, which is why none of it was ever visible: the door was dark, not merely thin. Measured before touching anything: `grep -rn enrol_flex --include=*.py` outside the seam file returned one hit, a comment.\n\n  THE PRIVATE GRAMMAR WAS ALSO WRONG, and this is the part that would have gone on being wrong after any purely cosmetic wiring. The seam minted `flex-{unit}-{date}`, with NO VENUE IN THE KEY. One unit registered into two venues over one day -- the legitimate multi-venue book `dispatch_and_settle_stacked` models -- collides on a single correlation id, so the second venue's answer arrives on the first submission's id and `observe_outcome` checks it against the wrong offer. Reproduced as a mutation, not asserted: restoring that grammar reds two tests, one on each side, and the company ends the run holding ONE reference where it made two registrations.\n\n  WHAT THIS PASS DID. `FlexEnrolmentBook.submit_enrolment` gives the book a second DOOR into ONE minting site (the seam is handed a `FlexEnrolment` the caller already assembled; `submit` is handed an offer, and now delegates). `enrol_flex` on BOTH implementations routes through it: company encoder -> venue's independently-written decoder -> company outcome reader, returning the venue's `FlexEnrolmentOutcome` rather than the company's own envelope. `flex_enrolments` -- a list of things the company SAID, published as if it were a list of things that HAPPENED -- is REMOVED rather than kept alongside, and replaced by `flex_registrations` (references the venue minted) and `flex_awaiting_answer`.\n\n  THE VENUE CLOCK IS FAIL-CLOSED, and this is the one design decision in the pass that cost anything. The obvious convenience is to default the venue's read time to the submitter's `as_of`, which is exactly the defect `sim.flex_dispatch` names about itself: a WINDOW_ALREADY_CLOSED refusal keyed on a timestamp the SENDER controls is a refusal the sender can switch off. A seam built with no world clock therefore raises `FlexVenueUnreachable` -- an unavailable counterparty is a FAILED crossing, never a silently accepted registration (R15 fail-silent). The control proves both halves: the same already-closed window that the fallback would have accepted is REFUSED once a real world clock is present.\n\n  THE RUNNING LOOP NOW HAS A FIRST LEG. `background/flex_dispatch_triad.py::measure_l2` used to begin at a settlement statement: the world settled `FLEX_UNIT_1` -- a KEYWORD DEFAULT -- to a company that had never enrolled it, and the L2 gap was scored on that statement. It now registers through the production seam a day before the record opens, and reports `settlement_solicited`: the unit read off the DECODED settlement lines against the unit the VENUE said it registered. Two quantities from two sides, so an emitter that ignored its own `unit_id` argument cannot score solicited by agreeing with itself -- which is precisely the mutation that reds it.\n\n  NO PUBLISHED FIGURE MOVED, and it was measured rather than reasoned. HEAD's `measure_l2` was loaded from `git show HEAD:background/flex_dispatch_triad.py` into a module outside the package and run beside the new one over three parameter settings (no delivery model; a stochastic delivery model; a stochastic model with a biased baseline). All ten reported figures -- gap, gap_l1_belief, delivery_learning_gain, learned_delivery_ratio, baseline_error_frac, payment_at_risk_gbp, both revenue totals, both dispatch counts -- are IDENTICAL in every setting. A test pins the same claim inside the suite by recomputing the gap independently from truth and belief.\n\n  LANDED: `FlexVenueUnreachable` in `company/interfaces/sim_interface.py`\n\n  LANDED: `_FlexEnrolmentDesk` in `company/interfaces/sim_interface.py`\n\n  LANDED: `submit_enrolment` in `company/market/flex_participation.py`\n\n  LANDED: `solicit_registration` in `background/flex_dispatch_triad.py`\n\n  LANDED: `LOOP_UNIT_ID` in `background/flex_dispatch_triad.py`\n\n  LANDED: `test_the_seam_enrols_END_TO_END_and_hands_back_the_VENUES_OWN_reference` in `tests/company/interfaces/test_sim_interface.py`\n\n  LANDED: `test_MUTATION_a_seam_with_NO_VENUE_CLOCK_refuses_instead_of_using_the_SUBMITTERS` in `tests/company/interfaces/test_sim_interface.py`\n\n  LANDED: `test_ONE_unit_into_TWO_venues_over_ONE_day_gets_TWO_registrations` in `tests/company/interfaces/test_sim_interface.py`\n\n  LANDED: `test_the_LIVE_seam_runs_the_SAME_exchange_as_the_stub` in `tests/company/interfaces/test_sim_interface.py`\n\n  LANDED: `test_the_seam_publishes_no_door_onto_the_VENUES_OWN_book` in `tests/company/interfaces/test_sim_interface.py`\n\n  LANDED: `test_MUTATION_one_unit_in_TWO_VENUES_on_one_day_is_TWO_conversations` in `tests/company/test_flex_participation.py`\n\n  LANDED: `test_both_doors_into_the_book_mint_the_SAME_correlation_id` in `tests/company/test_flex_participation.py`\n\n  LANDED: `test_MUTATION_a_world_settling_a_unit_NOBODY_enrolled_scores_UNSOLICITED` in `tests/background/test_flex_dispatch_triad.py`\n\n  LANDED: `test_the_request_leg_does_not_touch_the_SCORE` in `tests/background/test_flex_dispatch_triad.py`\n\n  R15 EVIDENCE, three production mutations run in a detached git worktree at HEAD with this pass's six files copied in, each redding exactly its named test with the rest of the battery passing as the null control (the tree itself is the null: 72 passed unmutated). M1, the venue-clock guard replaced by a fallback to the sender's `as_of`: 1 failed, 33 passed. M2, the venue dropped from the correlation id (the seam's old private grammar restored): 2 failed -- one per side of the wall -- 48 passed. M3, the solicited check reading its own `LOOP_UNIT_ID` argument instead of the decoded settlement lines: 1 failed, 21 passed.\n\n  THE INDEPENDENT INSTRUMENTS, none of them edited here. `tools/wall_channel_census` reads channel C conversations 3 of 3 with the flex seam BOTH ROLES LIVE, surface pins 3 of 3 (flex still v2 = e9d4eb24a8d69735), and every frozen channel matching its list exactly -- no red opened and none newly cleared, which is the correct reading: pass 53 cleared the census's question, and this pass answers a question the census does not ask (whether the leg carries traffic). `tools.epistemic_verifier` PASS. `tools.company_network_isolation` still reports its four frozen routes and its R15 battery is 12 passed -- the new `company.interfaces.sim_interface -> sim.flex_dispatch` edge opens NO new route to an endpoint.\n\n  WHAT REMAINS, named and not claimed. (a) `measure` (L1) and `measure_l3` still begin at a dispatch instruction; only the L2 loop registers first. (b) The venue does not REFUSE to dispatch an unregistered unit -- `dispatch_and_settle` never consults `VenueRegistrations`, so the world's own teeth are still missing and `settlement_solicited` is a harness observation rather than a world law. That is the next item and it is a change to dispatch physics with several callers, which is why it is not folded in here. (c) The mock counterparty and the company's book live in one `_FlexEnrolmentDesk` object; the control for that is on the PUBLIC surface (nothing the company can reach hands out `VenueRegistrations`) rather than on process separation.\n\n  LEVEL HELD AT 2, and not re-litigated: pass 53 recorded that Q9 and Q15 need an act in a RESERVED class, so the target is unreachable in this epoch. This pass inherits that reading rather than re-deriving it."
- "2026-08-21 pass 55 LAND, level HELD at 2, loop_stage stays build. PASS 54 WROTE ITS RECORD AND COMMITTED NOTHING, THE FIFTH PASS RUNNING -- AND IS THE FIRST WHOSE RECORD SURVIVED RE-MEASUREMENT UNALTERED. The tick drew EP6 and found six files dirty against HEAD (three production, three test) carrying the flex enrolment seam wiring, plus a finished pass-54 row describing a tree no commit held. Commits 131b86df7, 21c286c15, 4e8253db9 and 6f667b545 each wrote that sentence about passes 43, 44-45, 46 and 52. This pass is the landing. Everything below is observed-with-evidence unless labelled inferred (R9). MEASURED AT: HEAD 72d628afd.\n\n  WHY THIS ROW EXISTS AT ALL, since it lands no capability and corrects nothing. The landing-tick discipline this atom built for itself -- re-run the record's own mutations before committing it -- has fired four times and reported a defect every time: pass 45 found three real defects in pass 44's gap, pass 47 found two false measurements in pass 46's, pass 53 found 675 lines with no control in pass 52's, and pass 53's own addendum found three more in mine. A mechanism whose only recorded outcome is FIRED cannot be told from one that always fires. This is its null control, and it is the reason to write it down rather than land silently: pass 54's numbers were checked by someone who was not there, and they were right.\n\n  THE THREE R15 MUTATIONS, RE-RUN AT THIS TICK, each in a detached git worktree at HEAD (`git worktree add --detach`, a real repo -- the census fails a tar extract closed and says so) with pass 54's six files copied in and only the named part swapped, never on the shared worktree, which carried 200+ other-lane modifications throughout. The unmutated tree is the null: 72 passed. M1, the venue-clock guard replaced by a fallback to the sender's `as_of`: 1 failed, 33 passed, redding exactly test_MUTATION_a_seam_with_NO_VENUE_CLOCK_refuses_instead_of_using_the_SUBMITTERS. M2, the venue dropped from the correlation id (the seam's old private grammar restored): 2 failed -- one per side of the wall -- 48 passed. M3, `settlement_solicited` reading its own `LOOP_UNIT_ID` argument instead of the decoded settlement lines: 1 failed, 21 passed. ALL THREE FIGURES MATCH PASS 54'S ROW EXACTLY, including which test reds and how many pass beside it.\n\n  THE NO-FIGURE-MOVED CLAIM, WHICH NO MUTATION CAN CHECK, VERIFIED INDEPENDENTLY AND IT IS STRONGER THAN CLAIMED. HEAD's `measure_l2` was loaded from `git show HEAD:` into a module outside the package and run beside the worktree's over pass 54's three settings (no delivery model; a stochastic delivery model; a stochastic model with a biased baseline). Pass 54 claimed ten identical figures; measured, all SIXTEEN shared keys are identical in every setting, no key was LOST, and the only new keys are the two this pass's subject adds (`enrolment_reference`, `settlement_solicited`). Run in the main repo deliberately and not in the detached tree: the tool halves differ but the DATA CACHE is one, which is the comparison the claim is about -- the detached tree carries no `sim/cache`, and a figure comparison across two caches would be this atom's own two-trees artefact, which pass 53 retracted before publishing.\n\n  A RED THAT IS NOT MINE, RESOLVED BEFORE REACTING TO IT. `python3 -m tools.company_network_isolation` exits 1 on this tree, reporting four company-side modules that can reach a real endpoint. That is the FROZEN FOUR the director's ruling names, not a new route: the same command exits 1 on a CLEAN HEAD tree, measured in the detached worktree with `git checkout -- .` first. The new `company.interfaces.sim_interface -> sim.flex_dispatch` edge opens nothing -- the venue codec shells no fetcher -- and the control's own R15 battery is 44 passed including test_MUTATION_a_new_route_fails_the_gate. Recorded because a bare exit code on a shared tree is the easiest thing on this project to misattribute to whatever lane read it last.\n\n  THE INSTRUMENTS, none of them edited here, so each reading is an outside verdict. `tools.epistemic_verifier --all` PASS on 544 files. `tools.wall_crossing_dispositions` OK -- 6 live crossings, 91 ruled (cut 85, owed 6, grandfathered 0), unchanged from pass 53, so this pass creates no wall crossing. tests/architecture/test_epistemic_wall_indirect_ratchet.py + test_epistemic_wall_ratchet.py + tests/tools/test_company_network_isolation.py 44 passed -- the indirect ratchet, the control that reds on a NEW indirect crossing, is green with its allowlist UNWIDENED. tests/design/test_simplifications_store.py + test_atom_notes_store.py 60 passed.\n\n  MAP EDIT AND THE COUNT, which is the control that refused pass 53. This append ROLLS the store (a two-file atomic write: the live file plus a new `archive/EP6_wall_protocol_typing.*.yaml` chunk), and the count invariant is TREE-WIDE, so a stale number here refuses every lane's commit and not only this one. Pass 54 already moved `simplifications_count` 49 -> 50 for its own row; this entry moves it to 51, and the archive chunks are committed in the same pathspec as the live file, because a roll landed half is two files disagreeing about one history.\n\n  WHAT DID NOT CHANGE. No published figure moved and none was recomputed -- verified above rather than asserted. No production line was written by this pass: the six files landing here are pass 54's, unmodified by me, and the only bytes this pass adds are this row and the count. THE LEVEL DOES NOT MOVE and is not re-litigated: Q9 and Q15 need an act in a RESERVED class, so level_target 3 is unreachable in this epoch whatever is built, and `tools.discovery_pass_ceiling` says explicitly NOT to record `infeasible_here` because that verdict would retire the payable half.\n\n  WHAT REMAINS, inherited from pass 54 and not re-opinionated. (a) `measure` (L1) and `measure_l3` still begin at a dispatch instruction; only the L2 loop registers first. (b) The venue does not REFUSE to dispatch an unregistered unit -- `dispatch_and_settle` never consults `VenueRegistrations`, so the world's own teeth are still missing and `settlement_solicited` is a harness observation rather than a world law. That is the next item and it is a change to dispatch physics with several callers. (c) Q3's BRANCHING conversation, the larger item passes 44, 47 and 53 all left. (d) The census's `LEG_ENVELOPE_NAMES` third/fourth-leg blindness, filed by passes 48 and 50 and still filed.\n\n  THE LESSON, and it is the inverse of the one this atom has drawn four times running. The gap between a record and its commit is where the defects live -- but the discipline that closes it is only worth its cost if it can come back empty, and this is the first tick that proves it can."
- "2026-08-21 pass 56 LAND, level HELD at 2, loop_stage stays build. PASS 55 WROTE ITSELF DOWN AS THE LANDING AND DID NOT COMMIT EITHER, WHICH MAKES SIX PASSES RUNNING -- AND ITS FIGURES, RE-MEASURED AT A NEW HEAD BY A SEAT THAT WAS NOT THERE, ARE IDENTICAL. The tick drew EP6 and found the same six files dirty against HEAD (three production, three test, 578 insertions / 39 deletions), the store rolled to archive chunk 039 with the roll uncommitted, the map count already moved to 51, and TWO finished rows -- pass 54's and pass 55's -- both describing a tree no commit held. This pass is the commit. Everything below is observed-with-evidence unless labelled inferred (R9). MEASURED AT: HEAD c43439739, one publish-provenance commit later than the HEAD pass 55 measured (72d628afd) and touching a single site data file, nothing in this atom's file_scope.\n\n\n  WHAT THIS PASS RE-MEASURED, AND WHY THESE AND NOT THE MUTATION BATTERY. Pass 55 was itself the landing-tick verifier of pass 54 and added no production byte, so re-running M1/M2/M3 would check pass 54's code against pass 54's claim a second time and say nothing about the only thing that has changed since: the HEAD underneath. What CAN move under a new HEAD is every instrument that reads the whole tree, so those are what this pass ran, and all four reproduce to the digit. The three file_scope test files: 72 passed -- the same number pass 55 reports as its unmutated null. `tools.wall_crossing_dispositions`: OK, 6 live crossings (4 direct, 2 indirect via a bridge package), 91 ruled (cut 85, owed 6, grandfathered 0), so the landing still creates no wall crossing and rules none away. `tools.epistemic_verifier --all`: PASS on 544 files. The store pair tests/design/test_simplifications_store.py + test_atom_notes_store.py: 60 passed.\n\n\n  THE ROLL LANDS WHOLE OR NOT AT ALL. Pass 55's append crossed ROLL_WATERMARK, so the live file and `archive/EP6_wall_protocol_typing.039.yaml` are one atomic write, and the count invariant reading them is TREE-WIDE -- half a roll refuses every lane's commit, not only this atom's. This row rolls the store again where the watermark demands it; every chunk moves in the same pathspec as the live file, and the map's `simplifications_count` moves in the same commit. The map edit landing here is pass 54's and pass 55's as well as mine: the file_scope widened by the four files pass 54 wrote in, and the count 49 -> 52.\n\n\n  WHAT DID NOT CHANGE. No production line in this commit is this pass's: the six files are pass 54's, unmodified by pass 55 and unmodified by me, and the only bytes pass 55 and pass 56 add between them are two rows and a count. No published figure moved and none was recomputed. THE LEVEL DOES NOT MOVE and is not re-litigated: Q9 and Q15 need an act in a RESERVED class, so level_target 3 is unreachable in this epoch whatever is built.\n\n\n  WHAT REMAINS, inherited unchanged and not re-opinionated. (a) `measure` (L1) and `measure_l3` still begin at a dispatch instruction; only the L2 loop registers first. (b) The venue does not REFUSE to dispatch an unregistered unit -- `dispatch_and_settle` never consults `VenueRegistrations` -- so `settlement_solicited` stays a harness observation rather than a world law. That is the next item and it is a change to dispatch physics with several callers. (c) Q3's BRANCHING conversation, left by passes 44, 47 and 53. (d) The census's `LEG_ENVELOPE_NAMES` third/fourth-leg blindness, filed by passes 48 and 50 and still filed.\n\n\n  THE LESSON, now measured twice rather than argued once. Pass 55's case for writing a row that lands no capability was that a landing-tick check whose only recorded outcome is FIRED cannot be told from one that always fires, and it supplied the first empty run. This is the second, at a different HEAD and from outside the build. The cost side is visible in the same place: six rows of prose have now been written about a body of work that took one commit to land, and the discipline only earns that cost while the commit follows in the SAME tick as the row. It did here."
- "2026-08-21 pass 57 BUILD, level HELD at 2, loop_stage stays build. THE WORLD COULD STILL DISPATCH A UNIT NOBODY HAD ENROLLED; ONLY THE HARNESS OBJECTED. Everything below is observed-with-evidence unless labelled inferred (R9). MEASURED AT: HEAD 7f94bae95, with all five touched files clean against HEAD before the first edit and still this lane's only modifications on them at landing.\n\n  WHICH ITEM, AND WHY IT WAS ALREADY CHOSEN FOR ME. Passes 54, 55 and 56 each closed with the same four-item remaining list and the same sentence about its second item: \"the venue does not REFUSE to dispatch an unregistered unit -- `dispatch_and_settle` never consults `VenueRegistrations`, so `settlement_solicited` stays a harness observation rather than a world law. That is the next item and it is a change to dispatch physics with several callers.\" Three passes named it and none took it, which is exactly the shape a deferred item takes on the way to becoming permanent. `python3 -m tools.discovery_pass_ceiling` still reports this atom in its fourth state -- THE TARGET IS UNREACHABLE HERE AND BUILD WORK REMAINS -- with five payable rows and an explicit instruction not to record `infeasible_here`, so the payable half was taken again.\n\n  THE DEFECT, STATED AS THE DIFFERENCE BETWEEN A REPORT AND A RULE. Pass 54 gave the L2 loop a first leg and scored `settlement_solicited`: the unit read off the DECODED settlement lines against the unit the venue said it registered. Two quantities from two sides, and it is a real check -- but it is an OBSERVATION made after the fact by the one layer allowed to see both sides, and the world went on being able to produce the statement being observed. Nothing in `sim/**` consulted `VenueRegistrations` before putting a line on the wire, so `unit_id` remained a KEYWORD DEFAULT and a unit that had never enrolled was still settleable; the only thing standing between this loop and a payment nobody had asked for was a harness that remembered to look. A gap the harness can only report is not a law the world obeys, and the next loop written -- L1 and L3 both, today -- simply would not look.\n\n  WHAT LANDED, AND THE FOUR DECISIONS INSIDE IT THAT COST SOMETHING. (1) CONTAINMENT, NOT OVERLAP. `VenueRegistrations.covers` asks whether a declared availability window CONTAINS the delivery window, where the pre-existing `overlapping` asks whether two registrations collide. They are different questions and the class now holds both: overlap is right for \"may I register this?\" and wrong for \"may I call this?\", because an instruction that starts inside availability and runs past its end calls the unit over minutes it never offered. The mutation collapsing one into the other is run below and is the plausible mistake rather than an invented one. (2) THE BOOK IS A REQUIRED KEYWORD on both wire legs, not a defaulted one. A `registrations=None` that skips the check is this project's own fail-open pattern -- the control passes for every caller who forgot the argument -- so the legs refuse to be called without it and a test asserts the TypeError. (3) THE CHECK READS WHAT WAS EMITTED, not what was passed in: `unit_id`, `venue` and both window bounds come off the constructed payload, so an emitter that ignored its own `unit_id` argument is caught by the same check that catches a stranger. (4) AN EMPTY BOOK REFUSES. `covers` returns None for a venue that has registered nobody, and None is a refusal; the fail-open case has its own test because that is where this class of control usually dies.\n\n  THE OWNERSHIP MOVE THAT MADE IT POSSIBLE, and it is the part with a portability argument rather than a defect behind it. The venue's `VenueRegistrations` was minted INSIDE `_FlexEnrolmentDesk`, a company module, and died with the seam object -- so the counterparty's state was born on the wrong side of the wall and nothing else in the world could consult it. It is now injectable (`build_sim_interface(flex_registrations=...)`), and the HARNESS -- the one layer that holds the world -- constructs it, hands it to the seam as counterparty state, and hands the SAME instance to the venue's dispatch legs. What the venue registered and what the venue will honour became one fact instead of two that happened to agree. Nothing on the company's surface changed: `test_the_seam_publishes_no_door_onto_the_VENUES_OWN_book` is untouched and green, and `flex_registrations=None` keeps the old behaviour for every caller with no world to speak of.\n\n  A CONTROL REDDING ON ITS OWN SUCCESS CASE, MET AGAIN AND FLIPPED RATHER THAN DELETED. `test_MUTATION_a_world_settling_a_unit_NOBODY_enrolled_scores_UNSOLICITED` asserted the harness SCORED a stranger's settlement unsolicited. With the world refusing, the stranger's lines never reach the wire to be scored, so the test reds -- this project's recorded class, and the correct move is the one pass 53's addendum used: its useful half was never \"the score is False\" but \"a unit nobody enrolled must not be settled\", and asserting the REFUSAL is strictly stronger than asserting the score. It is flipped. The observation is NOT thereby made unfalsifiable, which would be the quiet cost: a second test mutates the DECODER instead of the emitter (a mis-keyed feed, or the same statement read for the wrong party -- downstream of the law, which fires at emission) and `settlement_solicited` still scores False.\n\n  LANDED: `UnregisteredDispatch` in `sim/flex_dispatch.py`\n\n  LANDED: `covers` in `sim/flex_dispatch.py`\n\n  LANDED: `_require_registered` in `sim/flex_dispatch.py`\n\n  LANDED: `SolicitedRegistration` in `background/flex_dispatch_triad.py`\n\n  LANDED: `test_MUTATION_a_unit_this_venue_NEVER_REGISTERED_cannot_be_settled_at_all` in `tests/sim/test_flex_dispatch.py`\n\n  LANDED: `test_an_EMPTY_book_refuses_rather_than_passing__the_fail_open_case` in `tests/sim/test_flex_dispatch.py`\n\n  LANDED: `test_MUTATION_a_call_running_PAST_the_declared_availability_is_refused` in `tests/sim/test_flex_dispatch.py`\n\n  LANDED: `test_MUTATION_a_registration_at_ANOTHER_VENUE_does_not_license_this_one` in `tests/sim/test_flex_dispatch.py`\n\n  LANDED: `test_the_book_is_REQUIRED_and_not_a_defaulted_argument` in `tests/sim/test_flex_dispatch.py`\n\n  LANDED: `test_MUTATION_a_world_settling_a_unit_NOBODY_enrolled_is_now_REFUSED` in `tests/background/test_flex_dispatch_triad.py`\n\n  LANDED: `test_the_company_holds_the_SAME_reference_the_world_will_honour` in `tests/background/test_flex_dispatch_triad.py`\n\n  R15, FOUR PRODUCTION MUTATIONS RUN AT THIS TICK, each in a detached git worktree at HEAD (`git worktree add --detach`, a real repo) with this pass's five files copied in and only the named part swapped -- never on the shared worktree, which carried 200 other-lane modifications throughout. The unmutated tree is the null control: 62 passed, and it was re-run between mutations rather than assumed. M1, `covers` returns a reference unconditionally (the book is never consulted): 6 failed, 56 passed -- all four refusal tests plus the triad's flipped refusal and the same-book test, which is the correct blast radius for removing the consult itself. M2, containment collapsed to `overlapping`: 1 failed, 61 passed, redding exactly the boundary test. M3, `venue` dropped from the book's key so a Capacity Market registration licenses a Balancing Mechanism call: 1 failed, 61 passed, redding exactly its named test. M4, the fail-open shape -- `registrations` given a `None` default and the loop skipped when absent: 1 failed, 61 passed, redding exactly the required-keyword test. A NOTE ON M2, because the first version of it proved nothing and the correction is the lesson: the fixture originally shortened the registration by a WHOLE period, which leaves the last delivery window butt-jointed to the registration -- and overlap refuses a butt joint too, so the mutation passed. It is cut by HALF a period, which is the only shape where overlap and containment actually disagree. Every null control is stated with its test: the exactly-covering book accepts; the forged-venue book licenses the venue it was really made at; the same book returns None for a unit it never registered.\n\n  NO PUBLISHED FIGURE MOVED, measured rather than reasoned. HEAD's `measure_l2` was run in the detached worktree and this tree's beside it over three settings (no delivery model; a stochastic delivery model; a stochastic model with a biased baseline), with the SAME injected synthetic record on both sides -- injected deliberately, because a comparison across two data caches is this atom's own recorded two-trees artefact. All 15 reported keys per setting, 45 across the three, are identical; NO key was lost and NO key was added. Transport is not a belief and neither is a refusal that never fires.\n\n  THE INSTRUMENTS, none of them edited here, so each reading is an outside verdict. `tools.epistemic_verifier --all` PASS on 544 files. `tools.wall_crossing_dispositions` OK -- 6 live crossings (4 direct, 2 indirect), 91 ruled (cut 85, owed 6, grandfathered 0), unchanged from passes 53-56, so this pass creates no wall crossing and rules none away. `tools.company_network_isolation` exits 0 on its four frozen routes and its R15 battery, with the two epistemic-wall ratchets, is 44 passed with the allowlist unwidened -- the harness-to-world edge added here opens no route. `tools.wall_channel_census` reads IDENTICALLY at HEAD and on this tree on every channel this pass could touch; the two places the readings differ are both another lane's and are named rather than glossed: channel D/F move because `docs/reports/run_output_latest.json` is uncommitted in the shared tree (which is also what takes the census exit code to 1), and the `paid down on C_envelope (1)` line is present at a CLEAN HEAD too, so it was neither opened nor cleared here.\n\n  EVIDENCE: tests/sim/test_flex_dispatch.py 38 passed (32 at HEAD); tests/background/test_flex_dispatch_triad.py 24 passed (22 at HEAD); 219 passed across the downstream consumers (tests/interface, tests/company/interfaces/test_sim_interface.py, tests/company/test_flex_participation.py, tests/sim/test_flex_dispatch_stacked.py); the four mutations above with the figures given; ruff clean on all five changed files.\n\n  WHAT DID NOT CHANGE. `emit_dispatch_instructions_over_wire` is belted and still has NO production caller -- it is a leg with a control and no traffic, stated the same way pass 53 stated `FlexEnrolmentBook`, not counted as traffic. The in-process `emit_dispatch_instructions` / `emit_settlement_lines` are deliberately UNBELTED: they are constructors, they are what this module's own truth tests measure, and belting them would put the check on the object path as well as the wire without adding a refusal the wire does not already make.\n\n  WHY THE LEVEL STILL DOES NOT MOVE, inherited from passes 38-56 and not re-litigated: Q9 and Q15 need an act in a RESERVED class (contacting real organisations, spending real money), so level_target 3 is unreachable in this epoch whatever is built, and the ceiling says explicitly NOT to record `infeasible_here` because that verdict would retire the payable half.\n\n  WHAT REMAINS. (a) `measure` (L1) and `measure_l3` still begin at a dispatch instruction; only the L2 loop registers first, and until they do the STACKED wire emitters cannot be belted -- belting them without a registration leg would refuse every stacked run, which is why they are not belted here. That is now the next item, and it is the same item as (a): one repair, not two. (b) Q3's BRANCHING conversation, left by passes 44, 47 and 53. (c) The census's `LEG_ENVELOPE_NAMES` third/fourth-leg blindness, filed by passes 48 and 50 and still filed. The target decision recommended at passes 38-56 -- retarget this atom to level 2 for epoch 3 and move Q9/Q15 to EP19_counterparty_qualification_paths -- is unchanged and still the director's (R13)."
- "2026-08-21 pass 58 BUILD, level HELD at 2, loop_stage stays build. THE LAST UNBELTED WIRE LEGS WERE THE STACKED ONES, AND THE LOOP THAT READ THEM HAD JOINED NO MARKET. Everything below is observed-with-evidence unless labelled inferred (R9). MEASURED AT: HEAD 2f2aed651, with all four touched files clean against HEAD before the first edit and still this lane's only modifications on them at landing.\n\n  WHICH ITEM, AND IT WAS NAMED BY THE PASS BEFORE IT. Pass 57 closed with a four-item remaining list whose first item was its own: \"`measure` (L1) and `measure_l3` still begin at a dispatch instruction; only the L2 loop registers first, and until they do the STACKED wire emitters cannot be belted -- belting them without a registration leg would refuse every stacked run.\" `python3 -m tools.discovery_pass_ceiling` still reports this atom in its fourth state -- THE TARGET IS UNREACHABLE HERE AND BUILD WORK REMAINS -- naming Q2, Q3, Q5, Q13, Q14 payable and instructing explicitly NOT to record `infeasible_here`, so the payable half was taken again. This is Q13's axis: the crossings that do not check who is speaking, one layer in.\n\n  THE DEFECT, AND IT IS NOT THE PASS-57 ONE REPEATED. `measure_l3` is the loop that scores the STACKING over-claim -- the real failure mode of a real aggregator that books the same MW at two venues. It emitted both feeds for `FLEX_UNIT_1`, a KEYWORD DEFAULT, at venues nobody had enrolled with, and the company then formed a stacked belief about contention between markets it had never joined. So the measurement was of a world that could not have happened: a portfolio cannot contend at a venue that never registered it. The single-venue belt could not have caught this even had it been applied, and that is the whole content of the pass: the stacked question is (unit, VENUE), not unit. A party correctly registered at the Balancing Mechanism and instructed at a DSO local constraint is a KNOWN unit at an unjoined venue, and a check keyed on the unit alone waves it through -- then the party is instructed and paid at a market it never entered.\n\n  WHAT LANDED, AND THE THREE DECISIONS INSIDE IT THAT COST SOMETHING. (1) THE COMPANY REGISTERS FROM ITS OWN DECLARATION, not from `truth.venues`. They hold the same specs on the default path, so this buys nothing today and is the difference between a control and a tautology: a loop that registered from the world's own book could never be short a venue, which is precisely the defect the belt exists for. `venues` comes from the caller. (2) ONE BOOK, N REGISTRATIONS. Every venue is asked separately -- a real party signs a separate agreement per market function -- and the answers land in ONE `VenueRegistrations` handed to both emitters. `VenueRegistrations` already scopes by (unit, venue), so one unit into several venues over one window is accepted (the legitimate stacking this atom models) while a second registration at the SAME venue over an overlapping window is still refused UNIT_ALREADY_ENROLLED. Nothing in that class changed. (3) `unit_id` ON `StackedRegistration` RAISES RATHER THAN PICKING when the outcomes disagree: a stacked portfolio is ONE physical unit offered into several venues, which is what makes the MW contend at all, so two unit ids would mean the loop had silently become two portfolios and the conservation law it measures would be scoring an aggregate nobody owns.\n\n  WHAT L1 TURNED OUT TO BE, corrected against the code rather than carried forward from the sentence that named it. Pass 57's item named `measure` alongside `measure_l3`, and on inspection `measure` EMITS NOTHING OVER THIS SEAM AT ALL: it holds truth and belief in one process and its company side reads `outturn_price`, which is a different channel. There is therefore no wire leg in L1 to belt and no crossing for a registration to license; giving it one would be ceremony, and a registration nothing consults is exactly the shape this atom keeps catching elsewhere. The item was two loops in one sentence and only one of them had a crossing. L1 is struck from the remaining list on that ground rather than closed by having been done.\n\n  LANDED: `registrations` as a REQUIRED keyword on `emit_dispatch_instructions_stacked_over_wire` in `sim/flex_dispatch.py`\n  LANDED: `registrations` as a REQUIRED keyword on `emit_settlement_lines_stacked_over_wire` in `sim/flex_dispatch.py`\n  LANDED: `StackedRegistration` in `background/flex_dispatch_triad.py`\n  LANDED: `solicit_registration_stacked` in `background/flex_dispatch_triad.py`\n  LANDED: `_record_window` in `background/flex_dispatch_triad.py`\n  LANDED: `feeds_solicited` in `background/flex_dispatch_triad.py`\n  LANDED: `test_MUTATION_a_stacked_world_calling_a_unit_NOBODY_enrolled_is_refused` in `tests/sim/test_flex_dispatch.py`\n  LANDED: `test_MUTATION_a_book_missing_ONE_venue_refuses_only_that_venues_feed` in `tests/sim/test_flex_dispatch.py`\n  LANDED: `test_an_EMPTY_book_refuses_the_stacked_legs_too__the_fail_open_case` in `tests/sim/test_flex_dispatch.py`\n  LANDED: `test_MUTATION_a_stacked_call_running_PAST_the_declared_availability_is_refused` in `tests/sim/test_flex_dispatch.py`\n  LANDED: `test_the_stacked_book_is_REQUIRED_and_not_a_defaulted_argument` in `tests/sim/test_flex_dispatch.py`\n  LANDED: `test_MUTATION_a_stacked_world_calling_a_venue_the_portfolio_NEVER_JOINED_is_REFUSED` in `tests/background/test_flex_dispatch_triad.py`\n  LANDED: `test_the_L3_solicited_OBSERVATION_can_still_be_False__it_is_not_always_green` in `tests/background/test_flex_dispatch_triad.py`\n\n  R15, FOUR PRODUCTION MUTATIONS RUN AT THIS TICK, each in a detached git worktree at HEAD (`git worktree add --detach`, a real repo) with this pass's four files copied in and only the named part swapped -- never on the shared worktree, which carried 280 other-lane modifications throughout. The unmutated tree is the null control: 73 passed, re-established between mutations rather than assumed. M1, the belt removed from both stacked legs (the shape this build shipped in until this pass): 5 failed, 68 passed -- all four new refusal tests plus the triad's, which is the correct blast radius for removing the consult itself. M2, `responses[:1]` -- check the first message and emit the rest, the plausible mistake on a multi-response leg: 1 failed, 72 passed, redding exactly the missing-venue test and nothing else, which is what makes that test the stacked-specific one rather than a copy. M3, the fail-open shape (`registrations` given a `None` default and the loop skipped when absent): 1 failed, 72 passed, redding exactly the required-keyword test. M4, `feeds_solicited = True` hardcoded -- the observation reporting its own intention: 1 failed, 72 passed, redding exactly the not-always-green test.\n\n  NULL CONTROLS, INSIDE THE TESTS RATHER THAN BESIDE THEM, because three of the four refusals above would also pass on a book that refused everything. The full book admits both stacked feeds. The PARTIAL book (one venue registered, two called) licenses its own venue's feed when handed a truth carrying only that venue -- so the refusal is the missing registration and not an inert book. The exactly-covering book accepts where the half-period-short one is refused; half a period, not a whole one, because a whole one leaves the window butt-jointed and `overlapping` refuses a butt joint too, so it would not discriminate (pass 57's recorded correction, reused rather than rediscovered). And the restored `solicit_registration_stacked` runs the same record clean.\n\n  THE FALSIFIABILITY OF `feeds_solicited`, which is where an observation sitting downstream of a law usually dies. The world now REFUSES at emission, so a check that read the emitted messages could only ever be True. This one reads the DECODED feeds, so the discriminating mutation is downstream of both: an instruction read for a venue the company never joined still scores False. It is the INSTRUCTION leg and not the settlement leg, and the reason is recorded because the first attempt proved nothing: `learn_delivery_ratio_from_lines` ALREADY refuses to learn from a settlement line at a venue the company has no offer in, so mutating that leg raises there and says nothing about this observation at all.\n\n  NO PUBLISHED FIGURE MOVED, measured rather than reasoned. HEAD's `measure_l3` was run in the detached worktree and this tree's beside it over three settings (perfect delivery; a stochastic delivery model; a SOURCED Capacity Market availability price, which is the only setting that exercises the AVAILABILITY venue and its registration). All 15 reported scalar keys per setting, 45 across the three, are IDENTICAL; NO key was lost. One key was added, `feeds_solicited`, and it is a structural fact rather than a figure -- a registration is not a belief and neither is a refusal that never fires.\n\n  THE INSTRUMENTS, none of them edited here, so each reading is an outside verdict. `tools.epistemic_verifier --all` PASS on 544 files. `tools.wall_crossing_dispositions` OK -- 6 live crossings (4 direct, 2 indirect), 91 ruled (cut 85, owed 6, grandfathered 0), unchanged from passes 53-57, so this pass creates no wall crossing and rules none away. `tools.company_network_isolation` exits 0 on its four frozen routes. `tools.wall_channel_census --worktree` was diffed line-for-line against HEAD's reading: channels A, B, C and E are IDENTICAL, including the `paid down on C_envelope (1)` line, which is present at HEAD too and was therefore neither opened nor cleared here. The two channels that differ (D and F) are another lane's and are named rather than glossed: `docs/reports/run_output_latest.json` is uncommitted in the shared tree, which is also what takes the census exit code to 1 on this tree and left it 0 in the clean worktree.\n\n  A CARRIED CLAIM THAT WAS FALSE WHEN I INHERITED IT, and correcting it is the more useful half of this paragraph. Passes 50 and 57 both closed saying the census's `LEG_ENVELOPE_NAMES` scores WallRequest and WallResponse only and is therefore blind to the interim and notification legs. IT IS NOT, and has not been since pass 51: the live lookup carries all four names, the finding that asked for it (WORKER_FINDING_THE_WALL_CENSUS_SCORES_TWO_LEGS_OF_A_FOUR_LEG_WALL_2026-08-21) is DISCHARGED with five named tests landed at e84b6d54c and sits in `docs/staging/done/`, and its four discharging nodes are green at this tick. Pass 57 restated pass 50's sentence without re-reading the tool -- the record outran the code, in the direction that costs an atom a repair it thinks it still owes. Struck from the remaining list on measurement, not on having been done here. What IS still true, and is a different and smaller thing: the census's leg model does not score the STACKED flex feeds separately from the single-venue ones, so this pass's belt is invisible to it either way -- the reading is unchanged by design and not by blindness.

  EVIDENCE: tests/sim/test_flex_dispatch.py 43 passed (38 at HEAD, 5 added); tests/background/test_flex_dispatch_triad.py 30 passed (24 at HEAD, 6 added); 311 passed across the downstream consumers (tests/sim/test_flex_dispatch_stacked.py, tests/company/test_flex_participation.py, tests/interface, tests/company/interfaces/test_sim_interface.py, tests/company/interfaces/test_wall_protocol.py); the four mutations above with the figures given; `ruff check` clean on all four changed files.\n\n  WHY THE LEVEL STILL DOES NOT MOVE, inherited from passes 38-57 and not re-litigated: Q9 and Q15 need an act in a RESERVED class (contacting real organisations, spending real money), so level_target 3 is unreachable in this epoch whatever is built, and the ceiling says explicitly NOT to record `infeasible_here` because that verdict would retire the payable half.\n\n  WHAT REMAINS, one item shorter and one item struck. (a) Q3's BRANCHING conversation, left by passes 44, 47 and 53 -- now the next item, and the oldest untouched one on the list. (b) STRUCK: the census's `LEG_ENVELOPE_NAMES` third/fourth-leg blindness, carried by passes 50 and 57 and measured false at this tick -- see the correction above. (c) Q13's own second half, and this pass did NOT move it -- said plainly, because a belt and a frame are easy to conflate and only one of them authenticates. `_UNFRAMED_CROSSINGS` in `tests/company/interfaces/test_wall_protocol.py` still names BOTH `company/comms/susceptibility_estimator.py` and `company/market/flex_participation.py`: the flex crossing reads `decode_response`, not `decode_framed_response`, so nothing on it checks WHO is speaking. What landed here is the venue's own registration law -- may this unit be called at all -- which is a different question from whether the message came from the venue it claims. And there is still NO cryptography anywhere on this wall: what is modelled is the REFUSAL, not SMKI/DIP signing. STRUCK: L1's registration leg, for the reason recorded above -- `measure` has no crossing to license. The target decision recommended at passes 38-57 -- retarget this atom to level 2 for epoch 3 and move Q9/Q15 to EP19_counterparty_qualification_paths -- is unchanged and still the director's (R13)."
map_notes:
  name: 'Protocol typing of the wall: every SIM/company boundary crossing becomes a typed, versioned message with an explicit request/response separation in time, so a mock counterparty and a real one are indistinguishable to the company. The wall IS the go-live seam. Named in the ruling §1 as an Epoch-3 floor item. W4_1_typed_adapters (SATURATED) built the adapter PATTERN; this is the protocol layer every named adapter below plugs into.'
  origin_note: 'C-S3 (asynchronous wall contracts: request and response are separate events in time, never same-step) is the whole point and the thing most likely to be quietly dropped, because same-step is easier and works until it doesn''t. The SIMPLICITY GUARD binds equally hard: no adapters-for-future-adapters, no protocol cathedral -- the wall already provides the seam. Gates every named adapter below.'
map_records:
  evidence:
  - docs/staging/done/DIRECTOR_RULING_FUTURE_COMMITMENT_SETS_2026-08-08.md
  - docs/design/refs/ADVISOR_RESEARCH_COUNTERPARTY_APIS_EPOCH3_2026-08-05.md
  - docs/design/GO_LIVE_SEAM_AND_INTERNAL_SEAMS_DESIGN.md
  - interface/contracts/wall_envelope.py
  - interface/contracts/payment_observable_seam.py
  - interface/contracts/conversation_seam.py
  - interface/contracts/flex_observable_seam.py
  - tools/epistemic_wall.py
  - tests/architecture/test_epistemic_wall_ratchet.py
  - docs/design/EP6_WALL_CONFORMANCE_CENSUS_DISCOVER.md
  - docs/design/WALL_CROSSING_DISPOSITION_REGISTER.md
  - tools/wall_crossing_dispositions.py
  - company/interfaces/wall_protocol.py
  - tests/company/interfaces/test_wall_protocol.py
  - docs/staging/DIRECTOR_INSTRUCTION_PASS_CEILING_AND_EP1_EP6_PROMOTION_2026-08-19.md
