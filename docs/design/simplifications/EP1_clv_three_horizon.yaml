atom_id: EP1_clv_three_horizon
map_notes:
  origin_note: 'Activity-based pricing is a standing CLAUDE.md constraint -- flat margin makes some customers net-negative -- so a CLV that ignores cost-to-serve at the customer level reproduces the exact error the constraint names. R12 applies hard here: CLV is a DIAGNOSTIC, never a target, and no pricing or retention lever may be tuned to raise it. Coupled-triad shape when built: COMPANY estimates through the wall, HARNESS measures the estimate against SIM realised value.'
map_records:
  evidence:
  - docs/staging/done/DIRECTOR_RULING_FUTURE_COMMITMENT_SETS_2026-08-08.md
  - 'docs/design/THE_VALUE_CYCLE_FRAMING.md (C4: customers are discovered, not read)'
  - saas/clv_model.py
  - saas/enterprise_value.py
  - company/analytics/customer_value_view.py
  - company/interfaces/customer_value.py
  - company/core/three_horizon_clv.py
  - company/crm/clv_cohort_book.py
  - company/crm/clv_sensitivity_model.py
  - company/finance/segment_profitability.py
  - company/finance/portfolio_dashboard.py
  - saas/clv_seed.py
  - docs/staging/WORKER_FINDING_THE_LIFETIME_ESTIMATE_DOES_NOT_MOVE_WHEN_THE_BELIEF_DOES_2026-08-13.md
  - docs/staging/WORKER_FINDING_THE_BOOK_VALUE_COUNTS_CUSTOMERS_WHO_HAVE_ALREADY_LEFT_2026-08-13.md
simplifications:
- |
  2026-08-13 DISCOVER/FRAME ONLY, level stays 0, loop_stage stays idle (worker tick, LANE 3
  idle draw). No BUILD code written, no CLV designed, nothing wired -- this atom is epoch-2
  BUILD-gated on director-owned curriculum sequencing (R13), and EPOCH_GATING_AND_ATOM_
  AUTHORSHIP rule 1 makes DISCOVER/FRAME available while BUILD is not. `maturity_map.yaml`
  NOT edited: nothing in the map moves for this pass, this atom's records are rehomed here
  (`records_rehomed: [evidence]`), and the mint text is director-ruling provenance that a
  worker tick does not rewrite. Output is this record plus two queued findings.
  Everything below is `observed-with-evidence` unless labelled `inferred` (R9).

  MEASURED AT: HEAD de32e8c6b. Live published `docs/reports/run_output_latest.json`
  (4,152,276 bytes, mtime 2026-08-13 21:08) and `site/data/company.json` read directly;
  shipped modules read and, where stated, EXECUTED as they sit on disk; nothing
  monkeypatched, nothing regenerated. One of this atom's two cited evidence paths was DEAD
  (the known moved-path class -- the director ruling had been archived to
  `docs/staging/done/`) and is repaired above. Eleven paths added: the code a reader of this
  atom actually needs, none of which the record previously named.

  ==========================================================================================
  FINDING 1 (FRAME) -- THIS ATOM IS NOT GREENFIELD. Six CLV implementations already exist.
  Five have zero non-test callers. The one CALLED "three-horizon" answers a different
  question from this atom's three horizons, and the live one is a seventh thing again.
  ==========================================================================================

  The mint says "no atom on the map touches CLV today". That sentence is TRUE OF THE MAP and
  was checked: `grep -in clv docs/design/maturity_map.yaml` returns 5 lines and no more --
  this atom's `id` and `name` (4077, 4080), two other atoms' notes ASSERTING the same
  absence (4027, and 4543's "zero atoms touching CLV" measured at mint on 2026-08-08), and
  one SITE2 exit criterion listing CLV among company-only figures (5154). No atom OWNS a CLV.

  It is not true of the TREE. Caller census, whole repo, importers excluding each module's
  own file:

      module                                   non-test importers
      saas/clv_model.py                        4   (enterprise_value, home_move_win_rate,
                                                    annual_report, generate_customer_data)
      saas/enterprise_value.py                 19  (the live run, the site generators, the
                                                    seam, board_dashboard, tournament_runner)
      saas/clv_seed.py                         4   (tools/run_phase0c, run_phase1c x3)
      company/core/three_horizon_clv.py        0
      company/crm/clv_cohort_book.py           0
      company/crm/clv_sensitivity_model.py     0
      company/finance/segment_profitability.py 0
      company/finance/portfolio_dashboard.py   0

  THE LIVE CHAIN, one path only: `saas/clv_model.build_clv` -> `saas/enterprise_value.
  build_enterprise_value` -> `company/analytics/customer_value_view` -> the seam
  `company/interfaces/customer_value` -> `simulation/run_phase4c_on_phase2b.py:345` -> the
  run output -> `site/data/company.json` -> `/company/` and `/customers/`. It is a
  shifted-Beta-geometric expected lifetime times an annuity factor at a hard-coded
  `DISCOUNT_RATE_ANNUAL = 0.10`.

  THE FIVE ORPHANS are tested-only: each is imported by its own test file and nothing else.
  They are not dead in the "delete on sight" sense -- `clv_cohort_book` is literally this
  atom's third horizon (cohort summaries keyed by acquisition year/channel/segment) and
  `clv_sensitivity_model` is the CAC<CLV/3 acquisition-budget lens -- they are UNWIRED. The
  class is the already-filed one: a capability's remedy already exists, orphaned, and the
  common failure here is an orphaned control rather than an absent one.

  THE NAME COLLISION, which is the part that will mislead the draw that opens this atom.
  `company/core/three_horizon_clv.py` (149 lines, 263 lines of tests, "Phase EC") is NOT
  this atom's three horizons. Its axis is TIME OF ESTIMATE:

      H1Commitment  what was promised at the sale
      H2Actuals     what has been earned since, from recorded revenue/cost events
      H3Forecast    the re-forecast now, with an H3Signal of on_track/deteriorating/at_risk

  This atom's axis is SCOPE OF VALUE: contract-term, tenure-expected, portfolio-cohort. The
  two are orthogonal and both are worth having -- an existing H1/H2/H3 tracker does not
  supply a contract-term/tenure/cohort triple, and a reader who greps "three horizon CLV"
  will find a module that answers the other question. `company/finance/portfolio_dashboard.py`
  already cites "Average CLV (H3 forecast) vs H1 commitment" in its own docstring, so the
  collision is already propagating inside the repo.

  A DEFECT INSIDE THAT MODULE, found while reading it and stated here because it is the
  nearest thing on disk to this atom's FIRST horizon. `H1Commitment` takes `contract_start`
  and `contract_end`, exposes `contract_years`, and then never uses it:

      h1_clv_gbp = expected_annual_margin_gbp * retention / (1 + discount_rate - retention)

  -- a perpetuity. `contract_years` is reachable only in the `denom <= 0` branch. Executed
  on the shipped class, margin £100, churn 0.20, discount 0.08:

      1-year fixed    contract_years=0.9993   h1_clv=285.71
      2-year fixed    contract_years=1.9986   h1_clv=285.71
      1-DAY contract  contract_years=0.0027   h1_clv=285.71

  The value of a commitment is invariant to the length of the commitment. If any EP1 build
  reaches for this class as its contract-term horizon, it inherits a perpetuity wearing a
  contract's clothes. Filed here rather than as a third staging finding because the module
  has no callers -- nothing published is wrong today, which is exactly RECORDED severity,
  and the repair belongs to whoever wires it.

  ==========================================================================================
  FINDING 2 -- THE LIVE ESTIMATOR, TWO DEFECTS, BOTH QUEUED AS BLOCKING FINDINGS.
  ==========================================================================================

  Both filed to `docs/staging/`, both severity BLOCKING, lane B_commercial, because a
  published figure may be wrong in each case. Neither fixed in this tick: each is estimator
  or population surgery inside a live publish chain on a BUILD-gated atom's file scope
  (SELF_INTERRUPT_DISCIPLINE -- queue by default).

  (a) `WORKER_FINDING_THE_LIFETIME_ESTIMATE_DOES_NOT_MOVE_WHEN_THE_BELIEF_DOES_2026-08-13.md`
      -- R15 mutation on the shipped `build_clv`: two accounts, identical observed tenure,
      opposite churn beliefs; SWAP the beliefs and neither estimate moves (SAFE 50.000 /
      RISKY 23.926, both runs, three decimal places). The per-account inputs are only
      `{customer_id, t_churn, T}`; alpha/beta are pooled across the whole book into one
      scalar pair. The company's belief about a customer enters only through the portfolio
      mean. In the live book: corr(expected_lifetime, latest_churn_probability) = +0.093
      across 13 accounts, sign backwards, and the joint-highest-churn account carries the
      longest projected life on the book.

  (b) `WORKER_FINDING_THE_BOOK_VALUE_COUNTS_CUSTOMERS_WHO_HAVE_ALREADY_LEFT_2026-08-13.md`
      -- 5 of 13 accounts churned in-run (C3 2020-06-30, C5 2020-12-30, C1 2021-12-30, C6
      2024-03-30, C4 2024-09-29), all `home_move_won: false`, all still carrying forward CLV
      in every later snapshot and in the published `enterprise_value_gbp`. £35,597 = 51.8%
      of the residential book's CLV. The valued population is "has renewal history", never
      "is still supplied", though `churned_billing_accounts` sits in the same output file.
      The `/company/` featured household is C1 -- churned -- rendered with `clv_gbp 2840.50`
      and `expected_lifetime_periods 14.94` and no attribute saying it left.

  ==========================================================================================
  FINDING 3 (FRAME) -- THE GAP THIS ATOM IS SCORED ON IS NOT MEASURED ANYWHERE, AND THE
  PATTERN TO COPY IS ALREADY RUNNING NEXT DOOR.
  ==========================================================================================

  The mint's own scoring rule is "the company's OWN estimate ... allowed to be wrong and the
  gap is the score" (COUPLED_TRIAD: no company capability is complete until it has faced a
  world that can defeat it). Nothing in the repo compares a CLV estimate to realised value:
  no belief/truth CLV pair in the run output, none in `docs/observability/
  coupled_gap_ledger.json` (checked by name), and `couples_with: []` on this atom.

  The pattern EXISTS one field away. `run_output_latest.json::customer_events` already
  carries, per renewal, the SIM's `churn_probability`, the company's `company_churn_estimate`
  and the derived `churn_estimate_error_pct` -- a belief/truth pair produced and published on
  every renewal. CLV's realised counterpart is cheaper still: the run knows what each account
  actually earned after each snapshot date, and `clv_snapshots` already stores the belief at
  each year end for ten years. An ex-post CLV gap is assemblable from artefacts that exist
  today; it has simply never been assembled.

  WHAT FOLLOWS, recorded rather than asked (NEVER_ASK_WITHOUT_RECOMMENDING). For the draw
  that opens EP1:

  1. FIRST TASK IS A CENSUS, NOT A DESIGN. `level_current: 0` and `file_scope: []` describe
     an atom with nothing built; six implementations exist and one is live on a public
     surface. The level must be re-derived from that census when the atom opens, not assumed
     from the record. Not changed here: a level move is a BUILD-side act on a BUILD-gated
     atom.
  2. THE THREE HORIZONS MAP ONTO WHAT EXISTS, WITH ONE HOLE. Tenure-expected = the live
     `clv_model` chain (defective per Finding 2, repairable). Portfolio-cohort =
     `company/crm/clv_cohort_book.py`, built and unwired. Contract-term = the genuine hole;
     `three_horizon_clv.H1Commitment` is the nearest artefact and prices a term as a
     perpetuity. EP1's honest shape is therefore REPAIR + WIRE + BUILD ONE, not build three.
  3. NAME THE COLLISION BEFORE WRITING A LINE. Either rename `company/core/three_horizon_clv`
     to what it is (a commitment-vs-actual-vs-reforecast tracker) or name this atom's triple
     something that cannot be confused with it. Two different "three-horizon CLV"s in one
     repo is a defect a reader pays for repeatedly.
  4. THE EXIT CRITERION IS THE GAP, NOT THE ESTIMATE. Per COUPLED_TRIAD and R12: publish the
     estimate, publish the realised, publish the error, and let the error be large. The
     control that must be able to fail is the swap-the-belief mutation in Finding 2(a) --
     an estimator whose per-customer output does not move when its per-customer input moves
     fails it, which today's does.
  5. COST-TO-SERVE IS ALREADY PER-CUSTOMER AND MUST STAY THAT WAY. `site/data/company.json::
     cost_to_serve` publishes n=19 with by_segment/by_payment_channel/by_tenure splits
     (resi mean £446.92, I&C mean £3,418.46). The origin note's warning -- a CLV that
     ignores customer-level cost-to-serve reproduces the flat-margin error -- is currently
     satisfied by the live chain (`build_clv` divides `cost_to_serve['by_customer']` net
     margin) and must not regress when the three horizons are assembled.
