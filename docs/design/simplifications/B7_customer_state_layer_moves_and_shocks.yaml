atom_id: B7_customer_state_layer_moves_and_shocks
simplifications:
- '2026-07-29 REGISTERED into the map (worker tick) from docs/design/BACKLOG.md item B7 on the director''s from_rich_20260729_173731 instruction (full quote + no-blocked_on rationale in B3''s note). SCOPE RECONCILED AGAINST EXISTING ATOMS BEFORE MINTING, so this is not a second W2_5: W2_5_life_event_stream is at target (L3/3) and emits job loss / illness / divorce / retirement / new child from ONE shared generator; W2_4_household_budget covers budgets and W2_2_population_draw the draw. NONE of them emits a HOUSE MOVE (the credit-exit + two-deemed-entry pattern BACKLOG B7 names as its own exit test) or an income shock as a state transition, and saas/home_move_win_rate.py is the company-side acquisition view of moves, not a world-side event. This atom is therefore the UNFINISHED remainder of B7, deliberately scoped to the gap rather than re-registering settled work. DoD (BACKLOG B7): each transition emits as a first-class event on its OWN named RNG substream (C-S2 discipline -- the 01:09Z shared-life-event-RNG incident is why: a new draw must never shift another subsystem''s outputs); each has a downstream effect the company observes ONLY through behaviour, never a tagged trait (the wall); replay is deterministic; a test asserts a move produces the credit-exit + two-deemed-entry pattern. KNOWN ADJACENT DEFECT, not silently inherited: the W2_5 emission/reconstruction defect (new_baby/divorce gated by generation order) is a queued latent in that atom''s own row -- this atom must not build on top of it without checking whether the same ordering trap applies to moves. No BUILD code, level_current 0.'
- '2026-07-29 DISCOVER (0->1) + FRAME (1->2) DONE on a lane-3 idle fork, no BUILD code, level HELD at 0. FRAME artefact: docs/design/B7_CUSTOMER_STATE_MOVES_AND_SHOCKS_FRAME.md (evidence-listed, so the atom reads FRAME-saturated to supervisor._atom_has_frame_doc and leaves the idle re-draw pool). DISCOVER verified against DISK this tick (R7), claims labelled observed/inferred (R9). (1) CONFIRMED at source: simulation/life_events.py:68-82 EventType has 13 members and NONE is a move; _LIFE_EVENT_SUBSTREAMS:250-263 registers 12 substreams and none is a move; apply_events:601-693 has no move branch. (2) CONTRADICTED EXPECTATION -- home_move_won IS NOT A HOME MOVE: simulation/customer_events.py:136-141 rolls it on EVERY churn regardless of cause, and the retained decision at :134 is assembled from bill shock, market conditions (:109-111), income stress (:115-119), satisfaction (:120-123) and a retention offer (:131-133), not one of which is a move. saas/home_move_win_rate.py:4-11 states the conflation in prose ("the occupant moves out (churns)"), so the tree simultaneously holds that 100% of churn is a home move (win-rate purposes) and 0% of churn is a home move (everywhere else). (3) A LIVE R15 FAIL-OPEN FOUND, not hypothetical: simulation/churn_journey.py:56,152-165 defines HOME_MOVE_CHURNED and is_catchable() to EXCLUDE unpredictable move-churn from recall/precision (citing PROCESS_MODEL.md s2), but record_home_move has ZERO non-test callers -- only tests/simulation/test_churn_journey.py:145,154 -- while run_phase2b.py:940,1219-1226,1370-1383,1397 uses the register and never records a move. So is_catchable() returns True for 100% of live churns and the company''s churn model IS being scored against churn nobody could predict, with the designed carve-out inert: a filter passing vacuously on an empty target set. (4) THE W2_12 DISCOVER DOC''S TRIGGER CLAIM IS FALSE: W2_12_CHANGE_OF_TENANCY_DEBT_PHYSICS_DISCOVER.md:28 names company/crm/life_events.py MOVE_IN/MOVE_OUT as "the trigger ... Built. Emits the events; W2_5 dependency" -- but that module is COMPANY-side (company/crm/life_events.py:18-19), is not W2_5 (simulation/life_events.py, which has no move type), and does not emit (LifeEventLog.record:71-77 is a passive append nothing generates into). maturity_map.yaml:569 depends_on [W2_5_life_event_stream] is therefore satisfied by a module that cannot supply the trigger, and naming a company-side log as the world-side source is a wall inversion. (5) FIVE BUILT CoT ENGINES, ZERO LIVE INVOCATIONS: change_of_tenancy_register.py, occupancy_register.py (:40,48), company/billing/cot.py (:51,69), deemed_contract.py, account_closure.py all have green tests and no non-test caller -- the only non-test hits are a reason STRING (credit_refund.py:12) and a comment (run_phase4c_on_phase2b.py:313). The pipe is built end-to-end downstream of a source that does not exist (FAIL-SILENT in pure form). (6) THE STRUCTURAL PREREQUISITE IS MISSING: simulation/household.py:87-120 keys Household on customer_id with NO mpan/premise_id/supply_point anywhere in the world layer, while company/crm/occupancy_register.py:21,40 is keyed on MPAN -- the company side already assumes a premise key that persists across occupants and the world cannot supply one. A move is by definition the event where premise identity persists and occupant identity does not, so it cannot be expressed until that split exists. (7) NO COMPOSITION STATE: household.py:88-120 has bedrooms but no occupants/household_size; population_draw.py:704-732 draws accommodation/cars/nssec/heating_fuel plus region(:614)/tenure(:459) and no composition axis; W2_13 (map:571-589, L0, blocked_on director_level_up) owns composition->consumption, so B7 owns composition as a TRANSITIONING state and W2_13 reads it -- ownership split registered so neither invents a schema. (8) INCOME SHOCK is 3-valued (household.py:70-73), magnitude-free, duration-free and single-cause (life_events.py:196-203 states illness shares job_loss''s recovery because income_stress "is a single state variable, not tracked per-cause"), BUT already well-wired: arrears_engine.py:75-95,397,428 / payment_timing.py:48-53 / customer_events.py:115-119 / sim_satisfaction.py:81-82 / feedback_survey.py:58-59. So switching+arrears yes, CONSUMPTION no -- B7''s income half is a DEPTH problem not a plumbing problem. (9) C-S2 AUDIT: W2_5 IS COMPLIANT -- life_events.py:266-277 derives each substream from sha256(f"{base_seed}:{name}") per event type per household off a stable md5 base seed (:280-288), so the 01:09Z incident IS structurally fixed there. Two unregistered gaps: life_events.py:276 keys on a BARE f"{seed}:{name}" with no stream-name prefix, unlike population_draw.py:152/:400, so any future module using the bare pattern silently shares W2_5''s key space (not a live defect; a trap B7 must not enter); and customer_events.py:98,140 build ad-hoc Randoms outside any registry (isolated by construction, enumerable by no test). (10) THE GENERATION-ORDER TRAP DOES APPLY, AND WORSE: life_events.py:407-596 evaluates types in fixed within-year order mutating income_stress, date-sorting only at :597 while apply_events re-sorts at :620 -- but a move TERMINATES an occupancy, so events already emitted later in that year become retro-INVALID rather than merely mis-gated. A move therefore cannot be a 14th peer inside the yearly loop; it must sit ABOVE the generator and define the window life events are generated within. FRAME: split the conflated customer_id into a persistent PREMISE identity (physical attributes, MPAN, consumption physics -- never moves) and a mortal OCCUPANCY identity (people, income state, composition, the billing relationship), with premise==occupancy==customer byte-identical to today for any premise with one lifetime occupancy (the life_events.py:346-355 default-is-byte-identical discipline, as a TEST). A move at premise P emits ONE occupancy_ended (credit exit: final read or estimate, final bill, modelled non-payment) plus TWO occupancy_started -- a stranger drawn fresh from population_draw at the VACATED premise after a void gap, and the mover at their NEW premise (in-book or out-of-book) -- both landing on DEEMED terms, so the exit-test pattern falls out of the identity split rather than being asserted. home_move_won is re-founded not deleted: after B7 "did we win the mover" is about the new premise and "did we keep the premise" is about the incoming occupant, two questions one roll currently conflates. Income shock gains MAGNITUDE and DURATION beside IncomeStress (which becomes a derived view so all five consumers keep working), recovery becomes SCHEDULED rather than an annual 50% coin-flip (life_events.py:177-178), and the missing consumption leg feeds the existing self_rationing atom rather than duplicating it. R13: BASELINE = move hazard by tenure, void-gap, p(final bill unpaid), notification-lag INCLUDING the never-told fraction, deemed duration, shock magnitude/duration, formation/dissolution rates, cross-occupant consumption response -- all to be externally sourced at BUILD and DELIBERATELY UNNUMBERED here, because inventing a move rate is exactly the fabrication R13 forbids. CURRICULUM (director-reserved) = the book''s tenure mix (the real difficulty dial: a 60%-private-rent book has several times the move rate), any named scenario dialling move rates or shock severity, and WHETHER THE LAYER IS ACTIVE in a live run -- explicit precedent at life_events.py:346-355, mechanism built, activation director-facing. NEITHER: saas/home_move_win_rate.py:38-41/:47-55 are company-side competitive BELIEFS currently imported into the world''s roll by customer_events.py:89 (admitted cross-seam at :17-22); after B7 the outcome should be world physics and the constant a thing SCORED, logged as seam debt for remediation-on-touch. C-S2 SUBSTREAMS, namespaced stream customer_state (population_draw.py:152 pattern, NOT the bare life_events one) keyed per-premise/per-occupancy not sequentially (the _cohort_substream:390-401 pattern): move_hazard, move_date, move_destination, incoming_occupant, void_gap, final_bill_outcome, notification_lag, deemed_duration, income_shock_onset, income_shock_magnitude, income_shock_duration, composition_change. Binding: never extend _LIFE_EVENT_SUBSTREAMS (different layer); onset and magnitude stay SEPARATE streams or changing the magnitude distribution shifts WHICH customers get a shock (the 01:09Z shape one level down); a test enumerates the registry and asserts adding a name leaves every other stream byte-identical. WALL: the company CAN see a final read (or the estimate when none is taken), an inbound "I''m moving" contact, a cancelled DD, a consumption discontinuity, an unpaid final bill, a new name registering, and SETTLED VOLUME AT A PREMISE WITH NOBODY CONTRACTED (how a real supplier finds a void it was never told about); it CANNOT see the move date until told (sometimes never), the reason, the forwarding address, the incoming occupant''s income/composition/tenure (it re-discovers all of it -- which is what makes a move the prime acquisition moment), whether a 3-month-unread premise is void/vacant/unread, or the income shock (only its shadow in payment behaviour, months later). New typed premise_occupancy_seam on the conversation_seam.py/payment_observable_seam.py pattern with FORBIDDEN_TRUTH_FIELDS including move_date, move_reason, occupancy_id, income_shock_magnitude/duration, household_size, outgoing_occupant_id, void_gap. The company-side hook ALREADY EXISTS AND IS INERT: life_event_detector.py:89-90 declares metering_changed for exactly this and nothing ever sets it True. TRIAD: SIM = the identity split + move transaction + shock depth behind the wall on named substreams; COMPANY = C7 plus the four uninvoked CoT engines finally acquiring a source, inferring the move from observables and ALLOWED to bill the wrong occupant, chase the new occupant for the old one''s debt, miss a void, or waste a retention offer on someone already gone; HARNESS = a tools/couple_b7_c7.py in the couple_* family (in tools/ so the epistemic verifier does not scan it) reporting detection LAG plus never-detected fraction (gap_metric.py:39,333 detection family), MISATTRIBUTION harm via harm_cost:162 with a DERIVED not declared ratio because chasing an innocent incoming occupant is a Consumer Duty harm, and CATCHABILITY HONESTY -- the fraction of scored churn that was genuinely un-catchable, exactly the number is_catchable() was built to produce and today cannot, which corrects a CURRENTLY PUBLISHED company metric. R15 for when BUILD opens, each with its mutation: TAUTOLOGY = put move_date on the seam (FORBIDDEN_TRUTH_FIELDS must fire) PLUS an identical-observations-identical-belief leg because an import-only wall test is fail-open against a value laundered through a caller argument (the hole B8 found at run_phase2b.py:1163-1172); assert two-deemed-entries against the BILLING outcome over correct non-overlapping windows reconciling to settled volume, never by counting the objects the constructor just made; set the harm ratio to 1.0 and the outcome ranking must change or the ratio is decorative. FAIL-OPEN = THE CLASS THAT IS ALREADY LIVE HERE: run with zero moves emitted and any "move-churn is excluded from recall" control must go RED, which forces non-emptiness INTO the assertion -- this one control would have caught finding (3); same for final-bill reconciliation with no rows; reject non-finite/None move dates FIRST not via comparison guards (life_events.py:370-384 is this project''s own worked precedent for why operand order made the original safe only by luck); malformed hazard params fail CLOSED. FAIL-SILENT = the layer inactive while every test is green, which is the OBSERVED status quo of the CoT stack not a hypothetical, so the control must be an INVOCATION check on real run output (moves-per-run > 0 when active) and the mutation is to disable the call site while leaving unit tests intact, proving only the run-level control catches it; a registry import failure must ABORT, never degrade to a shared RNG; one unseeded draw in the move path must turn a full-run replay-determinism test RED. C-S1 is load-bearing here (final read, notification and new registration arriving singly/late/out of order is the NORMAL case for a move, not an edge case); C-S3 the notification and response are separate events, never same-step; C-S5 annual hazards against a half-hourly settlement clock needs an explicit invariance statement or a named R10 simplification at L3+. R12: detection lag and misattribution harm are DIAGNOSTICS -- no threshold tuned to shorten the lag, no move rate adjusted because bad debt came out high. OPEN FOR BUILD, registered not answered: whether the split lands in household.py or a new premise_occupancy.py; whether B7 or W2_12 wires the four engines (W2_12 is blocked_on director_level_up at L0 so B7 must not assume it moves first -- recommendation: B7 emits and provides the seam, W2_12 consumes, registered now so both do not build the same fan-out); whether home_move_won is renamed at the same touch (yes, but separately -- it is a published artefact field with its own consumers); and ALL baseline sourcing. LEVEL HELD AT 0: MATURITY_MAP.md s3 sets L1 at "been BUILT in any form" and this fork wrote no code, so claiming a level for analysis alone would be unearned -- saturation via the FRAME artefact, not a level bump, is what correctly stops the re-draw.'
