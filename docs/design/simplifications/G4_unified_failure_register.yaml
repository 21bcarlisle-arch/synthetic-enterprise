atom_id: G4_unified_failure_register
simplifications:
- '2026-07-15 REGISTERED (RETRO_ACTIONS_THE_THREE_GAPS.md ATOM 1, director-approved QUEUE, ranked highest). Class-level gap: 11 retros, nothing indexes ACROSS them; strikes counted within a retro not across -> two-strike redesigns fired late (wake-doorbell + tmux-injection both hit third strike independently). Mechanism: append-only register keyed by theme tag with GLOBAL strike-count per class, appended by the retro ritual (skill) so it cannot be skipped.'
- '2026-07-16 FRAME (Lane-3 H17, doc-only, NO LEVEL MOVE -- L1 ''Skeletal'' requires BUILT code per the rubric and this atom is epoch-gated for BUILD, so a doc cannot move it off L0): docs/design/UNIFIED_FAILURE_REGISTER.md -- failure-CLASS taxonomy derived from 8 real retros + a GLOBAL per-class strike counter (R3 fires on the Nth-in-class across ALL components, not per-component), append-only JSONL store, Method-door surfacing, R15 mutation plan (strike alarm must fire on a planted 2nd-strike, must not fail-silent on an empty/malformed register). Stays L0 until built.'
- '2026-07-16 FRAME (H17 Lane-3): detectable canonical FRAME terminus authored (pointer to the complete UNIFIED_FAILURE_REGISTER.md design); closes the _atom_has_frame_doc filename false-negative so the idle draw stops re-offering G4. BUILD deferred (idle/epoch-gated).'
- '2026-07-18 BUILD OPENED (loop_stage idle->build) by DIRECTOR_TWIN approval (route_blocking_decision, item buildopen-g4-2026-07-18, confidence high, DEFERS no) -- canon v3 §3a. Mechanises R3 (two-strike redesign) per docs/design/UNIFIED_FAILURE_REGISTER.md: a mechanical cross-component failure-CLASS tally so a per-class repeat hiding across DIFFERENT components is caught (the wake-doorbell -> tmux-injection -> 6h-blackout class R3''s per-component human counting missed). Foundation-first (Epoch-2; product Epoch-3-gated). Twin HARDEN condition (R15): the register is itself a control, must ship a mutation test proving it FIRES on a real per-class repeat (fail-open-green is the defect it catches). Dispatching one worktree build fork (docs/retrospectives/ + tools/ + tests/). NO level_current change -- L3 stays PROPOSED per §0.'
- '2026-07-18 BUILD REVERTED to idle (fail-closed) + fork reaped: the fronts_reconciler alarmed DRAW_OFF_FRONT and RED-wedged the publish gate. ROOT: G4 (lane G_data_learning) is NOT a member of any OPEN front (SIM_ACTORS=W1/W2 lanes, SUPPLIER=D/B/E/C/F lanes) and has no per-atom director BUILD_OPEN in gate_authorizations.jsonl. The TWIN''s canon-§3a epoch-approval does NOT equal fronts.yaml region-membership -- the enforced fronts+gate-wall model authorizes a BUILD promotion IFF the atom is in an OPEN front OR has a director console BUILD_OPEN, and G4 has neither. Lesson: check fronts.yaml region-membership (not just twin approval) BEFORE opening an atom. Escalated to the director as a governance gap (twin §3a vs the fronts model disagree on off-front H/G-lane builds); G4 BUILD awaits a director front-open covering H/G lanes or a per-atom BUILD_OPEN.'
