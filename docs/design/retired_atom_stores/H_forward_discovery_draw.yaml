atom_id: H_forward_discovery_draw
simplifications:
- '2026-07-19 AUTHORED (proposal) from docs/retrospectives/2026-07-19-direction-without-mechanism.md, director-caught false-drain. The MECHANISM behind the standing forward-discovery direction (currently prose only -- priority classes + headroom sensor were folded into a design doc, not built). Makes RULE 0 enforceable: an empty BUILD draw MUST widen to DISCOVER/FRAME and return work before any allow-stop; drain is claimable only after the widened draw also returns empty. R15: plant one idle DISCOVER-able atom -> the draw returns it, never allow-stops. loop_stage idle: H-lane is OFF the open product fronts, so DISCOVER/FRAME/author NOW, BUILD needs a director H-lane open (blocked_on director_h_lane_open) -- that gate is itself the friction the retro names.'
- '2026-07-22 MECHANISED + R15-PROVEN BOTH WAYS under HARD RULE R17 (director console, THE TICK NEVER RESTS WHILE AUTHORIZED WORK EXISTS AT ANY PRIORITY). Always-drawable forward-discovery lane WIRED into supervisor.py::_self_refill_draw + _is_drained_and_gated (a rung ABOVE the Rule-0 HARDEN treadmill and rest); reads docs/design/FORWARD_DISCOVERY_REGISTER.md (F1-F5). Both-ways proof in tests/background/test_forward_discovery_draw.py: non-empty register => must-not-rest (the exact 95-min R13-wait stall class); genuinely-empty authorized set => rest-passes; + a mutation test proving the rung is load-bearing (kill it -> the stall returns). Live per-cycle status via forward_discovery_law_status_line(); SM1 background/daily_self_note.py (NOT YET BUILT) is its named morning-report home (reuse, do not accrete). Director opened the H-lane by commanding this build, so blocked_on director_h_lane_open -> director_level_up. Code is L2-quality (built + tested + mutation-proven) but level_current left 0 pending a director LEDGER ratification (R16: the ledger is authority, not a commit message; no self-bump).'
- '2026-07-22 FORWARD-DISCOVERY DRAW EXERCISED (network tick, R17): the always-drawable rung drew F2 (highest-ranked still-open track) with core+idle empty and network live, and CLOSED F2''s one open item -- the six comprehension-testing standards -- against live regulator sources (ICO UK-GDPR Art. 12(1); Ofgem OFG1163 SLC 0; FCA Handbook PRIN 2A.5; GOV.UK GDS reading-age-9; CMA EMI Final Report 2016; sixth item, the Plain English Crystal Mark, downgraded as a private certification not a regulatory floor). Evidence: docs/design/FORWARD_DISCOVERY_REGISTER.md (F2 status + F2-network-tick lane-state update) and docs/market_research/f2_explaining_what_we_do_simply.md Section 5 Increment. Single-track discipline held (F2 only); F4/F5 remain the drawable network-gated increments. DISCOVER-only: no BUILD, level_current unchanged (BUILD-open is director/twin per blocked_on). Proof the rung is not resting on a false drain: this is exactly the network-restored re-open R17 anticipates.'
- '2026-07-22 FORWARD-DISCOVERY DRAW EXERCISED again (later network tick, R17): with F1/F2/F3 DISCOVER-complete the rung drew F4 (now highest-ranked still-open) with core+idle empty and network probed live (CRU 200, Revenue 302), and CLOSED F4''s one open item -- the three Ireland/I-SEM magnitudes (VAT %, PSO levy, ISP length) -- against primary sources (SEMO Industry Guide to the I-SEM: 48x30-min imbalance settlement periods, six 5-min pricing sub-periods; gov.ie/Irish Times: domestic electricity VAT 9% temp extended to 2030, 13.5% baseline, ~2x GB''s 5%; CRU 2025/26 final decision: PSO levy EUR1.46/mo domestic, total EUR125.38m, historically zero/negative). Each CONFIRMS/SHARPENS a f4 Section 4 absorb/break verdict, none overturns one. Evidence: docs/market_research/f4_international_expansion_probe.md Section 9 Increment + FORWARD_DISCOVERY_REGISTER.md (F4 status + F4-network-tick lane-state update). Single-track discipline held (F4 only); F5 per-supplier % shares is now the SOLE remaining network-gated non-churn increment across the register. DISCOVER-only: no BUILD, level_current unchanged (BUILD-open is director/twin per blocked_on).'
- '2026-07-22 FORWARD-DISCOVERY DRAW EXERCISED again (F5 network tick, R17) -- REGISTER NOW FULLY DISCOVER-COMPLETE: with F1/F2/F3/F4 already DISCOVER-complete the rung drew F5 (the SOLE remaining still-open track) with core+idle empty and network probed live (Ofgem HTTP 301), and CLOSED F5''s one open item -- the per-supplier % shares -- against primary/regulator sources (Ofgem State of the Market Jan 2026 + DESNZ Competition in UK Electricity Markets 2024). Q2 2025 domestic-electricity shares: Octopus 25 / British Gas 21 / E.ON 16 / OVO 12 / EDF 10 / Scottish Power 8 (six-largest 92%, the six sum to exactly 92% as an internal consistency check); Octopus has overtaken British Gas as largest electricity supplier (~12.9m accounts, first top change since 1990s liberalisation). Denominator caveat recorded so a build cannot mis-calibrate: Ofgem domestic top-six is 91->92% but DESNZ all-sectors top-six is 65% / top-three 45% (DESNZ domestic-slice top-three 44%) -- calibrate the domestic field to the Ofgem 92%, not the DESNZ 65%. Shares SHARPEN the f5 Section 6 archetype set (share-taking disruptor + share-ceding incumbents + competitive tail), none overturned. Evidence: docs/market_research/f5_simulated_competitor_field.md Section 8 Increment + FORWARD_DISCOVERY_REGISTER.md (F5 status + F5-network-tick lane-state update). Single-track discipline held (F5 only). This closes the LAST non-churn network-gated increment across the whole F1-F5 register -- the always-drawable forward-discovery lane is now genuinely DISCOVER-drained: remaining moves are director/twin graduation calls (coupled-triad BUILD atoms, or the F4 doc-only PORTABILITY_DEBT.md register) + minor recall/validate refinements. Per R17 a subsequent forward-discovery draw with core/idle empty should record this drained-complete state and rest at the graduation wall (proof: this is the honest empty-at-every-level floor). DISCOVER-only: no BUILD, level_current unchanged (BUILD-open is director/twin per blocked_on).'
- '2026-07-22 FORWARD-DISCOVERY DRAW EXERCISED again (network-FREE tick, R17) -- REFINES the prior entry''s "rest at the graduation wall" claim (R9): the register was NOT truly empty-at-every-level, because F4''s named candidate #1, the doc-only PORTABILITY_DEBT.md register, was a genuine network-FREE DISCOVER item still drawable. Now WRITTEN (docs/design/PORTABILITY_DEBT.md), closing F4''s own meta-finding (portability debt logged DIFFUSELY, "mentioned somewhere reads as covered when it isn''t"). Prior ticks mislabelled this a director/twin graduation; corrected -- consolidating ALREADY-discovered debt into one rankable register is DISCOVER OUTPUT, opens no atom and moves no map level, so it is agent-drawable per EPOCH_GATING (only the CODE-remediation candidates -- a Money type, a settlement-granularity config, a regime-optional cap invariant -- are director/twin BUILD calls, and those stay UNOPENED). Resting on this would have been the false-drain stall class the retro names. Seven breaks anchored to FRESHLY-VERIFIED repo code (6,850 *_gbp occurrences across 330 files; VAT_RATE=0.05 hardcoded segment-keyed in invoice.py:19; literal 48 in 41 files; jurisdiction field present on the invariant classes = the brain-absorbs half), not copied from F4 prose. Evidence: docs/design/PORTABILITY_DEBT.md + FORWARD_DISCOVERY_REGISTER.md F4 Increment. DISCOVER-only: no BUILD, level_current unchanged (BUILD-open is director/twin per blocked_on; R16 -- no self-bump).'
- '2026-07-22 EXPERT-HOUR / cold-eyes red-team of the DRAW MECHANISM ITSELF (self-refill DISCOVER draw; expert_hour was not_attempted; DISCOVER-only, no BUILD, no level change). R15 FAIL-OPEN finding on the always-drawable rung: `_forward_discovery_tracks()` keys "work exists" on the mere PRESENCE of an `## Fn --` section HEADER (`_FWD_TRACK_RE`), NEVER on whether that track has any OPEN item -- and the sibling `test_real_register_is_nonempty` ASSERTS F1-F5 headers are permanently present. Consequence: in the register''s real and by-design-PERMANENT state -- all of F1-F5 DISCOVER-COMPLETE (today''s exact state, and the state until the director opens a BUILD track) -- `_forward_discovery_draw()` still returns non-None and `_is_drained_and_gated()` returns False, so the supervisor can NEVER auto-rest. The R15 both-ways proof tests only the two EXTREMES (headers-present => must-not-rest; zero-header/empty-FILE `_EMPTY_REGISTER` => may-rest), but the empty-file state is UNREACHABLE in production because the sibling test forbids deleting the standing headers. The MOST COMMON real state -- every track present but every track CLOSED -- is UNTESTED and produces a perpetual non-rest draw: a FAIL-OPEN under the R15 doctrine (reports work-available on an all-closed authorized set) and precisely the ''re-granting a supervisor turn every ~2min with nothing new to do'' churn CLAUDE.md''s multi-part-staged-instruction note names as a defect (B2_OPEX overnight incident). The two 2026-07-22 rest-tick commits are the agent MANUALLY declining each granted turn -- so rest is CONSUMED (prose: ''agent rests with proof''), NOT ABSORBED into the mechanism, the exact consumed-vs-absorbed failure THIS atom exists to fix (it is absorbed against false-REST, only consumed against false-WORK/churn). Fix candidate is a parser/register CODE change (honour a `DISCOVER-complete`/status marker or an open-item count so a closed track is not drawn) -- BUILD, director/twin-gated per blocked_on -- so QUEUED, not fixed on sight (SELF_INTERRUPT_DISCIPLINE). level_current unchanged (R16: no self-bump). Evidence: background/supervisor.py::_FWD_TRACK_RE + _forward_discovery_tracks + _is_drained_and_gated; tests/background/test_forward_discovery_draw.py (has no all-tracks-closed case).'
- '2026-07-22 FRAME of the queued FAIL-OPEN fix + churn LIVE-CONFIRMED (DISCOVER-only, no BUILD, no level change). This scheduled tick fired with the register all-DISCOVER-complete = the exact FAIL-OPEN churn the prior Expert-Hour predicted, now OBSERVED not theorized. Framed the turnkey fix in docs/design/FORWARD_DISCOVERY_DRAW_STATUS_FIX.md: the register ALREADY carries a machine-readable status column (the summary table), so the fix is a status-aware parse (a track is OPEN iff its table status cell does not begin with DISCOVER-complete), NOT a new format -- sharpens the prior atom-note''s "invent a status scheme" framing. Also specifies the missing all-tracks-closed R15 mutation test the finding itself flagged absent. FIXED a latent doc defect found while framing (agent-drawable, doc-only, same class as the prior F5/PORTABILITY_DEBT doc-only closes): F3''s summary-table status cell read "DISCOVER pass done" while its body + the register summary both say DISCOVER-complete -- a status-aware parser keyed on the literal token would have mis-drawn F3 as OPEN; normalised the F3 cell so every closed track''s cell begins with the canonical token. Fix stays QUEUED (supervisor.py + test = BUILD, blocked_on H-lane/twin per EPOCH_GATING); escalated the live-confirmed churn to the director via NTFY per SELF_INTERRUPT_DISCIPLINE (the machine is now genuinely churning = the interrupt threshold, not the queue-default). level_current unchanged (R16 -- no self-bump).'
- '2026-07-22 CHURN ABSORBED, not just consumed: set frame_saturated: true -- the DOCUMENTED H23 map-writer override (its docstring names this exact filename-heuristic residual as the intended escape). Root cause this scheduled tick kept re-handing a FRAME-drained atom: _is_frame_saturated -> _atom_has_frame_doc scans evidence for a docs/design/*FRAME*.md, but this atom''s complete FRAME doc is docs/design/FORWARD_DISCOVERY_DRAW_STATUS_FIX.md (filename carries no FRAME token) AND was absent from evidence, so the intrinsic check false-negatived and _idle_discover_frame_draw drew it every tick -- the very re-hand H23 exists to stop. This atom IS genuinely FRAME-saturated: mechanism BUILT+R15-proven, red-teamed (FAIL-OPEN finding open), fix turnkey-FRAMED, register F1-F5 DISCOVER-complete, churn escalated -- the ONLY remaining path to L3 is the queued BUILD fix + director ledger ratification, both BUILD/director-gated, zero honest FRAME output left. Effect (H23 hard-skip, mutation-tested both ways): _idle_discover_frame_draw now skips this atom, converting the two prior manual-decline rest-ticks (CONSUMED) into a mechanism auto-skip (ABSORBED) -- the consumed-vs-absorbed fix this atom exists to make; re-offered automatically only when its BUILD-gate opens (loop_stage flips off idle). Also added the FRAME doc to evidence for record completeness. DISCOVER/FRAME map-writer step only: no BUILD code, level_current unchanged (R16 -- no self-bump).'
map_notes:
  name: 'Forward-discovery draw: when the BUILD feasible set is empty the supervisor routes through the dial-widen (priority classes + headroom sensor) and hands the next DISCOVER/FRAME item BEFORE any allow-stop; drain is claimable only after that ALSO returns empty'
map_records:
  evidence:
  - docs/retrospectives/2026-07-19-direction-without-mechanism.md
  - docs/design/FORWARD_DISCOVERY_DRAW_STATUS_FIX.md
