id: A8_experiment_loop_speed
level_current: 2
append_evidence:
  - "tools/tournament_runner.py::run_tiered_tournament + benchmark_tiered (Lever 3 TIERED TESTS: a cheap SCREEN tier culls the bottom candidates before the expensive FULL tier runs; fail-closed R15, deterministic C-S2, per-tier RSS hint for the in-process calibration hazard)"
  - "tests/tools/test_tournament_runner.py (tiered-tests: unit + R15 mutation-style guards + opt-in E2E measurement TOURNAMENT_RUNNER_E2E=1)"
append_simplification:
  - >-
    2026-07-16 H17 Lane-1 BUILD: LANDED the third named A8 in-scope lever (TIERED TESTS)
    and level HELD at 2 (honest, not bumped). Found this lever ALREADY BUILT but UNCOMMITTED
    in the working tree from a prior turn (tools/tournament_runner.py +262, its tests +246,
    ` M` unstaged) -- invisible and at risk (R2). Verified before landing rather than trusting
    the claim (R1): its opt-in E2E test had NEVER been run and FAILED on first execution --
    math.ceil(6 * survive_fraction=0.34) = 3 survivors, but the test asserted 2 (the author's
    own comment intended "top third = 2 of 6" but mischose the fraction; 2/6 = 0.333, so 0.34
    tips ceil to 3). The run_tiered_tournament math.ceil semantics ("survivors >= this fraction",
    max(1,...)) is correct and matches the passing unit tests -- the defect was the E2E's
    illustration parameter, not the code. FIX: E2E survive_fraction 0.34 -> 0.33 (yields exactly
    2 survivors / 4 culled, honouring the documented intent AND better illustrating the lever's
    point: cull the MAJORITY cheaply), code semantics unchanged. RE-RAN GREEN: tiered E2E 1 passed
    in 202.83s -- screen culled 4 of 6, full tier ran ONLY the 2 survivors (full_summary lives_total=2),
    tiered total_wall < naive_wall assertion held (a real, load-varying speedup, exact seconds not
    pinned by design). Fast suite 31 passed / 4 skipped; blast-radius tests/tools + tests/background
    2131 passed / 5 skipped, zero regressions; epistemic verifier PASS (510 files). FAIL-CLOSED
    (R15) proven by mutation-style tests: survive_fraction outside (0,1] refused; a screen-FAILED
    life can never be promoted to the full tier however generous the fraction; the publish-path
    output-dir guard fires. Determinism (C-S2): survivor selection is a pure (score desc, life_id asc)
    sort, no RNG. HONEST LEVEL RATIONALE (held at 2): A8's headline is closing the ~8-10x Epoch-4
    feasibility gap. The BIGGEST lever (mock-interface / RecordedSimInterface composition) is still
    BLOCKED on ARCH1 wiring -- re-verified this turn: saas/reporting/annual_report.py (what each life
    subprocess runs) has zero SimInterface/build_sim_interface references, so SIM_RECORDED_TRACE is a
    no-op on the real run path; wiring it is a cross-cutting saas/company/sim change OUTSIDE A8's
    tools/background/tests file_scope. Of A8's OWN in-scope secondary levers, parallelism/self-calibration
    (lever 2, landed earlier today) and tiered tests (lever 3, this pass) are now built + mutation-tested,
    but deterministic-stage caching remains unbuilt and the headline ~8-10x gap is NOT closed by tiering
    alone (tiering cuts N for the expensive tier; it does not turn a 500s full life into seconds). So this
    is a genuine, measured, in-scope slice -- not the atom's completion. loop_stage stays build; unblocks
    toward L3 when ARCH1 wires build_sim_interface into the annual_report run path (mock composition win)
    and/or deterministic-stage caching lands.
written_by: claude-code/H17-Lane1-A8
written_at: 2026-07-16T16:14:14Z
