id: H23_frame_saturation_draw_marker
level_current: 2
append_simplification:
  - "2026-07-16 H17 LANE-1 BUILD (fork, F1 inbox) -- closed the residual FALSE-NEGATIVE
    registered as this atom's own queued finding (simplification note[4], SELF_INTERRUPT).
    DEFECT (observed, R9): background/supervisor.py::_atom_has_frame_doc keyed saturation
    on the `docs/design/frame/` path PREFIX, so ~11 idle BUILD-gated atoms carrying a
    COMPLETE per-atom FRAME doc directly under `docs/design/` (the older non-canonical
    path -- W1_10 docs/design/W1_10_FRAME.md, H20 docs/design/H20_PARALLEL_MAINTENANCE_LANE_FRAME.md,
    H21 docs/design/H21_SELF_CONTAINED_ESCALATION_FRAME.md, ...) read as un-saturated and
    were re-handed to the idle DISCOVER/FRAME draw indefinitely -- the exact treadmill
    this atom exists to stop, just relocated to a different atom set. FIX: broadened the
    prefix from `docs/design/frame/` to `docs/design/` (any depth); the `FRAME`-in-FILENAME
    gate is unchanged, so shared surveys (LANE3_..._BUILD_GATE_SURVEY, no FRAME) and
    DISCOVER-stage docs (..._DISCOVER.md, no FRAME) are still correctly NOT counted --
    a genuinely-unframed atom is never falsely marked saturated. Computed, cannot decay
    (MAKE_IT_STICK: chose the broadened intrinsic check over the decay-prone per-atom
    `frame_saturated: true` setter -- remedy (b) of note[4], not (a)). SAFE on the live
    map: verified every non-canonical *_FRAME.md is owned by exactly ONE atom (no shared
    FRAME-named doc outside frame/), so no false-positive via a shared doc. R15 mutation
    test tests/background/test_frame_saturation_draw_marker.py extended +5 tests (16 total,
    all green): non-canonical-path FRAME doc reads saturated (the fix); a DISCOVER-stage
    doc under docs/design/ still reads un-saturated (false-positive guard, C13 case);
    FIRES integration on BOTH draw entry points; a guard-mutation control that restores
    the narrow pre-fix prefix and asserts the atom IS then re-handed (fails if the fix is
    reverted). VERIFIED on the LIVE map: post-fix idle draw now SKIPS W1_10/H20/H21
    (saturated=True) and re-points at genuinely un-framed atoms (H8, A4, C4, plus C13/W1_5
    which have real FRAME work); G4_unified_failure_register REMAINS a residual false-
    negative (its FRAME doc docs/design/UNIFIED_FAILURE_REGISTER.md carries neither FRAME
    nor its id/slug in the filename -- out of reach of any safe filename heuristic) and
    still needs the documented `frame_saturated: true` explicit override, a one-line
    MAP-WRITER step a Lane-3/BUILD fork cannot do (F1). Full suite tests/background/
    test_supervisor.py 143 green (no regression in the two touched draw functions),
    epistemic verifier PASS (510 files, harness-infra only). Level HELD at 2 (H23 was
    already at target; this is a correctness/HARDEN increment closing a residual leak, not
    a level move). Authority: H23 is loop_stage=build, twin-APPROVED for BUILD-open
    (director_twin_log.jsonl @ 2026-07-16T17:57:36, canon v2 SS3a, reversible/not a
    one-way door), Epoch-2 open; Rule 0 (PRIME DIRECTIVE) authorised yielding the
    genuinely churn-saturated Lane-3 FRAME dial to land this registered mechanism fix
    (empty/churning feasible set is a DIAL defect, take the work that exists)."
  - "2026-07-16 DEPLOYMENT GAP (R2, observed with evidence -- NOT self-fixable this turn):
    the running background/executor_daemon.py (PID 704593) started 19:27:11, SEVEN minutes
    BEFORE the frame-saturation guard itself landed (commit d2f1b420a @ 19:34:02), so it
    holds the PRE-GUARD supervisor module in memory and keeps dispatching headless turns a
    STALE draw of the path-convention-saturated 6 (H17/W1_2/B4/B5/W4_2/W1_3) -- exactly the
    doorbell THIS turn was handed (R7: verified against live code, which correctly excludes
    them). This fix (and the original guard) are 'committed != running' until that daemon is
    RESTARTED. A fork CANNOT safely restart the daemon that owns its own process mid-turn
    (killing PID 704593 risks losing this turn's pushed result via the broken stdout pipe),
    so this is flagged for the interactive session / next daemon lifecycle, not actioned
    here -- the honest R2 deployment step, decoupled from the code fix which is landed and
    pushed."
