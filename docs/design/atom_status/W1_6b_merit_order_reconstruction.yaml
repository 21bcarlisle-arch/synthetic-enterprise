id: W1_6b_merit_order_reconstruction
# PROPOSED level only (FRAME §4: any level claim stays blocked_on: director_level_up).
# Engine + falsifiable test built; 2 of 3 exit criteria MET; criterion 1 PARTIAL (3/5 calm
# cells) with a named, non-fabricable data blocker. NOT L3 — the headline reconstructibility
# criterion is honestly not yet met. The level gate reverts an unratified bump; that is expected.
level_current: 1
append_evidence:
  - sim/merit_order_reconstruction.py
  - tests/sim/test_merit_order_reconstruction.py
  - simulation/run_merit_order_reconstructibility.py
  - docs/fidelity/W1_6b_merit_order_reconstructibility_evidence.md
append_simplification:
  - >-
    2026-07-28 BUILD W1_6b (worker tick, front SIM_ACTORS open, file_scope sim/tests-sim only;
    map untouched per the fork/atom_status contract). Built a structural typed-SRMC merit-order
    dispatch engine (sim/merit_order_reconstruction.py): ordinary-day price = SRMC of the marginal
    (last-dispatched) plant against residual demand, NOT a fitted gas-floor multiplier. Carbon is
    ACTIVE (grounded CPS ~£18/tCO2, HMRC time-invariant) where the live price_engine held it at 0.0;
    merit-order shape comes from declining marginal CCGT efficiency (best-build 53.7% -> worst-vintage,
    band anchored to the DUKES fleet-average), NOT A1*x; oversupply collapses toward a curtailment
    floor; tight hours climb convexly to the £6,000/MWh ceiling. Every constant grounded (DUKES
    5.10.C/5.14, DESNZ GHG 2024, HMRC CPS) or a NAMED R10 GAP. EXIT CRITERIA -- (2) unmoved-baseline
    invariant MET (gas_floor_alone reproduces gas/THERMAL_EFFICIENCY 0.50 zero-carbon; naive family
    checked by identity); (3) R15 mutations MET (mis-ordered stack fires the monotone check; per-cell
    verdict fires against aggregate-hiding crisis-carry); (1) reconstructibility PARTIAL -- measured
    on 82,760 real calm-window ordinary hours the reconstruction WINS 2018/2019/2020 (incl. both
    renewables-heavy cells 2019 +3.64, 2020 +3.25 where the live form posted NEGATIVE lift) but LOSES
    the low-carbon early cells 2016/2017. Root cause (R12 diagnostic, PROVEN load-bearing, NOT a cue
    to tune): the ordinary-hour markup grows +£6.6->+£14.2/MWh 2016->2020 tracking the real EU-ETS
    carbon surge; with only flat CPS-only carbon available it overshoots the low-ETS early cells. The
    binding missing input is the EU/UK-ETS market carbon TIME-SERIES -- a NAMED R10 GAP (DISCOVER §4b)
    that R13/FRAME §4 forbid fabricating from memory. NO interim tuning applied (R12/FRAME §3c). ets_
    price_gbp_per_tonne is wired through every function (default 0.0) so a later citable ETS series
    closes 2016/2017 WITHOUT any curve-fit -- the engine is structurally complete, starved of one
    grounded input. NEXT: queue a DISCOVER for the ETS annual-average series (do NOT re-BUILD; do NOT
    tune). PROPOSE blocked_on: director_level_up for the level, and the ETS-series DISCOVER as the L3
    unblock. Evidence: docs/fidelity/W1_6b_merit_order_reconstructibility_evidence.md.
written_by: worker-tick/W1_6b_merit_order_reconstruction
written_at: 2026-07-28T00:00:00Z
