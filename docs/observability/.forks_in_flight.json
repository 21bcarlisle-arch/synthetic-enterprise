{
 "purpose": "Durable, machine-readable record of BUILD forks dispatched but not yet merged. Written because the 2026-07-30 tick found the draw is BLIND TO WORK IN FLIGHT (registered on H10): it dispatched a SITE_EH2 fork while an uncommitted SITE_EH2 build already sat in a prior tick's worktree. Any tick MUST read this before dispatching, and must not re-dispatch an atom listed here as in_flight.",
 "written_by_tick": "2026-07-30T00:0xZ worker tick",
 "updated_by_tick": "2026-07-30T01:3xZ worker tick (W1_6b + F1b + HX2 concurrent draw)",
 "caveat": "This file is a RECORD, not a mechanism. Nothing enforces that it is read or kept current -- it is prose-in-JSON and will decay exactly like the doorbell's fork-lifecycle rule unless the reaper/draw-guard is actually built (H10 BUILD candidate). Do not mistake its presence for a working control.",
 "DRAW_GUARD_NOW_BUILT_2026_07_30T01xxZ": {
  "what_was_built": "background/supervisor.py::_unmerged_work_paths + _atom_collides_with_unmerged + _prefer_unmerged_free, wired into BOTH code-writing draws (_maturity_map_draw_concurrent for BUILD and _site_lane_draw_concurrent for SITE). The draw now reads GIT REALITY -- branches with commits not in trunk, plus uncommitted edits in every linked worktree -- and deprioritises any atom whose declared file_scope overlaps that work.",
  "why_this_one_should_hold_where_two_records_did_not": "Both prior attempts (.build_in_progress.json marker, and THIS file) required someone to voluntarily WRITE them, and both were empty/unread at the moment they were needed. A branch with unmerged commits is a FACT of the repo -- it cannot be forgotten. MAKE_IT_STICK: mechanism, not exhortation.",
  "proven_on_real_state": "With the live repo as-is: SITE_EH1_segment_disclosure collides=True, SITE1_expert_doors collides=True, W1_6b_merit_order_reconstruction collides=False. i.e. the guard returns exactly the verdict THIS tick had to reach by hand, and would have prevented both double-dispatches.",
  "R15": "tests/background/test_unmerged_work_draw_guard.py -- 9 tests against a REAL throwaway git repo (never the live tree), including test_MUTATION_guard_without_git_signal_redraws_the_rival (removes the signal, asserts the rival IS re-drawn, so the control is measured not assumed), a Rule-0 all-colliding-still-draws test, a fail-open test, and a non-'main' trunk test closing the fail-silent hole.",
  "HONEST_ABOUT_WHAT_IS_STILL_NOT_BUILT": "Only the DRAW-SIDE half exists. There is still NO REAPER: dead worktrees are not automatically detected or cleaned, and nothing merges a finished fork home. This tick removed the LOSS risk a different way -- all five rescue branches are now pushed to origin (verified sha-for-sha), so `git worktree prune` can no longer destroy them -- but that is durability, not lifecycle. A fork that dies still needs a human/tick to notice and land it.",
  "ALSO_STILL_TRUE": "The guard is a SOFT deprioritise, not a wall (Rule 0). If every candidate collides it keeps the full set, so a map saturated with unmerged work CAN still re-offer in-flight work -- by design, since a guard that zeroes the feasible set is itself a Rule-0 defect. The real remedy for that state is landing the backlog below, not tightening the guard."
 },
 "rescues_pushed_to_origin_2026_07_30": "All five branches in rescued_awaiting_merge are now on origin (pushed + verified sha-for-sha this tick). The ~4,270 lines are no longer one `git worktree prune` from loss. They are still UNMERGED and still need reconciliation -- durability is not disposition.",
 "PREDICTION_CONFIRMED_THE_RECORD_DID_NOT_WORK": {
  "what_happened": "The very next scheduled tick (00:2xZ) drew W1_9_dsr_flex_markets, SITE1_expert_doors and SITE_EH1_segment_disclosure -- ALL THREE already had work in flight or were explicitly deferred by this file one tick earlier. SITE_EH1 was listed here as 'dispatched_awaiting_return'; SITE1_expert_doors was listed here under deferred_this_tick as NOT provably disjoint. The draw read none of it.",
  "worse": "SITE_EH1 had in fact been dispatched to TWO concurrent forks, not one -- a second, independent implementation (836 lines, population-declaration approach) was found in an unregistered worktree. Neither fork knew about the other. The double-dispatch pathology this file recorded for SITE_EH2 was already happening again, unrecorded, for SITE_EH1.",
  "cost_if_unfixed": "Three dead forks held 2,689 uncommitted lines. Combined with the 1,581 lines rescued the previous tick, ~4,270 lines have now come within one `git worktree prune` of being lost.",
  "conclusion": "A record that must be voluntarily read is not a control. The fix is the H10 draw-guard/reaper: the draw itself must exclude atoms with live or unmerged work, and dead worktrees must be reaped automatically rather than by whichever tick happens to look."
 },
 "in_flight": [
  {
   "atom": "W1_6b_merit_order_reconstruction",
   "file_scope": [
    "sim",
    "tests/sim",
    "docs/fidelity"
   ],
   "dispatched": "2026-07-30T01:3xZ",
   "level": "0->3",
   "note": "re-verify the 2026-07-28 efad226db build claim before any level move; crit-1 was 3/5 calm cells pending a carbon time-series DISCOVER (R10 gap, no tuning per R12/R13)"
  },
  {
   "atom": "F1b_company_comms",
   "file_scope": [
    "company/comms",
    "tests/company/comms"
   ],
   "dispatched": "2026-07-30T01:3xZ",
   "level": "0->2",
   "note": "re-verify the R15-proven L2 claim + audit the estimator for an epistemic-wall violation (must learn from observed replies ONLY)"
  },
  {
   "atom": "HX2_stall_set_coverage_verdict",
   "file_scope": [
    "background",
    "tests/background",
    "docs/design"
   ],
   "dispatched": "2026-07-30T01:3xZ",
   "level": "0->3",
   "note": "four director-named events; verdict already-detected / add-detector / argue-not-stall-class, each to the R15 standard"
  }
 ],
 "rescued_awaiting_merge": [
  {
   "branch": "worktree-agent-a7f7adeb627f11410",
   "sha": "8a7a90f660d45ab4339fda0ddada44d1d0824b69",
   "atom": "SITE_EH3_figure_reconciliation_and_periods",
   "lines": 772,
   "gated": false,
   "committed_with": "--no-verify",
   "blocker": "collides with SITE_EH1 fork scope; and the site/company render half is a SILENT NO-OP without its generator half -- land both halves together or neither"
  },
  {
   "branch": "worktree-agent-a6ad9f2b324019a71",
   "sha": "d7b3db614dfc9e3faf8c3df8c0ac1cf672d46305",
   "atom": "SITE_EH2_predictions_ledger_can_fail",
   "lines": 809,
   "gated": false,
   "committed_with": "--no-verify",
   "blocker": "STILL UNRECONCILED, branch INTACT. Its rival (aebe35df16c7670fd) LANDED on main as a7f71b3b0 on 2026-08-03, so main now carries that implementation of tools/generate_proof_data.py + site/proof/index.html + site/proof/_door_harness.mjs. This branch is NOT superseded wholesale: it is a much broader 34-file branch off a DIFFERENT merge-base (a0dbc262f) also carrying docs/design/PUBLISH_GATE_TESTS_WRITE_LIVE_SURFACES_FINDING.md, saas/reporting/annual_report.py, simulation/household_segments.py, tools/generate_dashboard_data.py, tools/generate_world_data.py and 6 test files incl. SITE1 proof-door work. Reconcile the non-proof-data remainder against main; do NOT merge blind and do NOT prune. Its proof-data half is the one that lost the reconciliation."
  },
  {
   "branch": "worktree-agent-abdcad5f797657eb3",
   "sha": "3f79e7250",
   "atom": "SITE_EH1_segment_disclosure",
   "lines": 818,
   "gated": false,
   "committed_with": "--no-verify",
   "rescued_by_tick": "2026-07-30T00:2xZ",
   "blocker": "RIVAL to 464998e56 (below) -- same atom, two independent implementations. Also collides with the SITE_EH3 rescue on tools/generate_dashboard_data.py + tools/generate_world_data.py."
  },
  {
   "branch": "worktree-agent-a416449f398f3b333",
   "sha": "464998e56",
   "atom": "SITE_EH1_segment_disclosure",
   "lines": 836,
   "gated": false,
   "committed_with": "--no-verify",
   "rescued_by_tick": "2026-07-30T00:2xZ",
   "approach": "population-declaration on every runtime anchor row: UNDECLARED is rejected by the gate, MISMATCH is rendered visibly. Explicitly changes no RAG rating, benchmark or sim value (R12/R13).",
   "blocker": "RIVAL to 3f79e7250 (above). Reconcile the two; do not assume either is correct."
  }
 ],
 "merged_to_main": [
  {
   "atom": "W1_9_dsr_flex_markets",
   "rescued_from": "worktree-agent-a4d7fb957f71f8125",
   "rescue_sha": "18a0da8f0",
   "lines_rescued": 1035,
   "merged_by_tick": "2026-07-30T00:2xZ",
   "how": "FILE-SCOPE cherry-pick, NOT a branch merge -- the branch was behind main and carried stale site/ and docs/ artifacts that would have REVERTED published data (the clobber harm recorded in cb1a00af2). Only sim/flex_dispatch.py, company/market/flex_participation.py, background/flex_dispatch_triad.py and the new tests were taken.",
   "what_was_missing": "The rescued build had ZERO tests and its HARNESS half was never written -- the triad imported the stacked API but never called it. 54 tests + measure_l3/build_gap_summary_l3 were written this tick before the level moved.",
   "level": "0->3 self-certified to gate_authorizations.jsonl (R16)"
  }
 ],
 "deferred": [
  {
   "atom": "SITE1_expert_doors",
   "reason": "file_scope is all of `site`, so it overlaps BOTH its own remediation children (EH1, EH2) AND the two unreconciled EH1 rivals. Not provably disjoint -- cannot be a concurrent fork. Also: polishing the parent before its own Expert-Hour findings land is backwards. Drawn again by the 00:2xZ tick despite being deferred here one tick earlier."
  },
  {
   "atom": "SITE_EH1_segment_disclosure",
   "reason": "TWO rival implementations now sit rescued (3f79e7250, 464998e56). The next tick's job on this atom is RECONCILIATION, not a third implementation. Dispatching a fresh fork would create one."
  }
 ],
 "DISJOINTNESS_CHECKED_THIS_TICK": "Verified against GIT REALITY, not the record: for each of the 5 unmerged branches, diffed merge-base..branch (NOT main..branch, which shows staleness as false collisions -- main..branch reported 3,700+ phantom deletions and false hits on background/supervisor.py). Real changed-file counts are 5-8 per branch, all confined to tools/ and site/docs surfaces. ZERO overlap with sim/, company/comms/, background/, tests/sim, tests/background, docs/design. The three atoms drawn this tick are provably disjoint from each other and from all unmerged work.",
 "landed_2026_08_03": {
  "branch": "worktree-agent-aebe35df16c7670fd (REAPED: worktree removed, branch -D'd after landing)",
  "atom": "SITE_EH2_predictions_ledger_can_fail",
  "landed_as": "a7f71b3b0 on main, pushed (verified reachable from origin/main)",
  "how": "ADOPTED, not cherry-picked: main had not touched any of the four non-generated files (tools/generate_proof_data.py, site/proof/{index.html,_door_harness.mjs,test_predictions_ledger_can_fail.py}) since the branch point, so the branch content was checked out onto main and committed with explicit pathspec under tree_lock. The three original commit SHAs (8a8340022, 23c3e8694, 336db0261) are recorded in the landing commit message; the branch objects are gone bar the reflog.",
  "gate": "Landed THROUGH the full pre-commit gate -- 213 targeted + 434 site tests green. The rescue commits had been made with --no-verify, so this is the first time this work was gated.",
  "defect_fixed_on_adoption": "test_live_repeated_c9_prediction_is_one_row pinned live-derived literals (relog_count == 4, the date 2025-06-29, 're-logged 4x') inside a control that gates publishing. The decision log is appended to every run, so it would have wedged the publish gate -- the same class as the 4-day blackout. Rewritten relationally and R15-mutation-proven both ways.",
  "reconciliation_NOT_done": "The registry required the two rival SITE_EH2 implementations be 'reconciled on the R15 requirement, not merged blind'. That reconciliation was NOT performed. This one was chosen on evidence -- it is the only one that passes the gate, and it carries the explicit R15 can-record-a-MISS control the atom was minted for -- but the rival's remaining 30+ files were never diffed against it. See the surviving rival entry above."
 }
}