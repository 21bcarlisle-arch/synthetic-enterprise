# Idle-atom predicate report (BUILD_THE_BACKLOG.md P0, 2026-07-13)

**Snapshot:** 87 atoms, 49 below-target, 5→21 in BUILD after this action.

## Why each below-target atom was not building — grouped by cause

### BUILD-GATED, epoch-2, never opened (12) — the real governance gap
These were epoch-2 and buildable but `loop_stage: idle` — never BUILD-opened. The
morning's BUILD-open named ~6 specific atoms, not a policy, so these sat idle:
`A6_coupled_triad_gap_metric` (dial3 — **the linchpin every twin's gap needs**),
`W2_2_population_draw` (dial3), `SITE1_expert_doors` (dial3), `H10_worktree_isolation`,
`G3_method_ip_worktree_retro`, `C11_segment_debt_policy` (dial3), `W4_2_verifier_timing_extension`,
`A1_learn_loop_chair`, `F6_bill_integrity_structural` (dial1), `F7_obligations_register_coverage` (dial1),
`H15_publish_gate_failure_alert` (dial1), `H16_idle_detection_stability_gate` (dial1).

### DEPENDENCY-BLOCKED (6) — the company twins
Every affordability twin depends on its **Epoch-3 world atom** AND on **A6**:
C6←W2_4+A6, C7←W2_5+A6, C8←W2_6+A6, C9←W2_7+A6, C10←W2_8+A6, C12←W2_10+A6.
So the twins could never build while A6 was unopened and the world atoms were epoch-deferred.

### EPOCH-DEFERRED, ≥E3 (27) — cannot move level_current by design
Counted separately (FRAME/DISCOVER only until their epoch opens). **Includes the entire
affordability WORLD cluster** (W2_4–W2_10, all **Epoch 3**), the weather-physics cluster
(W1_3–W1_10, E3), and E4/E5 atoms (B4, A4, A5, C4, C5, D4, F4, F5, H8, ARCH1).

### IN-BUILD (was 4): H11, BRAND1, H12, H14.

## The correction to the advisor's hypothesis (evidence, per R9)
The advisor guessed "the morning BUILD-open named 6 atoms, so the cluster is still
BUILD-gated — a governance bug." **Partly right, but the sharper truth:** the affordability
*world* atoms (W2_4–W2_10) are **Epoch 3**, not epoch-2-never-opened. Building them now is a
**curriculum decision to open the Epoch-3 affordability cluster ahead of Epoch 2's exit test** —
which the director has explicitly made in this directive. The genuine epoch-2 governance gap was
narrower: **A6 (the gap-metric linchpin) and W2_2 were never opened**, which alone blocked all 6
twins.

## Action taken (this directive)
BUILD-opened 16 atoms: **A6** + **W2_2** (profile B registered) + the affordability world cluster
(W2_4–W2_10, Epoch-3, director-opened) + the twins (C6–C12). The coupled-triad gate
(`world_l3_blocked`) still enforces the coupling: each world atom is capped at L2 until its twin is
≥L2 and the belief-vs-truth gap is measured — so they build as coupled pairs, not as scenery.
Weather-physics W1_x stays FRAME-only (director's "physics first, don't start building" holds).

## Dial weights in play
Affordability world + twins: dial 3 (except C6/C8/C11/C12 world-facing dial 3). A6 dial 3.
The low-dial epoch-2 leftovers (F6/F7/H15/H16 dial 1) rank below the cluster.
