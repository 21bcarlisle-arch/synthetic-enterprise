# [DIRECTOR-RULING] — Wedge #3: unwedge NOW, and the rung-1 rule gets MECHANISED this time (2026-07-24)

**Type:** [DIRECTOR-RULING] via advisor bridge. Priority zero.

## 1. Unwedge (third wedge in 36 hours)

The publish gate has failed 6+ consecutive times since ~06:23Z; the tick has drawn nothing since 06:20Z despite the alarms. Diagnose with evidence (R9): cause, whether it is the fold's link/nav changes tripping a gate invariant (the pattern of wedge #2), fix, flush, and R11-verify the folded site + £80k/customer page live. One-line cause in the NTFY.

## 2. The meta-finding, named: the rung-1 wedge rule was consumed and never built — SECOND instance on the same rule

Yesterday's UNWEDGE_PUBLISH_PRIORITY_ZERO ruling ordered: "a publish-gate wedge older than 60 minutes is priority-zero drawable work — mechanise with a failing test." This morning proves it was never mechanised: 2h17m of alarms firing into tick silence, again. That is the second consumed-not-absorbed on the same rule. **Build it now, before anything else after the unwedge:** the scanner treats an active wedge alert older than 60 min as rung-1 drawable work; failing test reproduces this morning's exact state (gate failing + alerts firing + tick idle); prove both ways; deploy R2-verified into the running scanner (shadow-then-swap rails apply). The daily note carries this as a named breach of its own enforcement list.

## 3. Then resume

SITE_MODEL_SPINE evidence pages per the campaign; premise-demand publish and the follow-ons ranked doc remain owed and named in tonight's note.

**Risk & proportionality:** unwedge = diagnosis-first; scanner change under the shadow rails. Tag: **priority zero; proceed.**

— Advisor bridge, 2026-07-24.

---

## §4 (advisor addendum) — The sanity daemon's bill findings: route into the rung-4 defect backlog now

Three standing/new findings, finally read in full: **C4g (2017-07-31): the total bill is NEGATIVE** — implausible for a UK residential customer; **C3 (2019-03-31): VAT £5.11 misaligned with 20% on ex-VAT charges**; and the standing "£58.66 less than…" total-consistency flag. Bill accuracy is Tier-1 domain. Route all three into the defect backlog as **verify-first atoms**: check each against the raw bill record (the Qwen skeptic has known false positives — verify before acting); any confirmed defect is absurdity-class → R10 class fix extending the bill-invariant library (negative-total and VAT-consistency invariants), per DOMAIN_SENSE_AND_COMPLIANCE. Report verify verdicts in tonight's note.

## §5 (advisor addendum) — Answer the coupling question with journal evidence

Second time in twelve hours: publish gate wedges → tick goes silent → alarms fire into nothing. Yesterday's priority-zero asked "were turns attempted and dying at the gate — enumerate"; this time answer with the journal: for the silent window (06:20Z→unwedge), list the tick cycles — drew/rested/errored — and state the mechanism coupling wedge to silence. If turns are dying at the gate, that is its own class (turns must fail loudly and count as incidents, not vanish); if the scanner rests because the wedge blocks a precondition, that precondition is the bug. Either way it gets a named failing test alongside the rung-1 mechanisation.

— Addendum by advisor bridge, same morning.
