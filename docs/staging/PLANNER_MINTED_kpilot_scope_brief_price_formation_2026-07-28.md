# [PLANNER-MINTED] K-pilot deliverable 1 — scope brief for wholesale price formation, written BEFORE assembly (2026-07-28)

**Source ruling:** DIRECTOR_RULING_KPILOT_SCOPE_FIRST_2026-07-28.md — deliverable 1 (WORK THIS CREATES).
**Serves:** DIRECTOR_AXES Axis 1 (Website) + Axis 3 (Believability). Fidelity-ledger row: price-formation (maturity_map W1_6 weather→price chain, L3). Campaign follow-on: the knowledge-page pilot on `/knowledge/wholesale-price-formation/` (SITE_MODEL_SPINE knowledge lane).
**Real-world fidelity gained:** a reader-facing definition of what a *complete* treatment of GB wholesale price formation must contain — the traded-product structure (baseload/peak, seasons/quarters/months, day-ahead/within-day and why shape matters), gas as the global anchor (NBP/TTF, LNG, gas setting the power price for most hours), weather's route into demand AND generation, interconnectors/imports, CfDs and renewable support and their effect on the merit order and on what consumers pay, negative prices (when/why/how often), and seasonality across the year — so the page can be measured against real understanding, not against what we already hold.
**Lane:** DISCOVER / FRAME (doc-only; no `sim/`/`company/`/site code).
**Target level:** doc-only artefact in `docs/design/` (no maturity-map level claimed).
**Exit criteria:**
- A scope brief exists at `docs/design/KPILOT_SCOPE_BRIEF_PRICE_FORMATION.md` listing the sections a complete treatment must contain, the questions each answers, and the phenomena that MUST appear — written independently of the current page (assembly does not define scope; §1 of the ruling).
- **Blind-ish per the ruling's §1.4 board-spec pattern:** the brief is derived from an instrument that cannot see the current build — propose the method in the doc (candidate: reuse/extend BOARD_SPEC_004_PRICE_FORMATION as the blind practitioner anchor, or a fresh blind pass; state which and why). The brief must NOT be reverse-engineered from `site/knowledge/wholesale-price-formation/index.html`.
- Every director-named gap in ruling §3 appears as a required item in the brief (traded-product structure, gas-anchor, weather, interconnectors, CfDs, negative prices, seasonality).
**Deps:** NONE — this is the FIRST deliverable and blocks #3 (decomposition) and #5 (delta). Runs before further assembly.
**Propose-then-proceed window:** proceed by default (ruling tagged **proceed**; doc-only, fully reversible, machinery untouched). One-way-door walls untouched.

## Deliverable (verbatim + guidance)
> 1. A scope brief for the topic, written before further assembly — blind or blind-ish, method proposed.

Ruling §1: "assembling from existing material can only reproduce existing material, gaps included, because the material does not know what it is missing." Write the scope brief FIRST, independently of what we hold; then assemble against it (#2/#4); the brief-vs-assembly DELTA (#5) is the primary finding. The ~80%-derivation thesis is WITHDRAWN as a design goal (R12) — measure derivation if useful, never let it shape scope.
