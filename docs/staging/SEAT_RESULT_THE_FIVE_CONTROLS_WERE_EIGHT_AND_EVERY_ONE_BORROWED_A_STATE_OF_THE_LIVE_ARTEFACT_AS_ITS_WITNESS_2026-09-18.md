**Severity:** LATENT · **Lane:** A_strategy_governance · **Epoch:** 3 · **Atom:** value-arms-error-bar

# The five controls were eight, and every one of them had borrowed a state of the live artefact as its witness

**Filed:** 2026-09-18 · **Claim id:** `republish-the-arms-decomposition-over-one-priced-book`
**Grades:** `47d3115f7` (the renewal-funnel repair), `b329e702b` (the arms fix),
`49f49dd8e` (the keyed-to-today's-answer class)

---

## The drawn item's first clause was SPENT, and the merge was owed for a different reason

The item said *"Merge `origin/main` first (HEAD is one behind `47d3115f7`; verify it really does
repair the three `test_the_renewal_funnel.py` legs rather than taking this line's word)."*

Measured, not taken on the line's word:

| the item's claim | measured |
|---|---|
| HEAD is one behind `47d3115f7` | **false** — `47d3115f7` is already an ancestor of HEAD |
| the three renewal-funnel legs need repairing | **false** — `24 passed` at HEAD |

So the premise as written was spent. The merge was still owed, for a reason the item did not give:
`origin/main` carried **135 lines** of the rival lane's per-term gate census inside the two files
this work edits, and my local HEAD had none of it. Landing on a stale HEAD would have set up the
merge that adopts one side's rewrite and silently deletes the other's.

**The duplicate-work check was right to fire and the answer is CARRY ON.** The live claim
`the-product-gate-census-answers-per-record-while-the-guard-refuses-per-term` holds the same two
paths, but its subject is the product gate's unit (the term); this one's is the arms decomposition
over one priced book. Different work, same files — which is exactly why the merge came first.

## The five controls were EIGHT, and the count was established by a one-variable run

The item and `SEAT_RESULT_..._SIX_CONTROLS_REFUSE_THE_REPUBLISH...` named five of mine. I promoted
the 09-18 run inside a `git archive` extract and ran the file: **12 failed, 235 passed**. Then the
one-variable version — the same extract with only the promotion reverted: **4 failed, 243 passed**.

The four that fail in BOTH arms are extract artefacts, not findings: a clean `git archive` extract
has no `.git`, so every control whose subject is a git tree reads `None` on both sides. They are
`test_every_input_to_the_published_supplier_claim_IS_IN_THE_PUBLISH_SURFACE`,
`test_the_published_supplier_claim_answers_THE_SAME_from_HEADs_committed_bytes`,
`test_the_objective_difference_is_read_from_the_TREES_and_never_from_the_filename` and
`test_the_control_arm_is_PINNED_so_a_promotion_cannot_withdraw_the_experiment`.

**Eight are caused by the promotion** — three more than the item carried, and two of the three are
controls the PREVIOUS invocation added in its own unlanded staleness work, which had the identical
defect it was written to fix.

## They are one class, and this file had already named it and not applied it

Every one of the eight **borrows a state of the live artefact as its witness**, and the promotion
takes that state away:

| control | the borrowed state |
|---|---|
| `..._selection_leg_and_its_error_bar_are_published_together` | the live pair has a bar |
| `test_the_error_bar_bounds_the_FIGURE_THE_HEADLINE_STATES` | the live pair has a bar |
| `..._spread_from_another_world_bounds_nothing_however_it_is_stamped` | the live pair has a bar |
| `..._seed_spreads_does_not_let_NEVER_ASKED_pass...` | the live pair has a bar |
| `..._floor_drawn_over_a_DIFFERENT_book_is_refused_however_recent_it_is` | the stamp rule admits the live floor |
| `..._later_run_in_this_world_that_disagrees_about_the_split...` | the live run is level-dominant |
| `..._leg_whose_own_redraws_straddle_zero_states_no_direction_however_stable` | the live draw agrees with its family's centre |
| `..._level_arm_may_not_say_it_priced_the_same_renewals_unless_the_run_says_so` | no run on disk answers `same_priced_population` |

The mechanism for this already existed **in this same file**: `_stamped_after`, written 2026-09-09
because *"ten controls whose subject is the WORLD guard, the LEG guard or the STABILITY guard were
refused by the STALENESS guard instead, and each reported the failure of the guard it names rather
than the one that fired."* It was never applied to these eight. This is the eleventh through
eighteenth instance of a class with its own helper, its own docstring and its own price already
written down — and `_floor_without_a_book`, four hundred lines away, carries the same lesson about
borrowing an ABSENCE as a witness, which is precisely defect eight.

**The repair is the one those docstrings prescribe: bind the pair, keep only a live leg that can
get MORE true.** No assertion was relaxed.

## The live leg is not decoration — the gate is load-bearing RIGHT NOW

On the promoted book the selection family sits **2.50 standard errors from zero against its own bar
of 2.11**. Absent the one-book gate the page would state a direction — off a family measured over a
different book from the run every other figure is drawn from. That is the live-page defect
`b329e702b` predicted the guard would prevent and `49f49dd8e` anticipated the class of.

So the live artefacts are not left unasserted. `test_the_LIVE_pair_states_no_sign_for_the_RUN_while_its_family_is_another_book`
asserts the **biconditional**: membership, the run's position in its family's range, and the sign
stand or fall together with "one book", whichever way the live pair answers, and the family's own
statistics are NOT blanked with them.

## R15 — six mutations, each applied, run and reverted

| mutation | leg that reddened |
|---|---|
| `_one_book`: absence answers `True` | the reachability partition, `{True, False}` |
| `_seed_spreads`: drop the world-mismatch gate | `..._another_world_bounds_nothing` |
| never compose the centre clause | the straddle-zero A2 witness |
| `_staleness_caveat` always fires | the `bounded_pair` fixture, **loudly** — 2 failed, not skipped |
| invert the derived witness sides | the flip/agree placement guard |
| force `one_book = True` past the gate | the live leg — **`assert ('negative' is None)`**, which is the live-page sentence verbatim |

The fourth is the one that matters most for a constructed fixture: a witness that stops standing for
its branch must RED, never skip.

## The NINTH, found by the gate, and a bound that could never bind

The gate refused the first landing on one red the original doc had listed as "not established":
`test_replicating_the_rows_moves_the_null_and_not_the_answer`. Established here, by the same
one-variable method: **green at HEAD with none of my changes, red with the promotion** — so it is
mine, and it is the ninth instance of the same class.

It walked a fixed ladder of `(1, 2, 4, 5, 6)` copies and then asserted `escaped_at <= 8`. **That
bound could never bind**: no rung above 6 existed to produce a value between 7 and 8, so what it
actually asserted was "the escape happens by the sixth copy". How many copies that takes is a
property of the RUN — it is set by how far the observed statistic sits from a half:

| | old book | 09-18 book |
|---|---|---|
| observed within-year AUC | — | **0.4574** (0.0426 from a half) |
| escape happens at | ≤ 6 copies | **10 copies** |

The ladder now searches to a cap of 32 instead of asserting where the escape lands, which is the
actual content of "more rows is not more evidence". **Not a relaxation** — mutating `_null` to a
null that never closes reds it, naming the run's own distance from a half:
*"the figure never left its own null within 32 copies (observed 0.4574, 0.0426 from a half) ... which
is a finding about the run, not a licence to lower the bar."*

**And that file is being edited in place by another lane right now** — a second hunk at `:194` that
is not mine and that reds `test_BOTH_availability_branches_can_be_taken`. This landing carries
HEAD's bytes plus this lane's hunk only, so their in-flight work is neither swept nor inherited.

## WHAT IS LANDED HERE, AND WHAT IS NOT

**Landed: the nine control repairs and the merge. NOT landed: the promotion.** The item's "done"
is the page rendering the 09-18 book, and that is one control short — see the tenth below. Landing
the repairs first is not a consolation prize: they are what the promotion is blocked ON, they are
correct independently of it, and leaving them unlanded would mean the next invocation re-derives
all nine.

**The proof they are keyed to the property and not to either book is that they pass on BOTH:**

| tree | result |
|---|---|
| HEAD, canonical = 09-10 run (unpromoted) | **285 passed, 0 failed** |
| HEAD, canonical = 09-18 run (promoted) | **288 passed, 0 failed** |

A control keyed to today's answer cannot do that. That is the whole claim of this change, and it is
measured rather than argued.

## THE TENTH, and it is why the promotion is not landed

The site lane refuses the promotion on
`site/test_the_stratified_concordance_reaches_the_reader.py`:
`test_a_run_whose_belief_DOES_rank_within_the_year_keeps_its_household_reading` and
`test_the_reading_a_reader_meets_is_the_one_the_producer_DECLARES`. Both are **green at HEAD with
none of my changes** and red with the promotion, so they are mine, and they are the same class
again — but the borrowed state is one layer further back than the other nine, so the repair is NOT
the same edit and I will not guess at it.

The fixture rewrites every `believed_p_retain` to rank retained above departed, which makes the
**stratified** figure clear its null (AUC 1.000 on 423 same-year pairs) exactly as intended. But
the household sentence is gated on the **UNSTRATIFIED** figure being outside its own null, and on
the 09-18 book that figure reads:

```
belief_vs_outcome.discrimination_auc = None
null_bound.inside_the_null            = True
```

So the page correctly says *"this run does not distinguish the belief from a coin flip"* and never
reaches the earned branch. **The fixture mutates the scored rows but not the precomputed
`discrimination_auc`, and it relied on the old book's size to carry the pooled figure past its
null.** On a 104-decision book it does not.

**The open question, which is a producer question and not a fixture question:** the 09-18 artefact
carries `discrimination_auc = None`. Whether that is a gap in the run producer or the honest answer
for a book this size decides the repair — construct the pooled ranking in the fixture, or file a
finding against the producer. Establish that before touching either. Do not relax the gate: it is
the one that caught a withdrawn claim being printed BECAUSE it was withheld (2026-09-04).

## A SENTENCE THE PROMOTION WILL MAKE FALSE, found on the way

`WITHDRAWN_CLAIMS`, entry dated 2026-09-18, currently tells the reader:

> *"A corrected re-run in which the two arms do price one book exists and is not published here
> yet; when it arrives, the bounded signs go with it."*

Promoting the 09-18 run publishes exactly that re-run, so that sentence becomes false **on the same
page, in the block whose entire job is to correct false sentences.** It must be updated in the same
commit as the promotion, not after it.

## The feed the promotion WOULD publish (measured, not landed)

Generated from the reconciled bytes with real git history available, so its own provenance resolves:

| field | value |
|---|---|
| `run_generated_at` | **2026-09-18T05:43:40Z** |
| `producing_commit.short` | **b329e702b** |
| `producing_commit.objective.established` | **true** |
| `selection_leg.sign` / `sign_is_stateable` | **None / false** |
| `single_run.is_a_member_of_the_family` | **false** |
| `realised.split.selection_gbp` | **+£4,327.01** (was −£332.64) |
| `realised.split.level_share_of_advantage` | **0.055** (was 1.020) |

The page now renders the 09-18 book, the reversal is published, and the bar refuses a direction and
says why. That is the item's "done" as written.

## THE MERGE WAS NOT OPTIONAL, AND THE MEASUREMENT IS WHY

I nearly landed this lane's hunks alone, on the reasoning that a three-way merge against
`origin/main` is clean for both files — which it is; I checked rather than assumed. The feed is
where that reasoning fails. Running BOTH generators over one extract and differencing the rendered
feed field by field: **43 fields differ, and 41 of them are the rival lane's entire
`decisions.who_the_method_has_priced.per_term` block, absent from this side.** Landing this lane's
generator alone would have published a feed with that block silently deleted — the merge that
adopts one side's rewrite, happening at the FEED rather than at the file, where no stale-copy
refusal would have seen it.

The reconciled generator also `import`s `tools.decisions_by_account_class`, which exists only on
`origin/main`, so the ordering is forced rather than chosen: these bytes cannot be landed onto a
HEAD that has not taken the merge.

## Still owed

1. **The folded-eighteen floor on the post-fix tree** — ~18 hours — which is what restores a sign
   to either leg. Until it runs the page's "we cannot tell" is the honest answer, not a placeholder.
2. **The one-variable run** that would attribute P2's reversal (`selection_gbp` −£332.64 → +£4,327.01).
   Four commits touching the value arm landed between the runs; the reversal is published
   **unattributed and the page says so**, which is the correct half of that trade.
