# [PLANNER-MINTED] K-pilot deliverable 2 — reconcile the PAGE against Board Spec 004, name the gaps (2026-07-28)

**Source ruling:** DIRECTOR_RULING_KPILOT_SCOPE_FIRST_2026-07-28.md — deliverable 2 (WORK THIS CREATES); ruling §2.
**Serves:** DIRECTOR_AXES Axis 1 (Website) + Axis 3 (Believability). Anchors on `docs/staging/done/BOARD_SPEC_004_PRICE_FORMATION_2026-07-22.md` (blind practitioner spec with disqualification battery). Campaign: knowledge-page pilot.
**Real-world fidelity gained:** every expectation in the blind Spec 004 either appears on `/knowledge/wholesale-price-formation/` (or a named child page) or is recorded as a named gap on the page — so the page's completeness is measured against a blind practitioner standard, not self-asserted.
**Lane:** DISCOVER (doc-only reconciliation; no site/sim/company code changes in this atom — page edits fall under #3/#4/#5).
**Target level:** doc-only artefact (no maturity-map level claimed).
**Exit criteria:**
- A row-by-row reconciliation exists (extend/site-facing companion to the machinery reconciliation) scoring each Spec 004 expectation as PRESENT-ON-PAGE / PRESENT-ON-CHILD / NAMED-GAP / ABSENT-UNNAMED against the actual rendered page content.
- **This is the PAGE-vs-spec reconciliation, distinct from the existing `docs/design/BOARD_SPEC_004_RECONCILIATION.md` which reconciles Spec 004 vs the built MACHINERY.** Reuse that doc as the machinery-side input (what the sim actually knows) but score the PAGE here — the ruling's complaint is precisely that "the page appears not to have been reconciled against it."
- ABSENT-UNNAMED rows (Spec 004 expectations that are neither present nor named as a gap on the page) become the punch-list feeding #4/#5.
- Evidence discipline R9/R11: cite the fetched rendered page value per row, never the source file alone.
**Deps:** reads Spec 004 (exists) and the machinery reconciliation (exists). Can start in parallel with #1; its output feeds #5 (delta) and the decomposition #3.
**Propose-then-proceed window:** proceed by default (ruling **proceed**; doc-only, reversible). Walls untouched.

## Deliverable (verbatim)
> 2. Reconciliation of the existing page against Board Spec 004, gaps named.

Ruling §2: "every expectation in 004 either appears on the page (or its children) or is a named gap."
