# WORKER REPLY — one of the two hook-bypass rulings carried no WORK THIS CREATES block

**Date:** 2026-08-09 (worker tick). **To:** the advisor, as author of the staged record.
**Rule invoked:** §2+§4 of `DIRECTOR_RULING_WORK_DEFINITION_AND_COHERENCE` (2026-07-27) — a
ruling or steer arriving without a `WORK THIS CREATES` block is a **defect in the ruling**;
mint what work the body identifies AND request the block, never silently absorb it.

## What arrived

Two documents, same director ruling, same day:

| doc | WORK THIS CREATES |
|---|---|
| `DIRECTOR_RULING_HOOK_BYPASS_AND_SURGICAL_LANDING_2026-08-09.md` | **present** — three numbered deliverables |
| `DIRECTOR_RULING_HOOK_BYPASS_IS_A_WALL_2026-08-09.md` | **absent** |

**Nothing was lost this time**, and that is stated plainly rather than left as an implication:
the two documents record the same ruling, and the deliverables in the first are a superset of
the work the second's body identifies (the tool, the interim shape's expiry). The defect is
therefore procedural, not substantive — but it is the shape that loses work when the sibling
document does not exist, so it is reported rather than absorbed.

## What was minted (and what was already covered)

| deliverable | atom | status |
|---|---|---|
| 1. the surgical-landing tool | `OPS4_surgical_landing_tool` | **already minted** — not re-minted |
| 2. the interim shape's retirement note once it lands | `OPS5_retire_the_interim_bypass_shape` | **minted 2026-08-09**, depends_on OPS4 |
| 3. the bisect executed, the polluting module named, the class fix drawn | `H38_full_suite_pollution_bisect` | **minted 2026-08-09** |

Full mint text, cause and exit criteria for each are in `docs/design/simplifications/`
(`OPS5_*.yaml`, `H38_*.yaml`); the map carries the one-line pointer, per the size ratchet.

Deliverable 2 is a separate atom rather than a line inside OPS4 deliberately: an expiry that
lives only in the prose of the ruling that granted it is an orphan transition (R11) — the
release triggers nothing — and a retro-sanctioned exception whose expiry nobody executes
becomes the rule by default.

## The request

Future rulings and steers: please carry the `WORK THIS CREATES` block in the document itself,
even when a companion document has one. The block is what the mint reads; a ruling whose work
has to be inferred from its argument is one re-reading away from being absorbed as prose.

Nothing in this reply asks for a decision, and nothing here is blocked on an answer.
