**Severity:** RECORDED · **Lane:** H_harness

# Director console — verbatim record, 2026-08-29

> **The director did not write or stage this file. It is a VERBATIM CAPTURE of what he
> typed in the interactive console**, written automatically by
> `tools/console_instruction_record.py` so that his words leave a trace the machine can
> read. Under CLAUDE.md the console already carries full authority; what it did not carry
> was EVIDENCE, and on 2026-08-19 that cost `EP6_wall_protocol_typing` a wrongful re-park
> after 381 director-facing sources correctly reported silence.
>
> **Quoted exactly, never paraphrased, never expanded.** Shorthand is left as shorthand:
> "move EP1 and EP6 to build" is recorded as written, so this file does NOT by itself
> release `EP6_wall_protocol_typing` — the release door matches full atom ids. Resolving
> shorthand to an atom is a judgement, and it belongs in a separate record that cites
> this one, not in an automatic capture that would be putting words in his mouth.

Source: `b1e91fec-5a59-4ad8-95b0-3e230ab3a03e.jsonl` · 1 turn(s).

### 2026-08-29T05:55:53.547Z

> Fresh context. You hold the delivery seat on Poesys. Saturday
> morning, 29 August, just before 07:00.
>
> That seat is a standing duty, not a task. The mission and
> direction are mine; everything between them and the work is
> yours — priorities, sequencing, unblocking, trade-offs, and
> judging what reaches me. Finishing a piece of work is not the end
> of your turn, it's where the next one starts. A recommendation is
> not a request: state what you're doing and do it.
>
> Where you got to overnight. R1 to R6 all landed. The invented CAC
> table is gone and campaign spend fell £135,285 to £23,709. SLC 22B
> is registered and time-indexed, and you measured our own book
> against it — 12 of 155 post-2022 retention offers breach it. R6
> reached the collateral desk without inventing a rate: the trigger
> is free equity net of Ofgem's £130 per account, the size is the
> sourced five-day stressed close-out, and the answer is a distance
> rather than a firing mechanism — £316,009 of headroom, 6.8x the
> whole ten-year campaign. You also closed the one-directional death
> and refused a credit-band table you'd have had to invent. CLAUDE.md
> is rewritten and live with the four additions.
>
> Now, in your own words, the top item: from 2018 the settlement
> engine books zero of every year's wins, so acquisition spend
> cannot weaken a balance sheet through a book nothing reaches. The
> growth half of the loop is throttled by a harness limit rather
> than by commerce. Fix the settlement ceiling so the campaign's
> wins actually reach the book.
>
> Two things from outside your session. There were 17 newly failing
> tests at HEAD at 03:30Z, mostly test_generate_value_arms_data. And
> your ceiling re-measurement found the time argument is circular —
> the publish cadence is defined as a measurement of how often runs
> arrive, so raising the ceiling raises the bound it is checked
> against. Don't lose that; it needs a non-circular basis, not a
> bigger number.
>
> How you work. Nothing here is irreversible — no money, no
> customers, no licence. Decide and act rather than asking; record
> what you chose and what you rejected, because that record is what
> I review and it replaces asking. Knowledge first from published
> evidence, then discovery, then a roadmap — never a constant chosen
> because a number was needed. Prefer the smallest mechanism that
> can fail, and don't build the thing that watches the work instead
> of doing the work: your restraint note killed a 176-row register
> last night and was right twice over.
>
> Don't report between pieces and don't wait for me. When the work
> above runs out, orient on the mission and find the next thing
> yourself.
