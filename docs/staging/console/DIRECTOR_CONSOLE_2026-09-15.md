**Severity:** RECORDED · **Lane:** H_harness

# Director console — verbatim record, 2026-09-15

> **The director did not write or stage this file. It is a VERBATIM CAPTURE of what he
> typed in the interactive console**, written automatically by
> `tools/console_instruction_record.py` so that his words leave a trace the machine can
> read. Under CLAUDE.md the console already carries full authority; what it did not carry
> was EVIDENCE, and on 2026-08-19 that cost `EP6_wall_protocol_typing` a wrongful re-park
> after 381 director-facing sources correctly reported silence.
>
> **Quoted exactly, never paraphrased, never expanded.** Shorthand is left as shorthand:
> "move EP1 and EP6 to build" is recorded as written, so this file does NOT by itself
> release `EP6_wall_protocol_typing` — the release door matches full atom ids. Resolving
> shorthand to an atom is a judgement, and it belongs in a separate record that cites
> this one, not in an automatic capture that would be putting words in his mouth.

Source: `5f6046d3-dca3-43ec-9f7b-424985e6b2ae.jsonl, 6535b098-1718-4644-9099-b933540227a4.jsonl, 671b023c-d188-4e19-81f8-7783c50fde6d.jsonl, b178d1c6-4d3e-4e02-8419-c86aac847f41.jsonl` · 2 turn(s).

### 2026-09-15T08:54:34.580Z

> Fresh session. You hold the delivery seat on Poesys. It's Tuesday
> morning, 15 September.
>
> Mission and direction are mine; priorities, sequencing, unblocking
> and judging what reaches me are yours. Finishing a piece of work
> is where the next one starts. Nothing here is irreversible — no
> money, no customers, no licence. Decide and act, record what you
> chose and what you rejected. CLAUDE.md and the startup anchors
> carry the rest.
>
> Context for the gap: I powered the PC off on Friday and only just
> turned it back on. So nothing was wedged — the machine was simply
> off. Last commit reached origin 11 September 05:20Z and the site
> last published on the 10th.
>
> First job is making sure everything came back. Check the timers
> are armed and firing, the daemons are running the current code,
> the publisher is alive, and tmux is hosting what it should. A cold
> start is where user services fail to return and where a session
> that died mid-work leaves uncommitted files behind, so check for
> both.
>
> Then tell me what was in flight when the power went. The last
> commits before the silence were the sample work reaching the
> reader with all five predictions holding, so there may be finished
> work stranded in the tree. On the 10th it carried about 4,100
> uncommitted insertions across 57 files plus 11 untracked modules,
> oldest 30 August — say whether that survived the restart and
> whether anything new sits on top of it.
>
> Then carry on. The priority I set on the 11th stands: wire the
> sample apparatus into the running world.
> demand_vector_coverage, stock_joint_generator, space_filling_sample
> and demand_case_coverage had no importer anywhere, so the world was
> still drawing its population the old way while the apparatus
> measured a cloud it drew itself.

### 2026-09-15T12:44:06.654Z

> [Request interrupted by user for tool use]
