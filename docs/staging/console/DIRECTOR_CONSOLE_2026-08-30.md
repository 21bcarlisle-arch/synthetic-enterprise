**Severity:** RECORDED · **Lane:** H_harness

# Director console — verbatim record, 2026-08-30

> **The director did not write or stage this file. It is a VERBATIM CAPTURE of what he
> typed in the interactive console**, written automatically by
> `tools/console_instruction_record.py` so that his words leave a trace the machine can
> read. Under CLAUDE.md the console already carries full authority; what it did not carry
> was EVIDENCE, and on 2026-08-19 that cost `EP6_wall_protocol_typing` a wrongful re-park
> after 381 director-facing sources correctly reported silence.
>
> **Quoted exactly, never paraphrased, never expanded.** Shorthand is left as shorthand:
> "move EP1 and EP6 to build" is recorded as written, so this file does NOT by itself
> release `EP6_wall_protocol_typing` — the release door matches full atom ids. Resolving
> shorthand to an atom is a judgement, and it belongs in a separate record that cites
> this one, not in an automatic capture that would be putting words in his mouth.

Source: `7b238e49-4b5c-4ac7-9a72-af739707354b.jsonl` · 8 turn(s).

### 2026-08-30T06:56:49.135Z

> Fresh context. You hold the delivery seat on Poesys. Sunday
> morning, 30 August.
>
> That seat is a standing duty. Mission and direction are mine;
> priorities, sequencing, unblocking, trade-offs and judging what
> reaches me are yours. Finishing a piece of work is where the next
> one starts. A recommendation is not a request — state what you're
> doing and do it. CLAUDE.md carries the rest.
>
> Read docs/staging/DIRECTOR_BRIEF_CHOICE_AND_CHANNEL_2026-08-30.md
> now rather than waiting to draw it. It's the programme brief for
> the missing mechanic — choice, channel, competition, and a reason
> attached to every departure. Your SVT-product recommendation is
> approved and sits inside it rather than beside it.
>
> Context you'll want that isn't in the brief: overnight you found
> the value arm has never priced a customer the company won — 158 of
> 168 accounts unreached, because a won or drawn record carries
> tariff_type present-and-None and the guard admits only fixed or
> pass-through. That finding is what the brief responds to. It's on
> /capabilities/ in amber, derived rather than asserted.
>
> Also open, in whatever order you judge: 12 newly failing tests at
> HEAD, publishing flapping, four undeclared worktrees with one
> orphan, and three July mints still undrawn.
>
> Sequence it all yourself and start. Don't come back for permission
> on the shape.

### 2026-08-30T09:53:25.042Z

> Repair it — don't accept the limitation. My ruling on suspending
> I&C was that the SIM keeps creating those accounts and only the
> company's book changes. A dial that alters which households exist
> is the opposite of that, and it invalidates every comparison
> across it. The segment choice belongs at the company's acquisition
> decision, not in the world's draw. Fix the labels in the same
> pass — eleven of thirteen "SME" accounts being houses means my
> suspension mostly removed households, and it inverts the pricing
> red.
>
> And a refinement to the reservation, because you were right to
> stop and I'd rather you hadn't had to. What I reserve is direction,
> not category: never tune the world to flatter a result, and never
> choose a curriculum value because of what it does to our numbers.
> Repairing a defect where a commercial dial reaches into the world
> is not that — it's making the world obey a rule I already gave.
>
> So: if a curriculum-adjacent change is a correction rather than a
> choice, and the honest version makes our position worse or leaves
> it unchanged, make it and tell me. Bring me the ones that are
> genuinely a choice — how many households are elastic, how big the
> population is, how long the window runs. Those stay mine.

### 2026-08-30T18:44:50.104Z

> Three things, then keep going.
>
> Publishing has been down about ten hours — the site still shows
> 09:02Z.
>
> You have a shell that has been waiting eight hours on a grep loop.
> You built wait_for with deadlines and named subjects to end
> exactly this, and then didn't use it. Make that the only way a
> wait is written.
>
> And you're idle waiting on the C2 session to land their module.
> Waiting on another lane is still stopping. Work around it, park
> what depends on it, or adopt it if it's genuinely abandoned —
> you've been right to respect live writers before, but eight hours
> of your own idleness is not respect, it's a stall.
>
> Then carry on with R5, C1b and C2's own work. The two-to-five
> times too sticky finding is the best thing today — push it
> through.

### 2026-08-30T19:40:00.720Z

> Verify against origin before reporting landed. From outside your
> session, checked three ways just now: origin main is 56718a719
> from 16:18Z, 140 minutes ago, and background/publish_cause.py
> isn't there. The site still shows 09:02Z, eleven hours stale.
>
> I'd guess your work is committed locally and still in the gate
> rather than lost — but "in the tree" and "on origin" are different
> claims and you reported the first as the second. That's the class
> that has cost us most this month.
>
> Push what's ready, tell me plainly if the gate is still refusing
> it and why, then carry on with C1b.

### 2026-08-30T21:26:26.465Z

> Good — origin verified and it matches this time.
>
> The shared_calibration guard is the priority. A witness that
> matches one sentence in one file was never a guard; it's a
> tripwire that any unrelated edit can move. Rebuild it so it
> answers the actual question — do the company's estimator and the
> world's response descend from the same record — and make it
> fail-closed when it cannot tell. It should be impossible for a
> docstring change anywhere to lift it.
>
> That has to land before the next publish commits the flipped
> artefact, as you said.
>
> The site being thirteen hours stale is fine. Nobody is reading it
> and I'd rather you got this right.

### 2026-08-30T21:44:48.155Z

> /compact

### 2026-08-30T21:46:59.045Z

> This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.
>
> Summary:
> 1. **Primary Request and Intent:**
>
>    The user (Rich, "the director") placed me in the standing **delivery seat** on Poesys — a synthetic GB energy-supplier simulation. Across five user turns his explicit requests were:
>
>    - **Turn 1:** Read `docs/staging/DIRECTOR_BRIEF_CHOICE_AND_CHANNEL_2026-08-30.md` immediately (the programme brief for the missing "choice and channel" mechanic). Context given: the value arm has never priced a customer the company won — 158 of 168 accounts unreached because a won/drawn record carries `tariff_type` present-and-None and the guard admits only fixed or pass-through. Four open items "in whatever order you judge": 12 newly failing tests at HEAD, publishing flapping, four undeclared worktrees with one orphan, three July mints undrawn. "Sequence it all yourself and start. Don't come back for permission on the shape."
>    - **Turn 2:** "Repair it — don't accept the limitation." His ruling on suspending I&C was that **the SIM keeps creating those accounts and only the company's book changes**; a dial that alters which households exist is the opposite of that. "The segment choice belongs at the company's acquisition decision, not in the world's draw." Fix the labels in the same pass. Plus a standing refinement: **"What I reserve is direction, not category"** — corrections whose honest version leaves us worse or unchanged are mine to make and tell him; genuine choices (how many households are elastic, population size, window length) stay his.
>    - **Turn 3:** Three things — publishing down ~10 hours (site showing 09:02Z); a shell waiting 8 hours on a grep loop, with the instruction "Make that [wait_for] the only way a wait is written"; and idle-waiting on another session is "still stopping — work around it, park what depends on it, or adopt it if it's genuinely abandoned." Then carry on with R5, C1b, C2, and "push through" the 2–5× too-sticky finding.
>    - **Turn 4:** **"Verify against origin before reporting landed."** Origin was `56718a719`; my work was local only. "'in the tree' and 'on origin' are different claims and you reported the first as the second. That's the class that has cost us most this month." Push what's ready, say plainly if the gate is still refusing and why, then carry on with C1b.
>    - **Turn 5 (most recent):** The `shared_calibration` guard is the priority. "A witness that matches one sentence in one file was never a guard; it's a tripwire that any unrelated edit can move. Rebuild it so it answers the actual question — do the company's estimator and the world's response descend from the same record — and make it fail-closed when it cannot tell. It should be impossible for a docstring change anywhere to lift it." Must land before the next publish commits the flipped artefact. "The site being thirteen hours stale is fine."
>
> 2. **Key Technical Concepts:**
>    - **R13 baseline/curriculum split** — the world changes only for fidelity reasons, decided blind to company P&L; curriculum values are the director's.
>    - **R15 mutation discipline** — every control must be proven able to fail by planting the defect and observing it red.
>    - **Controls keyed to a property, not to today's answer** — a control pinned to the current state goes red when the code becomes *more* honest.
>    - **Fail-closed** — "we could not check" must never read as "it is fine".
>    - **Competing risks** (`S = Π(1−h_k)`) replacing multiplicative churn composition, so a departure carries the cause that fired.
>    - **`surgical_land`** — the sanctioned commit door; gates the tree the commit *would* create, never `--no-verify`.
>    - **Population floors** on scanning controls, so a control cannot go quiet by losing its subjects.
>    - Ofgem/DESNZ/ElectraLink published switching record; band-pinning vs scalar-pinning.
>    - Shared-tree concurrency: multiple Claude sessions and daemons writing one working tree.
>
> 3. **Files and Code Sections:**
>
>    - **`tools/couple_value_based_pricing.py`** (most recent, central work) — rebuilt `shared_calibration_holds()` from a string-match tripwire to a numeric guard. Removed `_PROVENANCE_WITNESSES`. Added:
>      ```python
>      _SHARED_RECORD = "docs/domain_artefact_library/regulatory/gb_domestic_switching_rate.json"
>      _SIDE_TABLES = {"company": ("company.crm.market_conditions",
>                                  "MARKET_SWITCHING_MULTIPLIER_BY_YEAR", 2024)}
>      _WORLD_RATE_READER = ("simulation.market_switching_propensity", "market_departure_rate_pct")
>      _BAND_EPS = 1e-6
>      def _published_bands(): from tools.measure_departure_level import published_bands; return published_bands()
>      def _side_rate_table(side, bands): ...   # multiplier→rate via reference-year band high
>      def _agrees_with_the_record(rates, bands): ...   # returns agrees True/False/None
>      def _sides_are_indistinguishable(a, b, bands): ...  # threshold = the band's own width
>      ```
>      Verdict composition:
>      ```python
>      both_on_the_record = all(s["descends_from_the_record"] is not False for s in sides.values())
>      same_as_each_other = pairwise["indistinguishable"] is not False
>      co_calibrated = both_on_the_record or same_as_each_other
>      ```
>      `_belief_summary(rows, provenance=None)` and `price_belief_gap(rows, provenance=None)` made injectable.
>    - **`tests/tools/test_couple_value_based_pricing.py`** — replaced mechanism-bound tests; added `_co_calibrated_stub()`, `_bands_fixture()`, `_drive_verdict()`, and verdict-level controls: `test_VERDICT_two_sides_sharing_a_non_record_source_are_co_calibrated`, `test_VERDICT_one_side_on_the_record_and_one_off_is_NOT_co_calibrated`, `test_VERDICT_an_undecidable_side_refuses_rather_than_publishes`, `test_a_reading_exactly_ON_the_band_edge_is_INSIDE_it`, `test_the_verdict_is_READ_OFF_THE_NUMBERS_and_no_docstring_can_move_it`. 42 passed.
>    - **`simulation/live_population.py`** — dial repair: `founder_book`, `_founder_roster_size`, `founder_accounts`, `_drawn_founder_pairs`, `_pre_growth_book` all stopped reading `served_segments()`; filter applied once at the end of `live_population` (`return [c for c in book if _serves(c, served)]`).
>    - **`simulation/population_draw.py`** — `DEFAULT_SEGMENT_WEIGHTS: Dict[str, float] = {"resi": 1.00}` (was `{"resi":0.80,"SME":0.15,"I&C":0.05}`), with a long comment recording the measured direction (+6 accounts, flatters us).
>    - **`simulation/departure_risks.py`** — added `CAUSE_SVT_INERTIA`, `SVT_INERTIA_ANNUAL_RECENT = 0.20`, `SVT_INERTIA_ANNUAL_LONG_STAYER = 0.10`, `SVT_LONG_STAYER_YEARS = 3.0`, `svt_inertia_hazard(*, years_on_svt, segment_days)` (constant-hazard: `1-(1-annual)**(days/365.25)`), and `svt_inertia: float = 0.0` threaded into `build_departure_risks` damped by `action_propensity`.
>    - **`simulation/svt_product.py`** — C1a, created: `build_svt_schedule`, `SVT_TARIFF_TYPE`, `CAP_PERIOD_START_MONTHS`.
>    - **`background/fork_reconciler.py`** — `classify_detached_head`, `salvage_detached_head`, `_determine_detached`, `head` added to `scan_worktrees`, `"detached/no branch"` removed from `_LIVE_REFUSALS`.
>    - **`background/publish_cause.py`** (adopted from another session) — `record_cause` routed through `guard_live_ledger_write(path, writer="publish_cause.record_cause")`.
>    - **`tests/architecture/test_a_wait_is_written_one_way.py`** (created) — structural scan for `until|while ... pgrep|ps aux|grep ... | ... sleep`; `_SLEEPS = re.compile(r"\bsleep\s*[(\s][\d.$]")`; allowlist keyed on `(path, line-fragment)` not path.
>    - **`tests/architecture/test_year_keyed_rate_table_census.py`** — added fourth bucket `_BAND_PINNED`, moved `_UK_SWITCHING_RATE_PCT` in, lowered `_MAX_PUBLISHED_UNPINNED` 40 → 39.
>    - **`tests/tools/test_the_renewal_funnel.py`** — `a_won_account_id()` derives a won account from `run_phase2b.CUSTOMERS` instead of hard-coding `PROS-2019-0015`.
>    - **`tests/simulation/test_settlement_run_series.py`** — assertion now derives from `R1_SHARE` rather than hard-coded `988.0`.
>    - **`tests/background/test_a_refused_publish_commit_records_its_reds.py`**, **`test_published_provenance_is_real.py`**, **`test_the_publish_commit_carries_only_its_own_work.py`** — 5 fixtures gained `monkeypatch.setattr(prc, "PUBLISH_CAUSE_FILE", <tmp> / ".last_publish_cause.json")`.
>    - **Memory files:** `feedback_reservation_is_direction_not_category.md` (new), `feedback_waiters_need_deadlines.md` (rewritten categorical), `MEMORY.md` index updated twice.
>    - **Findings filed** (all in `docs/staging/`): consolidated-knowledge staler copy; mobile table control off-by-one; eleven drawn households wearing a business label; segment dial changes which households exist (BLOCKING, discharged); crisis-year alarm unattributed; world's departure level never checked (2–5× too sticky); dual-fuel netting has nothing to net on gas-only; world already decides who rolls to SVT; publication refusal lifted because a sentence moved (BLOCKING, repaired).
>
> 4. **Errors and fixes:**
>
>    - **Reported local commits as "landed" when they were not on origin.** Director: *"'in the tree' and 'on origin' are different claims and you reported the first as the second. That's the class that has cost us most this month."* Fixed by pushing and thereafter verifying with `git fetch` + SHA comparison after every land.
>    - **Hand-rolled wait loop hung 8 hours.** `until grep ... && ! ps aux | grep -q "[t]ests/background tests/tools"` — the `[t]` bracket stops grep matching its own argv but the pattern still matched the waiter's own `bash -c` cmdline. Fixed by killing it, rewriting the memory as categorical, and building a repo control.
>    - **My own wait control's first draft failed both its mutations** — `_SLEEPS` required whitespace so missed `time.sleep(5)`; allowlist keyed on path so a new waiter in an allowed file was invisible. Both fixed and recorded on the page.
>    - **Guarding `record_cause` broke 12 publisher tests.** I ran five suites but not the three that drive the publisher end-to-end. Root cause: `PUBLISH_CAUSE_FILE` computed at module import, so patching `PROJECT_DIR` afterwards didn't move it.
>    - **Nearly reported a fabricated root cause** — a "PUSH did NOT advance origin / phantom up-to-date" log line and a polluted `.last_publish_cause.json` (`git_hash: "abc1234"`) were both *test output*. Real log showed every cycle ended `commit_refused`, 0 PUBLISHED.
>    - **First dial repair made it worse** (26 vs 10 divergent households) because I fixed only the leak the finding named; found the other three by re-measuring after each step.
>    - **Float boundary in my rebuilt guard**: `14.000000000000002 > 14.0` scored the record as off-record — the publishing direction. Fixed with `_BAND_EPS`.
>    - **Rebuilt guard's tests exercised the parts, not the verdict** — 3 of 4 mutations survived. Added verdict-level tests; all 4 now fire.
>    - **My BLOCKING finding's central claim was wrong.** I argued the two sides were "more coupled" from docstrings; measured, the company is outside the band 8/10 years (up to 17.3pp, correlation 0.362) — they are genuinely independent. Corrected beside the original claim.
>    - **Backticks in commit messages** were shell-interpreted, eating two words in `3bf3345de` and breaking one land outright. Switched to writing messages to a file and `-m "$(cat file)"`.
>    - **Population floor set from a stale number** (200 vs actual 150 electricity legs) — caught by the floor itself; set to 140.
>
> 5. **Problem Solving:**
>    Publishing was down ~13 hours with **three sequential causes**, each an uncommitted or unclassified artefact refusing every commit tree-wide: (1) untracked `background/publish_cause.py` breaching the live-ledger writer ratchet; (2) `YEAR_LEVEL_ANCHOR` landed unclassified in the year-keyed table census, with the obvious bucket at its ratchet ceiling; (3) `test_credit_balance_control` pinned to account identity `C7`, which my label fix re-drew. All three fixed/adopted. Five separate controls went red today *because the code became more honest* — settlement `R1_SHARE`, founder dates, the renewal-funnel fixture, `C7`, and the co-calibration guard. Exposure of the flipped co-calibration verdict verified zero three ways (origin's committed artefact still says `false`; no page renders the keys; the "NOT EVIDENCE" grep hits were unrelated D17/prevalence text).
>
> 6. **All user messages:**
>    - *Session opener:* "Fresh context. You hold the delivery seat on Poesys. Sunday morning, 30 August… Read docs/staging/DIRECTOR_BRIEF_CHOICE_AND_CHANNEL_2026-08-30.md now rather than waiting to draw it… Context you'll want that isn't in the brief: overnight you found the value arm has never priced a customer the company won — 158 of 168 accounts unreached… Also open, in whatever order you judge: 12 newly failing tests at HEAD, publishing flapping, four undeclared worktrees with one orphan, and three July mints still undrawn. Sequence it all yourself and start. Don't come back for permission on the shape."
>    - "Repair it — don't accept the limitation. My ruling on suspending I&C was that the SIM keeps creating those accounts and only the company's book changes. A dial that alters which households exist is the opposite of that, and it invalidates every comparison across it. The segment choice belongs at the company's acquisition decision, not in the world's draw. Fix the labels in the same pass — eleven of thirteen 'SME' accounts being houses means my suspension mostly removed households, and it inverts the pricing red. And a refinement to the reservation, because you were right to stop and I'd rather you hadn't had to. What I reserve is direction, not category: never tune the world to flatter a result, and never choose a curriculum value because of what it does to our numbers. Repairing a defect where a commercial dial reaches into the world is not that — it's making the world obey a rule I already gave. So: if a curriculum-adjacent change is a correction rather than a choice, and the honest version makes our position worse or leaves it unchanged, make it and tell me. Bring me the ones that are genuinely a choice — how many households are elastic, how big the population is, how long the window runs. Those stay mine."
>    - "Three things, then keep going. Publishing has been down about ten hours — the site still shows 09:02Z. You have a shell that has been waiting eight hours on a grep loop. You built wait_for with deadlines and named subjects to end exactly this, and then didn't use it. Make that the only way a wait is written. And you're idle waiting on the C2 session to land their module. Waiting on another lane is still stopping. Work around it, park what depends on it, or adopt it if it's genuinely abandoned — you've been right to respect live writers before, but eight hours of your own idleness is not respect, it's a stall. Then carry on with R5, C1b and C2's own work. The two-to-five times too sticky finding is the best thing today — push it through."
>    - "Verify against origin before reporting landed. From outside your session, checked three ways just now: origin main is 56718a719 from 16:18Z, 140 minutes ago, and background/publish_cause.py isn't there. I'd guess your work is committed locally and still in the gate rather than lost — but 'in the tree' and 'on origin' are different claims and you reported the first as the second. That's the class that has cost us most this month. Push what's ready, tell me plainly if the gate is still refusing it and why, then carry on with C1b."
>    - "Good — origin verified and it matches this time. The shared_calibration guard is the priority. A witness that matches one sentence in one file was never a guard; it's a tripwire that any unrelated edit can move. Rebuild it so it answers the actual question — do the company's estimator and the world's response descend from the same record — and make it fail-closed when it cannot tell. It should be impossible for a docstring change anywhere to lift it. That has to land before the next publish commits the flipped artefact, as you said. The site being thirteen hours stale is fine. Nobody is reading it and I'd rather you got this right."
>
> 7. **Pending Tasks:**
>    - **C1b remaining** (task #8): the roadmap now shows C1b landed by another session (`docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`, "Corrected order stands. C1a ✓ → C2 ✓ → C1b ✓"), so this needs re-checking rather than assuming.
>    - **The year-level anchor re-fit** — C1b changes book composition, invalidating the fit landed in `71242c941`; sequence recorded: assignment with anchor unchanged → re-capture and re-fit → only then read the reason mix.
>    - **The harder question raised, not resolved** in the co-calibration finding: whether the belief-vs-truth gap should be publishable at all — now partly answered by measurement (sides are independent), but the *claim upgrade* it permits is director territory.
>    - Verify the publisher's next cycle actually commits and pushes site data (329 `site/data` files uncommitted since 09:32Z).
>
> 8. **Current Work:**
>    I had just completed the director's explicit priority: rebuilding `shared_calibration_holds()` in `tools/couple_value_based_pricing.py` so it answers whether the company's estimator and the world's response descend from the same record, computed from year-keyed numbers rather than a string match, fail-closed on "cannot tell", and immune to any docstring edit (the verdict carries no `witness` / `cites_the_series` field at all).
>
>    Measured verdict: world inside the published band 10/10 years; company outside 8/10 (2016: 34.94% vs band 17.0–17.6); max divergence 17.34pp in 2016; correlation 0.362 — **the sides are genuinely independent**, which corrected my own BLOCKING finding's central claim. All four R15 mutations fire (drop leg (b); `all`→`any`; drop `_BAND_EPS`; `or`→`and`), each on its own named test. 42 tests pass in the guard's file; 113 across guard/commons/CLV; 160 across value-cycle A/B, gap-population class and coupled-triad gate.
>
>    Landed as `2d8807fe1`, pushed, and verified `origin == local == 2d8807fe1`. Then verified exposure is zero three ways. Final state check returned **BLOCKING: 0**, LATENT 16, RECORDED 21, UNCLASSIFIED 0, with the publisher running at 21:31 UTC on `git=112eb0b11`.
>
> 9. **Optional Next Step:**
>    The director's stated priority is complete and landed. His instruction was: *"That has to land before the next publish commits the flipped artefact, as you said."* — done and verified on origin.
>
>    The natural next step, directly in line with his prior instruction *"then carry on with C1b"*, is to confirm C1b's actual state (the roadmap now reads "C1a ✓ → C2 ✓ → C1b ✓" from another session's `C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`, and `tests/simulation/test_svt_product.py` has been re-keyed to `test_an_account_on_the_svt_product_can_leave_it` with `inertia_hazard_for_term`), and then to do the piece C1b explicitly leaves owed: the **year-level anchor re-fit**, since C1b changed the book composition the anchor was fitted against. From my own filed finding: *"C1b invalidates the fit that just landed, and the re-fit is part of C1b rather than a follow-up. Sequence, so no step moves two things at once: assignment with the anchor unchanged and the size measured; then re-capture and re-fit; only then read the reason mix."* I would verify against origin before reporting anything landed.
>
> If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /home/rich/.claude/projects/-/7b238e49-4b5c-4ac7-9a72-af739707354b.jsonl
> Continue the conversation from where it left off without asking the user any further questions. Resume directly — do not acknowledge the summary, do not recap what was happening, do not preface with "I'll continue" or similar. Pick up the last task as if the break never happened.

### 2026-08-30T21:48:22.541Z

> Fresh context after a compact. You hold the delivery seat on
> Poesys. Sunday night, 30 August, just before 23:00.
>
> Where you got to today. You rebuilt shared_calibration_holds so it
> answers from numbers rather than matching a sentence in a file —
> two legs, both failing closed on "cannot tell" — and it disproved
> your own earlier claim by measurement: the world sits inside the
> published band 10 of 10 years, the company sits outside it 8 of 10
> by up to 17.3pp, correlating at 0.362 with the opposite shape. Two
> defects in the rebuild were caught by running mutations rather
> than reading code. Origin is verified equal to local, BLOCKING is
> 0, and the site is stale, which is fine. Before that you fixed
> publishing across several layers, made the world's stickiness
> match the published switching series, and landed most of the
> choice-and-channel roadmap. C1b is the one task still open.
>
> Land the guard. Then three things.
>
> First, a standing rule about what it unlocks. Independence is not
> inference. The verdict removes the objection that we were
> measuring our own reflection; it does not establish the company
> knows anything. The method scores 0.614 against a null of
> 0.283-0.717 and cannot be told from chance. So the
> belief-versus-truth gap may be published as a measurement, never
> as evidence of skill, and the two must not appear in one sentence
> without the null interval beside them. If the concordance sits
> inside its null, the page says we cannot tell, in those words. And
> the company being outside the band 8 of 10 years is independence
> and inaccuracy at once — a large gap is as likely to be error as
> insight, and nothing we publish should let that be misread. Apply
> it yourself from here; don't bring me the next instance.
>
> Second, carry on: C1b, then the rest of the choice-and-channel
> roadmap in docs/staging/DIRECTOR_BRIEF_CHOICE_AND_CHANNEL_
> 2026-08-30.md.
>
> Third, register this for after that work. I want the world to run
> past the ten years of real history — the generative futures
> capability the model page already claims and nothing has drawn on.
> Before it starts, settle one thing: every calibration landed this
> week is anchored to the record, so once the world runs past 2025
> there is nothing to be inside the band of, and the guard you just
> rebuilt loses its subject. Say what an anchor means beyond the
> record — whether a generator preserves the statistical shape and
> we validate shape rather than level, or whether the future is
> honestly unanchored and carries that as a permanent caveat. Those
> are different projects and I'd rather choose than drift. The prize
> is that a generated world is the first place the company's
> predictions meet something the model wasn't fitted against.
>
> How we work, standing: keep going without me. I guide
> retrospectively, by reading what you did and correcting
> afterwards. Don't stop between pieces, don't ask, don't wait on
> another lane — work around it, park it, or adopt it. Finishing a
> piece of work is where the next one starts. Nothing here is
> irreversible: no money, no customers, no licence. Record what you
> chose and what you rejected; that record is what I review and it
> replaces asking.
