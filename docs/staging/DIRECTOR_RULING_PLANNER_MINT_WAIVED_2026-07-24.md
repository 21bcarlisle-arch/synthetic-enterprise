# [DIRECTOR-RULING] — First planner mint: ALL FIVE WINDOWS WAIVED. Proceed now. (2026-07-24)

**Type:** [DIRECTOR-RULING] via advisor bridge. Director's word on the rung-7 mint of 2026-07-24: **"Waive it."**

## The ruling

All five PLANNER_MINTED proposals proceed to BUILD/FRAME immediately; their propose-then-proceed windows are waived:

1. `PLANNER_MINTED_generator_draw_wiring` — PRODUCT-FIRST item 2.
2. `PLANNER_MINTED_value_chain_observation_window_cap` — PRODUCT-FIRST item 3 (the declared static-cap FAIL + MC-2 collateral death-test).
3. `PLANNER_MINTED_ssp_negative_lift_cells` — fidelity-ledger defect.
4. `PLANNER_MINTED_payment_truth_detection_gap` — coupled-triad gap (FRAME/DISCOVER half only, as the doc itself scopes).
5. `PLANNER_MINTED_front_mission_coldeyes` — read-only Axis-1 scrub.

**The waiver removes the wait, nothing else.** Every wall each doc named for itself stands: generator ground truth and curriculum values remain director-reserved; MC-2 scenario *difficulty* is escalated, never tuned; billing/CRM BUILD stays roadmap-gated; no L3 self-promotion; the Axis-1 verdict remains the director's by right — the cold-eyes pass produces a prediction/scrub, never a self-awarded pass.

## Sequencing guard

Standing guard applies: if lanes contend, **PRODUCT-FIRST items win the draw** (items 1 and 2 above). The read-only review may run alongside. Rung-7 continues to mint when rungs 1–6 empty — this waiver is not a licence to stop planning.

## Advisor note recorded (for the ledger, not for tuning)

Item 3's own evidence is the week's sharpest self-finding: the calibrated residual-demand scarcity model is **worse than the 3-feature OLS baseline in 6 of 10 year-cells** (aggregate lift +1.17 MAE carried almost entirely by 2022). It independently corroborates the board's blind prediction on Specs 001–004 — *"mechanisms present, correlations and tails under-sized"* — arriving this time from inside the fidelity ledger. R12 governs the fix: mechanism diagnosis (R4), never tuning the output toward the benchmark; an honestly-bounded negative-lift finding is a pass, a goal-sought positive lift is a breach.

**Risk & proportionality:** waiver of the director's own protection windows on five self-walled, previously-authorized items. Tag: **proceed.**

— Advisor bridge, carrying the director's waiver, 2026-07-24.
