[PROJECT] PRIORITISATION RULES -- permanent harness amendment (director-approved). Encode into CLAUDE.md governance. Root cause addressed: this week the queue executed in staging order, not value order; the public site (Rich's repeated top concern) lost to better-specified backend items.

RULE P-1. ONE RANKED QUEUE, ONE ARBITER. docs/PRIORITIES.md is the SOLE work-order arbiter. Staging order, file arrival time, and directive size carry ZERO priority meaning. Every staged directive must declare its intended rank ("P1a", "queue behind X", "backlog"); if it doesn't, ask or park it as backlog -- never infer rank from arrival.

RULE P-2. DIRECTOR TWO-STRIKE ESCALATION. If Rich voices the same complaint or desire twice (any channel: NTFY, staged note, screenshot commentary), it becomes an automatic P1 proposal in the next NTFY: "You've flagged X twice -- promoting to P1 unless you say otherwise." His attention must never be the escalation mechanism.

RULE P-3. REFERENCE IMPLEMENTATION FIRST for anything visual/experiential. Build ONE exemplar (one page, one report, one surface), get Rich's eyes on it, THEN propagate the pattern. Never restyle N surfaces before one is confirmed right. Fuzzy-but-vital work is decomposed into artifact-level acceptance before entering the queue.

RULE P-4. EXPLICIT DEPTH/VISIBILITY SPLIT. While any director-facing surface is below bar (currently: the public site), the budget split is stated in PRIORITIES.md (currently ~70% front-end / 30% backend+fires) and honoured. The ratio changes only by director sign-off, recorded.

RULE P-5. PRIORITY FRESHNESS STAMP. PRIORITIES.md carries "last director review: <date>". Re-rank at every weekly budget reset and every retro. If the stamp is >7 days old, the next session's first NTFY requests a review. Stale priorities are treated exactly like stale data -- a defect, not a default.

Apply now: stamp PRIORITIES.md with today's reset review (the PRIORITY_RESET_PUBLIC_SITE.md order stands: infra fixes -> P1a Customer 360 -> P1b Supplier IA -> P1c six-section nav; 70/30 split active). Confirm with the CLAUDE.md diff summary.
