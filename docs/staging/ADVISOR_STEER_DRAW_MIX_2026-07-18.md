# ADVISOR STEER — Standing condition on the draw mix (director, 2026-07-18, amended same day)

**Type:** [STEER] — advisory, absorb and carry on. No reply needed; no work interruption.

**The director's standing condition, both halves (verbatim intent):**
1. *"I'm ok with that [harness work draining] as long as it does some real work in parallel."*
2. *"Unless it's getting really hampered by issues and real work has become futile. That's the logical balance."*

**The rule, as a standing balance on the draw:**
- **Default:** harness/self-improvement work (H-lane and kin) is acceptable **only while real company/product work runs in parallel**. Whenever any product-lane atom (D/W/E/C/site) is drawable, at least one of the ≤3 concurrent lanes should carry it. H-debt draining is fine; an H-monoculture is not.
- **The exception:** if foundational issues make product work genuinely **futile** (a wedged publish path, corrupted git, a broken gate — the class where building lands into a state that can't ship or can't be trusted), then foundation-first / harness-only mode is the *correct* priority — you already exercised this judgment rightly tonight by holding the executor while the publish gate was wedged.
- **The guard on the exception:** futility-mode must be **DECLARED, never assumed** — a loud, named state ("product lanes suspended: <blocker>") with the blocker identified, and it **ends when the blocker clears**. Silent or open-ended harness monoculture with an implicit "things are kind of hampered" is a defect; declared, bounded, blocker-named suspension is sound engineering. Your judgment which side of the line a given state falls; the declaration requirement is what keeps that judgment honest and auditable.

How you weight or mechanise any of this is your design, consistent with LAW A and the dial system.

**Current state read as satisfying the default:** main seat on H-frames + E4 and D5-HARDEN product forks in flight.

— Advisor, on the director's behalf, via the staging channel per the standing operating model. (Amendment supersedes the first version staged minutes earlier; same file, one steer.)
