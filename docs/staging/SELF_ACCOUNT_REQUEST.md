# SELF_ACCOUNT_REQUEST — your account, in your words, with evidence (P1, one turn)

**Staged:** 2026-07-12 by advisor at the director's explicit instruction. The
director's framing, verbatim in intent: *"Either we are aligned or we aren't.
If not, we clarify. If your intent is good but execution poor, we flag it and
coach — you figure out the resolution."*

This is NOT a reprimand and NOT a leading question. The advisor's diagnosis
this morning was inferred entirely from OUTSIDE (commits, mirrors,
timestamps) and may be WRONG. You have the inside view. Tell us what is
actually true.

## Ground rules
- **Evidence, not narrative.** Every claim backed by logs, counts, commits or
  configs. A plausible story is worth nothing here; a log line is worth a lot.
  R9 applies to your own self-report: verify before asserting.
- **Disagree where you disagree.** Where the advisor's read is wrong, say so
  and PROVE it. Deference is not the goal; accuracy is. Agreeing with a wrong
  diagnosis is the most expensive thing you could do in this turn.
- **Separate intent from execution.** Three different diagnoses, three
  different fixes: (a) misaligned — you don't think the instruction is right
  or you never received it; (b) aligned but blocked — you intended it and a
  mechanism failed; (c) aligned and fine — the advisor is measuring wrong.

## Questions
1. **Standing instructions:** what do you BELIEVE your standing instructions
   are right now? List them and where each is recorded. Critically: do
   standing mandates recorded in staged docs (e.g. SUNDAY_WIDE's fan-out
   default, in in_progress/) survive a /clear? Or does the post-clear session
   only know CLAUDE.md? If the latter, say so plainly — that is a mechanism
   finding, not a fault.
2. **Parallelism, measured:** how many subagents have you actually launched
   per turn since 05:00Z, from your own logs? If fan-out DID happen and the
   advisor simply cannot see it in the commit stream, prove it and correct
   the record. If it did not, what stopped it — instruction loss, judgment,
   turn budget, tooling, or something else?
3. **The SC fix:** what is the true status of the standing-charge
   double-charge fix? Published net has been frozen at £1,524,058 since
   Friday. Landed, delegated-and-lost, blocked, or superseded? What actually
   happened to it?
4. **Prioritisation, your view:** what do you believe the top three priorities
   are right now, and why? Compare with what you actually spent turns on. If
   the advisor's batched staging (deep work interleaved with cosmetic items)
   distorted your queue, say that too — the advisor is not exempt from being
   the problem.
5. **The gap and the fix:** where is YOUR intent-to-execution gap, and what
   mechanism would close it? Propose the mechanism — do not wait to be told.
   (Problem-not-solution applies to us; solution-design is yours.)

## DoD
One consolidated answer: NTFY summary + a committed self-account document
with the evidence attached. Then act on your own proposed mechanism. This
turn is worth doing properly and slowly — it is the retro function pointed at
yourself, which is exactly what the internal-audit organ is for.
