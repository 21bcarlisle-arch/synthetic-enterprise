**Severity:** LATENT · **Lane:** H_harness · **Epoch:** unassigned · **Atom:** `unminted`

# The interactive seat stopped mid-work, and this is what it was holding

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

## What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

## What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `CLAUDE.md`
- `PRIORITIES.md`
- `_r11_tmp.mjs`
- `docs/context-handshake-latest.md`
- `docs/design/PB3_book_growth_as_earned_outcome_exit_d_DISCOVER.md`
- `docs/design/simplifications/KNIFE3_wall_crossing_paydown.yaml`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `head.txt`
- `tests/background/test_a_refused_publish_commit_records_its_reds.py`
- `tests/background/test_file_api.py`
- `tests/background/test_producer_starvation_draw.py`
- `tests/background/test_publish_decoupling_exit.py`
- `tests/background/test_rest_ladder_isolation.py`
- `tests/background/test_seat_work_in_hand.py`
- `tests/simulation/test_drawn_smart_meter.py`
- `tests/simulation/test_phase27b_ccl.py`
- `tests/simulation/test_policy_cost_coverage.py`
- `tests/simulation/test_settlement_fold.py`
- `tests/tools/test_couple_pb3_book_growth.py`
- `tests/tools/test_head_green_census.py`
- `tests/tools/test_model_tier_report.py`
- `tests/tools/test_site1_proof_crawlability.py`
- `tools/couple_pb3_book_growth.py`
- `tools/head_green_census.py`
- `tools/revenue_sanity_check.py`

## Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, ToolSearch, Bash, Bash, Bash, Bash
- Tool calls this session: 238
- Last commit on the tree: `0d0b01579 the zero-carbon must-run fetcher landed four hours ago and NOTHING CALLS IT -- the dispatch that was supposed to consume it is still in the working tree, so every published carbon figure is still the flat 8,000 MW`

## What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

## Instances seen
- `the interactive seat stopped mid-work, and this is what it was holding` (first seen 2026-08-26)
- `WORKER_FINDING_REPEATING_ALARM_BACKGROUND_DOCS_TESTS_THE_TREE_ROOT_AND_ELSEWHERE_LEFT_UNCOMMITTED_2026-08-25` (first seen 2026-08-25)
- `WORKER_FINDING_REPEATING_ALARM_COMPANY_DOCS_SIMULATION_TESTS_AND_ELSEWHERE_LEFT_UNCOMMITTED_BY_A_2026-08-26` (first seen 2026-08-26)
- `WORKER_FINDING_REPEATING_ALARM_DOCS_SIM_TESTS_THE_TREE_ROOT_AND_ELSEWHERE_LEFT_UNCOMMITTED_2026-08-26` (first seen 2026-08-26)
- `WORKER_FINDING_REPEATING_ALARM_DOCS_TESTS_THE_TREE_ROOT_LEFT_UNCOMMITTED_BY_A_SESSION_2026-08-26` (first seen 2026-08-26)
- `WORKER_FINDING_REPEATING_ALARM_DOCS_LEFT_UNCOMMITTED_BY_A_SESSION_THAT_STOPPED_MID_WORK_2026-08-27` (first seen 2026-08-27)
- `WORKER_FINDING_REPEATING_ALARM_DOCS_TESTS_LEFT_UNCOMMITTED_BY_A_SESSION_THAT_STOPPED_MID_2026-08-27` (first seen 2026-08-27)
- `WORKER_FINDING_REPEATING_ALARM_DOCS_SIMULATION_TESTS_LEFT_UNCOMMITTED_BY_A_SESSION_THAT_STOPPED_2026-08-27` (first seen 2026-08-27)
- `WORKER_FINDING_REPEATING_ALARM_DOCS_SAAS_SIMULATION_TESTS_AND_ELSEWHERE_LEFT_UNCOMMITTED_BY_A_2026-08-28` (first seen 2026-08-28)
- `docs, saas, simulation, tests and elsewhere left uncommitted by a session that stopped mid-work holding # claim(s)` (first seen 2026-08-28)
- `background, docs, saas, simulation and elsewhere left uncommitted by a session that stopped mid-work holding # claim(s)` (first seen 2026-08-28)
- `background, docs, saas, sim and elsewhere left uncommitted by a session that stopped mid-work holding # claim(s)` (first seen 2026-08-28)
- `docs, saas, sim, simulation and elsewhere left uncommitted by a session that stopped mid-work holding # claim(s)` (first seen 2026-08-28)
- `background, company, docs, saas and elsewhere left uncommitted by a session that stopped mid-work holding # claim(s)` (first seen 2026-08-28)
- `background, docs, sim, simulation and elsewhere left uncommitted by a session that stopped mid-work holding # claim(s)` (first seen 2026-08-28)
- `company, docs, sim, simulation and elsewhere left uncommitted by a session that stopped mid-work holding # claim(s)` (first seen 2026-08-28)
- `docs, sim, simulation, tests and elsewhere left uncommitted by a session that stopped mid-work holding # claim(s)` (first seen 2026-08-29)
- `company, docs, saas, sim and elsewhere left uncommitted by a session that stopped mid-work holding # claim(s)` (first seen 2026-08-29)
- `docs, tests, tools left uncommitted by a session that stopped mid-work holding # claim(s)` (first seen 2026-08-29)
- `background, docs, simulation, tests and elsewhere left uncommitted by a session that stopped mid-work holding # claim(s)` (first seen 2026-08-30)
- `background, docs, tests, tools left uncommitted by a session that stopped mid-work holding # claim(s)` (first seen 2026-08-30)
- `background, company, docs, simulation and elsewhere left uncommitted by a session that stopped mid-work holding # claim(` (first seen 2026-08-31)
- `.claude, background, company, docs and elsewhere left uncommitted by a session that stopped mid-work holding # claim(s)` (first seen 2026-08-31)
## Still live
- **2026-08-28** — still live. 1 repeats over 0.4h without the state changing. No second document filed: this condition already has one.
- **2026-08-29** — still live. 1 repeats over 0.4h without the state changing. No second document filed: this condition already has one.
- **2026-08-30** — still live. 1 repeats over 0.4h without the state changing. No second document filed: this condition already has one.
- **2026-08-31** — still live. 1 repeats over 0.4h without the state changing. No second document filed: this condition already has one.
- **2026-09-01** — still live. 1 repeats over 0.4h without the state changing. No second document filed: this condition already has one.
## Episodes — what each dead seat was holding

### 2026-08-28 08:18 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/BLOCKED_ATOM_VISIBILITY.md`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/settlement_clocks.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_run_value_cycle_ab.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Write, Bash, Bash, Bash, Edit, Bash, Bash, Bash
- Tool calls this session: 353
- Last commit on the tree: `e3ee53cc5 the world defends and I cannot yet show what it costs: seventeen binary decisions cannot resolve it`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-28 09:20 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/BLOCKED_ATOM_VISIBILITY.md`
- `docs/design/maturity_map.yaml`
- `docs/design/maturity_map_closed.yaml`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/settlement_clocks.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_run_value_cycle_ab.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, ToolSearch, Bash, Bash, Bash, Write, Bash
- Tool calls this session: 96
- Last commit on the tree: `e3ee53cc5 the world defends and I cannot yet show what it costs: seventeen binary decisions cannot resolve it`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-28 13:02 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/blocked_atom_visibility.py`
- `background/process_run_complete.py`
- `docs/context-handshake-latest.md`
- `docs/design/BLOCKED_ATOM_VISIBILITY.md`
- `docs/design/maturity_map_closed.yaml`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/settlement_clocks.py`
- `tests/background/test_blocked_atom_visibility.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Read, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 410
- Last commit on the tree: `1c738789b the published headline said the arm earned more than flat rules, on a run where it earned GBP 4,724 less`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-28 15:54 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/claude_md_integrity.py`
- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `saas/reporting/annual_report.py`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/live_population.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/run_scenario.py`
- `simulation/settlement_clocks.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tests/tools/test_claude_md_integrity.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Read
- Tool calls this session: 370
- Last commit on the tree: `454525b84 the blocking test that kept re-wedging the publisher is not order-dependent: there is no ordering plugin, and its fixture copies a 107MB log a daemon is appending to`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-28 16:24 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `saas/reporting/annual_report.py`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/run_scenario.py`
- `simulation/settlement_clocks.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Read, Bash, Bash, Bash
- Tool calls this session: 25
- Last commit on the tree: `d2224824d the CLAUDE.md limit is in characters and `wc -c` is not; the 80-founder book is built and held`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-28 16:54 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `saas/reporting/annual_report.py`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/run_scenario.py`
- `simulation/settlement_clocks.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Read, Bash, Edit, Bash, Bash, Bash, Read, Edit
- Tool calls this session: 39
- Last commit on the tree: `f9612ba68 Auto-process run complete: report + LATEST.md + site/ (git=d2224824d, net=£159,423)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-28 17:20 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `saas/reporting/annual_report.py`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/run_scenario.py`
- `simulation/settlement_clocks.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Read, Edit
- Tool calls this session: 27
- Last commit on the tree: `68dea8cda knowledge first: what winning and keeping a GB customer actually costs, and the sourced model we already had and never wired`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-28 18:54 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `CLAUDE.md`
- `background/claude_md_integrity.py`
- `company/analytics/acquisition_cost_amortisation.py`
- `company/analytics/counterfactual_retention.py`
- `company/compliance/domain_invariants.py`
- `company/compliance/obligations_register.py`
- `company/finance/accounting_close.py`
- `company/finance/double_entry.py`
- `company/interfaces/growth_desk.py`
- `docs/context-handshake-latest.md`
- `docs/design/CLAUDE_MD_DRAFT_2026-08-28.md`
- `docs/design/CLAUDE_MD_REWRITE_NOTES_2026-08-28.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/growth_mandate.py`
- `saas/ledger.py`
- `saas/opex_ledger.py`
- `saas/reporting/annual_report.py`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/customer_events.py`
- `simulation/live_population.py`
- `simulation/net_new_acquisition.py`
- `simulation/run_phase2b.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/run_scenario.py`
- `simulation/settlement_clocks.py`
- `tests/architecture/test_a_cited_constant_has_a_caller.py`
- `tests/company/analytics/test_acquisition_cost_amortisation.py`
- `tests/company/compliance/test_no_acquisition_only_tariff.py`
- `tests/company/interfaces/test_growth_desk_seam.py`
- `tests/company/test_counterfactual_lift_by_class.py`
- `tests/company/test_phase_no_counterfactual_retention.py`
- `tests/saas/test_growth_mandate.py`
- `tests/saas/test_sourced_acquisition_costs.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_acquisition_funnel.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_net_new_acquisition.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tests/tools/test_claude_md_integrity.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_value_arms_data.py`
- `tools/pre_commit_test_gate.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`
- `tools/run_live_decisions.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Read
- Tool calls this session: 291
- Last commit on the tree: `2b34410d9 Auto-process run complete: report + LATEST.md + site/ (git=a1aefccaf, net=£159,423)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-28 20:25 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.3h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `CLAUDE.md`
- `background/claude_md_integrity.py`
- `docs/context-handshake-latest.md`
- `docs/design/CLAUDE_MD_DRAFT_2026-08-28.md`
- `docs/design/CLAUDE_MD_REWRITE_NOTES_2026-08-28.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/customer_events.py`
- `simulation/live_population.py`
- `simulation/run_phase2b.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/run_scenario.py`
- `simulation/settlement_clocks.py`
- `tests/company/compliance/test_no_acquisition_only_tariff.py`
- `tests/company/test_counterfactual_lift_by_class.py`
- `tests/company/test_phase_no_counterfactual_retention.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_net_new_acquisition.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_run_phase2b.py`
- `tests/simulation/test_settlement_clocks.py`
- `tests/tools/test_claude_md_integrity.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_value_arms_data.py`
- `tools/pre_commit_test_gate.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`
- `tools/run_live_decisions.py`

#### Where it had got to

- Last tools it ran, oldest first: Read, Write, Bash, Bash, Edit, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 231
- Last commit on the tree: `73759660f the arms are still priced on the invented costs, and the lane cannot credit the commit that fixed them`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-28 23:49 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `company/finance/margin_call_book.py`
- `company/risk/counterparty_collateral_desk.py`
- `company/risk/independent_amount.py`
- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/customer_events.py`
- `simulation/live_population.py`
- `simulation/run_phase2b.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/run_scenario.py`
- `simulation/settlement_clocks.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/company/risk/test_independent_amount.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_net_new_acquisition.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tools/generate_value_arms_data.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Write, Bash
- Tool calls this session: 626
- Last commit on the tree: `b08eb9869 chore(provenance): verification paused banner (git=bfce5ff50) -- the site keeps serving the last VERIFIED run and now says so; no unverified figure published (DIRECTOR_RULING_PUBLISH_DECOUPLING_2026-08-10 property 3)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 01:03 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/live_population.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/run_scenario.py`
- `simulation/settlement_clocks.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_net_new_acquisition.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tools/generate_value_arms_data.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Write, Edit, Bash
- Tool calls this session: 95
- Last commit on the tree: `e3cc43ce3 the guard sweep: the published CRM reconciliation builds its company side from the sim, and an empty CRM reads "Match: yes"`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 03:25 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/live_population.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/run_scenario.py`
- `simulation/settlement_clocks.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_net_new_acquisition.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tools/generate_value_arms_data.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Write, Bash, Bash, Read, ToolSearch
- Tool calls this session: 185
- Last commit on the tree: `1974f47a5 Auto-process run complete: report + LATEST.md + site/ (git=c8ff9cae9, net=£164,542)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 06:35 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/SETTLEMENT_CEILING_2026-08-24.md`
- `docs/design/SETTLEMENT_CEILING_ALLOCATION_2026-08-29.md`
- `docs/design/SETTLEMENT_CEILING_REMEASURED_2026-08-29.md`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `saas/growth_mandate.py`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/live_population.py`
- `simulation/net_new_acquisition.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/run_scenario.py`
- `simulation/settlement_clocks.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_net_new_acquisition.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tests/tools/test_generate_book_growth_data.py`
- `tools/couple_pb3_book_growth.py`
- `tools/generate_book_growth_data.py`
- `tools/generate_value_arms_data.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 407
- Last commit on the tree: `9563c1b81 Auto-process run complete: report + LATEST.md + site/ (git=d5d58da62, net=£164,542)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 08:07 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/run_scenario.py`
- `simulation/settlement_clocks.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tools/generate_value_arms_data.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Read, Bash, Bash, Edit, Edit, Edit, Bash, Bash
- Tool calls this session: 122
- Last commit on the tree: `07d063fe6 Auto-process run complete: report + LATEST.md + site/ (git=a9e3e18dc, net=£154,164)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 08:07 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_phase4c_on_phase2b.py`
- `simulation/run_scenario.py`
- `simulation/settlement_clocks.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_settlement_clocks.py`
- `tools/generate_value_arms_data.py`
- `tools/run_annual_report.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Read, Bash, Bash, Edit, Edit, Edit, Bash, Bash
- Tool calls this session: 122
- Last commit on the tree: `07d063fe6 Auto-process run complete: report + LATEST.md + site/ (git=a9e3e18dc, net=£154,164)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 10:20 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_scenario.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/generate_value_arms_data.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Write, Edit
- Tool calls this session: 180
- Last commit on the tree: `e4f588aa4 the growth page's refusal named a cause the generator cannot observe`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 10:50 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_scenario.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/generate_value_arms_data.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Read, Edit, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 26
- Last commit on the tree: `51869beb9 the 2026-08-29 lane 0 direction is discharged; archiving its finding on evidence`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 11:21 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/generate_value_arms_data.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Edit, Bash, Bash, Bash, Bash, Edit, Bash, Edit, Bash, Bash, Bash, ToolSearch
- Tool calls this session: 32
- Last commit on the tree: `5c3c94b61 Auto-process run complete: report + LATEST.md + site/ (git=51869beb9, net=£154,164)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 11:51 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/canon_claims.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_canon_drift_check.py`
- `tools/generate_value_arms_data.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Write, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, ToolSearch
- Tool calls this session: 68
- Last commit on the tree: `34f8ac5d5 delivery seat: direction for the next stretch`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 12:21 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.3h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/canon_claims.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_canon_drift_check.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Read, Monitor, Bash, Bash, Bash, Bash, Write, Edit
- Tool calls this session: 71
- Last commit on the tree: `9c626f05d the arms page publishes a book of 210 accounts and never said it is 17.9% of the business`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 13:22 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `company/risk/counterparty_collateral_desk.py`
- `docs/context-handshake-latest.md`
- `docs/design/ACCOUNT_POPULATION_CENSUS_2026-08-29.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/canon_claims.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `saas/capital/solvency.py`
- `saas/reporting/annual_report.py`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_phase2b.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/company/risk/test_mcr_account_population.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_canon_drift_check.py`
- `tools/generate_book_growth_data.py`
- `tools/run_frozen_baseline.py`
- `tools/run_value_cycle_ab.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 164
- Last commit on the tree: `40eabc220 Auto-process run complete: report + LATEST.md + site/ (git=9c626f05d, net=£154,164)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 14:21 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `company/risk/counterparty_collateral_desk.py`
- `docs/context-handshake-latest.md`
- `docs/design/ACCOUNT_POPULATION_CENSUS_2026-08-29.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/canon_claims.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/capital/solvency.py`
- `saas/reporting/annual_report.py`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_phase2b.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/company/risk/test_mcr_account_population.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_canon_drift_check.py`
- `tools/generate_book_growth_data.py`
- `tools/run_frozen_baseline.py`
- `tools/run_value_cycle_ab.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Read, Edit, Bash, Edit
- Tool calls this session: 111
- Last commit on the tree: `7ecf66106 chore(provenance): verification paused banner (git=81e25d88b) -- the site keeps serving the last VERIFIED run and now says so; no unverified figure published (DIRECTOR_RULING_PUBLISH_DECOUPLING_2026-08-10 property 3)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 14:52 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `company/risk/counterparty_collateral_desk.py`
- `docs/context-handshake-latest.md`
- `docs/design/ACCOUNT_POPULATION_CENSUS_2026-08-29.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/canon_claims.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/capital/solvency.py`
- `saas/reporting/annual_report.py`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_phase2b.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/company/risk/test_mcr_account_population.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_canon_drift_check.py`
- `tools/generate_book_growth_data.py`
- `tools/run_frozen_baseline.py`
- `tools/run_value_cycle_ab.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Write, Bash, ToolSearch, Write, Bash
- Tool calls this session: 43
- Last commit on the tree: `d124e38f6 chore(provenance): verification paused banner (git=81e25d88b) -- the site keeps serving the last VERIFIED run and now says so; no unverified figure published (DIRECTOR_RULING_PUBLISH_DECOUPLING_2026-08-10 property 3)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 15:22 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `company/risk/counterparty_collateral_desk.py`
- `docs/context-handshake-latest.md`
- `docs/design/ACCOUNT_POPULATION_CENSUS_2026-08-29.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/canon_claims.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/capital/solvency.py`
- `saas/reporting/annual_report.py`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_phase2b.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/company/risk/test_mcr_account_population.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_canon_drift_check.py`
- `tools/generate_book_growth_data.py`
- `tools/run_frozen_baseline.py`
- `tools/run_value_cycle_ab.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Read, Bash, Write, Bash, Bash, Read, ToolSearch, Bash, Read, Read
- Tool calls this session: 30
- Last commit on the tree: `540c227a9 chore(provenance): verification paused banner (git=6c0271d17) -- the site keeps serving the last VERIFIED run and now says so; no unverified figure published (DIRECTOR_RULING_PUBLISH_DECOUPLING_2026-08-10 property 3)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

---

## DISPOSITION 2026-08-29 — ADOPTED in part; this alarm was the publish wedge

**Not archived. Read this before re-drawing it.**

The 12:24 episode above was not a latent tidiness problem: it was the cause of a ~2h PUBLISH-GATE
WEDGE (3 recorded failures, then a 4th at `6c0271d17` while the diagnosis was in flight).

**The chain.** The C1/C3 schematic corrections reached the front door's ALT TEXT in
`site/index.html` and were committed. The two surfaces carrying the same correction —
`docs/design/canon_claims.yaml` (the register) and `site/assets/model-on-a-page.svg` (what a
sighted reader actually meets) — plus the register's literal membership pin in
`tests/tools/test_canon_drift_check.py`, were the work this seat was holding when it died. They
stayed uncommitted from 12:24. At HEAD the register's anchor sentences were therefore no longer on
their page, so
`test_every_live_claim_is_bound_to_a_sentence_still_on_its_page` reported UNBOUND and blocked all
publishing.

**Why nobody saw it in the tree.** The publish gate runs pytest inside a `git archive HEAD`
checkout. The working tree was GREEN the whole time and measured nothing — the unstaged half was
exactly what made it pass. Running the single test in the shared tree reproduces nothing; the
refutation is `git archive HEAD | tar -x` and run there.

**Adopted, per this document's own Adopt branch** — the edit was sound, the transport failed:
committed as `ec376c60d` (canon_claims.yaml + the SVG + the membership pin, landed together via
`tools.surgical_land`). 32/32 green at HEAD in the gate's own checkout shape.

`surgical_land` refused the first attempt, and its refusal was the measurement: landing the register
without the pin is the silent scope-shrink `EXPECTED_CLAIM_IDS` exists to catch. The renamed claims
keep their predicate and only flip `expects` present→absent, so they stay keyed to the channel
rather than to today's verdict — the checker returns SUPERSEDED the day `green_stance` or
`collateral_call` is wired into `simulation/`.

**What is still owed, and why this stays live.** Only the canon-drift subset was adopted. The other
paths this alarm lists — `company/risk/counterparty_collateral_desk.py`, `saas/capital/solvency.py`,
`saas/reporting/annual_report.py`, `sim/scenario/gas_scenario_generator.py`,
`tools/run_value_cycle_ab.py` and the rest — belong to other lanes and remain uncommitted. Archiving
this document now would claim work that did not land. It is archivable when those are committed or
reverted, not before.

**The general lesson, for the next reader.** A seat that dies mid-work does not merely leave a
messy tree: if what it was holding is *half* of a two-surface repair, it wedges whatever gate reads
the committed half. Correcting one rendered surface and not the other is half a fix, and the half
left behind is the one that blocks publishing.

### 2026-08-29 16:32 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `company/risk/counterparty_collateral_desk.py`
- `docs/context-handshake-latest.md`
- `docs/design/ACCOUNT_POPULATION_CENSUS_2026-08-29.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `saas/capital/solvency.py`
- `saas/reporting/annual_report.py`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_phase2b.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/company/risk/test_mcr_account_population.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/generate_book_growth_data.py`
- `tools/run_frozen_baseline.py`
- `tools/run_value_cycle_ab.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, ToolSearch, Bash, Read, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 81
- Last commit on the tree: `9b1ceda50 grade the arms pre-registration on leg 1: the prediction is REFUTED, and not by either mechanism it offered`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 18:36 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Read, Bash, Bash, Bash, Read, Read, Read, Bash, Bash, Bash
- Tool calls this session: 247
- Last commit on the tree: `3f6c29525 the arms page will not name a winner inside its own error bar, and both its inputs are now one book on one day`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 19:06 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 23
- Last commit on the tree: `c9de6aca4 Auto-process run complete: report + LATEST.md + site/ (git=3f6c29525, net=£154,164)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 19:06 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 23
- Last commit on the tree: `c9de6aca4 Auto-process run complete: report + LATEST.md + site/ (git=3f6c29525, net=£154,164)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 19:36 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Write, Edit
- Tool calls this session: 26
- Last commit on the tree: `c9de6aca4 Auto-process run complete: report + LATEST.md + site/ (git=3f6c29525, net=£154,164)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 19:36 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Write, Edit
- Tool calls this session: 26
- Last commit on the tree: `c9de6aca4 Auto-process run complete: report + LATEST.md + site/ (git=3f6c29525, net=£154,164)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 20:07 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/FOUNDER_BOOK.yaml`
- `docs/design/held/FOUNDER_BOOK.yaml.held`
- `docs/design/held/README.md`
- `docs/design/held/founder_book_live_population.patch`
- `docs/design/held/test_founder_book.py.held`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_customer_events.py`
- `tests/simulation/test_founder_book.py`
- `tests/simulation/test_live_population_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Read
- Tool calls this session: 19
- Last commit on the tree: `c9de6aca4 Auto-process run complete: report + LATEST.md + site/ (git=3f6c29525, net=£154,164)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 22:08 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Edit, Bash, Bash, Bash, Write, Bash, Write, Read, Edit, Bash
- Tool calls this session: 363
- Last commit on the tree: `3d0066d6d a map count was bumped past a record that is in no commit, and tests/design is red at HEAD`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 22:38 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/simplifications/SPINE_1_scenario_world_state.yaml`
- `docs/status/LATEST.md`
- `sim/scenario/gas_scenario_generator.py`
- `simulation/run_scenario.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 15
- Last commit on the tree: `3d0066d6d a map count was bumped past a record that is in no commit, and tests/design is red at HEAD`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 23:08 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 41
- Last commit on the tree: `651684c02 the map-count finding records its own discharge, and names the premise that expired`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-29 23:08 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 41
- Last commit on the tree: `651684c02 the map-count finding records its own discharge, and names the premise that expired`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 01:09 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/status/LATEST.md`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tools/run_frozen_baseline.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Write, Edit, Bash
- Tool calls this session: 266
- Last commit on the tree: `8fb60e276 the finding that said it had escalated had not, so the escalation went and the finding is archived`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 03:39 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/status/LATEST.md`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_renewal_funnel.py`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Edit, Bash
- Tool calls this session: 370
- Last commit on the tree: `82030bb58 name the departures the AUC is computed over, or say why the run cannot`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 04:09 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/status/LATEST.md`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_renewal_funnel.py`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 49
- Last commit on the tree: `fa65478f3 the page said the belief ranks backwards and gave the reader no way to rule out our own sign error`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 04:39 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_renewal_funnel.py`
- `tools/generate_value_arms_data.py`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Read, ToolSearch
- Tool calls this session: 55
- Last commit on the tree: `fa65478f3 the page said the belief ranks backwards and gave the reader no way to rule out our own sign error`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 06:53 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_renewal_funnel.py`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 311
- Last commit on the tree: `717472661 the letter told him it was a clean two-point slope; the artefact says one clean point and no slope`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 07:24 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/fork_reconciler.py`
- `docs/context-handshake-latest.md`
- `docs/design/CHOICE_AND_CHANNEL_DISCOVERY.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `simulation/renewals.py`
- `simulation/run_phase2b.py`
- `simulation/svt_product.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_product.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_renewal_funnel.py`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 22
- Last commit on the tree: `717472661 the letter told him it was a clean two-point slope; the artefact says one clean point and no slope`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 07:55 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_renewal_funnel.py`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Read, Edit, Bash
- Tool calls this session: 27
- Last commit on the tree: `fc3106698 thirty-one green tests on a page that renders nothing: the live-surface check the charts mint has owed since July`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 08:22 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_renewal_funnel.py`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 23
- Last commit on the tree: `cd07caa71 the C2 design, and the trap a printed table caught before a line of it was written`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 08:22 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_renewal_funnel.py`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 23
- Last commit on the tree: `cd07caa71 the C2 design, and the trap a printed table caught before a line of it was written`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 08:55 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_renewal_funnel.py`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Write, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Read, Bash
- Tool calls this session: 54
- Last commit on the tree: `15a9703ae the tools shard: ten reds in all, and two of them were controls asking to be made wrong`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 14:07 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/gap_ledger_reconciler.py`
- `background/process_run_complete.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/EP13_CARBON_INTENSITY_DISCOVER_FRAME.md`
- `docs/design/maturity_map.yaml`
- `docs/design/simplifications/EP13_adapter_carbon_intensity.yaml`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_ep13_per_fuel_oracle_bound.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/ep13_per_fuel_oracle_bound.py`
- `tools/generate_dashboard_data.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Write, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Read, ToolSearch, Bash
- Tool calls this session: 753
- Last commit on the tree: `3e90ae5e1 the world took the shape of the published switching series and normalised its level away`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 16:10 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.3h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/gap_ledger_reconciler.py`
- `background/process_run_complete.py`
- `background/publish_cause.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/EP13_CARBON_INTENSITY_DISCOVER_FRAME.md`
- `docs/design/maturity_map.yaml`
- `docs/design/simplifications/EP13_adapter_carbon_intensity.yaml`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `simulation/market_switching_propensity.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_publish_failure_names_which_of_the_three_it_was.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_market_switching_propensity.py`
- `tests/simulation/test_net_new_acquisition.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_ep13_per_fuel_oracle_bound.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/ep13_per_fuel_oracle_bound.py`
- `tools/generate_dashboard_data.py`
- `tools/measure_departure_level.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Write, Read, Edit
- Tool calls this session: 430
- Last commit on the tree: `1596019fc a departure that is not a renewal can now be written down, and it carries no cause it has not measured`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 16:40 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/gap_ledger_reconciler.py`
- `background/process_run_complete.py`
- `background/publish_cause.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/EP13_CARBON_INTENSITY_DISCOVER_FRAME.md`
- `docs/design/maturity_map.yaml`
- `docs/design/simplifications/EP13_adapter_carbon_intensity.yaml`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `simulation/market_switching_propensity.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_publish_failure_names_which_of_the_three_it_was.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_market_switching_propensity.py`
- `tests/simulation/test_net_new_acquisition.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_ep13_per_fuel_oracle_bound.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/ep13_per_fuel_oracle_bound.py`
- `tools/generate_dashboard_data.py`
- `tools/measure_departure_level.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Write, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 35
- Last commit on the tree: `1596019fc a departure that is not a renewal can now be written down, and it carries no cause it has not measured`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 17:50 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/gap_ledger_reconciler.py`
- `background/process_run_complete.py`
- `background/publish_cause.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/EP13_CARBON_INTENSITY_DISCOVER_FRAME.md`
- `docs/design/maturity_map.yaml`
- `docs/design/simplifications/EP13_adapter_carbon_intensity.yaml`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `tests/background/test_a_publish_failure_names_which_of_the_three_it_was.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_ep13_per_fuel_oracle_bound.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/ep13_per_fuel_oracle_bound.py`
- `tools/generate_dashboard_data.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: ToolSearch, Bash, Bash, Bash, Read, Read, Bash, Bash, Bash, Bash, Write, Bash
- Tool calls this session: 121
- Last commit on the tree: `56718a719 the departure level is a quantity with units again, and the ratio is a ratio of the record`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-30 20:20 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/gap_ledger_reconciler.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/EP13_CARBON_INTENSITY_DISCOVER_FRAME.md`
- `docs/design/maturity_map.yaml`
- `docs/design/simplifications/EP13_adapter_carbon_intensity.yaml`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_ep13_per_fuel_oracle_bound.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/ep13_per_fuel_oracle_bound.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_value_arms_data.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, ToolSearch, Bash, Monitor, Bash, Monitor, Bash
- Tool calls this session: 329
- Last commit on the tree: `ff44d0bce C1b's assignment needs no new rule: the world already decides who rolls onto SVT`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-31 01:09 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/gap_ledger_reconciler.py`
- `company/crm/market_conditions.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/EP13_CARBON_INTENSITY_DISCOVER_FRAME.md`
- `docs/design/simplifications/EP13_adapter_carbon_intensity.yaml`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `simulation/customer_events.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `simulation/run_phase2b.py`
- `simulation/svt_product.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/architecture/test_year_keyed_rate_table_census.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/company/pricing/test_value_arm_in_the_renewal_chain.py`
- `tests/controls/test_control_mutation.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_ep13_per_fuel_oracle_bound.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/ep13_per_fuel_oracle_bound.py`
- `tools/generate_dashboard_data.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`
- `tools/svt_generated_share_check.py`

#### Where it had got to

- Last tools it ran, oldest first: Edit, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, ToolSearch
- Tool calls this session: 577
- Last commit on the tree: `bae6a2a28 the publish wedge is a level declared ahead of its code, and the publisher can never carry code`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-31 02:10 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.3h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/gap_ledger_reconciler.py`
- `company/crm/market_conditions.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/EP13_CARBON_INTENSITY_DISCOVER_FRAME.md`
- `docs/design/simplifications/EP13_adapter_carbon_intensity.yaml`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `simulation/customer_events.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `simulation/run_phase2b.py`
- `simulation/svt_product.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/architecture/test_year_keyed_rate_table_census.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/company/pricing/test_value_arm_in_the_renewal_chain.py`
- `tests/controls/test_control_mutation.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_ep13_per_fuel_oracle_bound.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/ep13_per_fuel_oracle_bound.py`
- `tools/generate_dashboard_data.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`
- `tools/svt_generated_share_check.py`

#### Where it had got to

- Last tools it ran, oldest first: Edit, Edit, Edit, Edit, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 151
- Last commit on the tree: `2b9fb79d9 C3 is blocked on a run, and the artefact named the run but never where its code lives`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-31 03:40 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/gap_ledger_reconciler.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/EP13_CARBON_INTENSITY_DISCOVER_FRAME.md`
- `docs/design/simplifications/EP13_adapter_carbon_intensity.yaml`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `simulation/customer_events.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `simulation/run_phase2b.py`
- `simulation/svt_product.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_ep13_per_fuel_oracle_bound.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/ep13_per_fuel_oracle_bound.py`
- `tools/generate_dashboard_data.py`
- `tools/inference_claim.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`
- `tools/svt_generated_share_check.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, ToolSearch, Bash, Bash
- Tool calls this session: 207
- Last commit on the tree: `fe4df178b Auto-process run complete: report + LATEST.md + site/ (git=2b9fb79d9, net=£145,881)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-31 05:10 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/gap_ledger_reconciler.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/EP13_CARBON_INTENSITY_DISCOVER_FRAME.md`
- `docs/design/simplifications/EP13_adapter_carbon_intensity.yaml`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `simulation/customer_events.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `simulation/run_phase2b.py`
- `simulation/svt_product.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_ep13_per_fuel_oracle_bound.py`
- `tests/tools/test_population_anchor.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/ep13_per_fuel_oracle_bound.py`
- `tools/generate_dashboard_data.py`
- `tools/inference_claim.py`
- `tools/population_anchor.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`
- `tools/svt_generated_share_check.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Read, Write, Bash, ToolSearch
- Tool calls this session: 186
- Last commit on the tree: `4240e1478 the guard's own refusal reached no reader, so a page could say the method might be chance while saying nothing about independence`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-31 07:53 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/EP13_CARBON_INTENSITY_DISCOVER_FRAME.md`
- `docs/design/simplifications/EP13_adapter_carbon_intensity.yaml`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `saas/reporting/annual_report.py`
- `simulation/customer_events.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `simulation/run_phase2b.py`
- `simulation/svt_product.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_ep13_per_fuel_oracle_bound.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_population_anchor.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/ep13_per_fuel_oracle_bound.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_value_arms_data.py`
- `tools/population_anchor.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`
- `tools/svt_generated_share_check.py`

#### Where it had got to

- Last tools it ran, oldest first: Read, Edit, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 375
- Last commit on the tree: `340ba58ed the finding that opened the question kept the answer it was corrected by`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-31 08:23 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.3h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/EP13_CARBON_INTENSITY_DISCOVER_FRAME.md`
- `docs/design/LADDER_APPLIED_TO_CHURN_2026-08-31.md`
- `docs/design/simplifications/EP13_adapter_carbon_intensity.yaml`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/customer_events.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `simulation/run_phase2b.py`
- `simulation/svt_product.py`
- `tests/architecture/test_churn_carries_per_customer_signal.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_ep13_per_fuel_oracle_bound.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_population_anchor.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/capture_departure_factors.py`
- `tools/couple_value_based_pricing.py`
- `tools/ep13_per_fuel_oracle_bound.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/population_anchor.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`
- `tools/svt_generated_share_check.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Edit, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 51
- Last commit on the tree: `340ba58ed the finding that opened the question kept the answer it was corrected by`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-31 09:23 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.3h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/EP13_CARBON_INTENSITY_DISCOVER_FRAME.md`
- `docs/design/simplifications/EP13_adapter_carbon_intensity.yaml`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/customer_events.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `simulation/run_phase2b.py`
- `simulation/svt_product.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_ep13_per_fuel_oracle_bound.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_population_anchor.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/ep13_per_fuel_oracle_bound.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_value_arms_data.py`
- `tools/population_anchor.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`
- `tools/svt_generated_share_check.py`

#### Where it had got to

- Last tools it ran, oldest first: Edit, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 153
- Last commit on the tree: `c1b61dccc the baseline I subtracted from was another run's, and the reading I called anti-predictive is inside its null`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-31 10:32 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/customer_events.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `simulation/run_phase2b.py`
- `simulation/svt_product.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_value_arms_data.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`
- `tools/svt_generated_share_check.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 158
- Last commit on the tree: `d5303f115 the within-day timing is in CCGT, the one fuel the reconstruction never observes`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-31 13:03 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/delivery_lane.py`
- `background/gap_ledger_reconciler.py`
- `background/seat_continuation.py`
- `company/crm/churn_desk.py`
- `company/interfaces/churn_estimation.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `saas/reporting/annual_report.py`
- `simulation/customer_events.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `simulation/run_phase2b.py`
- `simulation/svt_product.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_seat_continuation.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/run_frozen_baseline.py`
- `tools/settlement_ceiling_probe.py`
- `tools/svt_generated_share_check.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Edit, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 345
- Last commit on the tree: `5ccc0e0c8 the withdrawn SVT table was published under the key a reader would take, and unmarked`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-31 19:11 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.3h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- `union-the-departure-routes-and-declare-the-denominator` — claimed 0.1h ago. The seat said: Union both departure routes in tools/measure_departure_level and tools/population_anchor._churn_by_year, on a DECLARED denominator, and re-fit YEAR_LEVEL_ANCHOR on the two-route capture. The band cont
  paths: (none declared)

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `.claude/hooks/lane_wall_hook.py`
- `background/autonomous_runner.py`
- `background/boot_sha.py`
- `background/delivery_lane.py`
- `background/fork_reconciler.py`
- `background/fork_salvage.py`
- `background/gap_ledger_reconciler.py`
- `background/process_run_complete.py`
- `background/schedule_manifest.yaml`
- `background/seat-executor.service`
- `background/seat-executor.timer`
- `background/seat_continuation.py`
- `background/seat_executor.py`
- `background/seat_work_in_hand.py`
- `background/supervisor.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/WHAT_THE_JOURNEY_WALK_FOUND_2026-08-31.md`
- `docs/design/WHY_THE_HANDOFF_WAS_NEVER_TAKEN_2026-08-31.md`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/architecture/test_the_journey_walk.py`
- `tests/background/test_a_draw_is_not_a_delivery.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_autonomous_runner.py`
- `tests/background/test_delivery_lane.py`
- `tests/background/test_duplication_is_refused_by_path.py`
- `tests/background/test_fork_reconciler.py`
- `tests/background/test_fork_salvage.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/background/test_the_seat_executor_stands_down.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_departure_risks.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_artefact_rerun_diff.py`

…and 19 more.

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, ToolSearch, Bash
- Tool calls this session: 862
- Last commit on the tree: `fd8c78303 the departure level reads on the whole book, and 2022 cannot be fitted by any anchor`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-31 19:41 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- `give-the-c2-reason-mix-its-svt-route` — claimed 0.1h ago. The seat said: Re-run `tools/capture_departure_factors` with `--out docs/reports/c2_departure_factors.json` on the current world so the sibling `c2_departure_factors_svt_segment_decisions.json` exists, then re-fit `
  paths: (none declared)
- `union-the-departure-routes-and-declare-the-denominator` — claimed 0.6h ago. The seat said: Union both departure routes in tools/measure_departure_level and tools/population_anchor._churn_by_year, on a DECLARED denominator, and re-fit YEAR_LEVEL_ANCHOR on the two-route capture. The band cont
  paths: (none declared)

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `.claude/hooks/lane_wall_hook.py`
- `background/autonomous_runner.py`
- `background/boot_sha.py`
- `background/delivery_lane.py`
- `background/fork_reconciler.py`
- `background/fork_salvage.py`
- `background/gap_ledger_reconciler.py`
- `background/process_run_complete.py`
- `background/schedule_manifest.yaml`
- `background/seat-executor.service`
- `background/seat-executor.timer`
- `background/seat_continuation.py`
- `background/seat_executor.py`
- `background/seat_work_in_hand.py`
- `background/supervisor.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/WHAT_THE_JOURNEY_WALK_FOUND_2026-08-31.md`
- `docs/design/WHY_THE_HANDOFF_WAS_NEVER_TAKEN_2026-08-31.md`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/architecture/test_the_journey_walk.py`
- `tests/background/test_a_draw_is_not_a_delivery.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_autonomous_runner.py`
- `tests/background/test_delivery_lane.py`
- `tests/background/test_duplication_is_refused_by_path.py`
- `tests/background/test_fork_reconciler.py`
- `tests/background/test_fork_salvage.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/background/test_the_seat_executor_stands_down.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_departure_risks.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`

…and 20 more.

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, ToolSearch, Read, Bash
- Tool calls this session: 52
- Last commit on the tree: `fd8c78303 the departure level reads on the whole book, and 2022 cannot be fitted by any anchor`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-31 21:12 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- `give-the-c2-reason-mix-its-svt-route` — claimed 0.6h ago. The seat said: Re-run `tools/capture_departure_factors` with `--out docs/reports/c2_departure_factors.json` on the current world so the sibling `c2_departure_factors_svt_segment_decisions.json` exists, then re-fit `
  paths: (none declared)
- `scale-each-household-s-profile-to-its-own-eac` — claimed 0.1h ago. The seat said: Make the household's drawn `eac_kwh` set the LEVEL of its consumption. `sim/profile_class_1.load_pc1_shape` returns Elexon GAD in kWh -- the average PC1 customer's ABSOLUTE series, 3,928 kWh annualise
  paths: (none declared)

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `.claude/hooks/lane_wall_hook.py`
- `background/autonomous_runner.py`
- `background/boot_sha.py`
- `background/delivery_lane.py`
- `background/fork_reconciler.py`
- `background/fork_salvage.py`
- `background/gap_ledger_reconciler.py`
- `background/process_run_complete.py`
- `background/schedule_manifest.yaml`
- `background/seat-executor.service`
- `background/seat-executor.timer`
- `background/seat_continuation.py`
- `background/seat_executor.py`
- `background/seat_work_in_hand.py`
- `background/supervisor.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/WHAT_THE_JOURNEY_WALK_FOUND_2026-08-31.md`
- `docs/design/WHY_THE_HANDOFF_WAS_NEVER_TAKEN_2026-08-31.md`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `tests/architecture/test_a_departure_reading_declares_its_population.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/architecture/test_the_journey_walk.py`
- `tests/background/test_a_draw_is_not_a_delivery.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_autonomous_runner.py`
- `tests/background/test_delivery_lane.py`
- `tests/background/test_duplication_is_refused_by_path.py`
- `tests/background/test_fork_reconciler.py`
- `tests/background/test_fork_salvage.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/background/test_the_seat_executor_stands_down.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_departure_risks.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`

…and 26 more.

#### Where it had got to

- Last tools it ran, oldest first: Edit, Edit, Bash, Bash, Bash, Bash, Bash, Bash, Bash, ToolSearch, Bash, Bash
- Tool calls this session: 268
- Last commit on the tree: `fd8c78303 the departure level reads on the whole book, and 2022 cannot be fitted by any anchor`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-08-31 22:43 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `.claude/hooks/lane_wall_hook.py`
- `background/autonomous_runner.py`
- `background/boot_sha.py`
- `background/delivery_lane.py`
- `background/fork_reconciler.py`
- `background/fork_salvage.py`
- `background/gap_ledger_reconciler.py`
- `background/process_run_complete.py`
- `background/schedule_manifest.yaml`
- `background/seat-executor.service`
- `background/seat-executor.timer`
- `background/seat_continuation.py`
- `background/seat_executor.py`
- `background/seat_work_in_hand.py`
- `background/supervisor.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/WHAT_THE_JOURNEY_WALK_FOUND_2026-08-31.md`
- `docs/design/WHY_THE_HANDOFF_WAS_NEVER_TAKEN_2026-08-31.md`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `tests/architecture/test_a_departure_reading_declares_its_population.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/architecture/test_the_journey_walk.py`
- `tests/background/test_a_draw_is_not_a_delivery.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_autonomous_runner.py`
- `tests/background/test_delivery_lane.py`
- `tests/background/test_duplication_is_refused_by_path.py`
- `tests/background/test_fork_reconciler.py`
- `tests/background/test_fork_salvage.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/background/test_the_seat_executor_stands_down.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_departure_risks.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`

…and 25 more.

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Write, Edit, Bash, Bash, Bash
- Tool calls this session: 184
- Last commit on the tree: `5dab107b6 the sink guard is an autouse FIXTURE, so an import at collection time walks straight past it -- filed, not fixed`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-09-01 04:48 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `background/process_run_complete.py`
- `background/publish_cause.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/wall_channel_census_baseline.json`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `tests/architecture/test_a_departure_reading_declares_its_population.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_behind_origin_publish_refuses_instead_of_deepening_the_fork.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_fork_salvage.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_process_run_complete.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/background/test_the_publish_commit_carries_only_its_own_work.py`
- `tests/background/test_tree_divergence.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_departure_risks.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_a_published_surface_is_reproducible_from_its_committed_input.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/fit_departure_hazards.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_proof_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`

…and 1 more.

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 926
- Last commit on the tree: `8fba12032 Auto-process run complete: report + LATEST.md + site/ (git=b17d86e3e, net=£131,289)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-09-01 05:18 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `background/process_run_complete.py`
- `background/publish_cause.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/wall_channel_census_baseline.json`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `tests/architecture/test_a_departure_reading_declares_its_population.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_behind_origin_publish_refuses_instead_of_deepening_the_fork.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_fork_salvage.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_process_run_complete.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/background/test_the_publish_commit_carries_only_its_own_work.py`
- `tests/background/test_tree_divergence.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_departure_risks.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_a_published_surface_is_reproducible_from_its_committed_input.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/fit_departure_hazards.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_proof_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`

…and 1 more.

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Write, Bash
- Tool calls this session: 32
- Last commit on the tree: `37b2f1bd7 Auto-process run complete: report + LATEST.md + site/ (git=8fba12032, net=£131,289)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-09-01 05:48 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `background/process_run_complete.py`
- `background/publish_cause.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/wall_channel_census_baseline.json`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `tests/architecture/test_a_departure_reading_declares_its_population.py`
- `tests/architecture/test_static_quality_ratchet.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_behind_origin_publish_refuses_instead_of_deepening_the_fork.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_fork_salvage.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_process_run_complete.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/background/test_the_publish_commit_carries_only_its_own_work.py`
- `tests/background/test_tree_divergence.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_departure_risks.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_a_published_surface_is_reproducible_from_its_committed_input.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/fit_departure_hazards.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_proof_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`

…and 1 more.

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Write, Bash, Write, Bash, Bash, ToolSearch, Bash
- Tool calls this session: 49
- Last commit on the tree: `bd6df225d delivery seat: direction for the next stretch`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-09-01 08:10 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/wall_channel_census_baseline.json`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `simulation/departure_risks.py`
- `simulation/renewals.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_fork_salvage.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_process_run_complete.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/background/test_the_publish_commit_carries_only_its_own_work.py`
- `tests/background/test_tree_divergence.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_departure_risks.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/fit_departure_hazards.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_proof_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Monitor, Bash, Bash, Bash, Bash, Bash, Monitor, Bash, Monitor
- Tool calls this session: 196
- Last commit on the tree: `b58b6cedc chore(provenance): verification paused banner (git=f2a8db90c) -- the site keeps serving the last VERIFIED run and now says so; no unverified figure published (DIRECTOR_RULING_PUBLISH_DECOUPLING_2026-08-10 property 3)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-09-01 10:41 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/wall_channel_census_baseline.json`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `simulation/renewals.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/fit_departure_hazards.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_proof_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Read, Edit, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 294
- Last commit on the tree: `78285a434 Auto-process run complete: report + LATEST.md + site/ (git=fb6e29e6d, net=£149,156)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-09-01 11:11 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/wall_channel_census_baseline.json`
- `docs/direction/DIRECTION.yaml`
- `docs/direction/decisions.jsonl`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `simulation/renewals.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/capture_departure_factors.py`
- `tools/couple_value_based_pricing.py`
- `tools/fit_departure_hazards.py`
- `tools/generate_dashboard_data.py`
- `tools/generate_proof_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Read, Edit, Bash, Bash, Bash, ToolSearch, Bash
- Tool calls this session: 38
- Last commit on the tree: `5994c17d4 define bill shock before measuring it again -- two experiences, two populations, and a practitioner correction`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-09-01 16:29 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/wall_channel_census_baseline.json`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `simulation/renewals.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tools/artefact_rerun_diff.py`
- `tools/capture_departure_factors.py`
- `tools/couple_value_based_pricing.py`
- `tools/fit_departure_hazards.py`
- `tools/generate_proof_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Write, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 738
- Last commit on the tree: `318066998 Auto-process run complete: report + LATEST.md + site/ (git=98db658f2, net=£149,156)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-09-01 17:59 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- `the-native-svt-capture-has-been-dead-for-three-stretches` — claimed 0.4h ago. The seat said: **Launch it first, then let go of it.** The capture is the only item on this list whose cost is wall-clock rather than attention, and it runs unattended. Start it, release the item, and work item two 
  paths: (none declared)

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/wall_channel_census_baseline.json`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `simulation/renewals.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_published_artefact_carries_the_split_the_code_computes.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/fit_departure_hazards.py`
- `tools/generate_proof_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 232
- Last commit on the tree: `e41263a8e merge origin/main: the SVT capture provenance finding meets the direction and published-section work landed on the other lane`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-09-01 18:30 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- Nothing was claimed. Whatever it was doing, it did not say.

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/wall_channel_census_baseline.json`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `simulation/renewals.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_assignment.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_published_artefact_carries_the_split_the_code_computes.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/fit_departure_hazards.py`
- `tools/generate_proof_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 28
- Last commit on the tree: `68ec6825b the native SVT capture ran to completion and two of its five predictions are split against the file`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-09-01 19:47 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- `the-native-svt-capture-has-been-dead-for-three-stretches` — claimed 0.2h ago. The seat said: **Launch it first, then let go of it.** The capture is the only item on this list whose cost is wall-clock rather than attention, and it runs unattended. Start it, release the item, and work item two 
  paths: (none declared)

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/wall_channel_census_baseline.json`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_published_artefact_carries_the_split_the_code_computes.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/fit_departure_hazards.py`
- `tools/generate_proof_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Write, Write, Edit, Edit, Write, Read, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 138
- Last commit on the tree: `9a03f3b44 the two captures differ by three producers and 2022's level anchor is doubled, so the single-cause attribution is unearned`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-09-01 21:18 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.3h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- `the-native-svt-capture-has-been-dead-for-three-stretches` — claimed 0.7h ago. The seat said: **Launch it first, then let go of it.** The capture is the only item on this list whose cost is wall-clock rather than attention, and it runs unattended. Start it, release the item, and work item two 
  paths: (none declared)

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/deadmans_switch.py`
- `background/fork_reconciler.py`
- `background/gap_ledger_reconciler.py`
- `background/seat_executor.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/UNLANDED_WHOLE_BOOK_LEVEL_ANCHOR_BLOCK_2026-09-01.md`
- `docs/design/wall_channel_census_baseline.json`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `tests/architecture/test_switching_rate_commons.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_published_artefact_carries_the_split_the_code_computes.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/fit_departure_hazards.py`
- `tools/generate_proof_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash
- Tool calls this session: 144
- Last commit on the tree: `77e1d68e6 Auto-process run complete: report + LATEST.md + site/ (git=0e316d3e9, net=£149,156)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-09-01 22:48 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.4h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- `the-only-control-holding-the-level-anchor-is-red-and-reports-a-constant-pass` — claimed 0.2h ago. The seat said: **Two tests are red at clean HEAD and they are the accountability route for the quantity every other item on this list depends on.** Verify before acting and do not take my word for it — extract with 
  paths: (none declared)

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/UNLANDED_WHOLE_BOOK_LEVEL_ANCHOR_BLOCK_2026-09-01.md`
- `docs/design/wall_channel_census_baseline.json`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `tests/architecture/test_a_departure_reading_declares_its_population.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_published_artefact_carries_the_split_the_code_computes.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/fit_departure_hazards.py`
- `tools/generate_proof_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Write, Write, Bash
- Tool calls this session: 254
- Last commit on the tree: `f7714a356 chore(provenance): verification paused banner (git=3f2ffd0aa) -- the site keeps serving the last VERIFIED run and now says so; no unverified figure published (DIRECTOR_RULING_PUBLISH_DECOUPLING_2026-08-10 property 3)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

### 2026-09-01 23:18 UTC

**Filed automatically by `background/seat_continuity.py`, not by a person.** The seat ran no
tool for **0.5h** and its process is gone. It did not stop on purpose: an
interactive session that finishes says so, and this one just stopped — which is the shape an
Anthropic API error leaves behind, four times now by the director's count.

This document exists so that nobody has to notice. It is a staged doc, so the next worker tick
draws it like any other work.

#### What it had claimed

- `the-only-control-holding-the-level-anchor-is-red-and-reports-a-constant-pass` — claimed 0.7h ago. The seat said: **Two tests are red at clean HEAD and they are the accountability route for the quantity every other item on this list depends on.** Verify before acting and do not take my word for it — extract with 
  paths: (none declared)

#### What it left in the tree, uncommitted

SOURCE paths only — the daemons' own output under `docs/observability/`, `site/` and the rest
of `tree_divergence.GENERATED_PREFIXES` is excluded, and so is `docs/staging/`, which is the
queue you are reading this from. This is the real state, and more reliable than anything the
session could have written about itself, because an API error is precisely the thing that
stops it writing.

- `background/boot_sha.py`
- `background/gap_ledger_reconciler.py`
- `company/billing/invoice.py`
- `docs/context-handshake-latest.md`
- `docs/design/A46_THE_PRICED_MENU_2026-08-30.md`
- `docs/design/ANNUAL_REPORT_IMPORT_DEBT.md`
- `docs/design/C1B_THE_BOOK_LANDS_ON_THE_SVT_PRODUCT_2026-08-30.md`
- `docs/design/CHOICE_AND_CHANNEL_ROADMAP.md`
- `docs/design/DRAWN_BOOK_TARIFF_TYPE_FIDELITY_DETERMINATION.md`
- `docs/design/UNLANDED_WHOLE_BOOK_LEVEL_ANCHOR_BLOCK_2026-09-01.md`
- `docs/design/wall_channel_census_baseline.json`
- `docs/market_research/gb_switching_rate_denominators.md`
- `docs/status/LATEST.md`
- `docs/status/PROJECT_STATE.txt`
- `saas/reporting/annual_report.py`
- `simulation/departure_level_anchor.py`
- `tests/architecture/test_a_departure_reading_declares_its_population.py`
- `tests/background/test_a_refused_publish_commit_reaches_the_wedge_detector.py`
- `tests/background/test_gap_ledger_reconciler.py`
- `tests/background/test_publish_gate_alert.py`
- `tests/background/test_supervisor.py`
- `tests/company/billing/test_invoice_characterization.py`
- `tests/company/compliance/test_crisis_bad_debt_validator.py`
- `tests/company/interfaces/test_counterparty_collateral_seam.py`
- `tests/controls/test_control_mutation.py`
- `tests/production_surface_guard.py`
- `tests/saas/reporting/test_a_churned_account_has_a_departure_record.py`
- `tests/saas/reporting/test_a_departure_route_carries_its_denominator.py`
- `tests/sim/test_scenario_spine_consumption.py`
- `tests/simulation/test_home_move_undeliverable_win.py`
- `tests/simulation/test_phase40a_pass_through.py`
- `tests/simulation/test_svt_product.py`
- `tests/test_isolation_guards.py`
- `tests/tools/test_a_published_rate_says_which_rate_it_is.py`
- `tests/tools/test_artefact_rerun_diff.py`
- `tests/tools/test_couple_value_based_pricing.py`
- `tests/tools/test_generate_value_arms_data.py`
- `tests/tools/test_settlement_ceiling_probe.py`
- `tests/tools/test_the_published_artefact_carries_the_split_the_code_computes.py`
- `tools/artefact_rerun_diff.py`
- `tools/couple_value_based_pricing.py`
- `tools/fit_departure_hazards.py`
- `tools/generate_proof_data.py`
- `tools/generate_value_arms_data.py`
- `tools/measure_churn_heterogeneity.py`
- `tools/run_arms_with_the_skill_funnel_20260830.sh`
- `tools/run_arms_with_the_skill_funnel_20260830b.sh`
- `tools/settlement_ceiling_probe.py`

#### Where it had got to

- Last tools it ran, oldest first: Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Bash, Write, Read, Edit
- Tool calls this session: 24
- Last commit on the tree: `e70ab4749 chore(provenance): verification paused banner (git=3f2ffd0aa) -- the site keeps serving the last VERIFIED run and now says so; no unverified figure published (DIRECTOR_RULING_PUBLISH_DECOUPLING_2026-08-10 property 3)`

#### What to do with it — decide, do not just re-run

**Adopt** if the uncommitted paths above are coherent work part-way to something: read the
diff, finish it, commit it. That is the cheap outcome and the usual one.

**Discard** if the diff is a half-applied edit that no longer makes sense — `git checkout --`
the paths and take the claim from scratch. Say which you did.

Do NOT assume the work is wrong because the session died. The failure was in the transport,
not in the edit; the tree state above is exactly what a healthy session would have had at that
moment.

Archive to `docs/staging/done/` once the paths above are either committed or reverted.

## DISPOSITION 2026-09-02 — drawn as a publish-gate wedge suspect, REFUTED as the cause, re-frozen

Drawn by the worker tick that cleared the 80-minute publish-gate wedge, where
`.publish_gate_state.json` listed this document in `cited_findings` alongside two others.

**It was not the cause, and this is not an opinion.** The red set at the wedge was enumerated as
exactly ONE test (`.last_gate_blocking_tests.json`, `total_red: 1`, census `complete` at
`f7714a356`): `tests/controls/test_daemon_loop_mutation.py::test_dms_isolated_accounts_for_every_check_run_cycle_calls`.
That test was reproduced red at HEAD `452132c8e`, its cause traced to `580c47101` adding
`_check_worktree_reap()` to `run_cycle()` without moving the fixture's neutralise-list, fixed, and
landed as `cd0dc6eb1` (gate-rc 0, 20/20). Nothing in this document is on that path. Being CITED as a
suspect and being the CAUSE are different things, and the citation was a coincidence of timing —
these three findings were simply the ones open when the wedge began.

**Re-frozen rather than archived, because its own archive condition is genuinely unmet.** This
document says: *"Archive to `docs/staging/done/` once the paths above are either committed or
reverted."* They are not. `git status` still shows uncommitted source paths, and the document has
accreted 23 instances and five "still live" days since 2026-08-26 — the condition it names is
ongoing, not stale. Archiving it tonight would be filing a live alarm as done, which is the failure
this project keeps paying for.

**What is owed, stated so the next drawer inherits a decision rather than a re-read:** the adopt/
discard call on the 2026-08-26 snapshot's uncommitted paths. That is a real piece of work and it is
not the wedge's, so it was not done under a priority-zero tick.
