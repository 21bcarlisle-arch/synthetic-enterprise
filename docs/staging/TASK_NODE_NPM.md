# Task: Install Node.js and npm system-wide

sudo apt update && sudo apt install -y nodejs npm

Verify with node --version and npm --version. If nodejs < v18, use NodeSource LTS instead.

Send NTFY: Node/npm installed. node: [version], npm: [version]. Claude Code auto-update unblocked.