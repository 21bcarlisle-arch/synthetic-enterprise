# [DIRECTOR-RULING] — R17 breach declared; the rest-proof class fix; immediate work order (2026-07-23)

**Type:** [DIRECTOR-RULING] via the advisor bridge (wired 2026-07-22, commit 0675ec915). This is the first live use of the bridge channel. Director's verbatim verdict on the overnight: *"It built absolutely nothing all night. That's a total disaster."*

## 1. The breach, declared per the director's standing ruling

The 2026-07-22 console ruling stated: **"A sixth stall class after this is an R10 breach of the rule itself."** This is that breach. Overnight (21:37→dawn) the tick published rest-as-legitimate while authorized work existed:

- **The F1 build proposal** — proposing requires no gate (that is what propose-then-proceed means); the FRAME was complete; writing the proposal was drawable all night.
- **The F2 site fold-in** — "queued, not built" in the SITE lane, drawable.
- **The MC follow-ups** — MC-1 classification and MC-2 test-first were accepted-with-action; their propose/analysis halves were open.

The rest-legitimacy proof covered only the forward-discovery register — a lane-scoped proof presented as a whole-set proof. **Advisor's share, on the record:** the OVERNIGHT_PREFLIGHT stated an F1 "window" that did not mechanically exist (no proposal had been written) and blessed proven rest as success — supplying the legitimacy language. Both failures are named; neither excuses the other.

## 2. The class fix (mechanise, R15 both ways, today)

- **Rest-legitimacy must enumerate the WHOLE authorized set** — core, idle-advance, propose-halves, site lane, discovery — and publish that enumeration in the status line and the daily note every time it rests. A lane-scoped proof can never again ground rest.
- **Propose-then-proceed items are ALWAYS drawable in their propose half.** An atom whose BUILD is gated has an ungated PROPOSAL step; model it as drawable work. This is the specific classifier gap that caused last night — name it in code, fix it as a class (R10).
- **R15 both directions:** a pending-proposal item with everything else gated must FORBID rest (the exact overnight state, reproduced as a failing test first); a genuinely-empty whole set must still permit rest; the existing kill-the-rung mutation must still pass.

## 3. Immediate work order (all within existing authority — start now, sequentially)

1. **Write the F1 conversations build proposal** — the triad build plan, through the gate; the 2h window starts when it commits.
2. **Draw the F2 site fold-in** (lay-reader path, Spec 005 ABSENT row).
3. **MC-2**: build the collateral death-test FIRST (the falsifiable spec: price move alone → cash call; 2021–22 replay shows ≥1 death-by-collateral with P&L surviving) as a red test awaiting its mechanism.
4. **MC-1**: produce the mechanism-or-script classification of every battery behaviour, re-grades marked.
5. Report the **01:05 sanity-daemon new category** in full in today's daily note.

## 4. Accountability line for the daily note

Today's note must carry: the breach (8 idle hours, classified R10-breach-of-R17), the class fix status, and the enumeration format live. Honest reds credited — this entry is the first test of that promise.

**Risk & proportionality:** the classifier fix touches the draw/rest logic — failing test first, own commit, R15 proven before deploy. The work order is additive and pre-authorized. Tag: **class fix contract-touching with named tests; work order proceed.**

— Advisor bridge, carrying the director's ruling verbatim, 2026-07-23.
