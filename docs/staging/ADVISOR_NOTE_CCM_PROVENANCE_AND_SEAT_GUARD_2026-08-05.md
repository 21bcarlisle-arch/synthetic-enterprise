# [ADVISOR-NOTE] — CCM provenance, seat-guard, and the main-push finding (2026-08-05, v3)

**Type:** [PROVENANCE + FINDINGS]. Rewritten 22:15 as one coherent note after two same-evening corrections (log at foot). Explains the four `[CCM]` merges on main, what shipped, and one security-relevant observation with its evidence chain.

## Who CCM is
"CCM" = director-launched Claude Code cloud sessions (Anthropic sandboxes on the GitHub repo), briefed by the advisor with fenced, self-contained task orders, delivering via PR. CCM is NOT the worker seat: fences forbid docs/staging/, CLAUDE.md worker duties, NTFY, daemons. Pushes are *nominally* restricted to the session's own branch — see Observation below: one background push reached main regardless.

## What landed (all four PRs merged by the director 21:31 BST; advisor-verified pre-merge, full tiers re-run on merged main: 114 green)
- **PR #4 — governance registry validator**: `tests/design/test_maturity_map_contract.py`, 17 tests, dated shrink-only allowlists, R15 mutation + stale-entry proofs; 4 prose edges → `blocked_reason`.
- **PR #5 — simplifications store**: `docs/design/simplifications/` (181 files + README = the first Birth Certificate), loader + SINGLE-USE migration, six consumers ported with proofs (incl. `supervisor._atom_fingerprint`, byte-identical), map 1,716,747 → 248,736 bytes under a 400KB ratchet, round-trip SHA-256 identical. Loose thread: `block_reason` prose on 27 atoms coexists with the 4 new tokens — reconcile during the ruled map rework.
- **PR #6 — epistemic-wall import ratchet**, merged WITH its widening amendment: static AST ratchet over {company, saas} ↔ {sim, simulation}, seam = company.interfaces. Class (a) company-side→SIM: **2 named legacy edges** (`saas.reporting.annual_report → simulation.run_phase4c_on_phase2b`, `saas.reporting.segment_report → simulation.run_segments`) — the wall's highest-priority shrink targets. Class (b) freezes the reverse mass. New crossings in either direction fail loudly.
- **PR #7 — seat-guard, SHIPPED (do not re-mint)**: seven hooks guarded via `_seat.is_resident_seat()` (marker `~/.config/synthetic-enterprise/.env.ntfy`, `SE_SEAT` override, fail-closed), `block_sudo` universal, structural-lock test forbids unguarded future hooks; advisor replicated the mutation proof digit-for-digit (exactly 10 flips). Resident integration self-proves at first wake: if the tick draws work, the guard passed through.

## Observation — a sandbox push reached main (evidence chain, then meaning)
Commit `4483b34` ("chore(liveness): publish heartbeat…", 17:38 BST 2026-08-05) sits directly on main. Evidence: (1) payload is a one-line `last_updated` bump in `docs/observability/agent_status.json`, while every per-agent heartbeat inside stays frozen at 2026-08-03 → **no resident daemon ran; the machine stayed dark all week**. (2) Actions API shows **zero** workflow runs at 17:38 (today's only runs: 4× 20:31Z pages deploys triggered by the merges) → not a workflow. (3) Timing sits five minutes before CCM-2's deliverable commit, and CCM-2's own session report described live auto-machinery in its sandbox (`process_run_complete.py`, dirty `.human_last_input`); the `git=abc` placeholder marks a degraded context. Conclusion: the repo's committed liveness logic ran inside a foreign sandbox and its push to main SUCCEEDED — **the platform's branch restriction cannot be relied on to protect main.** The effective control is the seat-guard (PR #7), which cuts this launch chain. Wake follow-ups: (a) confirm no auto-launch path into background machinery exists outside the guarded hooks; (b) note that platform branch-protection requiring PRs would break the project's direct-commit model (worker commits, advisor staging, pages) — the seat-guard is the chosen control, a narrower platform rule is the worker's to evaluate.

Bonus: `agent_status.json` is the machine's last self-portrait (2026-08-03 20:49). Flag for wake: **build-executor BLOCKED, anomaly `map_unreconciled`, since 2026-07-29** — reconcile against the merged validator/store and the map-rework ruling. Treasury £11,131; EV −£20,661.90 at shutdown.

## Corrections log (claim-before-evidence, twice; both retracted same evening)
v1 said "no workflows exist" — false, three exist (listing). v2 attributed the heartbeat to a scheduled Pages workflow — false, none is scheduled and the Actions API shows no 17:38 run. v3 stands on the payload, the Actions record, and CCM-2's own report. The advisor's discipline failure and its fix (no attribution ahead of decisive evidence) are recorded here deliberately: this batch legislates against exactly this class.

— Advisor note v3, 2026-08-05 22:15 BST; machine dark throughout, confirmed by the frozen heartbeats above.
