[PROJECT] TOP-LEVEL NAV + STORY RESTRUCTURE -- director directive. The site tells the story of a simulation containing a company; the mission is a company whose REAL-WORLD PRODUCTS are software, synthetic data, and a method. Give all three a home.

THE THREE PRODUCTS (currently homeless on the site):
P1. THE SOFTWARE/PLATFORM -- the CTO/CPO view. 449 modules, four-layer architecture, event ledger, epistemic boundary in CI, swappable adapters, canonical data pipeline, 15k+ tests.
P2. THE SYNTHETIC DATA -- customer behavioural trajectories, decision/event streams, calibrated market scenarios. Industry-valuable exhaust, currently invisible as a product.
P3. THE METHOD -- the harness: tiered governance, staging bridge, verify-by-fetch, consistency gates, two-strike redesign, retro practice. The transferable way-of-working, proven this week.

NEW NAV (six sections, narrative order for a stranger):
1. HOME/STORY (landing): the 60-second pitch -- mission line, headline metrics (net, EV, crisis survived, delta-EV-vs-baseline when it lands), the learning curve, three-product framing, entry links to each section. Replaces dashboard-as-landing.
2. SIM (The World) -- as per SIM_TAB_OVERHAUL.
3. SUPPLIER (The Company) -- as per SUPPLIER_TAB_OVERHAUL.
4. CUSTOMERS (The Relationships) -- portal/360s + case-study recommender.
5. PLATFORM (The Software) -- NEW SECTION, CTO/CPO view:
   - Architecture: the four layers, event flow, epistemic boundary (diagram, generated from the module inventory where possible)
   - Data mesh: the canonical artifacts, data lake, event ledger schemas, per-run pipeline -- what data exists, where it flows, freshness guarantees
   - Module/domain map: 449 modules by domain, auto-generated, with the universal/market/UK transferability tagging
   - API & adapter registry: the ports (market feed adapter live; boundary adapters planned: credit bureau, PSP, DCC) -- the go-live swap story
   - SaaS COVERAGE MAP lives here (Eliminated/Recreated/Interfaced -- already staged)
   - SYNTHETIC DATA CATALOGUE: the datasets as documented products (customer_sample, event streams, scenario sets) with schemas, sample fetch links, and calibration provenance (population-anchored)
6. METHOD (The Way) -- NEW SECTION, the process as product:
   - The operating model: human principal -> advisor -> agent -> daemons (the two-tap governance diagram)
   - The rules: R1-R6 + design laws + tier system, each with the incident that forged it
   - THE LOOPS SHOWN WORKING: staging->NTFY->action->verify as a live view (staging queue state, recent directives and their outcomes)
   - Retro library + the learning-curve of the build itself
   - Framed explicitly as a repeatable pattern: "how to grow a company with an autonomous agent" -- the harness as licensable method
7. PROJECT (The Journey) -- slims to: timeline, discoveries, system/agent health, investor summary. Company + Capabilities sub-tabs redistribute (Company->Method, Capabilities->Platform).

RULES: derive-or-die everywhere; in-world rule holds (Platform/Method are OUT-of-world sections and may show build metadata; Supplier/Customers/SIM stay in-fiction); design laws v4; consistency gate site-wide.
SEQUENCING: Home/Story + Platform skeleton first (biggest story gap), Method second (harvests the retro + rules docs already written), Project slim-down last. Tier 2 -- structure is pre-approved by this directive; Rich's eyes accept each landing.
