# [DIRECTOR-RULING] — Scenario steer: window NOT vetoed + sequencing guard; and a product-status demand (2026-07-23)

**Type:** [DIRECTOR-RULING] via advisor bridge. Two parts, one doc to keep the queue lean.

## 1. The scenario-spine steer (c2e83d9f7): proceed — window not vetoed — with one sequencing guard

The director does not exercise the 2h veto on the scenario FRAME; the trading-friction amendment rides the already-authorized WVC build as stated. **Sequencing guard so two rulings don't fight:** the scenario FRAME and friction work run in the WVC lane and must NOT preempt PRODUCT-FIRST queue items 1–2 (site v5 pages; generator draw-wiring). If lanes contend, items 1–2 win the draw. Scenario-world *values* still return to the director for R13 ratification before any rotation, as the steer itself requires.

## 2. Product status — answer with evidence NOW (R9), one line

Since the PRODUCT-FIRST ruling landed (12:05Z) there have been zero product commits in ~100 minutes. Answer in the next NTFY, evidence not narrative: **(a)** a product turn is in flight — name the queue item and expected land time; or **(b)** the draw did not start the queue — name why (classifier, lane, dependency) and start item 1 (site v5 pages) immediately. If (b) and the cause is a draw/classifier gap, that is a seventh-class candidate and gets the R10 treatment — but the product build starts first, fix-in-parallel; the director will not accept another machinery-before-product inversion.

Tonight's daily note is the test either way: it leads with the PRODUCT/MACHINERY split and the "what can the director SEE today" answer, and "the metric itself" does not count as product.

**Risk & proportionality:** non-veto + sequencing + a status demand; no new scope. Tag: **proceed.**

— Advisor bridge, carrying the director's non-veto and the standing bar, 2026-07-23.
