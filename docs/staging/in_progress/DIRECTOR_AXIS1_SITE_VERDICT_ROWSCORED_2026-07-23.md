> **STATUS (parked in in_progress/ — standing rubric, sub-items still open).** This is the axis-1
> rubric the whole SITE_V5 campaign scores against, not a one-shot task. **Applied so far:** RC1
> (registers-are-sources) — landed on /company (surface 3, plain-English governance layer) and /proof
> (surface 4, simplifications register moved to a labelled appendix). RC5 (no effort-as-outcomes) —
> landed on /company and /proof. **Still open:** RC2 (the real lay page — reconcile with SITE_V5's
> five-surface IA, which folds Simplified into Proof rather than making /simplified the lay page; the
> lay page lands with the Front door, surface 1), RC3 (charts on /now + /world chart grammar), RC4
> (site-wide brand/hierarchy pass incl. tilde-on-figures kill + chat-widget question — iteration 6/last).
> Unblocks when those surfaces are drawn by the open-campaign self-refill. Kept out of the supervisor's
> unprocessed-staging scan by living here, per the multi-part-staged-instruction convention.

# [DIRECTOR-RULING] — Axis-1 verdict, row-scored from the director's own screenshots; the five root causes; iteration order (2026-07-23)

**Type:** [DIRECTOR-RULING] via advisor bridge. The director reviewed six live pages (/now, /world, /method, /company, /project, /simplified) on his phone and rendered the verdict "awful". The advisor decomposed it with him into the specific verdicts below. **The ratified structure stands** (causal chain, clocks, honesty placeholders, wall crossings) — the failure is the presentation layer and content selection. Fix those; do not re-architect the IA.

## The five root causes (each is a class, fix as classes — R10)

**RC1 — Machine exhaust published as content.** /company "Recent decisions" renders raw internal decision-log text (commit-speak, advisor riders, string-replace failure notes) on the board pack; /simplified renders the 506-entry simplifications register raw. RULE: **internal registers and logs are sources, never surfaces.** Every human page gets a written presentation layer: plain-English summaries with drill-down links to the raw artefact. The raw registers move to a clearly-labelled appendix under /method for the forensically curious.

**RC2 — The lay path is not just absent; its slot is occupied.** "Simplified" in the nav must become the actual plain-language page (the F2 findings exist for exactly this): what Poesys is, in words a lay reader can say back — the Child's 2-minute test from Spec 005. The register vacates that URL.

**RC3 — No charts; wrong grammar where any exist.** /now gets one real visualisation per panel (panel 1: the temp→price coupled pair; panel 2: hedge-cover vs band + margin trend; panel 3: the household's billed/banked/balance over time; panel 4 stays an honest placeholder). /world's intra-day becomes a labelled line with axis, session high/low marked; forward terms become a line with the SIM/company gap shaded. Chart grammar: lines for series, bars for comparisons, scatter for relationships, axes labelled, every chart carrying its as-of clock. The advisor's hedging-desk renders are the reference bar.

**RC4 — Hierarchy and brand.** Apply BRAND_CONSTITUTION properly: BRAG colour as information (a RAG chip means something or is removed), one theme decision (if dark=live operational is intentional, say so in the constitution; otherwise unify), typographic hierarchy so each page has one thing the eye lands on, kill the tilde on board figures (give the number and its clock), and **remove the chat widget** unless the director explicitly wants it.

**RC5 — Effort metrics as outcomes.** Remove tests-passing/test-executions/phases from the investor summary headline. They may appear only inside the capped-budget economics story where effort-per-outcome IS the point, reframed as the board's battery permits. Outcome metrics lead: net margin, treasury, crisis survived, coverage of real settlement data.

## Row scores (the director's axis-1 baseline, to beat)

/now 2/5 (structure right, zero charts, mono-noise) · /world 3/5 (best concept, worst chart grammar) · /method 3/5 (strongest content, card-grid sameness) · /company 1/5 (RC1 flagship offender) · /project 2/5 (RC5 flagship; discovery cards good) · /simplified 0/5 (RC2 — wrong page in the slot).

## Iteration order (campaign rolls, no doorbells — seventh-class rule applies)

1. /simplified → the real lay page (RC2; F2 findings feed it). 2. /company presentation layer (RC1). 3. /now charts (RC3 — the director's own window). 4. /world chart grammar. 5. /project outcome-led summary (RC5). 6. Site-wide brand/hierarchy pass (RC4). Each iteration lands with a **row-scored self-claim against this doc and Spec 005**, R11 pixel-verified, so the director's next verdicts are per-row deltas, not "awful".

**Risk & proportionality:** presentation-layer work under the confirmed structure; additive, per-page commits; registers move, never deleted. Tag: **proceed — campaign.**

— Advisor bridge, carrying the director's screenshot review, 2026-07-23.
