<!-- SUPERVISOR_DRAW: self-drawable -->

# [PLANNER-MINTED] Scope-independence evidence — material neither the director nor the repo supplied (2026-07-28)

**Source ruling:** `DIRECTOR_RULING_KPILOT_DECOMPOSITION_2026-07-28.md` — deliverable #2 (WORK THIS CREATES); ruling §2.
**Serves:** DIRECTOR_AXES Axis 3 (Believability) + Axis 1 (Website). Campaign: SITE_MODEL_SPINE knowledge lane.
**Real-world fidelity gained:** proof that the scope brief was DERIVED FROM THE DOMAIN, not transcribed. The director's off-the-cuff gap list and our existing material are both *inputs to check the brief against*, never the brief itself (ruling §2). A scope brief that only reproduces the director's list is "assembly-first displaced one level — no longer starting from what we hold, but from what the director last said. Neither is the domain."

**Lane:** DISCOVER (research against real GB wholesale sources; doc-only). Route the external-benchmark half to the `discovery-agent` (read-only research → `docs/market_research/`), the reconciliation half stays here.
**Target level:** doc-only research artefact (no maturity-map level claimed). Feeds the topic-map (#1) scope paragraphs and the delta already published (scope-first #5).

**Exit criteria:**
- The (per-node) scope brief contains **material present that neither the director nor the repo supplied** — the ruling's explicit evidence bar (§2). Named, cited practitioner/market-structure content: e.g. traded-product granularity and why shape matters, storage/LNG mechanics on the gas node, EFA-period/auction microstructure on the electricity node — anchored to real published sources (Elexon/NESO/Ofgem/EPEX/ICE/National Gas), not to Board Spec 004 or the current page.
- An explicit **provenance ledger** per required scope item tagging its source as `director-list` / `repo-material` / `independent-domain` — so the *independent-domain* fraction is legible and the "transcription is not scoping" failure is falsifiable, not asserted.
- The joint test (ruling §3) is applied to at least the gas-vs-electricity split using independent-domain evidence for products, mechanisms and drivers (not the director's three bullets restated).
- R9 discipline: every claim labelled `observed-with-evidence` (fetched source) or `inferred`; no fabricated citations (a confabulated benchmark is worse than a named gap).

**Deps:** informs / co-produced with #1 (topic map) — the independence evidence IS the scope-paragraph content for #1's nodes; can run in parallel with #1. Does not block #3/#4.
**Coverage note:** the scope-first ruling's deliverable #1 (`docs/design/KPILOT_SCOPE_BRIEF_PRICE_FORMATION.md`, DONE) was "blind-ish" but ANCHORED ON Board Spec 004 — a repo-supplied instrument. That satisfied the first ruling's bar but NOT this one's sharpened §2 bar ("material neither director nor repo supplied"). This deliverable is the new independence requirement; NOT re-minted, but the existing brief is the baseline the independent material must exceed.
**Propose-then-proceed window:** proceed by default (ruling tagged **proceed**; doc-only research, reversible). Walls untouched. Network probe (curl) before declaring drained-pending-network.

## Deliverable (verbatim)
> 2. Evidence the scope work was independent — material present that neither the director nor the repo supplied.

Ruling §2: *"A scope brief asks: what must a complete treatment of this topic contain, for a reader who needs to understand it? — answered from the domain, from practitioner expectation, from the structure of the market itself. Evidence that this was done properly: the brief contains material neither the director nor the repo supplied."*
