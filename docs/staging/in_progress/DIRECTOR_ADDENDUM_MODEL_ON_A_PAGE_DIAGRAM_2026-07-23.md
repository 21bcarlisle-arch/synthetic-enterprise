# [DIRECTOR-DECISION → publish] — MODEL-ON-A-PAGE DIAGRAM, ADD TO SITE (2026-07-23)

**Type:** Director decision via advisor bridge — routine publish authority (R16-valid per the phone-native authority model; this is site content, not a reserved/safety/authz action). Addendum to `DIRECTOR_CANON_MODEL_ON_A_PAGE_2026-07-23.md` (adopted a9b73dda3) and to open finding **F-MOAP-1**.

**Priority: P3, product queue (SITE_V5 surface-1 iteration). Explicitly BEHIND the live publish-gate wedge (rc=1 @3d6820900 lineage). Do not consume this ahead of unwedging — the wedge is the crisis; this is content.**

---

## What this is

A director-approved visual companion to `docs/design/THE_MODEL_ON_A_PAGE.md` — the whole model as one diagram, iterated four rounds with the director today. Asset staged at:

`docs/staging/assets/poesys_model_on_a_page_v4.svg`
SHA-256: `be3c407044fc28799a1419a415ca55bb915493bd3e7c9c5b81d74b0049f7cd0f`

Structure: governance strip (one human — the director — the rest AI) → the world (physics + drawn population + ghosted `~ not yet` competitors) → "why we don't need millions" (value-not-volume; repetition-not-scale, using the ratified coverage knees ~200 homes / ~12 segments) → the WALL as a two-way interface (IN: market feeds, forecasts ± error, meter reads, payments, complaints & replies, settlement (late); OUT: bills, offers, messages) → the company loop with serve & converse as an always-on layer → the score (SURVIVE / EARN / ABATE) → the two timeframes with glyph grammar (`.` `_` `~`).

## Decisions already made (transmit as decisions, do not re-open)

1. **The wall is two-way, and bills are OUTBOUND.** Director ruling today. This corrects the canon doc's own wall sentence, which lists "bills" among what the company *sees*. Register as a finding on `THE_MODEL_ON_A_PAGE.md` (suggest F-MOAP-2) and amend that sentence when touched: the inbound observable is the payment/complaint that answers a bill, never the bill itself. Settlement (late) is added as a first-class inbound gate.
2. **Serve & converse is depicted always-on**, not a loop stage — a deliberate, director-approved deviation from the spine's linear "collects → serves and converses → settles" wording. Not a claim-status defect; conversations run through sales, queries, care and collections.
3. **Governance framing:** one human (director: mission, ratification, Tier-1), everything else AI (board stances, builders, harness). Harness named a product in its own right.
4. **Claim-status discipline inside the artwork:** competitors are ghosted `~ not yet`; carbon ledger carries `~` in the NOW column; TF2 items all carry `~`. One borderline phrase is delegated to you: the repetition chip says "lives rerun across worlds" as a design principle — if the claim-status rubric reads that as present-tense TF2, soften to "designed to rerun". Your call within the reading rule; do not escalate.

## Open — your design call

- **Host/placement rides F-MOAP-1.** The director has seen your default (front-door fold on the next surface-1 iteration, no resurrected /simplified door) and has not overridden it. Treat the diagram as part of that single lay-content host — not a new door, not duplicated across surfaces.
- **Implementation is free.** The SVG is the content-and-layout reference, not a pixel mandate. Embed it as-is, or rebuild it as a responsive native section per `BRAND_CONSTITUTION.md` (likely better on mobile — the SVG is a fixed 880-wide portrait). Colour stays information: BRAG semantics and the glyph grammar as used in the asset.
- Alt text / accessibility for whatever form ships.

## Risk

- **Touches:** site surface content only; no sim/company/saas code.
- **Blast radius:** the front-door surface (currently open, axis-1 verdict FAIL 1/5, iterating vs rubric) + the claim-status gate.
- **Probable failure modes:** (a) claim-status rubric flags the repetition chip or the competitors ghost — mitigation inline above (soften wording; ghost is already `~`-marked); (b) lay-content duplication against the F-MOAP-1 host decision — mitigation: one host, the diagram lives inside it.
- **Sequencing risk:** consuming this while the publish gate is wedged burns a tick on unpublishable content — mitigation: the priority line at the top of this doc.

## Verification ask

R11 to the rendered pixel on the live site (mobile viewport included), then NTFY when live. If you rebuild rather than embed, state which in the NTFY.

— Advisor bridge, carrying the director's "add to website" decision. 2026-07-23, ~20:30 BST.
