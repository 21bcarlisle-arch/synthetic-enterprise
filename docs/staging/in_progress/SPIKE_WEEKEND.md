# SPIKE_WEEKEND — maximum verified throughput until Monday reset (P1, time-boxed)

**Staged:** 2026-07-11 by advisor, director-decided. **Window:** now → Monday
03:59 (weekly reset). Afterwards return to a raised-but-normal pace (see
"after"). **Law A applies to us too:** the target is VERIFIED PROGRESS
(map cells moved, findings adjudicated, charters landed), not token burn —
tokens are simply unconstrained (14% used) and should never be the reason to
defer work this window.

## Rules of the spike (quality gates DO NOT relax)
- Single writer to main unchanged: one BUILD lane. Parallelism comes from
  native parallel subagents (research/analysis/evaluation), the background
  lane, and PR-proposing side sessions — never concurrent pushes to main.
- Every claim: pixel rule. Every phase close: fresh-context evaluator; a
  rising NEEDS_WORK rate is the spike's BRAKE — if quality slips, slow down.
- NTFY discipline protects the director's weekend: bounded-silence suspended
  in favour of a **twice-daily digest** (morning/evening: cells moved,
  findings, blockers) + immediate ntfy ONLY for [ACTION NEEDED] and Tier-1.
  Consolidated review pack for the director Monday morning.
- Escalation test stands: touches Rich's real world → ask; else flag-and-go.

## Priority order for the window
1. **MARGIN_REALISM steps 4-5** (hedge-tariff alignment; binding cap) with
   bridge attribution — closes the margin programme; director Expert Hour is
   its close, queue that for the Monday pack.
2. **Sanity follow-ups**: finish adjudications; skeptic memory; daily-digest
   alert mode proven.
3. **Epoch-2 foundations continue** (bitemporal log, as-of views, snapshot
   object integration) — bounded scope until the advisor's epoch framing
   arrives TODAY; when it does, its sequencing takes over this slot.
4. **DISCOVER/FRAME flood (parallel, background)**: charters for every lane
   at dial ≥2 that lacks one (B, C, E, W2, W3, A, G) — best-practice
   distillation, sub-capability trees, simplifications registers seeded.
   Subagent-parallel; output is documents; zero collision risk.
5. **HARDEN sweep (parallel, background)**: run the Expert-Hour simulation
   per company lane against the LIVE site — veteran-skeptic walk + benchmark
   vs the platform survey — findings into the adjudication ledger. This
   converts spare capacity into the exact review work that otherwise waits
   on the director.
6. Bill-shock redesign and any map-drawn work per dials as capacity allows.

## After the spike (standing change)
Default pace rises: parallel subagents become standard practice, background
lane permanently hot, DISCOVER/HARDEN always running for dial≥2 lanes. The
spike is the stress test of that operating mode; report what broke.

## DoD
Monday-morning consolidated pack: cells moved (before/after map snapshot),
charters landed, adjudication verdicts, evaluator NEEDS_WORK rate, what broke
under load, and the re-derived margin surfaces awaiting the director's Expert
Hour. Pixel rule on every claim in it.
