> **STATUS 2026-08-10 (worker tick) — PARTIALLY EXECUTED, parked here because the drain is not finished.**
> **Blocking sub-item: the root is at 79 non-marker files (down from 98), not <20, so the product interleave has NOT armed.**
> What unblocks it: continued triage of Groups C, D and the E remainder. Receipts for what has been
> dispositioned so far: `docs/staging/done/TRIAGE_RECEIPTS_2026-08-10.md`.
>
> - **Group A — DONE (15 files).** Moved to `docs/design/refs/` with an index (`refs/README.md`).
>   Both "verify the atom exists; mint if not" rows were verified and BOTH were missing:
>   minted `AO12_scale_probe_10k` and `A9_market_at_the_seams_design_law`.
> - **Group B — DONE for the unambiguous set (17 files, incl. `DIRECTOR_PRIORITY_PUBLISH_FIRST`
>   from `in_progress/`).** Five findings this page lists as fix-landed were read first and their own
>   status lines say QUEUED / class-open, so this page's own override sends them to Group D instead;
>   they are named in the receipts.
> - **Group C — STARTED.** `DIRECTOR_NOTE_SUSPECT_LIST` consumed (ratified mint landed as
>   `H42_wedge_suspect_list_rederived_from_the_red`). `DIRECTOR_NOTE_UNBLOCK_CHARTS` consumed (the
>   charts mint is unblocked, and the abolished-block receipt is recorded on the mint doc).
>   The rest of Group C is OPEN.
> - **Group D — OPEN.** No finding-atoms minted yet beyond the three above.
> - **Group E — OPEN.**

# [DIRECTOR-PRIORITY] — Backlog triage: the whole root, classified; drain to <20 then auto-arm the product interleave (2026-08-10)

**Type:** [PRIORITY — director decision 2C+3C]. This page IS the drain: execute it above feature draws until staging root < 20 files. Each disposition = one-line receipt in the tick digest. **When root < 20: the product interleave ARMS automatically** — one world/customer/product atom drawn per harness atom thereafter (director 3C, no further instruction needed). Where a row says worker-judges, the group rule decides; nothing here overrides a finding's own status line if it says its fix already landed.

## Group A — ARCHIVE AS CANON (move to docs/market_research/ or docs/design/refs/, index, done)
All eight ADVISOR_SCOPE_BRIEF_* (batteries already live in the AO8 registry — the prose is reference now) · ADVISOR_RESEARCH_PRE_RESEARCH_POINTERS · ADVISOR_RESEARCH_COUNTERPARTY_APIS_EPOCH3 · ADVISOR_RESEARCH_CREDIT_BALANCES · ADVISOR_REVIEW_DATA_ARCHITECTURE_AND_SCALE_PROBE (verify the 10k-probe atom exists; mint if not) · ADVISOR_REVIEW_MATURITY_MAP_TAXONOMY · ADVISOR_NOTE_CCM_PROVENANCE (historical) · ADVISOR_ANALYSIS_MARKET_PORTABILITY (+ verify `market`-at-the-seams has its design-law atom; mint if not).

## Group B — ANSWERED / SUPERSEDED (archive with a one-line receipt naming what answered them)
ADVISOR_FINDINGS_MISSING_TEST_TIER and STRUCTURAL_AUDIT (both corrected by AO7's import-measured audit) · ADVISOR_FLAG_SEAT_LIVENESS_WATCHDOG (the dead-man + PW1 observed-population ARE it) · DIRECTOR_PRIORITY_PUBLISH_FIRST, SIM_RUNNER_TRIAGE, EPISODE4, UNWEDGE_AND_ALARM_TEETH (all draws done) · DIRECTOR_RULING_HOOK_BYPASS pair + PUBLISH_GATE_SUBJECT (landed; policy lives in canon) · DIRECTOR_TASK_NO_CALLER census (answered) · WORKER_REPLY (answered in-place) · every WORKER_REPORT_*WEDGE* and WORKER_FINDING whose own text records its fix landed (episode-memory, second-wedge-cause, ruff-reds, oom-misclass, relative-hook-paths, field-swallow if the gate fix covers it — per each doc's status line).

## Group C — CONSUME AS LIVE INSTRUCTION (act, then in_progress/done)
DIRECTOR_RULING_PUBLISH_DECOUPLING (4-item WORK block — the site-breathes build) · DIRECTOR_RULING_FUTURE_COMMITMENT_SETS (verify E2–E5 atoms + attach-hooks all exist per its block; close the gaps) · DIRECTOR_RULING_INFRASTRUCTURE_POSTURE (Qwen retirement re-pointing + bootstrap/acquisition/cutover B-atoms — the llama 6.4GB prize) · ADVISOR_PROPOSAL_SEAT_CUTOVER_AND_DR (fold into the same OPS set) · ADVISOR_FLAG_RESOURCE_HEADROOM_GOVERNOR (OOMs ongoing — high) · ADVISOR_RETRO_FAILURE_MODES (verify Birth-Certificate law mechanisation; mint residue) · DIRECTOR_NOTE_SUSPECT_LIST (ratified mint) · ADVISOR_FINDINGS_MONEY_CORE (verify the two named defects' fix status; mint what remains) · ADVISOR_FINDINGS_CLAUDE_MD_DECAY (trim landed; the gate-blind-to-own-edits half — verify/mint) · in_progress trio: FORK_BRANCH_TRIAGE (mint one 16-branch cleanup atom), MISPARKED_BUILD_QUEUE, PLANNER reversibility.

## Group D — MINT THEN ARCHIVE (findings naming un-built mechanisms; one atom each, correct lane)
THE_NAIVE_ARM_KEEPS_THE_LIVE_TONE (published-figure defect — product lane) · THE_MODELS_STORAGE_HEATER_IS_NOT_ONE (W-lane) · THE_SECOND_DIRECTION_NEEDS_ITS_OWN_POPULATION · HEATING_REGIME_CONDITIONING_IS_BINARY · MUTATION_VALID_ON_ONE_SUBPOPULATION · WORST_OF_N_NOT_SCALE_INVARIANT · EVER_FLAGGED_BLIND_TO_UN_KNOWING · DETECTION_RESIDUAL_IS_MISALLOCATION · DERIVED_ARTEFACT_STALENESS (verify the class fix covers; else mint) · TWO_NUMBERS_ONE_NAME (naming-law sweep atom) · TWO_UNIMPORTABLE_PHASE2A_MODULES · PYC_CACHE mutation-harness class · RENAME blindness pair · TEST_NAMED_FOR_AN_ASPECT · PRE_COMMIT_GATE_MAPS_NO_TESTS_TO_A_DATA_FILE · A_LEVEL_CAN_BE_DECLARED / DETACH_NOT_IN_REPO (gate landed — archive-answered) · KNIFE2_ORPHANED (adopted — archive) · remaining 2026-08-10 findings by the same rule: fix-landed → B; mechanism-owed → mint here.

## Group E — WORKER-JUDGES remainder
Anything unlisted: classify by the group rules above; when genuinely uncertain, one-line question to the channel rather than silent parking.

— Director 2C/3C via the advisor; the drain is the priority, the receipts are the audit, and the interleave arms itself at <20.
