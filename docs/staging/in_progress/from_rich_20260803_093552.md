<!-- PARTIALLY ACTIONED 2026-08-03 (worker tick) -- PARKED IN in_progress/ BECAUSE THE BUILD HALF IS LIVE.
  CHECK HALF: DONE and pushed -- docs/design/ITEM_E_WEATHER_AGGREGATION_CHECK_2026-08-03.md, commit 94170e9b9,
  answered to the director by NTFY citing that SHA. Result: 3 of the 4 things asked about are real gaps, and
  the per-premise averaging suspicion is CONFIRMED (fabric_physics.py:105 DEFAULT_LATITUDE_DEG=53.0 with no
  production caller ever passing a real location; no tilt/azimuth transposition, so no per-orientation sun).
  A correction was recorded rather than worked around: PREMISE_FABRIC_PHYSICS_DISCOVER.md item E claims
  population-weighted temperature is "substantially built" -- it is not; that claim conflated demand-weighted
  DEMAND with weighted TEMPERATURE.
  BUILD HALF: three forks dispatched on disjoint file_scopes -- (1) cumulative HDD windows in sim/weather_hdd.py,
  (2) a new sim/weather_weighting.py for population-weighted temperature + capacity-weighted wind with real
  sourced weights, (3) local latitude + per-orientation sun in simulation/fabric_physics.py.
  UNBLOCKS/ARCHIVES WHEN: all three forks have come home (merged to main through the pre-commit gate, or reaped
  with their work preserved) and their levels are recorded. Do NOT archive this file before then -- the build
  half is the substance of the instruction, and the check alone does not discharge it. -->

<!-- Dispatcher: NORMAL (classified 2026-08-03 09:36 UTC) -->
# Inbound NTFY message from Rich

Item E from the fabric doc is still open. Do the check now: what does the weather engine already provide for population-weighted temperature, capacity-weighted wind and cumulative heating-degree windows, and what's missing? Then build the gaps. Also confirm the per-premise physics is getting genuinely local weather and per-orientation sun, not a national average — if it isn't, that's the averaging fault back one layer up.
