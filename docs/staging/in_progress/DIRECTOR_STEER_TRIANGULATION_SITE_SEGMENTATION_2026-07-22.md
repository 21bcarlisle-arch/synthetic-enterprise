> **PARKED in in_progress/ 2026-07-22 (agent).** DISCOVER→FRAME done, doc-only:
> `docs/design/TRIANGULATION_SITE_SEGMENTATION_FRAME.md` — reconcile-against canon fixed for both
> specs, the axis-1 scoring-rubric structure and the coverage-matrix structure proposed, the
> learning-value-leak elicitation guard recorded, 3 candidate atoms named. **Genuinely-open sub-item
> blocking closure:** board Specs 005 and 006 have NOT landed (commissioned blind, in parallel) — the
> line-by-line reconciliation, the populated axis-1 rubric, the coverage matrix, and the
> decision-linkage metric-amendment decision all wait on their arrival. Unblocks when either board
> spec lands in staging.

# DIRECTOR STEER — Triangulation extended: the website (Spec 005) and segmentation (Spec 006) (2026-07-22)

**Type:** [STEER] — two further blind board specifications are being commissioned. Same triangulation discipline as Specs 001–004. Mechanism yours.

## Spec 005 — The website (showcase + director's window)

The board will specify, blind to the build, what a site must be when it is simultaneously (a) the public showcase of an autonomous-supplier experiment and (b) the sole operating window for its single human director. When it lands:

- Reconcile line-by-line as before — but with one addition: **the board's expectations become the director's standing scoring rubric for axis 1.** His one-line verdicts ("site n/5 — reason") will reference it. Structure the reconciliation so each expectation is individually scoreable and trackable over time.
- Existing canon to reconcile, not fork: POESYS_SITE_BRIEF (v4-reconciled), SITE_CONSTITUTION, BRAND_CONSTITUTION, the operational-window causal-chain structure the director ratified, and the claim-status publication discipline. Where the board's blind expectation conflicts with ratified canon, that is a finding for the director — flag it, do not silently pick a side.

## Spec 006 — Segmentation

The board is asked what segmentation should *change about how a supplier acts* — which decisions it should drive, what makes it useful versus decorative, which GB domestic segments a veteran would insist exist. When it lands:

- **Coverage test:** every segment or dimension the board names blind, check against the population-coverage schema and the value-frontier segments. A board-named segment absent from the data-derived structure (or vice versa) is a first-class finding — it either exposes a coverage gap or evidences that the data-driven route found something practitioners miss. Both are valuable; report both honestly.
- Reconcile against: the learning-value objective (do NOT leak it to the board via any channel), the ~12-segment knee, the protected-groups floor, the wall discipline (discoverable-through-observables), and D-SEGMENT/coverage canon.
- The board's "useful vs decorative" test should be evaluated as a candidate addition to the learning-value metric itself — a segment that changes no decision has no learning value; if their framing sharpens ours, propose the amendment.

## Process (unchanged)

Primary/literature DISCOVER need not wait on the board. Each spec's disqualification battery joins the standing practitioner fidelity oracle. Findings drive proposals via propose-then-proceed; no silent scope changes. Reconciliations written for practitioners, carried to Movement 3.

**Risk & proportionality:** doc-and-analysis, reversible; any site rebuild or schema change the reconciliations imply routes through existing gates (site = director's Expert-Hour; generator = reserved). Tag: **narrow/reversible.**

— Advisor, carrying the director's direction, 2026-07-22.
