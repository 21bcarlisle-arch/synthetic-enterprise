<!-- SUPERVISOR_DRAW: self-drawable -->

# [PLANNER-MINTED] Wholesale-domain topic map — nodes, typed edges, per-node scope paragraphs (gas ≠ electricity) (2026-07-28)

**Source ruling:** `DIRECTOR_RULING_KPILOT_DECOMPOSITION_2026-07-28.md` — deliverable #1 (WORK THIS CREATES); ruling §3, §4, §5.1. Second K-pilot verdict; SUPERSEDES the sequencing of the scope-first ruling (`d557a58e3`) but keeps its §6-Unchanged mechanics.
**Serves:** DIRECTOR_AXES Axis 1 (Website) + Axis 3 (Believability). Campaign: SITE_MODEL_SPINE knowledge lane. Fidelity-ledger row: price-formation (W1_6) — but the deliverable is now the DOMAIN GRAPH, not the single page.
**Real-world fidelity gained:** the knowledge graph holds the domain's REAL joints, so a conflated "gas+electricity in one page" surface becomes structurally impossible — the topics are separate objects with separate scopes and separate briefs (ruling §4). A reader navigating the graph sees GB wholesale as the market actually decomposes: separate gas and electricity topics, coupled by a typed `drives` edge, never merged.

**Lane:** FRAME / DISCOVER (doc-only design of the graph structure; no page rewrite here — that is #3/#4). Output is a topic-map artefact under `docs/design/` and/or the machine-readable graph the sidebar generator reads.
**Target level:** doc-only design artefact (no maturity-map level claimed). The graph it decides then drives #3 (node to depth) and #4 (stubs + re-home).

**Exit criteria:**
- A wholesale-domain topic map exists listing **nodes**, **typed edges**, and a **one-paragraph scope for each node** stating what a *complete treatment of that node* contains (ruling §5.1).
- **Gas wholesale and electricity wholesale are SEPARATE nodes** (ruling §3): they differ in products (NBP/TTF, storage/LNG/within-day→annual vs baseload/peak blocks, EFA periods, day-ahead auctions), mechanisms (storage cycles/LNG arbitrage/pipeline flows vs merit-order stacking/marginal plant), and drivers (global LNG/European storage/heat demand vs gas price/carbon/wind/demand/interconnectors). Coupling gas→electricity is an **EDGE (`drives`), never a merger**.
- The **general joint test is stated and applied across the domain**: *do these things share products, mechanisms AND drivers?* Shared → one topic; different → separate topics with a typed edge. Every proposed node/merge is argued against this test; where the joint is judged to sit elsewhere for a pair, that is argued explicitly with the test (ruling §3 invitation).
- Reconciled against the LIVE graph: `site/knowledge/` today holds only `wholesale-price-formation` + 7 empty topic-graph pointers (gas-price, carbon-price, merit-order-residual-demand, imbalance-cashout-settlement, hedging-forward-market, price-cap, gb-electricity-market). The map states, for each existing pointer/page, which node it becomes — nothing orphaned.
- No node's scope paragraph is a gap list (ruling §1 class rule + acceptance) — each paragraph explains what the node *covers*, with named gaps deferred to the residuals rung, never the body.

**Deps:** NONE — this is the FIRST deliverable of the second ruling and BLOCKS #3 (node to depth lands on the decided graph) and #4 (stubs created for the decided nodes; existing content re-homed).
**Coverage note (which are already covered):** the scope-first ruling's deliverable #3 (`docs/design/KPILOT_PAGE_DECOMPOSITION_PROPOSAL.md`, DONE) is a **single-topic** hub/children/stubs IA for price-formation ONLY — a precursor INPUT, NOT this domain-level graph. It does not separate gas and electricity as top-level topics, nor apply the shared-products/mechanisms/drivers joint test across the domain. This deliverable is a superset; NOT re-minted, but that proposal is reused as an input for the electricity-side sub-structure.
**Propose-then-proceed window:** proceed by default (ruling tagged **proceed**; doc-only structure/re-scope, reversible, nothing deleted). Walls untouched.

## Deliverable (verbatim)
> 1. The wholesale-domain topic map: nodes, typed edges, per-node scope paragraphs, gas and electricity separate.

Ruling §4: *"The graph's job is to hold the domain's real joints. Get the structure right and a mushy conflated page becomes structurally impossible… Therefore the DECOMPOSITION is now the deliverable, not the page."*
