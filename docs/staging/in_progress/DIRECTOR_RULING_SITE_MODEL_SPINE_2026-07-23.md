# [DIRECTOR-RULING] — Organise the site around THE MODEL: words → diagram → evidence. Financials reframed. Links canonical. (2026-07-23)

**Type:** [DIRECTOR-RULING] via advisor bridge, from the director's live review of the rebuilt surfaces ("a bit better" — progress acknowledged, structure of information still wrong). Three rulings; these refine the running site campaign's remaining iterations rather than opening a new one.

## 1. The organising principle: the site's evidential core IS the model

Derive a **master diagram** from `docs/design/THE_MODEL_ON_A_PAGE.md` (the spine: world → wall → company → score; actual-vs-forecast weather; prices and demand; tariffs and hedging; three clocks to settlement; carbon). The site's grammar becomes **words → diagram → evidence**: every node and arrow of the diagram is walkable — click an arrow, get (a) the claim in one plain sentence, (b) the diagram fragment highlighted, (c) **the chart that evidences that relationship in run data**, with its as-of clock and file link.

Initial evidence set (build these charts properly, per the chart-grammar rules in the axis-1 verdict): weather→demand (HDD vs consumption relationship); weather→price (the cold-and-still decile lift, already proven at D1 2.875); demand+price→bill (a real household's year); billed/settled/banked divergence (the three clocks on one chart); hedge cover vs spot through the crisis; churn vs market (render honestly as ABSENT — the moneyness trigger is a known gap). Arrows whose instruments don't exist yet (forecast-error-by-horizon, per-customer carbon) render **instrument-pending** — the diagram doubles as the maturity map made visible, which the canon's reading rule already requires. Timeframe-2 content appears greyed/pending, never present-tense.

**The diagram itself comes back to the director as pixels for approval before it becomes the nav spine** — one [ACT] with the rendered image; his verdict may adjust layout, not the canon it derives from.

## 2. Financials reframed: per-customer, per-cell, rates — never bare totals

The director's ruling, verbatim: *"I don't take financials as useful at all. Certainly not totals from a random sample of customers."* Correct: the drawn book is a curriculum sample, not a market share; totals are artifacts of N. Every financial surface converts to: **£ per customer** (margin, cost-to-serve, arrears), **rates** (collection %, hedge cover, complaint rate), **distributions across coverage cells**, and **£/tCO₂e when instrumented** — with **N and the draw stated on every panel**. Totals may appear only as footnotes marked "scales with drawn book; not meaningful in isolation." This applies to /company, /now panel 2, and the investor summary.

## 3. Link integrity is a publish-gate test

The director hit links resolving to old versions and formats. Rule: **one canonical page per URL; legacy pages redirect; zero old-format leaks.** Add an R11 link-walk to the publish gate: every internal link on every published page resolves to a canonical current page, or the publish fails. Run it now against the live site and fix what it finds.

**Risk & proportionality:** presentation + pipeline test; no data-model changes; diagram approval gated on the director's pixels. Tag: **proceed within the running campaign; diagram [ACT] before nav adoption.**

— Advisor bridge, carrying the director's review, 2026-07-23.
