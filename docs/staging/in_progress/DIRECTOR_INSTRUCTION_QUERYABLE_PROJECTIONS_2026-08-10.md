> **[IN PROGRESS — step 1 of 3 DONE, 2026-08-11]** Parked here, not archived, because sub-items 2
> and 3 of WORK THIS CREATES are genuinely still open. Do not re-mint.
>
> - **1. Atom(s) minted — DONE** (`79c3ecd2e`): `G12_queryable_projections` (the store) and
>   `G13_projection_consumers` (the proof-of-caller), lane `G_data_learning`, epoch 2, both
>   `loop_stage: build`. G12 is drawable NOW (no `blocked_on`, no `depends_on`); G13 depends on G12.
> - **2. v1 built — OPEN.** Blocking nothing: G12 is drawable on normal priority. Unblocked by a
>   draw, not by an authority.
> - **3. Proof-of-caller — OPEN**, behind G12 by design (`depends_on`).
>
> **Read before drawing G12:** the anchor this instruction cites, `DATA_LAKE_OBSERVABILITY.md`, has
> never existed in this repo, and `SAAS_COVERAGE_MAP.md:71` classes BI/data-warehouse as bucket A
> "eliminated by architecture" — a row a PUBLISHED 22.7% figure is computed from. Evidence and the
> recommended fork: `docs/staging/WORKER_FINDING_A_MINT_SOURCES_CITED_DESIGN_ANCHOR_HAS_NEVER_EXISTED_2026-08-11.md`.
> The instruction's own text is the anchor of record; that reconciliation is G12's DISCOVER/FRAME.

