**Severity:** RECORDED · **Lane:** H_harness · **Epoch:** unassigned · **Atom:** `unminted`

# RESULT — the dominant ungradable cause was itself a mixed class, and the debt is 26 of 28, not "far smaller than 27"

Answers `SEAT_PREREG_WHICH_CAUSE_DOMINATES_THE_UNGRADABLE_LEVEL_ZERO_ROWS_2026-09-25.md`.
**Four predictions confirmed, five refuted.** The defect is filed separately as
`docs/staging/SEAT_FINDING_FOURTEEN_OF_THE_SIXTEEN_ROWS_TOLD_THEIR_CONTROL_WAS_NEVER_WRITTEN_HAD_NO_ABSENT_PATH_AT_ALL_2026-09-25.md`.

Measured at `208195dd4`, over `tools/level_zero_contradicted_by_its_own_controls.ungradable_causes`
against the live map. **Zero pytest runs were spent**, as pre-registered: the classifier is a pure
function of the row, the disk and `git`, and the cause of any row that reaches the expensive pass is
`NOTHING_IN_THE_ROW` by construction. The 1078s/2.44GB row was not re-run.

## The distribution, as published before the repair — the answer to the item's question

| cause | rows | owes a repair |
|---|---|---|
| `CONTROL_NEVER_WRITTEN` | **16** | yes |
| `NOTHING_IN_THE_ROW` | 6 | yes (re-run the row, do not edit it) |
| `NAMES_ONLY_A_SCOPE` | 4 | yes |
| `POINTER_ROT` | 2 | yes |
| `HONESTLY_UNBUILT` | 2 | **no** |
| `CAUSE_UNDECIDABLE` | 0 | — |

Population **28**, sum of causes **30** (two rows carry two:
`C_supply_start_consumer_routing` and `SITE3_wall_exhibit_url_rename`, both `POINTER_ROT` +
a control cause). **26 of 28 rows owe a repair.**

## And the same distribution after it — the sub-split is the finding

| cause | rows |
|---|---|
| `CONTROL_UNNAMED` *(new)* | **10** |
| `NAMES_ONLY_A_SCOPE` | 8 |
| `NOTHING_IN_THE_ROW` | 6 |
| `POINTER_ROT` | 2 |
| `HONESTLY_UNBUILT` | 2 |
| `CONTROL_NEVER_WRITTEN` | 2 |

Same 28 rows, same 30 causes, same 26 owing a repair. What changed is that **14 rows are no longer
told something nothing measured**, and the six rows whose evidence is a directory now get the
repair for that (`name the FILES`) instead of the one for a control nobody wrote.

`CONTROL_NEVER_WRITTEN` keeps exactly its two honest members: `PB5_pounds_or_percent_resolved` and
`A51_the_plain_english_report_on_the_use_case_register_reaches_the_director`.

**No new surface was built to publish this.** `background/delivery_seat._by_cause` already groups the
ungradable set by cause into the orientation brief and takes its keys from whatever the producer
returns, so the new class reaches the reader without a second grouping — and a second spelling of one
grouping is the shape that drifts. What was missing was never a publisher; it was that the biggest
heading meant two different things.

## Predictions, scored beside themselves

* **P1 — population 28.** CONFIRMED, exactly. 110 live atoms, 28 candidates.
* **P2 — `CONTROL_NEVER_WRITTEN` dominant, ≥ 9.** CONFIRMED at 16 — and confirmed on the wrong
  half. The class being dominant was the premise; the class being a *mixture* is what mattered, and
  I did not predict it. The prediction that was right told me nothing the item had not said.
* **P3 — `HONESTLY_UNBUILT` second, 4–10.** **REFUTED.** It is 2, joint smallest. I reasoned from
  "a level-0 row names a plan", and that is not what these rows do: they name subject modules that
  are ON DISK and no control.
* **P4 — real debt strictly smaller than 27, ≥ 4 rows removed.** **REFUTED in magnitude.** Two rows
  owe no repair, so the debt is 26 of 28. The item's hope — *"the real debt may be far smaller than
  27 and cannot be told from it"* — is refuted: it can be told from it now, and it is 26.
* **P5 — exactly ONE cause owes no repair, and the item is wrong to say two.** CONFIRMED.
  `CAUSES_OWING_NO_REPAIR` is `{HONESTLY_UNBUILT}`. Had the item's framing been taken on trust, the
  scope rows would have been written off.
* **P6 — ≥ 1 row with several causes; the sum exceeds the row count.** CONFIRMED. 2 rows, 30 > 28.
* **P7 — `NOTHING_IN_THE_ROW` between 2 and 5.** **REFUTED**, it is 6. All five rows the module's
  docstring measured on 2026-09-16 (D27, D9, KNIFE3, H41, W2_31) are still there, plus `A50`. I
  predicted it had shrunk because two of the five are documented under other shapes; they are not —
  a row can be documented as one shape in prose and classify as another, which is the same
  prose-vs-code gap this whole instrument exists for.
* **P8 — the census does not reproduce in this isolated worktree.** CONFIRMED ON THE MECHANISM,
  NOT ON THE COUNT. `docs/observability/.head_red_observed.json` is present in
  `/home/rich/synthetic-enterprise` and **absent** in `/var/tmp/se-seat-executor`, so `reds_at_head`
  fails closed here and `KNIFE3` cannot be silenced. I did **not** run the pass to see 28 rather
  than 27 — that run is the one this work exists to avoid spending, and I am not claiming a number
  I did not measure.
* **P9 — the repair is a map edit paid for against the ratchet.** **REFUTED.** The repair is in the
  classifier. No map bytes were spent and nothing had to be drained.

## The prediction I should have made and did not

The item's stated WHY — *"nothing has COUNTED the distribution"* — was checkable before any work,
and it is false: counted 2026-09-16 over 31 rows, and again in a sibling prereg on 2026-09-25 which
names the 13 `CONTROL_NEVER_WRITTEN` rows and the four `NAMES_ONLY_A_SCOPE` rows by ID. I
pre-registered nine predictions about the answer and none about whether the question had already
been answered. **Every claim a drawn item makes about the tree is an un-re-asked prediction**, and
the item's WHY is one of them.
