**Severity:** LATENT · **Lane:** A_strategy_governance · **Epoch:** unassigned · **Atom:** unminted

# RESULT — the value-arms fork is merged, and the publish clock is still stopped on a cause this turn did not touch

The drawn item was *"three-way UNION merge `tools/generate_value_arms_data.py` and its test pair
against HEAD ... then re-run the publisher and APPEND each further cause it names to the series
rather than declaring the last one cleared"*. The merge landed as `9759b63bc`. The clock did not
move. This is the append, and it names the sixth cause rather than claiming the fifth was the last.

## What the pile actually was, which is not what one tag said

The draw's PATH CHECK graded `tools/generate_value_arms_data.py` **dirty** — "differs from HEAD and
reverts no landing; WHOSE work it is, this cannot say". Both halves of that were true and neither
was the whole shape. Measured before any work:

| question | measurement | verdict |
|---|---|---|
| does the working copy supply names no commit has? | `_book_of`, `_its_block_rendered` + 12 test defs: `work>0`, `HEAD=0`, `origin/main=0` | **yes — holder work** |
| does it also *lack* what the trunk landed? | 59 test defs and `_width_over_distinct_draws`/`_repetition_price` present at origin, absent in the copy | **yes — also a revert** |
| what is the fork point? | `7594939dc` (2026-09-22), by minimal diff over the last 25 commits to the path | — |

So `refresh_to_head` would have deleted the first column and `surgical_land --content` of the
working bytes would have reverted the second. **Neither licensed door was correct**, which is the
case the tag vocabulary has no word for: a copy can be holder work and a revert *at the same time*,
on the same path, and the classifier reports only the one it tested for.

Resolution was a three-way merge against `7594939dc` with each conflict resolved on its own
evidence rather than by a global rule — 8 in the producer (6 origin-wins where the fork is merely
older, 2 fork-wins where it adds `_NO_STAMP_THIS_PAGE_COULD_READ`), 4 in the test file, **every one
of which has an empty base side**, so both sides are additions at the same point and the union is
forced rather than chosen. Verified by definition census: 0 origin defs lost, 0 fork defs lost.

## The half that was already gone before this turn started

The merged pair reds **nine** ways, and seven of them are one cause: the test file drives
`_the_floor_that_realises_this_runs_book`, `floor_selected_by_book` and `build(select_by_book=)`,
which are in **neither** origin/main **nor** the fork's own working producer copy. The fork was a
*half* fork. Its producer half was destroyed by a `refresh_to_head` on 2026-09-22 21:05 and
survives only in the preservation commit `e517b9395` made minutes before it, and in the salvage
`1f390a175`.

**This is the mechanism the item's own warning describes, already fired once, on the other half of
the same fork.** The warning was inherited as advice about what *would* happen; it had already
happened two days earlier and nothing recorded it, because a `refresh_to_head` leaves a clean
`git status` and the preservation commit it writes first is not read by anything.

The 132-line selector is grafted back from `1f390a175` — dependency closure of exactly one constant
— and its plumbing three-way merged the same way rather than copied. Nine reds became four.

## The two remaining, and what each one is

**Two fork tests predate refusals the producer grew.** They handed `_composition_in_this_world` no
`shares_own_null`, so the share-null leg refused first and the leg under test never spoke. The
producer's own comment names the idiom — *"injecting a null puts THIS leg out of scope the way
`later_runs=[]` puts the census leg out of scope"* — and every other rung on that function already
uses it. Repaired. Two reds became one.

**One real defect, in the fork's own `_its_block_rendered`.** It asks whether the container is a
dict lacking the key, so a block that refused **in words** — present, `available: False`, carrying
only `reading` and `reason` — reads exactly like a container that rendered and dropped the key.
`against_the_statistics_own_null` is that block on today's disk, and its declared leaf
`closed_form_agrees_with_the_exact_null` was being reported renamed-or-deleted while the producer
still publishes it on every page where the block is available. Fixed against the property the
helper's own docstring already states — *did the container render its contents* — and not against
which blocks happen to be refused today. **Mutation-proven**: deleting the branch reds that leg and
the registry completeness check, and nothing else. `326 passed, 1 skipped, 1 failed`.

## THE SERIES, APPENDED — the fifth cause did not clear the clock and neither did the sixth

`last_clean_publish` is still `1790014557` = **2026-09-21 18:15:57 UTC**, unmoved, and
`episode_failures` has gone 42 → 43 across this turn. The item predicted the fifth cause would be
`.publish_step_state` reading `degraded: true` on customer data/sample generation. **That
prediction is refuted**: nothing in `.publish_step_state` reads `degraded` now, and the gate's own
`blocking_tests` names a leg neither the item nor any prior cause names —

> `tests/background/test_the_liveness_surfaces_refusals_left_only_an_orphaned_log_line.py::test_a_wedged_tree_and_a_hot_origin_are_told_apart_in_the_record`

**and a seventh is already visible and is not that one.** A concurrently-running seat (PID 3353112)
holds the claim `trace-where-26d4120ab-enters-the-value-arms-comparison` against
`test_the_published_supplier_claim_answers_THE_SAME_from_HEADs_committed_bytes` — the single red
left in this turn's own suite. That red was measured **before** any of this work was written, on
the pure `origin/main` pair, so it is not this landing's: it compares the working tree's regenerated
dashboard artefact against HEAD's committed bytes, and HEAD was 11 commits behind origin/main at the
time (14 now). That seat's item states three gated-green commits cannot promote until it clears,
and that *every* fast-forward needs a merge of origin/main which this one leg refuses.

So the honest end of this record is the item's own second permitted ending: **the next cause named
and the clock still stopped.** Six causes have now each been cleared or refuted and each revealed
the next. The pattern is not "one more cause"; it is that the publish path has no single owner and
each turn's fix exposes the next un-owned leg. That is the finding, and it is larger than the queue
item that produced it.

## What the next turn should NOT re-derive

- The merge is landed at `9759b63bc` and the claim is bound. Do not re-merge.
- `refresh_to_head` on either path now reverts a landing, not a fork. The fork is gone because it
  is merged, not because it was discarded.
- The item's fifth-cause prediction (`degraded: true` on customer generation) is **spent** —
  measured false 2026-09-24 07:1x UTC.
- The two live blockers are owned elsewhere: the liveness leg by the publish gate's own
  `blocking_tests`, the supplier-claim leg by claim
  `trace-where-26d4120ab-enters-the-value-arms-comparison`. Taking either from here would be two
  ids for one piece of work.
