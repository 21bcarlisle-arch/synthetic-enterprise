**Severity:** INFORMATIONAL · **Lane:** H_harness · **Epoch:** unassigned · **Atom:** `unminted`

# Pre-registration: how far the 28 ungradable rows can be discharged, and what stops the rest

Written BEFORE any census of the 28 rows beyond the two I had already opened by hand
(`SITE3_wall_exhibit_url_rename` and `C_supply_start_consumer_routing`). Drawn as LANE 0 DELIVERY,
claim `discharge-the-28-ungradable-level-rows-against-the-causes-the-tool-already-prints`.
Answers the item that followed `SEAT_FINDING_THE_MAPS_SILENCE_IS_UNFALSIFIABLE...2026-09-25.md`.

The number to move is `graded`, which reads **0 of 28** at `54a7b6242`. The instrument is
`tools/level_zero_contradicted_by_its_own_controls.py --json`.

## What the item assumes, said plainly so it can be refuted

The item's premise is that the 28 rows are *repairable rows*: it names three classes — a
`file_scope` naming a directory, a named control never written, and a rotted pointer — and says to
"repair the ROW where the tool says the row is wrong". That frames every ungradable row as either a
map defect or a pass defect.

**P1. The item's frame is incomplete: at least one row is neither.** I predict there is a row whose
`file_scope` is *correct about the tree it was written against* and whose subject was later
**abolished by a later decision** — not moved, not mislaid, not unbuilt. The tool cannot express
this state: `_path_known_to_git` asks only "did this path ever exist on any ref", and any `True`
becomes `POINTER_ROT`, whose printed repair is "repoint `file_scope` at where the path lives now".
For an abolished subject there is no *now* to repoint at, so the repair is unfollowable and the
reader either invents a target or gives up. (I have already opened `SITE3` far enough to believe
this; I am registering it as a prediction about the CLASS, whose size I have not measured.)

**P2. I predict the abolished-subject class is 2 or 3 rows of the 28**, and that its members are
concentrated in rows naming `site/` paths, because `03dd8c49e` (2026-08-20) deleted eleven pages in
one commit under a director ruling.

**P3. I predict `graded` can be moved OFF ZERO this turn, but not far** — my point estimate is
**1 to 3 rows graded**, and I expect the binding constraint to be that most rows are honestly
unbuilt rather than mis-pointed. Specifically I predict the 13 rows carrying `CONTROL_NEVER_WRITTEN`
cannot be discharged by editing the map at all, because the tool's own repair line for that cause
says so ("write the named control and prove it can fail; until then the row is ungradable and that
is the honest reading, not a defect").

**P4. I predict at least one row, once made gradable, comes back CONTRADICTED** — i.e. the repair
turns a silent row into a refusal. That is the outcome the instrument exists for and it is the one
that would establish cause A is false for that row.

**P5. I predict the four `NAMES_ONLY_A_SCOPE` rows (G4, SP2_2, H40, H48) CANNOT be discharged by
naming files**, because naming a file the atom has not written yet is a prediction dressed as a
declaration — the row would then read `HONESTLY_UNBUILT`, which is a more honest classification but
still not `graded`. If I am right, the directory→files repair the item ranks as "the largest class"
moves the number by ZERO.

## What done means for this turn, decided in advance

This is direction, not an atom, so I am fixing the exit here rather than discovering it afterwards:

1. `graded` is **> 0**, with the run that produced it recorded; **or** a stated, evidenced reason
   why it cannot be, filed as a finding.
2. Every row I touch is repaired **for the reason the tool names**, never to move the number. A row
   I cannot honestly repair is left alone and said so.
3. Any state the tool cannot express is filed as a finding against the TOOL, with a control that
   can fail — not worked around by hand in the map.

## The refutation I would accept

If, after the repairs, `graded` is still 0 and no row of the 28 is in a state the tool cannot
express, then P1, P3 and P4 are all wrong, the item's three-class frame was complete, and the
honest reading is that the 28 rows are simply unbuilt work — cause A, established.
