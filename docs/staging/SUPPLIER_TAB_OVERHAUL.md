[SUPPLIER + PROJECT] SUPPLIER TAB OVERHAUL -- director critique. Best content on the site, worst navigation. Site laws v4 apply throughout.

CELEBRATE AND ELEVATE (do not lose these in restyling):
- Per-customer margin heatmap (decade of per-account P&L) -- make every cell click through to that customer's 360 page and that year.
- Basis-risk chart (company forward-price error vs SIM truth per year) -- the epistemic wall CHARTED, the most on-thesis visual anywhere. Annotate the story (2022 under-pricing red, 2023+ over-forecast green) inline.
- Contango/backwardation, cost-stack-vs-revenue, extreme-events-vs-committee overlay -- keep.
- Insights Recommended Actions -- ELEVATE to the Overview page as "What the board should do next" with links to evidence. Auto-generated strategy advice is a headline feature, not a buried card.

FIX (consistency + bugs):
- Accounts waterfall shows CTS GBP0 every year vs Insights cost-to-serve GBP91,780 -- reconcile (gate covers it).
- "Worst Shock Month 2059%" -- explain or fix; a 2000%+ stat with no context reads as a bug.
- Monthly chart committee line fights its axes -- separate panel or proper dual-axis design.
- Trading min-HF 0.0% rows (2019-2024): annotate -- that IS the regime-blindness discovery visible in data; label pre-fix vs post-fix behaviour.

INFORMATION ARCHITECTURE (the big fix): 11 flat tabs -> grouped nav:
- PERFORMANCE: Overview, Financial, Accounts (annual->monthly drill everywhere)
- COMMERCIAL: Customers (heatmap + book + retention/decision outcomes)
- TRADING & MARKET: Trading, Market (merge where sensible)
- OPERATIONS: Monthly + Insights (rename "Run Insights" -> plain language)
- GOVERNANCE: Regulatory (add RAG compliance status per obligation from the compliance scorecard, not just "tracked")
- Query: persistent affordance (search-style, available on every page), not a tab; show recent Q&A history.
- Capabilities: MOVE to Project tab (it is build inventory, not supplier operations).

THE IN-WORLD RULE (new site law): supplier surfaces stay IN THE FICTION. No test counts, phase letters, or build metadata on the company's own dashboards -- a real supplier's MI pack does not display its vendor's sprint number. Build metadata lives on Project. Header keeps net + LIVE only.

THE SPINE (from DECISION_LOOP part 5, still missing here): the PORTFOLIO EVENT STREAM -- the live feed of commercial events and decisions (renewal windows, offers with H1 EV, outcomes, arrears steps) as the Supplier section's centerpiece page, filterable, each event expanding inline to its cause and linking to the customer. Observation -> action -> effect is the tab's grammar (WEBSITE_AS_SHOWCASE tab 2); the delta-EV-vs-baseline headline joins Overview when the frozen-policy baseline lands.

Progressive disclosure everywhere (annual->monthly->daily inline), light theme, derive-or-die, freshness stamps. Acceptance: Rich's eyes; report "awaiting Rich's visual review".
