<!-- DONE 2026-07-29 (bounded worker tick): deliverable #5 EXECUTED. The brief-vs-assembly DELTA is
     PUBLISHED on the live page as a named block "What this page does not yet establish"
     (site/data/knowledge_wholesale.json rungs.scope_delta + site/knowledge/wholesale-price-formation/index.html
     renderScopeDelta). The 12 delta rows = the KPILOT_PAGE_VS_SPEC004_RECONCILIATION §D punch-list (10 ABSENT-
     UNNAMED named as gaps + S9/S10 PRESENT-WEAK upgraded to explicit Partial), each mapping to its scope-brief
     S-section (S2..S14) -- registration-by-reference: every gap's backlog target is an already-enumerated
     scope-brief section (DIRECTOR_RULING_PUBLISHED_GAPS_ARE_THE_BACKLOG). S2 (traded-product structure,
     director's #1) named + flagged highest priority. R11: rendered-pixel verified via the page's OWN inline JS
     (_render_harness.mjs) -- test_scope_delta_renders_every_gap_to_pixel; R15: mutation + drop tests prove the
     control fires (test_scope_delta_mutation_changes_rendered_pixel). Full suite 31 passed. Live verification of
     poesys.net after Cloudflare Pages deploy noted in commit. Level (SITE L2) stays blocked_on director_level_up
     (R16 -- agent builds L2-quality, no self-bump). Walls untouched. Archived to done/. -->
<!-- SUPERVISOR_DRAW: self-drawable -->
<!-- RELEASED 2026-07-29T02:07Z (RUNG-7 planner tick): RELEASE CONDITION MET -- deliverable #4 (kpilot_rung5_charts_and_dod_gate) is DONE and archived to docs/staging/done/ ("DELIVERABLE 4 DONE -- all four rung-5 charts render... Self-drawable REMAINDER now EMPTY -> archive"); #1 scope brief + #2 reconciliation both DONE. All inputs for the brief-vs-assembly delta are present. Window authority = proceed-by-default (published PROVISIONAL/named-gap content is retractable, not a one-way public claim). Flipped blocked->self-drawable so the SITE/DISCOVER lane surfaces it (no-orphan-transition, R11): compute the delta = scope-brief(#1) minus assembly and PUBLISH the named gaps on the live page (R11 verify-to-pixel), registering each gap as a backlog mint-source. Level moves stay blocked_on director_level_up (R16). -->
> **[PARKED in_progress 2026-07-28]** BLOCKING SUB-ITEM: this is the LAST deliverable and consumes #1-4. UNBLOCKS WHEN: #4 (charts) lands and the assembly against the scope brief is done, so the brief-vs-assembly DELTA can be computed and PUBLISHED on the live page (R11 verify-to-pixel). Inputs ready: #1 scope brief + #2 reconciliation punch-list (13 ABSENT-UNNAMED rows) both DONE.

# [PLANNER-MINTED] K-pilot deliverable 5 — publish the brief-vs-assembly delta as the pilot's real finding (2026-07-28)

**Source ruling:** DIRECTOR_RULING_KPILOT_SCOPE_FIRST_2026-07-28.md — deliverable 5 (WORK THIS CREATES); ruling §1.3.
**Serves:** DIRECTOR_AXES Axis 3 (Believability — naming our own gaps on the surface) + Axis 1 (Website). Campaign: knowledge-page pilot — this IS the pilot's primary output per the ruling.
**Real-world fidelity gained:** the named gaps in our own understanding of GB wholesale price formation, published ON the page as such — "that delta is worth more than the page" (ruling §1.3). A reader sees not just what we know but, honestly, where we don't; the delta becomes backlog (per DIRECTOR_RULING_PUBLISHED_GAPS_ARE_THE_BACKLOG).
**Real-world fidelity gained (acceptance):** "a reader who knows nothing can learn from the page what the scope brief says they should learn — and where they cannot, the gap is named on the page."
**Lane:** SITE (published surface: the delta rendered on the page/hub) + DISCOVER (the delta computation is scope-brief minus assembly).
**Target level:** SITE L2 (live-verified rendered delta).
**Exit criteria:**
- The DELTA = {required by scope brief #1} − {actually present after assembly (page + charts #4, reconciled via #2)} is computed and PUBLISHED on the page as named gaps, not hidden in a design doc.
- R11: the delta's gap statements are verified on the LIVE rendered page (fetched value), not the source file.
- The published gaps are registered as backlog mint-sources (per DIRECTOR_RULING_PUBLISHED_GAPS_ARE_THE_BACKLOG_2026-07-28) — a released gap must trigger something (R11 no-orphan-transition), here: each named gap becomes a candidate atom.
- Acceptance test (ruling): a reader who knows nothing can learn from the page what the scope brief says they should learn, and every place they cannot is a named gap on the page.
**Deps:** #1 scope brief (the "required" side) AND #2 reconciliation + #4 charts (the "present" side). This is the LAST deliverable — it consumes 1–4. The ~80%-derivation thesis is WITHDRAWN (R12): measure derivation if useful, never let it shape scope or the delta.
**Propose-then-proceed window:** proceed by default (ruling **proceed**; published PROVISIONAL/named-gap content is retractable, not a one-way public claim). Walls untouched.

## Deliverable (verbatim)
> 5. The brief-versus-assembly delta published as the pilot's real finding.

Ruling §1.3: "The DELTA between brief and assembly is the primary finding — the named gaps in our own understanding, published as such. That delta is worth more than the page."
