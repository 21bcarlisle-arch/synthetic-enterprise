# EPOCH GATING GATES BUILD, NOT THOUGHT — and you may author your own atoms (P0)

**Staged:** 2026-07-12 by advisor, director-prompted ("it looks like it can't
think of its own work for future epochs — why not?"). Answer: because atom
authorship has been the director's/advisor's privilege, and because "epoch not
open" has been read as "no work of any kind". Both end now.

## The error to correct
You reported: *"the maturity map is now honestly exhausted — 31 atoms remain
deliberately parked pending their own epoch sequencing."* That is not an
exhausted map. **An empty drawable set is a DECOMPOSITION DEBT, not a finished
company.** It means the next epoch has not been broken into atoms yet — which
is itself the highest-priority work item, and the one thing you must never
respond to by idling.

## Rule 1 — Epoch gating gates BUILD, never thought
A parked atom is parked for **BUILD**. It is NOT parked for DISCOVER, FRAME,
research, red-team, charter-writing, anchor-hunting, or design. All 31 parked
atoms have such work available RIGHT NOW, and none of it violates epoch
discipline or the exit-test model:
- Epoch 3 (walled interfaces): design the typed contracts, research adapter
  patterns, specify the go-live seam, red-team the wall.
- Epoch 4 (tournament/generative futures): design the fitness function, the
  mortality rules, the anchored generators; red-team for goal-seeking leaks.
- Epoch 5 (go-live analysis): the NFR register (Part B/C of
  PRODUCTION_READINESS is still open), the regulatory/licence path.
- Every parked atom: its L3/L4/L5 definitions, its anchors, its verification
  design.
**Doing this makes epochs open FASTER and better — a running start rather than
a cold one.** Build remains gated on the director opening the epoch. Nothing
else is.

**AMENDMENT (2026-07-13, director in-console authorization, DIRECTOR_CANON.md v2
§3a):** "the director opening the epoch / opening BUILD" is now DIRECTOR_TWIN's
standing-approver call, not a wait on the real director. BUILD-within-the-open-
epoch (and epoch sequencing, scope, priority-within-dials) is delegated to the
twin: a blocking/awaiting-approval state auto-routes via
`background/director_twin.py::route_blocking_decision`, the twin answers from
canon in seconds (logged, director-reviewable, overturnable), and the builder
proceeds on its answer — so an all-idle (all-BUILD-gated) map never freezes
waiting on a human. Only genuine one-way doors and the Epoch-4 fitness function
still route to the real director. First exercised 2026-07-13: the twin APPROVED
(confidence high) opening BUILD on the six ranked Epoch-2 atoms
(`director_twin_log.jsonl` entry 2026-07-13T07:33:20).

## Rule 2 — You may AUTHOR atoms
When the drawable set runs low, decomposing the current and next epochs'
charters into candidate atoms IS your next task. Author them:
- Agent-authored atoms are marked as PROPOSALS (provenance field), and the
  director re-ranks/kills them at boundaries — he remains the priority
  authority, but he is NOT the bottleneck for generating candidate work.
- Proposals are immediately workable in DISCOVER/FRAME (zero-risk, document
  output). They may not enter BUILD until ranked.
- Decomposition quality bar: each atom names its lane, level definitions,
  evidence paths, file_scope, dependencies, and why it matters to the epoch.

## Rule 3 — "Nothing to do" remains structurally impossible
The invariant from R3 stands and is now completable: if the drawable set is
empty, the correct action is DECOMPOSE (rule 2) or THINK AHEAD (rule 1) —
never idle. An idle turn with parked atoms present is a defect. Alarm it.

## DoD
Rules 1-3 in CLAUDE.md; parked-atom discover/frame work drawn immediately (do
not wait for the next boundary — the map is "exhausted" as of now, so start);
agent-authored proposals appearing on the map with provenance; a test that the
drawable set is non-empty whenever ANY atom exists in any state; the Monday
pack lists the proposals for director re-rank. Then keep going — you have
~12 hours and a company to think about.