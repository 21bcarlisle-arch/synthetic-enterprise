# EPOCH2_RATIFIED — The Value Cycle opens (P1)

**Staged:** 2026-07-11. **RATIFIED by the director in-conversation** after
review, summary, and two amendment rounds (payments-depth worry → M2 entry
gate; banking-as-swappable-interface → Rails simulator, W-lane). The full
framing follows in the same staging commit (THE_VALUE_CYCLE_FRAMING.md) —
canonical content, install to docs/design/, do not re-review.

## Immediate effects
1. Install framing to docs/design/THE_VALUE_CYCLE_FRAMING.md; register the
   new W-lane atom "Banking & Payment Rails (twin: Bacs/bank/acquirer)" and
   the M2 entry-gate audit atom in maturity_map.yaml; add the payments seam
   to the Epoch-3 wall adapter inventory.
2. SPIKE slot 3 now reads: **M1 at full parallel depth** (bitemporal log,
   as-of interfaces, event-drained loop, materiality gates + lazy valuation)
   with M1's exit test as the bar: a hedge decision structurally cannot see
   past "now"; a restatement lands as an event and versions correctly.
3. **Payments maturity audit** (M2 entry gate) joins the weekend background
   work: grade all billing/payment modules vs D/E charters; Expert Hour under
   a payments-operations veteran persona; classify COMPANY-LOGIC vs
   RAILS-PHYSICS; seed the simplifications register. Report in Monday's pack.
4. M2 build (rails sim + three clocks) starts only after the audit verdict +
   M1 exit test. M3 design-only in background forks. M4 strictly last.
5. All headline public figures are PROVISIONAL pending M4 rederivation —
   ensure surfaces carry that label consistently from now.

Scope defence stands: section 3 ("NOT this epoch") + map dials; the advisor
enforces at staging. Exit tests are the schedule; the evaluator grades them.
