# [PLANNER-MINTED] Cold-eyes verdict on the landed FRONT-DOOR MISSION block (2026-07-24)

**Type:** RUNG-7 planner mint (WORK_IS_THE_DEFAULT 2026-07-23, rung 7). Minted from a ratified goal + a just-landed surface. **Propose-then-proceed.**

## Ratified goal served
- **DIRECTOR_AXES v1 — Axis 1 (Website):** *usefulness to him as an operational window* + *simplicity/clarity as a marketing tool — legible to someone who is not the builder.*
- **DIRECTOR_RULING_FRONT_MISSION_BLOCK_2026-07-24** (archived to `done/`): the front door now leads with *"carbon abatement through personalisation, measured in £/tCO₂e, yardstick £273/tCO₂e (2025), carbon ledger honestly absent"* — the cost-to-serve arbitrage leg moved to /proof. Confirmed live in `site/index.html` (mission block lines ~158–181, meta description updated, CTS leg pointer to /proof present).

## Why this is drawable (a review is product work, not a wall)
The mission block **landed and is live**, but its acceptance bar — Axis 1 is *the director's own eyes* (R11 residual) — has not yet been run against fresh, blindfolded eyes. The `cold-eyes-walk` skill exists precisely to manufacture that outside vantage before the director's verdict, so his time is spent on a surface already scrubbed of the obvious tells. This is the "next tick, full cold-eyes" follow-on the ruling's landing left open.

## Real-world fidelity / axis gained
A cold-eyes pass on the pitch catches the veteran-smell-test failures *before* the director does: does the front door pass the 10-second test; is the mission legible to a non-builder; does the honestly-absent carbon number read as rigour (good) or as a hole (bad); does any cohort-derived £ still lead (RC7 breach); does the CTS-on-/proof move actually resolve and carry its N / floor-not-figure framing. The output is a scored verdict the director can accept or overturn — advancing Axis 1 without spending his attention on first-order defects.

## Scope (propose)
1. Invoke the `cold-eyes-walk` skill against the LIVE front door (fresh instance, priors-before-pixels, persona = a UK-energy veteran + a non-builder marketer).
2. Score against Axis 1 (operational-window usefulness + marketing legibility) and the FRONT_MISSION_BLOCK ruling's three requirements (mission two-sentences in v4 register; the £273 yardstick; carbon honestly absent with its instrument named).
3. R11: verify to the rendered pixels — quote the actual on-page text/figures, not the source.
4. Register any finding as an atom/propose-doc (SELF_INTERRUPT_DISCIPLINE: queue, don't fix-on-sight unless a genuine honesty defect that misleads is live — those interrupt).

## Walls untouched
The axis *verdict* is the director's by right — this pass produces a *prediction/scrub*, never a self-awarded pass. No site structure change without a finding; no L3; no one-way door.

## Propose-then-proceed window
A read-only review — proceeds immediately under standing site-lane authority. Findings route to the queue; only a live *misleading* honesty defect interrupts.

---

## COLD-EYES VERDICT (actioned 2026-07-24, worker tick) — **PASS**

Read-only cold-eyes pass run against the LIVE front door `https://poesys.net/` (R11: pixels, not source; live fetch verified rendered text matches `site/index.html`). Personas: UK-energy veteran + non-builder marketer.

**Ruling requirements (DIRECTOR_RULING_FRONT_MISSION_BLOCK_2026-07-24) — all met, evidence-quoted from live render:**
1. *Mission as falsifiable claim, v4 register* — ✓ rendered: "The claim, falsifiable: that you cut carbon fastest by knowing each household well — shifting when it draws power, fitting the right assets, pricing the right tariff, having the right conversation…".
2. *£273 yardstick named as a government appraisal value (not a run/cohort figure)* — ✓ rendered: "the UK government's carbon appraisal value, £273/tCO₂e (2025)." In-band vs DESNZ 2025 central carbon values; correctly attributed.
3. *Carbon number honestly absent, instrument named* — ✓ badge "NOT YET MEASURED" + "the carbon ledger is designed, not yet instrumented — no number is shown here until each household's half-hourly meter reads meet real grid carbon intensity."

**RC7 (no cohort-derived £ may LEAD a public surface)** — ✓ CLEAN. The only £ on the page is £273 (government value). No run/cohort figure leads.

**CTS→/proof promise-link** — ✓ resolves. `https://poesys.net/proof/` loads; section heading "The cost-to-serve leg — the economics that would fund abatement", framed as a *gap* against the flat-margin benchmark (not a headline £). Not a broken/misleading link → not interrupt-class.

**Findings (SELF_INTERRUPT_DISCIPLINE — QUEUED, not fixed on sight; neither is a live misleading defect):**
- *F-A (minor, legibility):* the "NOT YET MEASURED" badge risks reading to a cold marketer as a *hole* rather than *rigour*; mitigated by the "the trajectory appears here first" caveat that recasts absence as anticipation. Borderline-acceptable — a director-eye judgement call, not a defect.
- *F-B (verification-coverage gap):* the /proof#economics-anchor figures are JS-loaded (static fetch shows "Loading…"), so the N / floor-not-figure framing on that target cannot be closed by WebFetch — needs a browser-level (Playwright) pixel check. This is a HARNESS coverage note, not a front-door defect.

**Verdict:** the mission block is a clean PASS on the ruling and RC7. The Axis-1 *verdict proper* remains the director's by right — this pass is a scrub/prediction, never a self-awarded pass.
