**Severity:** LATENT · **Lane:** H_harness · **Epoch:** 3 · **Atom:** none — Lane 0 delivery

# Four of the eighteen BLOCKING findings I could reproduce still refuse anything, and the lane hold is keyed to the document, not to the defect

**Measured 2026-09-09, delivery seat, on the shared tree at `a522f07d9`.** Drawn as Lane 0: *"for
each of the 27, run the lane gate it claims to block and record whether that lane is actually
refused TODAY, naming the refusal verbatim."*

The population is **29**, not 27 — `background.finding_severity` counts 29 BLOCKING over 136
documents. The doorbell's 27 was two documents stale.

---

## The first answer is that the question as posed has a tautological half

`tools/level_promotion_gate.py` (OPS11) refuses a level-raise in a lane via
`background.gate_authorization.lane_blockers(lane)`. Run today:

```
A_strategy_governance: HELD by 8
D_billing_metering:    HELD by 1
F_risk_compliance:     HELD by 1
G_data_learning:       HELD by 1
H_harness:             HELD by 16
```

**All 29 refuse their lane, and every one of them would refuse it whether or not its subject still
exists.** `lane_blockers` reads the document's header. It does not reproduce the defect. So "does
this finding refuse a lane today" answers YES for all 29 by construction, and answers nothing.

The question with content is the director's second clause — *does the named refusal still
reproduce?* — and that is what was measured.

## The number

Of the 29, I reproduced **18** decisively this turn. **Four still refuse. Fourteen do not.**

Eleven were not reached and are named below rather than guessed at.

---

## THE FOUR SURVIVORS, refusal quoted verbatim

**1. `SEAT_FINDING_THE_PUBLISHED_BOOK_GAINED_4913_BILLS_WITH_A_BASELINE_AND_NO_RATIO...` (D_billing_metering)**

```
tests/saas/test_a_published_bill_shock_can_be_recomputed.py
  ::test_the_baseline_is_present_exactly_when_the_shock_is
AssertionError: 4914 bill(s) publish a baseline with no ratio
```

Live, and **the count has moved since filing: 4,913 → 4,914 of 10,924 bills.** It is the
working-tree copy of `docs/reports/run_output_latest.json`, as the finding said. A drifting count is
evidence the defect is being regenerated by the publisher, not sitting still.

**2. `SEAT_FINDING_THE_CONSOLE_CAPTURE_READ_A_FOLDER_THAT_WENT_COLD...` (H_harness)**

```
FAILED tests/tools/test_console_capture_lapse.py
  ::test_THE_CHECK_READS_THE_SAME_ROOM_THE_WRITER_WRITES_TO
1 failed, 11 passed
```

This one carries a **`**Discharged:**` field naming that very file** — and one of its eleven
controls is red. `finding_severity` independently flags it `FALSE-DISCHARGE` for naming no test
node. The discharge claims the defect cannot recur; the named suite says it does.

**3 and 4. `WORKER_FINDING_EIGHT_WORKING_COPIES_WOULD_REVERT_A_LANDING...` and
`SEAT_FINDING_THE_STALE_COPY_REMEDY_HAS_NO_MOVE_FOR_A_RIVAL_COPY_HEAD_ALREADY_SUPERSEDES...`
(H_harness)** — one live refusal, two documents:

```
[stale-copy] WOULD REVERT A LANDING: 4
[stale-copy] NO OPINION (no reader for this suffix -- NOT a clean verdict): 368
```

**The class is live but halved: 8 → 4.** The named remedy still has no legal move for a copy HEAD
already supersedes, and `tools/stale_copy_refusal.py` still prints `HOLDER WORK` beside a path whose
partner it says *"references `tools.r1_inference_ceiling._scores_on_folds` — and NO tree supplies
it, committed or not."*

---

## THE FOURTEEN THAT NO LONGER REPRODUCE, with the evidence in each case

| finding | evidence it is spent |
|---|---|
| CM price has two homes (A) | **no live assignment survives.** `grep -E "^\s*_?CAPACITY_MARKET_GBP_PER_KW_YR\s*="` over `company/` returns nothing; `capacity_market_published_record` is the single home and `flexibility_potential.py:42` imports it |
| £75/kW is a capped T-1, household cannot hold a CM agreement (A) | `capacity_market_published_record.DOMESTIC_PARTICIPATION_REFUSAL` is live and applied at `flexibility_potential.py:88` |
| switching band's own publisher disagrees (A) | artefact carries `values_refuted_by_the_publisher`; `tests/architecture/test_a_refuted_commons_artefact_cannot_quietly_become_current.py` — **6 passed** |
| switching-rate artefact refuted (A) | **duplicate of the row above, by its own admission** — the document says the other lane "landed first" and its own artefact text "was discarded" |
| CM levy 2018 row (F) | artefact is now `version: 2`; its `derivation` reads *"ANNEX 9 DOES NOT PUBLISH A PER-YEAR FIGURE, and saying it did is what cost a BLOCKING finding on 2026-09-07"* |
| proof page told a reader a larger figure was smaller (G) | `SMALLER advantage`, `not a larger one`, `which fell further` — **all three absent** from live `site/data/value_arms.json` |
| site lane is red at HEAD (H) | `tools/site_lane_gate.py` → **669 passed, 37 skipped**, `✓ site tests green` |
| site lane's own remedy crashed (H) | the remedy runs to completion; lane green as above |
| stale-copy census crashes in the shared tree (H) | it ran to completion and returned graded verdicts — the crash is gone |
| fourteen modules only in the working tree (H) | `tools/orphan_ratchet` → **rc=0, silent** |
| two controls red at HEAD (H) | both named suites: `test_the_statement_shows_how_each_bill_reached_its_number.py` + `test_switching_rate_commons.py` → **122 passed, 2 xfailed** |
| source-scanning control matches by substring (H) | `tools/substring_source_scan_census.py` → **rc=0** |
| producer's named witness diverged (W2) | `17,453`, `17,739`, `LARGER advantage` — **all absent** from live `site/data/value_arms.json` |
| DIRECTOR_CANON the demand vector (W2) | **all five deliverables are minted**: `W2_29`…`W2_33` are present in `docs/design/maturity_map.yaml` |

## THE ELEVEN NOT REACHED THIS TURN

Named, not guessed at: anti-theft guard / draw ledger; holder-work rule counts names; pair-move
partner hand-launched; publisher's own remedy cannot clear its refusal; publisher's remedy reports
level about its worktree; R1 copy's missing partner; two-rooms repair's recorded cause; cap
composition twelve editions stale; page's departure headline; value-arms cluster is two lanes in
five files; two commons artefacts cite a moved publication.

One partial reading on the last of those: the commons `cannot_tell` count is **3 of 9** today
(`gb_domestic_switching_rate`, `ofgem_cap_unit_rate_composition`,
`ofgem_default_tariff_cap_windows`), against the "four of nine could not be asked" it was filed on.
Down one, not closed.

---

## THE FINDING

**BLOCKING here measures the filing, not the defect.** The lane hold is keyed to a document's
existence in `docs/staging/`; nothing anywhere re-asks whether the thing it describes is still
true. On the 18 I could reproduce, **78% were already spent** — and every one of them was still
holding its lane this morning.

`A_strategy_governance` is held by 8 documents. I proved 4 of them discharged and reached no
survivor in that lane at all. `H_harness` is held by 16, of which 6 are discharged and 3 survive.

This is the repo's own named shape from CLAUDE.md — *key a control to the property, not to today's
answer* — running backwards. The severity vocabulary is fine; **the hold is attached to the wrong
object.** A blocker should be held by a reproduction, not by a filename.

**It is also not a sweeper's job.** Reproducing a refusal took under a minute each, and it named
the specific path in every case; measuring the pile would have found none of the four. The four
survivors are worth more than the fourteen closures.

## WHAT IS NEXT

1. **The four survivors are the real queue** — the D baseline/ratio red first, because it is
   regenerating (4,913 → 4,914) and it fires for every lane whose commit touches a billing reader.
2. **The console-capture discharge is false and says so twice** — its own named suite is red and
   `finding_severity` already flags it. That is a header repair plus one control, not a finding.
3. **The fourteen closures need dispositioning**, each pointing at the evidence above — not at
   their age.
4. **The eleven unreached** are the next turn's census, same method.
5. The structural repair — holding a lane by a reproduction rather than a filename — is a real
   atom and is **not minted here**, because this pass measured one population once and a mechanism
   built off one sample is the shape this project keeps paying for.
