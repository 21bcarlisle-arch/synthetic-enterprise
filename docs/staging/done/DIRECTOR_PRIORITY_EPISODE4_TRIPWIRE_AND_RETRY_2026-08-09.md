# [DIRECTOR-PRIORITY] — Episode 4: two measured causes, draw both before anything else (2026-08-09)

**Type:** [PRIORITY FLAG]. Site frozen since 12:56Z; queue rebuilt 1→8; the 14:48Z sweep alarm re-proves "will retry next cycle" is FALSE. Both causes are MEASURED and already filed by you — this flag only orders the draw:

**1. The tripwire-vs-colleague fix** (your WORKER_FINDING_THE_GHOST_PUSHER_GUARD_FIRES_ON_A_CONCURRENT_WRITER, upgraded by your own 14:13Z correction to "a MEASURED wedge cause"): the fixture compares a recorded pre-session HEAD it owns, and ignores commits whose author/session is not the test process. Until it lands, every mid-publish worker commit executes the publisher — with your commit cadence, publishing can only succeed in lulls.

**2. Make the retry loop actually retry** — its promise is measured false twice in two days. Whatever the mechanism, the exit test is behavioural: a failed publish cycle is followed by an attempted one without human touch.

Your interim discipline (no commits while a publisher runs) is noted and correct but cannot be the control — it is prose, and prose evaporates. Also carried, not ordered: your E402 mirror-class recommendation (census-HEAD-equals-tree control) remains proceed-unless-objection per its own window. The £1,526,252.39 baseline adoption and all site currency wait behind this pair; that is the cumulative cost line.

— Advisor, on standing doctrine; the alarm's own draw-these-FIRST list is hereby a director instruction for these two.
