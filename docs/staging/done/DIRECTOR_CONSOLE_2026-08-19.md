**Severity:** RECORDED · **Lane:** H_harness

# Director console — verbatim record, 2026-08-19

> **The director did not write or stage this file. It is a VERBATIM CAPTURE of what he
> typed in the interactive console**, written automatically by
> `tools/console_instruction_record.py` so that his words leave a trace the machine can
> read. Under CLAUDE.md the console already carries full authority; what it did not carry
> was EVIDENCE, and on 2026-08-19 that cost `EP6_wall_protocol_typing` a wrongful re-park
> after 381 director-facing sources correctly reported silence.
>
> **Quoted exactly, never paraphrased, never expanded.** Shorthand is left as shorthand:
> "move EP1 and EP6 to build" is recorded as written, so this file does NOT by itself
> release `EP6_wall_protocol_typing` — the release door matches full atom ids. Resolving
> shorthand to an atom is a judgement, and it belongs in a separate record that cites
> this one, not in an automatic capture that would be putting words in his mouth.

Source: `ffcb5162-c57b-4509-934d-b57034445bf0.jsonl` · 11 turn(s).

### 2026-08-19T05:48:04.654Z

> Two things from outside your session. The site hasn't published
> since 00:43Z although commits keep flowing — which matches your
> own finding that a green gate is not a landed publish. And staging
> is at 91 and climbing.
>
> Then a harder question I want answered honestly, not defended:
> overnight was 23 commits and most were DISCOVER/FRAME passes with
> level unchanged and no build code — EP1 on pass 8, D30 and D27 and
> EP6 on pass 7 — plus repairs to your own controls. Is the loop
> converging? What would have to be true for those atoms to build
> rather than investigate again, and is anything about the current
> rules making investigation cheaper than building? If the interleave
> isn't firing, say so.
>
> Otherwise carry on: Explore, wall exhibit first, then the
> Knowledge checks, then PB3. Your call on the £1,526,252.39
> criterion was right — not reverting.

### 2026-08-19T06:04:01.281Z

> Build the pass ceiling, then move EP1 and EP6 to build.
>
> Then keep going without me. You have authority to promote idle
> atoms to build on your own readiness judgement — record the reason
> with each, tell me what you moved. 78 idle atoms is not a queue of
> questions for me. What stays mine is genuinely curriculum:
> difficulty values, targets, and anything that would tune an
> output. Everything else in that reservation, read narrowly, act,
> and show me the reading you took.
>
> The deeper problem is that your cheapest lawful path is the one
> that produces nothing — discovery free and unrefusable, build
> expensive and refusable. Don't fix it by making discovery
> expensive. Make it impossible for the system to run indefinitely
> on work that cannot change its own state. Build whatever that
> takes, including changing the drawing rules, and tell me
> afterwards what you changed and why.
>
> Two structural problems to resolve the same way — decide and act,
> flag the choice for me to review, don't hold.
>
> First, the annual report. It reads as a document nobody needs, but
> twelve modules import it — the publisher, the compliance register,
> regulatory checks, the tournament runner. It isn't an output, it's
> an intermediate others depend on, and 36 commits in five days have
> gone into keeping it consistent. End that cost: a report should be
> a rendering of data, never a thing other code reads. Make it true,
> in whatever order is safe, and tell me what broke on the way.
>
> Second, the thing underneath it. Eight-plus modules independently
> write site/data JSON and there is no query store. I think that gap
> is the common root of the CCL 67% disagreement, the £4.9m
> two-bases confusion, Low Carbon published twice from two tables,
> and the proof/ledger pair that has never agreed — all of them
> multiple writers deriving the same quantity with no single source.
> If you disagree, say so and act on your own reading instead.
>
> Read DIRECTOR_INSTRUCTION_QUERYABLE_PROJECTIONS_2026-08-10 first:
> I ruled it, it was never built, and it may already be the answer —
> a query store derived from committed truth, rebuilt each publish,
> never a second source of truth, anchored on the July
> DATA_LAKE_OBSERVABILITY note. If it solves this, build it. If it
> was scoped for a different problem, replace it and say what you
> replaced it with. Either way, tell me why it never drew — that's
> its own finding.
>
> Sequence it so nothing regresses and the site keeps publishing.
> Land it in reversible pieces with receipts, flag anything that
> moves a published figure using correct-caveat-withdraw, and keep
> Explore, the Knowledge checks and PB3 moving alongside. Nothing in
> here waits on me.

### 2026-08-19T06:43:10.001Z

> Two things.
>
> First, epoch latitude — with a guard, because I don't want to
> create chaos by relaxing this. Crossing an epoch boundary to scout
> or to type an interface is fine, and I'm content for you to take
> that latitude rather than artificially curtailing work. What I'm
> not licensing is the arc dissolving: the epochs still exist as
> commitment sets with their gates, and my evidence-pass rulings
> still hold. The failure I care about is a single lane going deeper
> and deeper across multiple epochs while the rest stands still —
> depth without breadth. Nothing currently measures the shape of the
> draw across lanes, only what may be drawn. Make that visible, act
> on it yourself when formation slips, and if you find this latitude
> is being used to pull whole future epochs forward rather than to
> scout, say so and tighten it — that would be me having got this
> wrong, not you.
>
> Second, housekeeping. Your /tmp clear-out this morning was a
> one-off sweep after the machine had already stopped, and that's
> the second resource exhaustion in ten days — the memory cleanse
> was the same shape. There's a governor for RAM and nothing for
> disk. I don't want a third instance: make file and workspace
> housekeeping a standing property of the system, with the same
> teeth as anything else — bounded lifetimes, alarms before
> exhaustion rather than after, and no reliance on anyone noticing.
> Own the best practice for it and write it down where it governs
> rather than where it's read.

### 2026-08-19T07:21:46.332Z

> This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.
>
> Summary:
> ## 1. Primary Request and Intent
>
> This is the `synthetic-enterprise` project (`/home/rich/synthetic-enterprise`) — a high-fidelity simulation of an autonomous UK energy supplier, run by an autonomous agent loop with Rich as MD/director. The conversation spans a full working day (2026-08-18 into 2026-08-19) with many sequential director instructions:
>
> 1. **Website structure programme** (SITE4–SITE11): accept four recommendations, build Step 0 (IA register) first, with one condition — show the director the rendered `/director/` content before it becomes crawlable.
> 2. **Pull Capabilities (`SITE7`) forward** ahead of Knowledge, to judge whether the structure works before four more steps build on it.
> 3. **Four Capabilities review changes**: gaps must be answerable plans not indictments; state the book size; every Live claim must route to understandable evidence; seams must show readiness not flat "Planned".
> 4. **Knowledge (`SITE5`)**: structural half first, then one pilot page for review, then the rest. Badge must distinguish "written, awaiting check" from "stub, never touched".
> 5. **Egress allowlist enquiry** → resulting three-lane ruling: company refused by construction, sim may ingest published sources, harness may fetch but bytes may become documentation/design/Knowledge and **never a runtime input the company reads**.
> 6. **New standing rule on figures** (replacing notify-first): never wait; three lawful outcomes — **CORRECT**, **CAVEAT**, **WITHDRAW**. Freezing is not one. Notify-first retained only for changes of method or basis.
> 7. **Convergence question**: is the loop converging? Answer honestly, not defensively. Then: build a pass ceiling, promote EP1/EP6, and make it *impossible* to run indefinitely on work that cannot change its own state.
> 8. **Epoch latitude with a guard**: crossing epochs to scout/type interfaces is fine; the arc must not dissolve; make the *shape of the draw across lanes* visible and act on formation slips.
> 9. **Housekeeping as a standing property**: bounded lifetimes, alarms before exhaustion, no reliance on anyone noticing. Own the best practice and write it where it governs.
> 10. **Two structural problems** (decide and act, flag for review, don't hold): the annual report is an intermediate 12 modules import (make it a rendering, never a thing other code reads); and the missing query store (read `DIRECTOR_INSTRUCTION_QUERYABLE_PROJECTIONS_2026-08-10` first, and say why it never drew).
>
> **Standing authority granted**: promote idle atoms to build on own readiness judgement, recording reasons. **What stays the director's**: "genuinely curriculum: difficulty values, targets, and anything that would tune an output. Everything else in that reservation, read narrowly, act, and show me the reading you took."
>
> ## 2. Key Technical Concepts
>
> - **Maturity map**: `docs/design/maturity_map.yaml` (316 atoms), atoms with `level_current`/`level_target`/`loop_stage` (idle/build/harden/verify/discover)/`epoch`/`lane`/`file_scope`/`depends_on`
> - **Epistemic wall**: company (`company/`, `saas/`) vs sim (`sim/`, `simulation/`); seam at `company.interfaces`
> - **R-rules**: R5 (alarms on transitions only), R9 (observed-with-evidence vs inferred), R10 (class fix not instance), R11 (verify to the rendered value), R12 (diagnostics never targets), R13 (curriculum is director's), R15 (controls must be able to FAIL; mutation tests; three killer patterns TAUTOLOGY/FAIL-OPEN/FAIL-SILENT), R16 (ledger is the record), R17 (tick never rests)
> - **Gates**: 10-gate pre-commit chain (`tools/git-hooks/pre-commit`) — test gate, level promotion, site lane, moap coherence, archive question, consolidation rhythm, size ratchet, orphan ratchet, AO2 write-time reuse gate, company network isolation (new)
> - **`tools/surgical_land.py`**: lands named paths, gating a **clean extract of the resulting tree** rather than the working tree
> - **Ratchet pattern**: freeze known debt, fail on growth, fail on stale entries
> - **Fail-closed direction** as a deliberate design choice, differing per control
>
> ## 3. Files and Code Sections
>
> **`site/ia_register.py`** (created, landed `8a3e002d6`) — the three-state IA register. `CANONICAL_NAV` is a single tuple; adding a tab is one line. Now contains Home, The World, The Company, Capabilities, Knowledge, Proof. Has `ORPHAN_DEBT`, `LEGACY_TAIL`, `NAV_EXEMPT` (`/shadow/`), `GENERATED_NAV` (`/evidence/`), `director_record_publication_violations()`.
>
> **`tools/render_site_nav.py`** (created) — renders nav from register into marked regions `<!-- IA-NAV:START ... -->`.
>
> **`tools/generate_capabilities_door.py`** (created) — prose curated, status derived via `moap_stage.compute_stage`. Key: `_EVIDENCE_LINKS_SHIP = False` withdrawing machine-facing links; `GAP_PLAN` dict mapping gaps to atoms; `plan_for()` fail-closed on phantom citations; `scale()` publishing both disagreeing book sizes (14 and 17).
>
> **`site/knowledge/review_state.py`** (created) — four states:
> ```python
> STUB = "stub"            # not written; the page says so itself
> UNCHECKED = "unchecked"  # written, never checked against the source
> DUE = "due"              # checked once, now past its threshold
> FRESH = "fresh"          # checked within its threshold
> NEVER = STUB             # legacy alias
> ```
> Thresholds: fast 92 / medium 183 / slow 365 days, per-subject.
>
> **`tools/company_network_isolation.py`** (created, landed `6050c2df3`) — transitive import-graph walk. `HTTP_MODULES`, `NETWORK_BINARIES = ("curl", "wget", "nc", ...)`, `SHELL_MODULES`. A shell counts iff the module also names a network binary (drops `git`, keeps `curl`). `KNOWN_ROUTES` = 4 frozen routes. `gate_violations()` fails on NEW or STALE.
>
> **`tools/discovery_pass_ceiling.py`** (created, **NOT YET LANDED**) — `DEFAULT_CEILING = 5`; `survey()`, `saturated_ids()`, `decisions()`. 15 of 114 atoms saturate.
>
> **`background/supervisor.py`** (modified, **NOT YET LANDED**) — added to `_idle_discover_frame_draw` after the `non_saturated` filter:
> ```python
>     try:
>         from tools.discovery_pass_ceiling import saturated_ids
>         over_ceiling = saturated_ids()
>     except Exception as exc:
>         log("IDLE DISCOVER/FRAME draw: the pass ceiling could not be computed ...")
>         return None
>     under_ceiling = [a for a in candidates if a.get("id") not in over_ceiling]
> ```
> Fails **toward stopping** (opposite direction to `_is_frame_saturated`).
>
> **`background/disk_headroom.py`** (created, **REFUSED — landing in progress**) — `PRESSURE_FLOOR_MB = 2048`, `CRITICAL_FLOOR_MB = 512`, `RECOVERED_FLOOR_MB = 3072`. `WATCHED = (Path("/tmp"), Path("/var/tmp"), PROJECT_DIR)`. `SCRATCH_PATTERNS` verified against creators (`wall-head-*`, `surgical-land-*`, `publish-gate-head-*`, `pytest-of-*`). Functions: `sample()`, `band()`, `reapable()`, `reap()`, `admit()`, `observe()`, `stdlib_shadows()`.
>
> **`background/background_worker.py`** (modified, **NOT YET LANDED**) — wired both governors into `main()` loop before `process_leftover_run_markers()`, wrapped in try/except so a governor cannot crash the worker.
>
> **`company/regulatory/ccl_ledger.py`** (landed `9a8bd2b7c`) — gas 2023→0.672, 2024→0.775, 2025→0.775; added `CCL_DISPUTED_YEARS` for 2016 electricity and 2022 gas.
>
> **`simulation/policy_costs.py`** (landed `f72135409`) — added 2025 entries: RO 33.06, elec CCL 7.75, gas CCL 7.75.
>
> **`CLAUDE.md`** (landed `394bd6322`) — added the three-lane rule; marked egress allowlist "ADVISORY, not enforced". 34,419 of 35,000 chars; 119 of 200 lines.
>
> ## 4. Errors and Fixes
>
> - **Byte-identical Knowledge pages claim — WRONG, retracted (`423a14660`)**: measured tag-stripped static source of JS-rendered pages. Compounded by a second error: the shared harness stubs `location.pathname` as `/`, so my first re-check *falsely confirmed* it. Only correct on the third pass.
> - **"Writing the pilot reduces review debt" — WRONG**: writing pages moved the count from 0→6 *risk*. Now pinned by `test_MUTATION_writing_a_page_does_not_reduce_the_risk_count`.
> - **"Eight remaining Knowledge pages" — WRONG**: six.
> - **`rc=75` tree-lock diagnosis — WRONG**: it's the publisher's own singleton; an instance was genuinely running. Director acknowledged: "I passed the rc=75 alarm to you as fact rather than as a claim to verify."
> - **Piping `surgical_land` through `tail` three times, losing diagnostics each time** — self-identified process fault.
> - **Stale working copy nearly reverted 57 lines** of another lane's committed map work; now routinely run `git diff HEAD -- <paths> | grep '^-'`.
> - **Deleted `/tmp/knifetree`, a registered git worktree**, during the manual sweep — then chased it for several turns as the cause of a red test.
> - **Real cause of the red test**: `/tmp/bisect.py`, an ad-hoc debug script whose *filename* shadowed stdlib `bisect` for any process with cwd `/tmp`. Quarantined; test passed.
> - **Four controls found pinning literals rather than properties**: CCL "stable" test, RO clamp test, KNIFE fixture, aged-staging test (`assert still_staged` — went red when its work was *done*, blocking the tree).
> - **Guessed scratch patterns** in the first disk-governor draft (`head-checkout-*` matches nothing); and watched only `/tmp` when the biggest producers write to `/var/tmp`.
>
> ## 5. Problem Solving
>
> - **`/tmp` at 99%** (152 MB free): 80 abandoned repo extracts. Cleared 6.2 GB.
> - **`resource_headroom.py` has NEVER RUN** — no caller, no unit, no state file, built 2026-08-10 after 64 oom-kills. This reframes the housekeeping problem: mechanisms get built and never wired (4 instances found).
> - **Convergence answer**: 08-09 = 106 commits/35 level moves (3 per move); 08-18 = 98 commits/**0** moves. 80 idle atoms below target; buildable set only 25. Discovery is inexhaustible **by construction**; a docs-only commit runs no tests while a BUILD pays 10–20 minutes.
> - **Query store**: `G12_queryable_projections` is **L2/2, built** (`tools/build_projections.py`, 21KB). `G13_projection_consumers` is L0, stage `build`, unblocked — `tools/lab_query.py` doesn't exist and nothing calls the store. It sat 8 days while EP1 took its 8th DISCOVER pass.
>
> ## 6. All User Messages
>
> 1. "All four recommendations accepted, with one condition on the director record: show me its rendered content before it becomes crawlable — it was written internally and I want to read it as a stranger would first. Everything else proceed on your sequence, Step 0 first. Good catch on /proof/; my instruction would have broken five live redirects."
> 2. "did you start knowledge?"
> 3. "Pull Capabilities forward — build it next, before Knowledge. You flagged it yourself as the one step with no ordering argument, and I want to see a real new tab to judge whether the structure works before four more steps are built on it. Knowledge follows straight after."
> 4. "Three things on Capabilities. 1. The gaps section reads as an indictment of the project rather than a plan… 2. The evidence links land a reader in the machine's own vocabulary… How and when is yours, including deciding this shouldn't ship until Evidence is rewritten. 3. The eight Knowledge pages are written and unreachable from anywhere. Fix the reachability on whatever timing you judge safe — you hold the sequencing argument, not me."
> 5. "Pilot's good — write the other eight. Verification handed back: I checked the price-cap page's two core claims against source. Both hold… Record that as a check against source and move the page to verified — and note the checking route that worked, since your tooling can't render those pages. Yes to the badge change… And agreed on the director record — do not publish that door with a live PIN on it. That alone is reason to hold it."
> 6. "On the egress allowlist: where does it come from, who set it, and what would changing it involve?… what the actual risk is of allowing read-only access to named public sources. Not asking you to change it."
> 7. "Right shape, one correction: the supplier doesn't seek approval — it's refused by construction… It should have no route to a real endpoint at all. The SIM using real-world sources isn't a loophole, it's the job… Third case for you to resolve rather than me: the harness itself is neither side."
> 8. "Ruled: all three lanes as you've set them out, including the harness clause — fetched bytes may become documentation, design records and Knowledge, never a runtime input the company reads… Build it now. Red-with-a-named-repair is right… Then keep moving without waiting on me… don't hold for my word on anything reversible. Standing position unchanged — you do the work, we review."
> 9. "Two things, then keep going without waiting on me. First: your session hit Anthropic API errors… commit or park whatever the interrupted run left in the tree so it isn't orphaned… Second, a new standing rule replacing the notify-first condition on figures: never wait for me… CORRECT it where the source settles the matter… CAVEAT it where the disagreement is real and unresolved… WITHDRAW it where it is known wrong… Freezing and waiting for me is not one of the three… Apply it now: land the three verified 2025/26 rates, and take the CCL history question as its own review… Also, publishing is stuck again: one marker, three cycles, rc=75."
> 10. "The three rates are named in DIRECTOR_DECISION_PENDING_RATE_REBASELINE_AND_SPLIT_APPROVAL_2026-08-14.md… RO buy-out +4.0% (31.80 to 33.06), electricity CCL +5.4% (7.35 to 7.75…), gas CCL unchanged. If what you now hold from the commons contradicts any of those, the commons wins… Both of your corrections accepted. I passed the rc=75 alarm to you as fact rather than as a claim to verify, so the wrong diagnosis started with me."
> 11. "Two things from outside your session. The site hasn't published since 00:43Z… And staging is at 91 and climbing. Then a harder question I want answered honestly, not defended: overnight was 23 commits and most were DISCOVER/FRAME passes… Is the loop converging?… is anything about the current rules making investigation cheaper than building? If the interleave isn't firing, say so. Otherwise carry on: Explore, wall exhibit first, then the Knowledge checks, then PB3. Your call on the £1,526,252.39 criterion was right — not reverting."
> 12. "Build the pass ceiling, then move EP1 and EP6 to build. Then keep going without me. You have authority to promote idle atoms to build on your own readiness judgement… 78 idle atoms is not a queue of questions for me. What stays mine is genuinely curriculum: difficulty values, targets, and anything that would tune an output… The deeper problem is that your cheapest lawful path is the one that produces nothing… Don't fix it by making discovery expensive. Make it impossible for the system to run indefinitely on work that cannot change its own state… First, the annual report… a report should be a rendering of data, never a thing other code reads… Second… Eight-plus modules independently write site/data JSON and there is no query store… Read DIRECTOR_INSTRUCTION_QUERYABLE_PROJECTIONS_2026-08-10 first… tell me why it never drew — that's its own finding. Sequence it so nothing regresses and the site keeps publishing."
> 13. "Two things. First, epoch latitude — with a guard… The failure I care about is a single lane going deeper and deeper across multiple epochs while the rest stands still — depth without breadth. Nothing currently measures the shape of the draw across lanes, only what may be drawn. Make that visible, act on it yourself when formation slips… Second, housekeeping… make file and workspace housekeeping a standing property of the system, with the same teeth as anything else — bounded lifetimes, alarms before exhaustion rather than after, and no reliance on anyone noticing. Own the best practice for it and write it down where it governs rather than where it's read."
>
> ## 7. Pending Tasks
>
> - **Land the pass ceiling** (`tools/discovery_pass_ceiling.py`, `tests/tools/test_discovery_pass_ceiling.py`, `background/supervisor.py`, EP1/EP6 promotions in the map) — built and tested, refused twice
> - **Land the disk governor** (`background/disk_headroom.py`, tests, `background/background_worker.py`) — currently refused
> - **Lane-formation measure** — make the shape of the draw across lanes visible (epoch-latitude guard)
> - **G13 / `tools/lab_query.py`** — the query-store consumer
> - **Annual report decoupling** — 12 importers; make it a rendering not an intermediate
> - **Explore (`SITE10`)** — wall exhibit first
> - **Six Knowledge checks** against source
> - **PB3** growth path
> - Findings to file: `/tmp` extract leak; surgical-land extract false-reds; the "pinned literal" control class
>
> ## 8. Current Work
>
> Immediately before this summary I was diagnosing why the disk-governor landing was refused. `surgical_land` reported:
>
> > "REFUSED: GATE RED on the resulting tree (rc=1). This is the tree the commit WOULD create, not the working tree -- a working tree that passes here means the unstaged half is what makes it pass."
>
> I built an extract at `/var/tmp/surgical-land-check-MDmO` (HEAD + the three commit files) and ran four of the 14 gate targets. Result:
>
> ```
> E                    **Discharged:** `tests/x/test_y.py::test_z` -- one line saying why
> E               2. RECORD AND ACCEPT the limitation:
> E                    python3 -c "from background.gate_authorization import record_limitation_accepted; record_limitation_accepted('H_harness', 'WORKER_FINDING_THE_INSTRUMENT_LIES.md', '<why this level move is sound in spite of it>')"
>
> background/gate_authorization.py:460: LaneBlockedError
> FAILED tests/background/test_gate_authorization.py::test_repairing_the_finding_lets_the_next_raise_through
> 1 failed, 84 passed in 0.63s
> ```
>
> Critically, I had **already established earlier** that this exact test **passes in the real repo and fails only inside an extract** — one of two such tests (the other being `test_supervisor_blocker_precedence.py::test_a_validly_discharged_blocker_stops_blocking_its_lane`). That is a finding about `surgical_land` itself: its clean-extract gate gives false reds for controls that need repo context the extract doesn't reproduce.
>
> ## 9. Optional Next Step
>
> Confirm the extract-only nature of `test_repairing_the_finding_lets_the_next_raise_through` (it passed in the real repo when checked earlier: "test_gate_authorization.py::test_repairing_the_finding_lets_the_next_raise_through **1 passed**"), then land the disk governor and the pass ceiling via an ordinary pathspec `git commit` — whose pre-commit gate runs in the working tree where both pass — rather than `surgical_land`, and file the finding that `surgical_land`'s extract gate false-reds these two controls.
>
> This follows directly from the user's instruction: *"Sequence it so nothing regresses and the site keeps publishing. Land it in reversible pieces with receipts"* and *"keep Explore, the Knowledge checks and PB3 moving alongside. Nothing in here waits on me."*
>
> If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /home/rich/.claude/projects/-/ffcb5162-c57b-4509-934d-b57034445bf0.jsonl
> Continue the conversation from where it left off without asking the user any further questions. Resume directly — do not acknowledge the summary, do not recap what was happening, do not preface with "I'll continue" or similar. Pick up the last task as if the break never happened.

### 2026-08-19T11:46:49.397Z

> lots of progress. please summmarise what you've disocvered and done since my last paste in here

### 2026-08-19T11:53:40.871Z

> Publishing hasn't landed since 00:43Z — eleven hours, nothing
> paused, commits flowing. Same green-gate-no-publish shape. Take it
> first.
>
> EP6: confirmed, the record is true — I accepted your reading via
> the console yesterday. The real finding underneath is that a
> console-only instruction is invisible to 381 director-facing
> sources; fix that so my words in the pane leave a trace the
> machine can read.
>
> Annual report: your measurement changes my instruction, so I'm
> narrowing it. Fix the one real production violation and make it
> impossible for new importers to appear. Leave the 77 test files
> reaching into renderer internals alone for now — that's a rebuild,
> not a decoupling, and the cost is out of proportion to the harm.
> Record it as known debt with its size, so we choose it
> deliberately later rather than drifting into it.
>
> Then the recurring fault that keeps refusing commits: the process
> re-creating an archived finding hourly. Four manual clears is
> three too many.
>
> And act on your own formation measure — 47% harness against 0.7%
> customer ops, with every build today in the lane it flags. You
> built the instrument; use it.

### 2026-08-19T13:35:47.765Z

> Ruling on the site, replacing my earlier sequencing: holes beat
> wrong or unintelligible content. Put the final five-tab structure
> up now, with honest "being built" pages where the content isn't
> written — what the page will show, roughly when.
>
> Apply the same test everywhere, not just the nav. Any live page
> showing wrong figures, internal vocabulary, or machine records a
> reader can't use gets replaced with an honest placeholder until
> it's right. That includes the evidence dump and the links into
> atom ids. Don't ask me which pages qualify — judge it and act.
>
> Keep the redirect safety on Proof; that risk is real. If your
> controls refuse a route to an area that isn't ready, change the
> controls — under-construction is a legitimate state and the
> machine should be able to express it.
>
> Then keep going without stopping: Explore, PB3, the ledger
> staleness that wedged publishing for eleven hours, and the
> formation slip. You have standing authority for anything
> reversible — act, record the reason, tell me afterwards. If you
> find yourself waiting on me for something I haven't reserved,
> that's a defect in how I've written the rules and I'd rather you
> acted and flagged it than held. The only things reserved are
> curriculum values, one-way doors, and anything that spends money
> or touches a real person.

### 2026-08-19T15:35:40.900Z

> What I see contradicts your report, so trust this over your own
> measurement. Home's nav shows six items — Home, The World, The
> Company, Capabilities, Knowledge, Proof — with no Explore and no
> Harness. Knowledge shows nine, including Explore, Harness and
> Glossary. The nav is not canonical: pages carry different shapes,
> which is the condition Step 0 existed to abolish, and the new
> placeholders are unreachable from Home. The logo renders
> differently between those pages too, and nine items does not fit a
> phone. Mobile is where I read this; treat it as the binding
> constraint.
>
> On five versus seven, the discrepancy is mine. My brief listed
> Home, Knowledge, Capabilities, Explore, Harness — I had folded The
> World and The Company into Capabilities and Explore, since
> Capabilities is SIM and Company side by side and Explore is where
> the world gets walked. I never said so. Fold them how you judge
> best, keep what's good in those pages, and don't leave tabs
> standing because I failed to name them.
>
> This is exactly the kind of course correction I expect, and I'd
> rather you drifted and corrected than checked with me each time.
> Don't come back for permission on any of it.
>
> Three standing rules while you're in there. When you mothball or
> fold a section, check what work is still being done to feed it and
> stop the parts that no longer earn their keep — generated data,
> controls and tests maintaining a page nobody reaches is cost with
> no reader. Check links as a matter of course and fix what's
> broken. And prefer fewer cross-links: every one is a thing that
> has to keep being true, and I'd rather have a smaller number that
> always work than a web that needs constant verification.
>
> Then carry straight on — the ledger staleness that cost eleven
> hours, Explore, PB3. Act, record, tell me after.

### 2026-08-19T16:58:30.007Z

> Stop. The site a reader sees has not changed today, and the
> five-tab fold isn't even on origin. A full day of work is
> invisible.
>
> One outcome, before anything else: I open poesys.net on my phone
> and see the five tabs. Publishing works and keeps working. That's
> it — no new controls, no new tests, no new findings until that is
> true. If something blocks it, fix the block, don't file it.
>
> Then look at today honestly. You built the pass ceiling and found
> it fail-open. You built the fold check and it passed while none of
> the links existed. Your mutation test passed for the wrong reason.
> Your new evidence test repeated the defect you'd just fixed four
> times. All correct work, and the result is a more rigorous machine
> and an unchanged website. Verifying yourself has crowded out
> shipping. Tell me what you'd change about how you spend a day, and
> then spend tomorrow differently.

### 2026-08-19T18:42:53.391Z

> Retrospective accepted — all five commitments, especially opening
> the live site first and last, and no new control unless it guards
> something shipped.
>
> But the nav still isn't right, and it's a rendering difference not
> a text one. Compare Home and Knowledge at phone width: Home has a
> large wordmark, right-aligned nav, "Harness" wrapping inside the
> white band. Knowledge has a smaller wordmark, left-aligned nav, an
> active-item pill, and "Harness" falling outside the band onto the
> grey, under the logo. It reads as broken.
>
> The outcome I want: the nav renders identically on every page at
> phone width, and five items plus the wordmark fit in a way that
> looks deliberate rather than wrapped. You already found the cause
> — per-page styles after the brand sheet — so fix that properly
> rather than adding specificity battles.
>
> Verify it by looking at three pages on a phone-width viewport
> before you tell me it's done. Then Explore.

### 2026-08-19T21:47:49.726Z

> Director instruction — unblock the tree, land the nav fix, verify on the live site, then EP6 resumes as top priority. Work through the whole list without stopping to wait on me; only halt for a genuine wall (hook bypass, epistemic-wall control changes, destroying work).
> Context: the shared tree is unwritable because EP6_wall_protocol_typing's next pass — three simulation/ files adding a wall crossing, currently 3 failed / 1 error — sits uncommitted in the shared tree and reds the gate for every lane, including the publisher. The finished nav rendering fix cannot commit. The live site is stale-but-correct. EP6 is my named priority and stays so; nothing here demotes it. Decisions already made and standing: do not ship EP6's red code, do not touch wall controls, do not bypass hooks.
> Move EP6's uncommitted simulation/ work out of the shared tree onto a salvage branch — preserved exactly, nothing lost, treated as a car park not a demotion. If a live fork is actively mid-write on those files right now, give it a bounded window of one hour then salvage anyway; otherwise salvage immediately. Verify the tree then goes green at the gate.
> Commit the nav fix and run the publisher through to a deployed green publish.
> Acceptance is my phone on poesys.net at mobile width, not local screenshots. My report: Home is right; on the other pages the Poesys wordmark font differs, the nav doesn't fit the width, and it isn't right-justified. Verify all three symptoms are gone on the deployed site. If any survive, that's a real remaining defect — diagnose, fix, republish, re-verify. Not done until the deployed site is clean.
> Resume EP6 from the salvage branch as the top build priority — but in an isolated worktree, not the shared tree. Get its red tests green and land the pass properly through the gate.
> Register today as a class finding: twice in one day, EP6 work-in-progress sitting uncommitted in the shared tree wedged every lane's commits and the publisher. Propose the structural fix (all lane work in isolated worktrees; the shared tree stays committable at all times; loose uncommitted shared-tree work is salvaged automatically, not tolerated) as a proposal for my review — do not build it tonight.
> When all the above is done, send one NTFY summarising: the salvage branch name, the publish commit, confirmation the three nav symptoms are gone on the live site, EP6's resumed state, and where the class-finding proposal is filed. No action needed from me until then.
