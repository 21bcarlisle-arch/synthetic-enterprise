[PROJECT] Weekend acceleration -- Rich's directive: burn the remaining budget harder until Monday 04:00 reset. 44% used, 56% available, ~60 hours.

AUTHORIZATION: work continuously through the weekend at increased intensity. Target consuming up to ~90% of the weekly budget by Monday 04:00 (leave ~10% headroom for Monday morning steering). Idle time is the enemy, not token spend.

HOW TO BURN PRODUCTIVELY (all three, simultaneously):
1. TIERED MODEL EFFECT: apply TIERED_APPROVAL_MODEL immediately -- everything below is hereby Tier 2 (pre-approved). No 4h windows on any of it. Start QD (emergent bad debt) NOW if its window hasn't already released.
2. DEEPER PHASES: raise per-phase ambition. Multi-module phases with real integration work are preferred over 20-test increments. Keep ALL quality gates (epistemic verifier, full test pass, consumer-verified artifacts) -- speed comes from scope and continuity, never from skipping gates.
3. NEVER RUN DRY: work the queue below in order; if genuinely blocked on one item, advance the next rather than idling or inventing padding. Coverage-sprint-style filler remains barred (Rule 0a) -- burn must land on named work.

THE WEEKEND QUEUE (Tier 2, in order):
Q1. QD emergent bad debt -- arrears cases from billing_ledger aging into provisions and write-offs; dunning cycle (reminder -> notice -> payment plan offer -> write-off); bad debt emerges from missed payments, never injected.
Q2. WEBSITE PART B -- the professional design system across all four sections + customer portal (per WEBSITE_INTEGRITY_AND_DESIGN). Include the consistency gate implementation; add the known gross-margin discrepancy (totals 6,452,602 vs narrative 6,467,309 on shadow overview) as a gate test case.
Q3. ADVISOR_GITHUBIO_MIRROR -- shadow pages + state JSONs on the github.io host (staged, may already be actioned; verify then close).
Q4. CHURN MODEL VALIDATION LOOP -- rerun NK recall/precision/F1 with QA+QB fixes in place; report before/after on the full run; iterate on remaining FN/FP clusters (this is P1 continuing -- the QB market signal should move recall measurably; prove it).
Q5. CUSTOMER PORTAL PER-FUEL DEPTH -- C4/C4g as fully separate legs (consumption, tariff, invoices, P&L per fuel) with combined as roll-up only; invoice + payment history from billing_ledger.
Q6. SHADOW-LIVE HARDENING -- daily decision log persistence + shadow-live index page; timestamped decision track record accumulating.
Q7. (if Q1-Q6 exhausted) Scenario suite extension on the equivalence-gated generator: multi-year synthetic crisis replays feeding the board scenario section.

NTFY cadence: per-phase completion NTFYs as normal (transitions only). Include budget-burn estimate in each ("~X% weekly consumed"). If budget hits 90% before Monday 04:00, stop new phases, finish in-flight, NTFY final status.

<!-- ARCHIVED 2026-07-05 (superseded, all done): Q1 emergent bad debt -> Phase QD.
     Q2 website design system -> Phase QO (+ QC/QF consistency gate; gross-margin
     discrepancy fixed as part of QF). Q3 github.io mirror -> Phase QG (verified).
     Q4 churn validation loop rerun -> PRIORITIES.md "Churn model validation loop
     rerun" entry (2026-07-04), root cause confirmed (passive cap structurally
     below RETENTION_THRESHOLD regardless of market multiplier). Q5 customer
     portal per-fuel depth -> Phase QN. Q6 shadow-live hardening -> Phase QE.
     Q7 (conditional backlog item) not reached / not needed -- superseded by the
     current design-wave directives (SIM/SUPPLIER/PROJECT_TAB_OVERHAUL,
     CUSTOMER_360_REDESIGN, WEBSITE_AS_SHOWCASE, NAV_STORY_PLATFORM_METHOD). -->
