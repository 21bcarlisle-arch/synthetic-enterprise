# [DIRECTOR-RULING] — SSP baseline recalibration HELD. The real fix is merit-order reconstruction, not coefficient recalibration. (2026-07-25)

**Type:** [DIRECTOR-RULING] via advisor bridge. Answers the director-reserved wall on `PLANNER_MINTED_ssp_negative_lift_cells` part (a).

## The ruling: part (a) is HELD — not authorized, not deferred-by-priority

The director's reasoning, verbatim: *"I really want pricing to be right in the end, not just look right now."*

The advisor recommended proceeding; **the director's call overrides it and the reasoning is recorded as the better one.** The deferral is no longer a queue-position matter (the spike-tail that outranked it closed on 2026-07-23) — it is a **rejection of the proposed fix's shape.**

**The evidence supporting the hold, from the mint's own tests:** Test B's *local per-year* refit still leaves 2020, 2021-crisis and 2025 negative. If stale coefficients were the whole problem, a local refit would close every cell. It does not. That residual is a **form limit** — evidence the structural story is wrong in calm regimes, not merely mis-parameterised. A regime-aware A0/A1 recalibration would lower MAE while leaving the wrongness intact: the per-cell table would look better and the price engine would be no more *formed* than before. Under R12 that is the shape of a goal-sought improvement even when each step is defensible.

## What the real fix is (named now, so the hold has a destination)

**Board Spec 004's reconstructibility test governs:** *power must be substantially reconstructible from gas, carbon, demand and wind on ordinary days — or it was not formed, only generated.* In calm GB hours the marginal plant is a gas plant and price approximates gas-over-efficiency plus carbon. A residual-demand scarcity term should earn its structure in **tight** hours; it should not be carrying ordinary ones. This is Spec 001's gas-first finding (F1) arriving from a second, independent direction — record the convergence.

**Therefore:** the SSP negative-lift close is re-scoped to follow the **merit-order / gas-first reconstruction** of the price engine, sequenced with the VALUE_CHAIN work and the Spec 004 reconciliation. When that lands, re-measure the same per-cell lift table against the same naive baseline. **That measurement — unchanged, unmoved, and honest — is the test of whether pricing became right rather than tuned.** Do not re-baseline the benchmark to suit the new engine.

**No interim tuning.** No per-cell fits, no regime-partition coefficient passes, no "temporary" recalibration while the structural work waits. R12 unchanged.

## What PROCEEDS now: part (b) only

Register the **named simplification (R10)** with its measured bound — the residual-demand scarcity model demonstrably earns its keep in scarcity regimes and loses to a 3-feature OLS baseline in calm cells — and enforce the **headline discipline**: the +1.17 full-window aggregate lift may not be stated anywhere without the per-cell truth beside it, since it is carried almost entirely by 2022. This is honest bookkeeping, not a fix; it stops a misleading claim standing while the real work waits.

## Mint status

The mint stays OPEN (rungs-empty premise remains FALSE). Part (a)'s draw-marker stays **blocked**, with its unblock condition rewritten: no longer "a director-priority R13 pass" but **"the merit-order reconstruction has landed."** Part (b) is drawable now.

**Risk & proportionality:** a registration and a publication rule; no engine change; the hold prevents a change that would have been hard to unwind once downstream organs re-baselined against it. Tag: **part (b) proceed; part (a) held pending merit-order work.**

— Advisor bridge, carrying the director's ruling — and recording that the director's judgment corrected the advisor's recommendation here. 2026-07-25.
