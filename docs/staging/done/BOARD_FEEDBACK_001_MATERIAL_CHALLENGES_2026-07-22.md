# BOARD FEEDBACK on Reconciliation 001 — four MATERIAL CHALLENGES (VERBATIM + instruction, 2026-07-22)

**Type:** [ANCHOR + INSTRUCTION]. The board worked the 001 reconciliation cold and returned the verdict below **verbatim**. Each material challenge requires a written answer: **accept-with-action or reject-with-reasons.** The answers form a document the director carries back to the board (Movement 3 continues). Actions route through existing gates (VALUE_CHAIN propose-then-proceed; nothing builds on sight of this doc).

---

## The board's verdict, verbatim

> Worked cold. The short version:
> Accepted with commendation — the verdicts are code-anchored, the Executive corrected its own FRAME, and the two FAILs include marking its own flagship cost-centre law as unenforced-in-code. Items 3, 7, and 11 accepted as demonstrated; the four findings noted as proposals to the Executive's own gate, where they belong.
> One caution — "MET-in-plan" warmth in §§4–6 must not read as achievement. The §7 tally (4/6/2) is the true state and the baseline the next sitting measures against.
> Four material challenges, each requiring accept-with-action or reject-with-reasons:
> **MC-1** is the sharpest: item 6's surviving half rests on CRISIS_PASSIVE_YEARS={"2022"} — a hardcoded calendar year. That's scripted history, not a mechanism: if the sim generates a novel spike in a year not on the list, does the book swell? The same question goes to item 7's £9,999 crisis SSP. Every battery behaviour must be classified mechanism-or-script, and script-based verdicts re-graded.
> **MC-2** converts F2 into a falsifiable test: the collateral loop counts as wired only when a price move alone produces a cash call, and a 2021–22 replay shows at least one path to death-by-collateral while the P&L still survives.
> **MC-3** holds item 5's PASS provisional — the joint tail is proven to exist, not to be the right size — and flags that this reconciliation never mentions the declared 10× spike-tail defect, whose coexistence with the £9,999 figure needs explaining.
> **MC-4** turns the Executive's own consumers-first question back on it: what decision consumes the deep gas plumbing today, given retail gas hedges nothing into it?
> The Board reads items 5 and 6 as ungradeble-in-final until the 002 and 004 reconciliations land — the three are one system, and the defect the Board most expects is consistent across them: mechanisms present, correlations and tails under-sized.

---

## Instruction

**MC-1 — accept as a CLASS discipline, not an instance fix (advisor's read; yours to confirm or argue).** The mechanism-or-script distinction is R10-shaped and epoch-critical: the evolutionary tournament (epoch 4) reruns lives against *generated* worlds, where novel crises must trigger correct behaviour — scripted calendar years cannot. Proposed action: (a) classify **every** §7 battery behaviour as `mechanism | script | calibrated-parameter`, publish the classification, re-grade any PASS/PARTIAL that rests on script; (b) register mechanism-or-script as a standing fidelity rule (scripts may exist as calibration scaffolding but can never ground a credibility verdict); (c) for item 6 specifically: derive book-swell from market state (spike magnitude/persistence vs the customer's deal), not from a year list — propose via the gate.

**MC-2 — accept; it is a ready-made R15 spec.** The collateral loop is "wired" only when: a price move alone produces a cash call (no hand-supplied loss), and a 2021–22 replay demonstrates ≥1 death-by-collateral path with P&L still positive. Build the test first, then the loop, via propose-then-proceed.

**MC-3 — answer the coexistence question explicitly and honestly.** The advisor's hypothesis, for you to verify or correct: the £9,999 is a *scripted crisis cash-out parameter* in the imbalance module (MC-1's point again), while the 10× spike-tail defect describes the *generated price engine's* under-drawn tail — two different organs, one of which the reconciliation should have cited and did not. State which organ produces what, why the reconciliation omitted the declared defect (R9 owed), and fold joint-tail *sizing* (not just existence) into the 002/004 reconciliations as the board directs.

**MC-4 — answer with the consumers-first audit.** Name what, if anything, consumes the gas plumbing today. If the honest answer is "nothing" — deep capability without a consumer — say so plainly and tie the fix to F1 (retail gas actively hedged INTO the existing plumbing), which gives the plumbing its consumer and answers the challenge in one move.

**Also record:** the caution (MET-in-plan ≠ achievement; the 4/6/2 tally is the baseline the next sitting measures against) goes into the reconciliation doc as a standing note, and items 5/6 stay provisionally graded until 002/004 reconciliations land — sequence those reconciliations accordingly.

**Output:** one answers document (accept-with-action / reject-with-reasons per MC, with anchors), written for the board, staged for the director to carry back.

**Risk & proportionality:** answers doc = analysis, reversible. MC-1's re-grade may lower the tally — that is the point, honest reds credited. All builds route via the gate. Tag: **narrow/reversible for the answers; contract-touching for any mechanism replacement — propose first.**

— Advisor, carrying the board's challenges and the director's process, 2026-07-22.
