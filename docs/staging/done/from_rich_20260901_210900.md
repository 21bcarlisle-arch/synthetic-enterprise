<!-- Dispatcher: URGENT (classified 2026-09-01 21:09 UTC) -->
# Inbound NTFY message from Rich

Two housekeeping things, neither urgent.

Six undeclared worktrees are accreting and being reported rather
than cleared — that's the isolation machinery working with nothing
tidying up behind it. Give them a lifetime.

And the channel under-reports you. Eight commits this evening
produced no message, while divergence and publishing alarms filled
the mirror. I've read that as a stall twice today when you were
working normally. Real work should reach the channel and routine
noise shouldn't crowd it out.

---

## DISPOSITION 2026-09-02 — both items delivered, verified on disk before archiving

Landed as `580c47101` *"give the worktrees a lifetime, and let the channel carry what got done"*,
which is an ancestor of HEAD. Verified here by reading the live state, not the commit message.

**Worktrees — given a lifetime.** `git worktree list` now holds two entries: the main tree
(refused by construction) and `/var/tmp/se-seat-executor`, the declared daemon home that
`declared_daemon_homes()` deliberately spares. Six down to the irreducible one. Three independent
causes were found and all three were fixed, which is why "reported rather than cleared" was the
literal architecture: `evaluate_worktree_reap` had had **no caller since 2026-07-18** while its
arming flag was set — a reaper that cannot run reads as one running and finding nothing; an
ownership claim never expired, and five markers named pid 215, the *tmux server*, so five
worktrees read as held by a live writer a day after their writers had gone (`OWNER_LEASE_SECONDS`
now derives a lease from the longest legitimate hold rather than trusting a bare pid); and every
refusal was a dead end, with `salvage_detached_head` written as the documented door out of
"detached ORPHAN" and never called. Every scrap is preserved under six `salvage/detached-*` tags —
salvage only ever ADDS a ref, which is why it can be automatic where reaping cannot.

**The channel — measured before it was changed.** 64 messages that day, 27 of them one divergence
condition; 19 commits landed; **zero** landing messages. Your own category `routine_landing` had
had no producer in three weeks and *could not* have one — every member of `KINDS` was a thing that
went wrong, a batch of those, a reply, or a fixture. `work_done` was added with a producer at the
one door every landing passes through. It is firing: the queue now carries `[LANDED]` lines for
`0e19a0e4c`, `3f2ffd0aa`, `cd0dc6eb1` and `fafb5a59a`.

**One thing to know, because you would otherwise read it as unrelated.** Wiring the reaper is what
wedged publishing for the next 80 minutes. Adding `_check_worktree_reap()` to `run_cycle()` without
moving the fixture's neutralise-list turned `test_dms_isolated_accounts_for_every_check_run_cycle_calls`
red, and that single test blocked every publish. The class guard caught it within one commit and it
was fixed in `cd0dc6eb1`. It also found something worse than a red: those 12 `run_cycle()` calls were
running the reaper **for real and armed**, because the enforce flag is an absolute path the fixture's
patch does not reach. No worktree was reaped by a test and no tag was written by one — measured, not
assumed — but the hazard was live. Your housekeeping request paid for itself twice.

Archived because both items are delivered and verified. The publish gate is green again at HEAD.
