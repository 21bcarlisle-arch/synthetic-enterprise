<!-- Dispatcher: URGENT (classified 2026-08-12 16:51 UTC) -->
# Inbound NTFY message from Rich

On the escalation channel: keep ntfy, stay on the free tier, cut
the volume to fit it. Batch and summarise everything that isn't
action-needed into periodic digests — divergence, drift, routine
landings, finding announcements. Instant sends reserved for things
I actually need to act on or know immediately: action-needed
alarms, blocked work, a decision waiting on me, publishing down.

Two requirements: nothing gets lost by being batched — a digested
item is still findable — and a dropped or rate-limited send can
never be recorded as sent.

---
## DISPOSITION (2026-08-12, worker tick) — BUILT, commit dea39a3bf (pushed)

Routing built inside `background/notify.py` (the one contract), not beside it.
`background/notification_digest.py` + `tests/background/test_notification_digest.py`.

* **Instant set is CLOSED** and is your four classes: action_needed, blocked_work,
  decision_waiting, publishing_down. Unclassified = INSTANT, so this cannot silence any
  of the 31 existing alarms nobody has classified yet; the cut is opt-in per call site.
* **Nothing lost by being batched** — the queue is append-only and never rewritten;
  "digested" is a high-water mark in a separate file. A digest that elides says so and
  names the file holding every line. `docs/observability/ntfy_digest_queue.jsonl`.
* **A dropped/rate-limited send is never recorded as sent** — a deferred item returns
  `deferred:<seq>`, never an id; a flush advances the mark ONLY on a confirmed delivery,
  so a 429 leaves every item pending for the next digest.

R15: three mutations (delivery fails open / flush consumes its queue / routing fails
open) each fire on their own named defect. 32/32 on the new file, 134/134 with the
notify-contract, deadman and health-check suites.

Migrated so far: `[TREE DIVERGENCE]`, `[HEALTH CHECK] Stack OK`. Remaining 29 real_alarm
sites keep today's behaviour until classified — that is the follow-on queue, not a gap.

OPEN, recorded not silent: `process_run_complete.maybe_ntfy` currently DROPS routine
sim-run completions rather than batching them (a standing CLAUDE.md rule that predates
this steer). Dropping conflicts with "nothing gets lost"; batching them conflicts with
"do not NTFY routine run completions". Left as-is and flagged rather than resolved
unilaterally, since the two rules are both yours.
