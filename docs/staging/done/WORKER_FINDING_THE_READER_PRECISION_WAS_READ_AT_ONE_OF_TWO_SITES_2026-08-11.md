# WORKER FINDING — the reader's precision was read at one of this figure's two render sites

**Severity:** LATENT · **Lane:** H_harness

**Found:** 2026-08-11, H27 Expert Hour #17 (worker tick, `H27_payment_belief_gap` 2→3 HARDEN draw)
**Class:** a control escaped by its own keying · **Disposition:** mechanism landed, reshape MINTED (D35)
**Answer to the draw:** still **L2**. Seventeen Hours, seventeen defects.

## What Hour #16 left, and what happened when it was pulled

Hour #16's lead 2: *"The precision is read; the ROUNDING before it is not. Two quantisations sit
between the scorer and the reader and only the outer one is measured."* Pulling on it found the
layer **above** it first.

`PUBLISHED_GAP_CONSUMERS` (atom D34, one Hour old) reads each published figure's reader precision
off the format spec in **one named function** per figure. The ageing headline reaches its reader
through **two**:

| site | renders | precision |
|---|---|---|
| `background/gap_metric.py::format_ageing_summary` | `balanced_bucket_displacement` | `.3f` |
| `background/gap_metric.py::_ageing_direction_note` | the same component | **`.6f`** |

The second one writes into the published component `ordinal_direction_caveat` — and a component
is precisely what Hour #15 established consumers read (*"the ledger writer, the live wiring and the
dashboard read `components` and never the prose"*). This is not a latent render site; it is the
caveat that travels with the number.

## The measurement

Artefact-side, reading no source at all: score the book and look for each figure's **own value** in
every string the consumer is handed. n=300, seeds 7/11.

```
ageing carrier   0.112963  (seed 7)     0.191535 (seed 11)
found rendered   6dp in `ageing.ordinal_direction_caveat`, on BOTH books
declared         3dp
epsilon          5e-4 declared   vs   5e-7 actual      -- a THOUSANDFOLD
```

Every band, floor and collapse certified for this dimension "at the reader's own precision" was
certified against the wrong step by three orders of magnitude.

**D34's own R15 list names the mutation that should have caught this** — *"one gap rendered at two
precisions"*. It fires only **inside the anchored function**, so the *shipped* two-precision state
was invisible to the control built to detect exactly it. Ninth escape of a control of this module
by its own keying, and the **third Hour running** to find its defect inside the previous Hour's
control.

**A second undeclared site the same sweep found.** `belief_population_mix` renders its own gap at
4dp into its own `.note` — the string the ledger writer puts in front of the Proof door. Same
precision, so no epsilon moved; as invisible to a function-anchored walk as the first.

## Hour #16's lead 2, answered — and it is NEGATIVE

The inner quantisation is **never** coarser than the render on any shipped figure, so the render
precision is not a decoration:

```
ageing              carrier quantised 6dp   render 6dp
detection_latency   carrier quantised 6dp   render 2dp
belief / mix / detection      unquantised (17dp)   render 4dp
```

That measured negative is now **pinned**, so a future coarsening of any carrier fires here. It also
leaves ageing's inner quantum **equal** to its finest render, which is why that figure's
`carrier_is_the_gap` claim (delta 4.1e-7) now sits within a hair of its own epsilon and holds by
construction — the component *is* `round(gap, 6)`.

## What landed (HARDEN)

* **`also_rendered_at` / `component_renders`** on `PUBLISHED_GAP_CONSUMERS`; ageing's
  epsilon-setting precision corrected to the **finest** site (6dp, with 3dp declared beside it) and
  the mix note declared.
* **`measure_component_render_sites`** — the independent half. It reads no source; it scores the
  book and looks for the figure's own value in every string the consumer receives (`components`
  **and** `.note`, because the note is what the ledger publishes). Its **discrimination rule**: a
  literal counts as a render of this figure only if it **changes with the figure** across two books.
  A one-book sweep reports `0.0` — which is in half the strings this module publishes — as this
  figure's reader precision, and would hand every band a 0.05 epsilon.
* **D34's rule reshaped, not dropped.** *"Two precisions → no epsilon, refuse to guess"* becomes
  *"the epsilon is half a step of the FINEST"* — a reader given 6dp somewhere can separate two
  companies at 1e-6 whatever a second site rounds to. The guard **moved onto the declared set**, so
  an undeclared precision still fires by name; a repaired verdict must not leave its guards on the
  old statistic.
* The epsilon refusal message **interpolated from the register** — it had "ageing 3dp" typed into
  it and would have gone on saying so.
* Both this control and D34's **wired into the CLI**, which D34's never was.

## R15 both ways — eight mutations

The pre-Hour register state (the 6dp site undeclared **and** the epsilon set from 3dp); a declared
site the scoring does not produce; an undeclared second precision inside the anchored function; a
declared-but-not-finest epsilon; a carrier quantised coarser than its render; a **single** scoring
(raises — one book cannot tell a render from a constant); an **inert** sweep finding nothing at all
(raises — an unavailable check is a failed check); and the discrimination rule proven to **reject**
a `0.0` that never moves and **accept** the same site once the literal tracks the figure.

## R12

**No published number moved.** All five figures bit-identical at seed 7 — ageing
`0.11296259117981663`, belief `0.1518987341772152`, mix `0.07999999999999997`, detection
`0.014505119453924915`, detection_latency `2.343137`. What moved is one declared epsilon and what
the instrument admits about itself.

## The hold record was one Hour stale when this Hour drew

Hour #16 landed its mechanism, its mint (D34) and its `simplifications` entry, and recorded itself
in `expert_hour_findings` — but **not** in `level_hold_note`, whose closing sentence still read
*"the next promoter runs Hour #16"* with Hour #15's leads. The one record whose whole job is to
answer a 2→3 draw was pointing at work already done, and the self-refill drew this atom again
against a discharged pointer. That is the 2026-08-08 class inverted. R9: checked, not assumed —
Hour #16 *was* recorded in the narrative list, so the Hour was not invisible; the **hold record**
was. Both entries are now written.

## Why still L2

L3 means Expert Hour says "this is real". A figure whose declared reader step is a thousand times
its actual one, certified by a control that could not see the second place the number is printed,
is not there. And this is again the tick that changed the instrument — the reputation-of-the-old-
instrument problem in its purest form. Hour #4's stated-in-advance criterion of **two consecutive
clean Hours** has still not been approached.

## Hour #18 leads, in order

1. **The reshape is D35 and it is unbuilt: the sweep stops at this process's edge.** It sees only
   strings this module's own `score_triad` returns. The same five figures are re-rendered by
   `measure_and_write` into `docs/observability/coupled_gap_ledger.json`, by the dashboard, and by
   the Proof-door page — writers this sweep never runs. Whether any of those renders at a precision
   the epsilon does not know about has **not** been measured.
2. **D34 and D33 are still unbuilt** (the per-figure resolution floors for `detection`,
   `detection_latency` and `ageing`; the bit-equality movement predicate). Four reshape atoms now
   sit at L0 behind this instrument, each minted by an Hour and none built.
3. **Carried forward, still untaken:** the interior collapses have no owner of their own (Hour
   #11's lead 1, five times deferred); and Hour #8's two — the pinned generated value
   `assert c["n_recon_detected_undated"] == 0`, and whether the other dimensions' normalisation
   notes have the same gap between what they DENY and what they ESTABLISH.
