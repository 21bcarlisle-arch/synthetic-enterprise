<!-- Dispatcher: NORMAL (classified 2026-07-16 16:58 UTC) -->
# Inbound NTFY message from Rich

publish_gate_wedged is REAL (rc=1 = test regression, not OOM). Fix it, don't ask me — this is a bug, not a decision. Root-cause the failing test at 7df80b55 (likely the same stale-assertion class as the test_health_check wedge — a test asserting a count/fact the fast-moving daemon has changed), fix root cause, unwedge, confirm the gate clears on next publish. And fold the CLASS into the maintenance lane + scheduled housekeeping: stale test assertions that wedge the pipeline are exactly the "surface tests referencing outdated facts" sweep — this is the second time this class has cost a wedge. Report via NTFY when clear.
