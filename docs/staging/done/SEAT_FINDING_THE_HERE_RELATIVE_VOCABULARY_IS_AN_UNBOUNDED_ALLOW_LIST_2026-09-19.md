# FINDING — an audit is blind along the axis it was cut on

**Severity:** BLOCKING
**Lane:** `H_harness`

BLOCKING is the honest header even though every instance named below is repaired and landed. What
is repaired is the instances; what remains is a live control defect carried forward from the
concurrent lane (`_here_relative_phrase` returns `search()`, so a literal claiming TWO directions
registers only the first) and a detector whose coverage of its own class is still unmeasured on
every axis but the one that was just censused. No published figure is wrong and none moved.

*Filed 2026-09-19 by the delivery seat, claim
`the-here-relative-detectors-noun-list-has-no-run-so-a-live-pointer-is-invisible-to-every-sweep`.
Method and predictions:
`docs/staging/records/SEAT_PREREGISTRATION_WIDENING_THE_HERE_RELATIVE_VOCABULARY_2026-09-19.md`.*

---

## Read this with the other lane's write-up, which landed first

`2957c2cc9` — `WORKER_RESULT_THE_POINTER_VOCABULARY_WAS_LAYOUT_FURNITURE_AND_THIS_SITE_POINTS_WITH
_UNITS_OF_ITS_OWN_RECORD_2026-09-19.md` — is the same drawn item, worked independently and landed
while this turn was gating. **Its noun audit is the better one and the merge takes it whole**:
16,083 strings censused, fourteen nouns added, and three candidates refused *with their
counter-examples on the record* (`reading`, `line`, `range`/`interval`). It also establishes the
admission rule this lane only groped at — *a noun earns its place by having NO comparison sense in
the live corpus*. Where the two turns repaired the same sentence, the merge keeps theirs.

**Saying plainly what that makes of this lane's own proposal.** This turn predicted the noun list
could not be closed and proposed a *partition* — every live `the <noun> above` either registered or
on an argued exempt list. The other lane answered the same problem better and more cheaply with a
curation rule plus published counter-examples. The partition is **not** the recommended remedy any
more, and this document no longer asks for it.

## What survives as this turn's contribution, and it is not a longer word list

**The participle axis, which the noun audit could not have reached.** `_HERE_RELATIVE` matches a
pointer two ways: a noun before `above|below`, and a *participle* before it (`shown|listed|set out|
stated|named|given|described|printed|quoted`). `published` was missing from those nine — and
`published above|below` was live **eight times in `tools/generate_value_arms_data.py` alone**. The
other lane's census was `<noun> above|below` by construction, so **no run of it could ever have
returned a participle**, however many strings it swept. Both censuses were sound; each was blind
along the axis it was cut on.

**And one of the eight was not merely unjudged — it was a live instance of the parent defect.**
`_staleness_caveat` published *"re-running the noise floor on the run published above is owed
work"*, and that string renders in **two** regions, `/capabilities/#arms-errorbar` **and**
`/capabilities/#arms-headline`. A reader meeting it in the headline is sent somewhere the reader
meeting it in the error-bar block is not. That is precisely the two-home shape this whole family of
controls exists to catch; it was on the deployed page; all four sweeps passed it.

All eight repaired by naming the subject — the rung's own precedent — and the feed rebuilt from the
merged producer so the page carries it.

## The generalisable half

**An audit is blind along the axis it was cut on, and reports that blindness as coverage.** Three
instances of one shape, all in this subject, within about twenty-four hours:

1. `dc92d2d65` repaired the pointers in `_population_repair_bias`'s clause, verified against a
   census this vocabulary built, and left a second pointer **in the same sentence** — because the
   detector could not see `run`.
2. `2957c2cc9` censused 16,083 strings for `<noun> above|below` and could not, by the shape of the
   query, see a participle.
3. This turn censused both axes and has **no claim at all** about pointers worded with neither —
   "opposite the third column", "two panels back", anything positional this vocabulary has no
   branch for.

**A repair verified by a detector inherits that detector's blind spot silently, and the flattering
reading is always available: the string is now clean.** This is not specific to prose detectors —
it is the shape of any audit whose query fixes a dimension.

**What to do about it next**, concretely, and it is cheaper than another word list: before adding
words to a vocabulary, ask what SHAPE of the query is unexamined. Here that is one question —
*which grammatical forms can carry a direction?* — and the answer is a short, finite list (noun
phrase, participle phrase, bare adverbial, prepositional) against which the current detector covers
two.

## Carried forward from the other lane, not fixed here

`_here_relative_phrase` returns `re.search()` — **the first match only**. A literal claiming two
directions registers one and the other is invisible to every rung. Live witness on the tree today:
`_current_world_contrast`'s `how_to_read_this` carries *"the figures above … the run above it"*,
and the census registers `figures above` and never sees the second. The remedy is `finditer`, and
`tests/tools/test_the_proof_pages_undriven_pointers.py` already has the `_phrases()`-returns-a-list
shape to copy. Left alone in both turns because that file is bound to a separate live claim
(`churn-truncation-destroys-the-decision-surface-before-the-arm-is-asked`) and its census is read by
every rung. **This is the next piece of work on this subject** and the fourth pointer is the witness
that makes it failable.

## Controls landed with this

`test_MUTATION_the_2026_09_19_widening_can_still_FIRE_on_the_wordings_it_was_added_for`. Both lanes
widened the vocabulary and both repaired every instance they exposed, so on the day it landed the
widening matched nothing on the site and no other leg in the file would have noticed its deletion.
The leg is keyed to the vocabulary, never to today's page, and is mutation-proven against the
merged detector: drop `run` → reds; drop `published` → reds; widen `_LANDMARK` to exempt everything
→ reds; restored → green.
