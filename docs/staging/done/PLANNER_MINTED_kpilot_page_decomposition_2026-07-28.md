> **[DONE 2026-07-28]** Deliverable #3 executed: decomposition proposal at `docs/design/KPILOT_PAGE_DECOMPOSITION_PROPOSAL.md` (hub + 5 Tier-1 children + named stubs; S1-S15 all slotted, none orphaned; reconciled to the live single-page IA). FRAME doc-only. Tier-1/Tier-2 phasing flagged for director latitude. Blocks #4/#5.

# [PLANNER-MINTED] K-pilot deliverable 3 — propose the page decomposition (hub / children / stubs) (2026-07-28)

**Source ruling:** DIRECTOR_RULING_KPILOT_SCOPE_FIRST_2026-07-28.md — deliverable 3 (WORK THIS CREATES); ruling §4.
**Serves:** DIRECTOR_AXES Axis 1 (Website). Campaign: SITE_MODEL_SPINE knowledge lane. Fidelity-ledger row: price-formation (W1_6).
**Real-world fidelity gained:** a coherent information architecture for the price-formation topic — the page currently "tried to be a hub and a full treatment simultaneously and achieved neither" (ruling §4); a reader gets a navigable hub with children scoped so each phenomenon (gas anchor, weather, interconnectors, CfDs, negative prices, seasonality, traded-product structure) lives where it belongs.
**Lane:** FRAME (design/IA proposal, doc-only; no page rewrite in this atom — the rewrite is #4/#5's assembly against the brief).
**Target level:** doc-only proposal in `docs/design/`.
**Exit criteria:**
- A decomposition proposal names: which topic is the HUB, which are CHILDREN (full pages) vs STUBS, and where each block of the current page's content belongs.
- The proposal is argued against the scope brief (#1) — every required section from the brief maps to a hub/child/stub slot; nothing orphaned.
- Director's latitude honoured (ruling §4: "Your call — you can see the material and the graph; the advisor cannot"): if a cut other than hub-and-children is judged better, it is argued explicitly.
- Reconciled against the current `site/knowledge/` graph (existing pages, backlinks, generated sidebar) so the decomposition slots into the live IA, not a greenfield one.
**Deps:** #1 scope brief (needs the full required-scope list to allocate hub/children); informed by #2 (Spec 004 gaps show which children are thin). Blocks #4 (charts land on the decided pages) and #5 (delta published on the hub).
**Propose-then-proceed window:** proceed by default (ruling **proceed**; doc-only IA proposal, reversible). Walls untouched.

## Deliverable (verbatim)
> 3. Proposed decomposition (hub / children / stubs).

Ruling §4: "The page tried to be a hub and a full treatment simultaneously and achieved neither. Propose the decomposition: which topic is the hub, which are children, where this page's content belongs, and which children are stubs versus full pages."
