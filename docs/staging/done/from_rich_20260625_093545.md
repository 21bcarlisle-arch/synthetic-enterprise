<!-- Dispatcher: NORMAL (classified 2026-06-25 09:35 UTC) -->
# Inbound NTFY message from Rich

Add to ~/.bashrc on Skynet: when SSHing in, automatically attach to the claude tmux session. Code: if [ -n "SSH CONNECTION" ] [ minus z "TMUX" ]; then tmux attach -t claude 2>/dev/null || true; fi — This means every time Rich SSHs in he lands directly in the claude session without needing to remember any commands. Also confirm: is CC always started inside tmux and never directly in a bare terminal?
