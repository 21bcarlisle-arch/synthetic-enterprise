<!-- Dispatcher: NORMAL (classified 2026-07-08 09:30 UTC) -->
# Inbound NTFY message from Rich

"Website pass R1 check failed on defect 4: five \u2014 escaped literals remain in site/supplier/index.html. Fix at the generator (escaping path), not the output, and grep all generated pages for \u and &#-style leaks before claiming done, per the staged instruction."

<!-- ACTIONED 2026-07-08: found 22 total \uXXXX JS-source escape occurrences across
     6 static site pages (not just the 5 flagged in site/supplier/index.html --
     site/index.html, site/project/index.html, site/platform/index.html,
     site/method/index.html, site/customers/index.html also had them: £ (£),
     · (middot), ✓/✗ (checkmark/cross), — (em-dash)). All were
     technically valid JS string-literal Unicode escapes (verified via node -e that
     '—' evaluates to a real em-dash) -- not rendering bugs -- but inconsistent
     with the literal-character style the rest of each file already uses (per
     5d0762ee's partial fix, which converted one instance but missed the other 21).
     Converted all 22 to literal UTF-8 characters directly in the source (these are
     static hand-authored SPA files, not Python-generated -- "the generator" here is
     the source file itself). Verified: node --check on the extracted <script> content
     of all 6 files parses clean; each file re-reads as valid UTF-8; grepped every
     *.html under site/ (find + grep -l, not a hand-picked list) for \u[0-9a-fA-F]{4}
     -- zero remain. Also checked for the &amp;#NNNN;-style double-escape leak pattern
     named in the instruction -- none found; the &#NNNN; entities present (£, en-dash,
     em-dash, checkmark, circles, lightning bolt) are legitimate single-escaped HTML
     entities in raw text nodes, not leaks. -->
