<!-- Dispatcher: URGENT (classified 2026-07-16 16:49 UTC) -->
# Inbound NTFY message from Rich

NOT a one-way door — false positive. You pattern-matched "open"+"epoch" and fired values_decision, but the question is opening BUILD on an atom INSIDE the already-open epoch — that's reversible, twin-answerable, and exactly what the open epoch is cleared for. The regex caught the word, not the meaning. Answer: PROCEED, open the build, twin handles it. And this is a perfect test case for the predicate fix you're building right now (#1): "open build on an atom in an open epoch" must produce NO escalation; only "open a NEW/next epoch" is the values door. Add it as a mutation test — a reversible in-epoch build-open that trips the escalation FAILS. Fold it in, then this whole class stops reaching me.
