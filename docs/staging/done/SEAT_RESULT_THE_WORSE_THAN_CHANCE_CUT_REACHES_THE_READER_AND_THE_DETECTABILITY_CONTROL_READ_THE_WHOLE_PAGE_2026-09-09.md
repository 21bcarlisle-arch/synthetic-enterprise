**Severity:** RECORDED · **Lane:** A_strategy_governance · **Epoch:** 3 · **Atom:** (Lane 0 delivery — the-worse-than-chance-cut-must-reach-the-reader-beside-the-cannot-tell-one) · **Class:** figures_on_a_superseded_clock

# RESULT — the worse-than-chance cut reaches the reader, and the control that went red was reading the whole page

The pair is promoted. A reader loading the arms page now meets the adverse half of the four-leg
result **with its own interval, its own p and its own n**, beside the cut that cannot tell — which is
the item's stated done.

| on the page now | figure |
|---|---|
| the estimand — every decision the arm PRICED, in pounds | **0.4210** on **161** decisions across **88** accounts |
| its own null, permuted on its own points | **[0.4458, 0.5540]**, `p = 0.0045`, `inside_the_null: false` |
| the verdict rendered | *reads WORSE than chance … the ranking is real and INVERTED* |
| the survivor cut, unchanged beside it | 0.5338 on 168, inside [0.4494, 0.5504], *we cannot tell* |
| the control row's agreement with the headline | published as **one permutation performed twice, not corroboration** |

## What the promotion cost, which is nothing

The reason the last turn held is that the arms run of `01:24:34Z` was **newer than the floor** of
`23:29:22Z`, so `_staleness_caveat` fired and `contrast_bounds` went unavailable — the headline
would have lost its direction entirely. A floor stamped `2026-09-09T06:57:00Z` had already finished:
same world `39a192ce04c1eda8`, same seeds 11111/22222/33333, same `--redraw-mode all`, and it carries
a clock. So both constants moved in **one commit**, which is the only legal way to move either.

The two floors agree to the last penny — `stdev £2,291.98`, `mean -£426.96`, `min -£3,036.25`,
`max £1,260.93` — produced by two different commits (`ac6433b87` and `4e853a83e`) hours apart. That
is a **deterministic re-run reproducing itself**, and it is worth saying out loud: the pair-move
here bought a legal timestamp and changed no measurement. The headline keeps its direction
(£17,444 clearing ±£1,457; £319 INSIDE ±£2,292, so the book still cannot resolve the selection leg).

## The control that went red, and why it was right to go red at the wrong thing

`test_the_detectability_block_going_unavailable_says_so_rather_than_going_quiet` blanks the
**survivor cut's** detectability block and asserts the page does not render "smallest departure".
It read the whole `arms-method` panel. The moment the estimand below carried an interval for the
first time, **its own** detectability block rendered the same phrase about a different population,
and the control went red reporting a withheld sentence that was never withheld.

This is the presence-versus-attribution shape: a whole-page presence check has no way to tell *"the
block I blanked came back"* from *"a second subject appeared"*, and it fails the day the page gains
one. Repaired by scoping to the survivor cut's own region — everything above the estimand's heading —
with a guard that makes the split load-bearing: when the estimand's block is on the page, the phrase
must be present in the panel and absent from the region, so the partition is what the assertion
rests on rather than a global absence that would pass for the wrong reason.

Nothing else on the page was pinned to the run that was replaced: `site/` is 662 passed / 37 skipped,
and the value-arms suites are 467 passed.

## Landed

* `429bfab5d` — `_control_leg_agreement` and its door control (the not-corroboration record)
* this commit — the pair promoted, the feed regenerated, the detectability control scoped

## What is NOT done

`CURRENT_WORLD_THREE_ARM_PATH` still points at the `00:19:54Z` run of 09-08 under a heading that says
"the world as it is now", while a later run leads the page. That is stated on the surface by
`_later_runs_in_this_world` and it was measured and held on 2026-09-09 for a reason that is a
property, not a preference — promoting it makes both panels publish the same £324 selection figure
and `_the_legs_own_regions` refuses a headline a reader cannot attribute. It stays held. Re-open it
only with a run that gives the two panels different figures, and measure by promoting rather than by
reading the two descriptions.
