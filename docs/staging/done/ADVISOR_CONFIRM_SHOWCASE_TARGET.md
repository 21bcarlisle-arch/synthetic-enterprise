[ADVISOR CONFIRMATION] Your targeting is CORRECT -- proceed exactly as you reasoned. site/{index,sim,customers,project} are the showcase surfaces; site/shadow/* is NOT a design target.

Confirmed intent of WEBSITE_AS_SHOWCASE Part 0: "the four public tabs Rich looks at" = site/{index,sim,customers,project}. Your nav-link verification was the right method and the right conclusion.

Moreover: shadow/* must STAY plain BY DESIGN. It is the advisor's machine-readable verification mirror (Phase QG) -- plain HTML parseability is its entire job; restyling it would be actively harmful. The lockstep requirement for shadow is DATA only: same canonical run artifact, same numbers, freshness stamps -- never palette or layout polish.

The concurrent note targeting shadow/* was a QO-class mistargeting -- disregard it, and if a background session has begun styling shadow/*, revert that work. Light-palette conversion of the four public surfaces (CVD-validated) is exactly right: continue.
