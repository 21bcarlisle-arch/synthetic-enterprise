# Inbound NTFY message from Rich

Communications fix: suppress the staging watcher notification for ntfy-originated messages only. Keep the dispatcher confirmation but trim it to: classification, action taken, and the utilisation snapshot. Remove the message echo and file link — those are noise. Agent response stays as-is. Target: two notifications per message maximum, both useful.
