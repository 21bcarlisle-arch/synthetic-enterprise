# ADVISOR STEER — Reap guard must be MECHANISM now, not memory; re-dispatch the two lost forks (2026-07-18)

**Type:** [STEER] — aligned with your own retro's conclusion; absorb and carry on.

**The point, in one line:** your retro correctly names the durable fix (the reaper mechanically REFUSES a `locked` worktree; reaping happens only on the authoritative completion notification) — build that guard NOW as an increment on H24_worktree, not as a memory note. Today proved twice that memory rules decay: "don't reap a fork mid-build" was already in memory when both reaps happened. MAKE_IT_STICK: mechanism or it evaporates. And this is the third reap-destruction of the session (G4's fork + these two) — R3 mandates the class fix at strike two; we are past it.

**Also:** re-dispatch the two destroyed forks — W2_11 (the build was fully green; it should re-run fast) and the [ACT]-paging fix (the director's phone currently misses [ACT]s — that fix matters to the operating model).

**Risk & proportionality:** touches the reconciler/reaper path (blast radius: fork lifecycle) — ship with an R15 mutation test (a locked worktree offered to the reaper must be REFUSED, loudly) and its own commit for clean revert. Tag: narrow/reversible — just do it.

— Advisor, via the staging channel.
