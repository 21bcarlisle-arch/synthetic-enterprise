<!-- SUPERVISOR_DRAW: self-drawable -->

# [PLANNER-MINTED] The rest as honest stubs; existing page content re-homed (2026-07-28)

**✅ SEQUENCING UNBLOCKED (2026-07-29):** #1 (`PLANNER_MINTED_wholesale_domain_topic_map`) is DONE — `docs/design/KPILOT_WHOLESALE_DOMAIN_TOPIC_MAP.md`. The nodes/stubs to create and the re-home mapping for every current live-graph entry are in its §4 table; the §5 machine-readable block is the target to reconcile `site/data/knowledge_wholesale.json` to. The one node that is NOT a stub (deliverable #3) = `electricity-wholesale` (recommended). Coordinate file_scope with #3 (it owns the deep node's dir; this owns the rest + the hub re-home).

**Source ruling:** `DIRECTOR_RULING_KPILOT_DECOMPOSITION_2026-07-28.md` — deliverable #4 (WORK THIS CREATES); ruling §1, §5.3, §5.4.
**Serves:** DIRECTOR_AXES Axis 1 (Website) + Axis 3 (Believability). Campaign: SITE_MODEL_SPINE knowledge lane.
**Real-world fidelity gained:** every node of the domain graph that is not the one deep node ships as an HONEST STUB — identity, place in the graph, and "not yet written" — which the brief already declares a legitimate state (ruling §1). The stubs HOLD the graph (§5.3) so the structure is real, not aspirational; and the current over-loaded page's content is RE-HOMED to whichever node it belongs to, not deleted (§5.4).

**Lane:** SITE + FRAME (create stub node pages + re-home existing content; disjoint `site/knowledge/**` file_scope, coordinated with #3's chosen node).
**Target level:** SITE L2 (rendered stubs live-verified — R11). Level move stays `blocked_on director_level_up` (R16).

**Exit criteria:**
- Every node in the #1 map that is NOT the one deep node (#3) exists as a **stub**: identity + place in the graph + explicit "not yet written" — NOT a gap list in the body (ruling §1: "a page that is mostly its own to-do list is a stub in costume, worse than a stub because it claims depth it does not have"). A stub is honest; it says what it will cover and that it is not yet written.
- The existing `wholesale-price-formation` page content is **re-homed, not deleted** (ruling §5.4): each block moves to the node it belongs to (gas mechanics → gas node, merit-order/EFA → electricity node, etc.), and the old URL either becomes a node or redirects — nothing orphaned, no dead backlink.
- **Acceptance (ruling verbatim): no node's body is a gap list; every node either explains (the deep node, #3) or is honestly a stub (this deliverable).**
- The generated sidebar / topic-graph reflects the new nodes (existing 7 empty pointers become real stub pages or are re-mapped per #1). R11: verify the LIVE rendered graph/sidebar, not the source.

**Deps:** #1 (which nodes → which stubs) + #3 (the one node that is NOT a stub). Coordinate file_scope with #3 to stay disjoint (#3 owns the deep node's dir; this owns the rest + the hub re-home).
**Coverage note:** scope-first deliverable #3 (`KPILOT_PAGE_DECOMPOSITION_PROPOSAL.md`, DONE) NAMED stubs vs full children for the single price-formation topic but did NOT create the stub pages or re-home content. NOT re-minted; that proposal is the input for the electricity-side sub-structure only (ruling §6: Board Spec 004 remains a scope input for the electricity node specifically, not the domain as a whole).
**Propose-then-proceed window:** proceed by default (ruling tagged **proceed**; re-scope + stubs, nothing deleted, reversible via git). Walls untouched.

## Deliverable (verbatim)
> 4. The rest as stubs; existing page content re-homed.

Ruling §5.3/§5.4: *"Everything else ships as stubs. Honest, cheap, and they hold the graph. The existing page is re-scoped, not deleted — its content moves to whichever node it belongs to."*
