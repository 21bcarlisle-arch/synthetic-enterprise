# [DIRECTOR-RULING] — Unblock batch: merit-order, cohort level-up, exit-criterion counter (2026-07-28)

**Type:** [DIRECTOR-RULING] via advisor bridge. Answers arising from the six question closures (`155251dad`). Three items; sequencing and mechanism are yours.

**Standing note on advisor availability:** the director's advisor budget is finite and roughly half spent for the week. Assume **fewer advisor interventions from here**. Consequences: batch escalations rather than splitting them; prefer one dense [ACT] over several; and where a decision is genuinely yours under an existing ratification, take it rather than waiting. The remaining advisor capacity should be spent on decisions only the director can make.

## 1. Merit-order — state what the window needs, do not wait silently

Q1's answer: `merit_order` is held behind an **R13 propose-then-proceed window** and is not drawing. It is the ratified next product draw (`61e368f7e`), the unblock condition on the SSP baseline hold (`90110af48`), and the answer to Board Spec 004's reconstructibility test.

**Required:** state plainly what that window needs — which values, which scope, which decision. If it needs a curriculum ruling, **propose it with your reasoning and the evidence** and it will be ruled; if the window has simply not elapsed, say when. A blocked item of this rank must not sit silent again: it is the item the director has twice named as most important.

**Unchanged:** R12 governs the work when it starts — mechanism diagnosis, never tuning toward the naive baseline; the per-cell lift table stays the fixed scorecard with the benchmark unmoved.

## 2. Cohort level-up — proceed at your authority, or batch it

Q3's answer: cohort assignment is **BUILT, blocked on level_up**.

**Ruling:** if the promotion is **L1 or L2**, the director twin's standing authorization applies — **proceed**, ledger-backed, no self-claims. If it is **L3**, batch it for the director's next ratification and say so. Either way the ruled activation (`e685eb76d`) stands: ratified values, no tuning, elicitation wall re-proven, and company-side segmentation remains recorded **untestable at the current book** rather than scored as working.

## 3. Exit-criterion counter — build it; it outranks further gap mints

Q2's answer: the counter is **not built — "not yet a control"**. It is deliverable 1 of the ratification (`d81197736`), and two of the four stall detectors are gated behind it.

**Ruling:** building it takes priority over further gap-list minting. Until it exists there is no way to answer "is the harness done for now" except by feel, which is the thing the criterion was ratified to replace. It must compute from primary state per LAW C, publish its count and last-reset cause in the daily note, and carry its R15 pair.

**This is breakage, not speculative machinery** — it is a ratified control that does not exist — so it is drawable under PRODUCT-FIRST's existing exception.

## 4. Empowerment

Sequencing across these three is **yours**. If you judge a different order better — for instance that merit-order should start before the counter is built — say so with reasoning and proceed on it; this ruling sets priority, not order of operations. If any item is already in flight or has been superseded since the answers were written, say so and skip it rather than redoing it.

## WORK THIS CREATES

1. Merit-order window requirement stated; a curriculum proposal returned if one is needed.
2. Cohort promotion taken at twin authority, or batched as L3 with the reason.
3. Exit-criterion counter built, computing from primary state, published in the daily note, R15 paired.

Acceptance: none of the three remains blocked-and-silent; each is either advanced, or blocked with its requirement stated in the open-question register.

**Risk & proportionality:** unblocking already-ratified work; no new scope; no curriculum value set here. Tag: **proceed.**

— Advisor bridge, 2026-07-28.
