# [DIRECTOR-RULING] — Rail-3 heartbeat: ship it where promised; classify the current quiet; planner-rung status (2026-07-24)

**Type:** [DIRECTOR-RULING] via advisor bridge. Three short demands, evidence not narrative (R9).

## 1. The rail-3 heartbeat is not discoverable at origin

The safety-rails addendum (ratified) requires: every tick writes one line — timestamp · drew/rested/exception · the whole-set enumeration — to a file **shipped by the existing auto-publish**, so liveness is verifiable from origin without SSH. `site/data/` holds only the legacy status files. If the heartbeat was built elsewhere: name the exact path and make it ship. If it was not built: that is a consumed-not-absorbed on a ratified rail — build it now, and say so plainly in tonight's note. Acceptance: the advisor can fetch the current tick verdict from origin within one publish cycle.

## 2. Classify the quiet since 10:37Z with the enumeration

11:38 BST → now: no commits, no NTFYs. Answer with the per-lane enumeration for that window: what was drawable and why nothing drew. Specifically address why these were not in any lane: the SITE_MODEL_SPINE evidence pages behind the diagram nodes (open campaign items), the **premise-demand publish** and **follow-ons ranked doc** (still owed from the night-enforcement list), and the 16 draining wedge markers.

## 3. The planner rung (rung 7): built or not?

Rung 4 went live on the night of the 23rd. Rung 7 — the bounded planning turn that MINTS proposals from DIRECTOR_AXES and the fidelity ledger when rungs 1–6 are empty — has never been confirmed. One line: built-and-tested (cite the R15), in progress (cite the branch), or not started (then it is the next draw, before any further campaign polish; the enforcement ruling already authorizes it).

## 4. Also report

The 10:22Z `from_rich_comment_*` staging: what the comment said, how it was validated, and what consumption did with it — the director will confirm authorship separately.

**Risk & proportionality:** verification + one possible build already ratified. Tag: **proceed.**

— Advisor bridge, 2026-07-24.
