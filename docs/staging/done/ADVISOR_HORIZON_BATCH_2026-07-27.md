# ADVISOR HORIZON BATCH — 2026-07-27
**[HORIZON — CONTEXT, NOT WORK]** · advisor-staged, director-confirmed in conversation 2026-07-27

## Consumption rule (this header is the whole contract)
- This document is CONTEXT for future consideration, not work. Nothing in it is drawable, buildable, or a steer to act now. It changes nothing about current priorities; where anything here seems to conflict with them, current priorities win.
- Consider these items at epoch boundaries and P-5 re-ranks.
- The only permitted immediate action, at your discretion: register any item as an idle atom (loop_stage: idle, no front) so it lives in the map and resurfaces mechanically at re-ranks. Registration ≠ authorization — the gate-wall model is unchanged; nothing here is BUILD_OPEN.
- Do not spend DISCOVER cycles on these items on the strength of this doc alone.
- Precedent: consume like EPOCH3_DIRECTION.md — re-rank input, never queue entry.

## Provenance
Distilled by the advisor from a director ideas run (external AI-research digests — MCTS/search literature, Kimi K3 architecture notes, ontology standards, Matt Pocock's workflow corpus, hardware-economics analysis — each assessed against canon before inclusion). Most external suggestions proved to be rediscoveries of existing architecture; the residue below is what survived the filter, plus the convergence record itself.

## Items (all HORIZON; director calibrations noted where given)

**1. Visual telemetry.** Problem: dense numerical diagnostics (CSV dumps) are a poor payload for anomaly-spotting; rendered charts are far denser and vision-legible. Requirement when taken up: failing runs and key diagnostics renderable as images; first candidate the spike-tail (model-vs-real SSP, 2022 worst cell). Extension: the same derived-from-real-run-data charts serve as Proof-door fidelity evidence (SITE_CONSTITUTION applies — derived via the pipeline, never static images), and screenshots of rendered surfaces enable literal claim=pixel R11 verification. Non-negotiable discipline: charts diagnose and communicate; numbers prove (R15 requires exact values — vision never verifies arithmetic).

**2. Unit-consistency as an R10 class.** Problem: unit errors (kW vs kWh, £/MWh vs p/kWh conversions, electricity-kWh + gas-kWh summation) are currently convention-guarded. Requirement when taken up: treat them as an absurdity class per R10 — structurally impossible at seams, not caught instance-by-instance. Mechanism is yours to design.

**3. Company-layer scale lens.** Director calibration (2026-07-27): the standing design question for company-side and wall-adapter work is "does this structure survive 10,000 customers?" — 10k is the realistic structural bar, 100k the aspiration. Rationale: the SIM stays honest-sized by the coverage-knee method, but the company layer is the real supplier's codebase in embryo and the wall is the go-live seam. A lens for Expert-Hour and design moments, not a new gate. Connect to PRODUCTION_READINESS_SCALE_ADDENDUM.md (already in in_progress/) rather than duplicating it.

**4. SIM batchability as a pre-scale-up precondition.** Requirement to register before any population scale-up lands: SIM compute paths remain deterministic, per-tick stateless, and batchable (data-oriented), so that population growth and the eventual epoch-4 tournament are bounded refactors, never rewrites. A registered constraint, not a build.

**5. Population trajectory (director direction).** The SIM grows beyond N=200 toward thousands of households, adding richness: house physics (EPC band, property type, build era, heating system, solar, insulation — anchors already researched in ASSUMPTIONS.md, household.py registered as the gap), human events, and conversations. The N=200 knee was the knee of the CURRENT model's dimensionality; re-derive the coverage/value knee at each richness step so N stays a derived output, never an ambition. Mission link: per-household physics is what makes £/tCO2e computable per household — abatement is a physics counterfactual.

**6. Conversation tiering.** When conversations enter the SIM (F1 — already DISCOVER-authorized in the forward register; that authorization is pre-existing, not granted here): most interactions as structured events; a sampled subset as full local-LLM conversations where texture pays (complaints, retention saves, vulnerability self-declaration — which currently has no simulation-side arrival mechanism, and a conversation is its natural carrier). Wall discipline: transcripts are observable interface events; the generating psychology stays SIM ground truth.

**7. Interview mode for director-authored artefacts.** For R13 curriculum, dials, and similar items only the director can author: instead of waiting for a finished spec, interview him — one question at a time, walking the design tree until shared understanding — matching his phone-native format and the existing one-step convention.

**8. Eval discipline for the non-deterministic tier.** Problem: R15 mutation-proof works for deterministic code; LLM-generated behaviour (SIM conversations, Qwen audit judgments) has no equivalent verification discipline — audits currently produce flags checked by eye. Requirement when taken up: an eval pattern — scored cases as first-class suites, a scorer library, judge-with-caching, repeated trials to measure variance. Pattern only (Evalite is the reference shape; it is TypeScript — do not adopt the tool).

**9. Smart-zone task sizing.** A token-denominated complement to G5 effort sizing: work sized so a pass completes within the model's high-quality context range; the statusline rate-limits sensor is existing instrumentation. Note for canon: fresh-context review (already law for anti-self-grading) gains a second, independent mechanistic justification — a reviewer sharing the implementer's long context reviews from a degraded position.

**10. Structured regulatory extraction.** Discovery-agent enhancement: primary rule text in → CANDIDATE invariant predicates out, always flagged provisional pending verification; never auto-entering domain_invariants.py. Why the gate is mandatory: the SLC 31A/21BA citation muddle is the live example of regulatory work from unverified text. (Elexon's Cloudflare wall also limits any auto-ingestion ambition.)

**11. Best-practice source-list additions** for the recurring harness review (extends the standing commitment, no action now): Matt Pocock's skills repository + AI-coding dictionary + Sand Castle framework; Anthropic's CCA-F exam blueprint + Academy courses (launched Mar 2026) as a checklist of what Anthropic itself treats as the brittle points (schema-validated tools, long-session state).

## Convergence record (context for the Method door and pitch material; no action)
Five independent traditions were checked this run and each converged on existing canon: I-MCTS forced failure-reflection ≈ R3/R9; UCB exploration pressure ≈ the naive organ's Socratic form; quant-finance point-in-time practice ≈ the wall/PointInTimeView (already recorded in ASSUMPTIONS.md's W1 section); Pocock's tracer bullets ≈ coupled triads and the L3 live-consumer bar, deep modules ≈ walled seams, fresh-context review ≈ the Expert Hour; hardware-economics analysis ≈ the local/frontier split. External validation vocabulary — usable on the Method door and in the publishing track.

## Risk section (pre-stage checklist)
- Touches: one file in docs/staging/ only. No code, no map edits, no gates, no levels.
- Blast radius: attention. Named failure mode: treating HORIZON items as work or burning DISCOVER cycles unrequested — the consumption rule above is the mitigation; the only immediate action permitted (idle-atom registration) authorizes nothing.
- Proportionality: reversible/narrow — delete the file to revert.
