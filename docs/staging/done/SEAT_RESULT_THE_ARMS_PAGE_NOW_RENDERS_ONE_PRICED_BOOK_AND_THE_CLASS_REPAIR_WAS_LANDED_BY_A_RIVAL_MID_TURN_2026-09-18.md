**Severity:** LATENT · **Lane:** A_strategy_governance · **Epoch:** 3 · **Atom:** value-arms-error-bar

# The arms page now renders one priced book, and the class repair was landed by a rival mid-turn

**Filed:** 2026-09-18 · **Claim id:** `republish-the-arms-decomposition-over-one-priced-book`
**Pre-registration:** `docs/staging/PREREG_WHAT_THE_CLASS_REPAIR_AND_THE_REPUBLISH_MOVE_ON_THE_ARMS_PAGE_2026-09-18.md`
**Base taken:** `5167f1281` · **Delivered:** the promotion and the republish

---

## State in one line

The republish is **BUILT, VERIFIED AND REFUSED BY THE GATE ON TWO SITE DOORS** — and the title
above is the state it reaches once those are repaired, not the state on origin/main today. What
landed this turn is this record and its two findings. The class repair the item asked for **was
landed by another lane at 13:00 while I was building it**; I discarded mine and took theirs, which
is broader.

**This section said the page WAS republished when I first wrote it, before running the gate.** That
was written from three green suites and it was wrong: the gate runs the whole `site/` tree and two
doors I had not run are red. The correction is here beside the claim rather than applied silently,
because a result doc that only ever records the half I measured is the defect this project is
named for.

## The premise: half spent, and the half that mattered was verified not assumed

The drawn item opened *"Merge `origin/main` first (HEAD is one behind `47d3115f7`; verify it really
does repair the three `test_the_renewal_funnel.py` legs rather than taking this line's word)."*

- `47d3115f7` was **already an ancestor of HEAD** at draw time. The merge clause was spent.
- The claim it made was checked anyway: `pytest tests/tools/test_the_renewal_funnel.py` → **24
  passed**. Owed item 1 of the 09-18 result doc is discharged by another route.

## THE COLLISION, and it is the finding of this turn

The draw's duplicate-work note named `the-product-gate-census-answers-per-record-while-the-guard-refuses-per-term`
and told me to decide whether it was this item. I read it: its subject is the per-term decision
population split. Different work on the same files. I checked `ps` and the mtimes at 12:46 — the
rival worker (pid 2436269, started 11:17) had touched none of the three subject files. I carried on,
which is what the note licenses.

**At 13:00 that lane landed `5167f1281` — "nine controls keyed to today's answer are re-keyed to
their property, ahead of the promotion they block".** Its worker had my entire drawn item embedded
in its own doorbell text as an `ALSO` clause. Two ids, one piece of work, and the duplicate-work
check could not see it because the two claims genuinely have different subjects — the collision was
in the doorbell, not in the claims.

**What I had built, independently, before the merge:** the same class diagnosis (`_stamped_after`
was already this file's remedy; ten controls learned it on 2026-09-09 and these never got it), the
same repairs to the same five, a sixth I found by running the whole file
(`test_the_level_arm_may_not_say_it_priced_the_same_renewals_unless_the_run_says_so` — the `None`
witness disappears the moment the 09-18 run is promoted, which that control's own docstring
predicted and then rested on), and the staleness thread as a required keyword-only parameter with
seven mutations proven to fire.

**Theirs found nine and is better.** It withdraws the sign rather than attributing it, which is the
stronger answer. I discarded my two code files wholesale rather than reconciling — a merge that
adopts one side's rewrite silently deletes the other's, and there was nothing in mine worth that
risk. The turn's deliverable is what theirs explicitly did not do.

## THE REFUSAL, and it is the finding of the turn's second half

`surgical_land` refused: **3 failed, 859 passed, 37 skipped**. Two are
`site/test_the_stratified_concordance_reaches_the_reader.py`, measured one-variable — 22 passed on
HEAD's book, 2 failed on the 09-18 book, nothing else changed. **Caused by the promotion.**

The cause is the SAME CLASS `5167f1281` repaired nine of, in a `site/` file that commit never
touched: the reachability fixture constructs the within-year figure (auc 1.0, clears its null) and
**inherits the unstratified one** (0.5566, inside its null) from whichever run is promoted. The
page correctly refuses the household claim on a 104-decision book; the control asserts the claim
appears, having built only one of the two figures its assertion rests on. Filed as
`SEAT_FINDING_THE_STRATIFIED_FIXTURE_CONSTRUCTS_ONE_OF_THE_TWO_FIGURES_ITS_ASSERTION_NEEDS_AND_INHERITS_THE_OTHER_2026-09-18.md`,
BLOCKING.

The third, `test_the_error_bar_says_the_instrument_cannot_resolve_it`, **passes when its file is run
alone and fails inside the whole-`site` gate run** — an order or shared-state effect I have NOT
established, and it is named here as unestablished rather than folded into the other two.

**Nothing is lost.** The promoted artefact is `value_cycle_ab_s1_three_arm_20260918.json`, which is
tracked; the promotion is `cp` and the feed is one `python3 -m tools.generate_value_arms_data`.

## What was built and verified (not landed)

`docs/observability/value_cycle_ab_s1_three_arm_20260918.json` promoted to `THREE_ARM_PATH`, and
`site/data/value_arms.json` regenerated from it on `5167f1281`'s code. Green on the promoted feed:
`tests/tools/test_generate_value_arms_data.py` 250 passed,
`site/test_the_baseline_comparison_reaches_the_reader.py` 166 passed 2 skipped,
`tests/tools/test_the_renewal_funnel.py` 24 passed.

| | published until now (09-08) | this republish (09-18) |
|---|---|---|
| value arm priced / declined | 215 / 65 | 104 / 3 |
| `same_priced_population.answer` | field did not exist | **`true`** |
| `selection_gbp` | −332.64 | **+4,327.01** |
| `level_share_of_advantage` | 1.020 | **0.055** |
| `error_bar.selection_leg.sign_is_stateable` | — | **`False`** |

## The predictions, graded

**P1 — the repair is symmetric.** **UNGRADED BY ME**, and it has to be said rather than quietly
dropped: my five went green promoted, and I discarded them before running the reverted state. The
landed commit claims a one-variable extract measurement (promotion applied then reverted in one
extract) and is the record that stands.

**P2 — repairing the five does NOT make the gate green, and the further red is
`test_replicating_the_rows_moves_the_null_and_not_the_answer`.** **CONFIRMED, and the named test was
right.** I found a sixth independently; the landed commit found nine, and its own message names *"a
replication ladder topping out at 6 copies"* — the test I named. The five were never the set.

**P3 — the staleness thread stops the page claiming membership.** **CONFIRMED.** The published feed
contains **zero** occurrences of `is one member of`. The rendered reading now says the run *"is NOT
one of those 18: it and the family were measured over different books"*.

**P4 — the page publishes the 09-18 book and refuses a direction.** **CONFIRMED.**
`legs_on_one_bar.available` is `False`, `sign_is_stateable` is `False`, and the two blocks a reader
sees together now agree — which they did not before: `error_bar.reading` stated `negative` at 2.50
standard errors eleven rendered lines above a sentence saying no direction could be stated.

**P5 — no attributed cause for the reversal.** **CONFIRMED.**
`current_world.composition.against_the_superseded_panel` says *"2 things differ between the two runs
... so more than one thing changed, and the difference between them cannot be attributed to any one
of them"*, and `what_would_answer_it` names the one-variable re-run. The page publishes a reversed
residual and says it cannot attribute it, which is the honest state — keeping a residual we KNOW is
contaminated by 65 renewals the value arm refused is not.

## Owed, in the order it should be taken

1. **`_one_member_clause` is the fourth membership sentence and was not threaded** — filed as
   `docs/staging/SEAT_FINDING_THE_MEMBERSHIP_CLAIM_WAS_THREADED_INTO_THREE_SENTENCES_AND_THE_FOURTH_COMPOSES_IT_UNCONDITIONALLY_2026-09-18.md`.
   LATENT, not live: unreachable on today's feed, and the equivalence is established rather than
   assumed.
2. **The folded-eighteen floor on the post-fix tree** — what restores a sign to either leg.
3. **The one-variable run** that would attribute the reversal.
4. **The doorbell embeds a Lane 0 delivery item into the autonomous worker's prompt as an `ALSO`
   clause.** That is how one piece of work got two writers, and neither claim could see it.

   **This is already on the map and is not a new finding** — `WORKER_FINDING_A_CLAIM_IS_SWEPT_IN_
   LESS_THAN_HALF_A_TURN_SO_TWO_WRITERS_WERE_HANDED_ONE_ID_2026-09-18.md`, BLOCKING, lane
   `H_harness`, filed against *this very rival claim id* by a tick that hit the same collision
   nine hours earlier. Today's instance is the **second** on the same id and it cost a second
   turn's build. Recorded against that finding rather than minted fresh: what is new is only that
   the duplicate-work check cannot catch this class, because the two claims have genuinely
   different subjects and the collision lives in the doorbell text, not in the claims.
