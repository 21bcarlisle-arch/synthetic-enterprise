# SEAT RESULT — the three facts reach the reader, and the wedge that held two lanes back was one argument shifted one place left

**Lane:** A_strategy_governance
**Severity:** RECORDED
**Date:** 2026-09-10
**Claim:** `the-value-arms-page-must-stop-claiming-per-household-inference-it-cannot-evidence`

**Follows:** `docs/staging/SEAT_RESULT_THE_PUBLISHED_SUPPLIER_CHECK_NOW_READS_ONLY_COMMITTED_BYTES_AND_THE_ITEMS_OWN_PAIRING_WOULD_HAVE_REFUSED_ON_EVERY_PUBLISH_2026-09-10.md`,
`docs/staging/SEAT_RESULT_THE_SELECTION_LEG_CANNOT_BE_SPLIT_ON_THIS_ARTEFACT_AND_THE_ONE_FIGURE_ARGUING_AGAINST_A_FLAT_WORLD_IS_A_BETWEEN_YEAR_EFFECT_2026-09-10.md`,
`docs/staging/SEAT_FINDING_THE_PAGE_PUBLISHES_A_BOOK_FROM_AN_OBJECTIVE_THE_COMPANY_NO_LONGER_RUNS_2026-09-10.md`

---

## The premise was two-thirds spent, and saying so is the first result

The drawn item asked for three legs to be built and landed. **All three were already built,
uncommitted, in the shared tree** when this turn opened — the within-year concordance and its
permutation null, the bind asymmetry, the objective comparison, the door additions for each, and a
thirteen-assertion door test (`site/test_the_stratified_concordance_reaches_the_reader.py`,
untracked). The prior turn wrote them and could not land them.

So the work of this turn was not the three legs. It was **the wedge**, and the wedge turned out to
be mostly one defect.

## The wedge

`tests/tools/test_the_value_arms_pages_undriven_pointers.py` was refusing every landing that
touched this cluster — 3 failed at `38051c0c0`, and 4 ERRORED in the shared tree. It is now
**4 passed**, and `site/test_the_baseline_comparison_reaches_the_reader.py` is **133 passed, 1
skipped** beside it.

### 1. One parameter removed, every argument after it shifted one place left

The published-supplier repair stopped reading `docs/reports/run_output_latest.json` and dropped
`build`'s `published_run` parameter — its third. Two call sites passed the remaining artefacts
**positionally**:

| call site | what it meant to pass 4th | what `build` then received |
|---|---|---|
| `_real_inputs` (pointer rung) | — | `AttributeError: no attribute 'RUN_OUTPUT_PATH'` at fixture SETUP |
| `site/test_the_baseline_comparison…:2335` | the current-world run | `None` — so `current_floor` got the run and `departure_rerun` got the floor |

The second is the one worth the line. It did not crash. It produced a *coherent* build in which the
current-world block was unavailable, and the fixture failed with its own honest message: *"the
committed runs no longer pair into a spoken current-world block (no re-run of the arms in the
current world was readable)"* — **a sentence about the artefacts, when the truth was a sentence
about the call**. A reader following that message goes to look for a missing run. Both call sites
are now keyword or arity-checked, so the next signature move is a `TypeError` naming the parameter.

*Correction, kept beside the claim rather than replacing it:* the prior turn recorded these two
baseline-comparison reds as reproducing at `38051c0c0` from `current_world.is_the_later_run` being
`False`. At `38051c0c0` `build` still carried `published_run`, so that positional call was correct
there and their diagnosis is not the one this turn met. **Two different causes, one symptom.** This
turn measured only its own tree; the HEAD reading is theirs and is not disputed here.

### 2. Two symbols owned a publishable sentence nothing could drive

`_current_world_contrast` and `_publisher_bound_statement` had no `_RECIPES` row, so their
here-relative prose was published-able and unjudged. Both now have one — the first through a
merge (`_how_to_read_this`), because it returns the whole current-world block and substituting a
bare string would have taken every leg below it off the shape it reads.

### 3. Two directions were false, and one was false in a way nothing could see

* `_current_world_bound`'s no-floor refusal said *"whether the figure below is distinguishable from
  zero"*. No door renders `why_no_bound` at all, so "below" was a direction taken from a place no
  reader stands. It now names the contrast — which is **strictly more informative**, because this
  function bounds two different legs depending on its argument and "the figure" never said which.
* `_publisher_bound_statement` said *"neither figure above"* about two ratios in the **first half of
  its own paragraph**. It now names them.

### 4. `current_world.how_to_read_this` reached no reader, and the door now renders it

This is the sentence that tells a reader the second panel is another **world** and not a revision of
the first. It has been written by the producer since the current-world block existed and no door has
ever rendered it. Rewording it to dodge the rung would have been the cheaper fix and would have
**hidden** that — a narrowing added for a false positive can only hide. It renders under the band in
`#arms-redraw`, below `#arms-headline`, which is what makes its "above" true; the referent is
registered as `.headline` and the rung now checks that direction on every run.

### 5. `_against_the_panels_figure` is drivable again, by opening the gate above it

Its sentence composes inside an argument `_current_world_clause` discards whenever
`current_world.is_the_later_run` is `False` — the state the page entered when the 09-08b re-take was
promoted onto the canonical name. So the branch is unpublishable *today* and publishable the next
time a current-world run is the later of the two. `_ALSO_ADMIT` forces the upstream **producer**
field open — never the artefact, because editing `is_the_later_run` in the input would also move the
superseded figure the sentence compares against, which is the thing being judged. Skipping the
symbol instead would have left a pointer unjudged until exactly the publish that puts it back on the
page.

## The three facts, as a reader now meets them

1. **The stratified concordance, as a FIGURE.** `0.444` renders in `<strong>` beside `0.627` in
   `#arms-decisions`, with its permutation null (`0.376–0.619`), its `402` same-year pairs and the
   `3,320` total. The household reading is withdrawn as the fifth `withdrawn_claim` row — and the
   page states plainly that 402 pairs **cannot tell either way**, which is the mirror overclaim the
   door test refuses separately.
2. **The bind asymmetry, at the leg.** `#arms-redraw` carries "THIS LEG IS NOT THE VALUE OF CHOOSING
   PER CUSTOMER" with 143 of 214 against 0 of 281 — the confound is in the QUANTITY, so no larger
   book removes it.
3. **The objective that priced the book,** in `#arms-note`: the producing commit's renewal objective
   charged nothing for a departure and HEAD's charges the sourced replacement cost, read from both
   blobs, so the clause disappears on its own when the canonical run is regenerated under HEAD.

Each has a null control asserting its branch can go **quiet** — a page that always withdraws is
telling the reader nothing, and would go on withdrawing on the day a run finally earns a household
reading, which is the outcome this work aims at.

## Also repaired, and it was a contradiction between code and its own comment

`_same_run_verdict`'s not-asked branch returned `not_older_than_verified: True` under a comment
reading **"NOT ASKED IS NOT PASSED"**, with the reason beside it. A reader — and any control keyed
to this leg — takes the boolean. It is now `None`.

## What this landing did NOT carry

* **The £67.48.** With the subject settled, the supplier this site publishes is NOT the baseline arm
  (£147,886.78 against £147,954.26, 0.046%, 6,748× the feed's own tolerance). Whether that is re-run
  nondeterminism is still unestablished. Handed on unchanged.
* **`_find_latest_run_json`'s mtime glob** — detectable, still unrepaired.
* **The provenance ordering.** Moving `value_arms.json`'s generation to after `record_verified`
  would restore a second witness for the run identity. That is a pipeline change.

## Standing state a reader should not mistake for a red of this landing

`tests/architecture/test_static_quality_ratchet.py` refuses in the shared tree at **I001 1308
against a frozen 1309** — the tree is one *better* than its baseline. None of the six paths in this
landing carries an I001, so the difference is another lane's uncommitted or untracked file and it
is absent from the extract this commit gates. **Not banked**: freezing a ratchet from the dirty
shared tree bakes in uncommitted wiring and wedges every lane.
