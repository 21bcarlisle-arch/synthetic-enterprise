# [DIRECTOR-PRIORITY] — Build the breathing NOW: stop winning wedges, change the game (2026-08-10)

**Type:** [PRIORITY — sequencing the already-ruled PUBLISH_DECOUPLING to the front]. Evidence: 25h frozen stamp, ~18 causes cured serially with excellence, re-wedged within the hour every time. The treadmill is structurally unwinnable while publishing demands whole-repo perfection; the director ruled the alternative yesterday — it just hasn't been built.

**Next tick, before any other draw including the drain:** implement THE SITE BREATHES items 1 and 3 minimally —
1. **The scoped publish-path suite**: publishing requires green on the code that produces and renders the published numbers, nothing else; other reds become the page annotation ("published with N open findings").
3. **Last-known-good under a dated banner**: even when the scoped gate is red, the site serves the newest verified snapshot with its provenance stamp and "verification paused since T" visibly rendered.
Item 2 (full annotation panel) and polish follow on normal priority. Exit test: a deliberately-injected unrelated red (e.g., a doc-drift) produces a PUBLISHED site with an honest annotation — never a frozen stamp. When that exit passes, the 85-marker queue drains under the scoped gate and every future "wedge cause" is a banner line, not an outage.

The drain, H42's wiring, and the mint-path fix all continue immediately after. Nothing else changes.

— Advisor, sequencing the director's own 2026-08-10 ruling; 25 hours is the ruling's mandate.
