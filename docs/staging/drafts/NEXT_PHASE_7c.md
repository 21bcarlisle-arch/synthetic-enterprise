# Proposed Next Phase: 7c — Full Run with Phase 7b Ledger

## Context

Phase 7b added payment lifecycle events to the ledger. The existing
`docs/reports/run_output_latest.json` and `docs/reports/ledger_latest.json`
were generated by the Phase 6b+7a run (commit 125b0c4), which predates Phase 7b.
The Transaction Log section in the published report shows the Phase 7a waterfall
(no payment events) because it was built from the cached JSON.

## What a Phase 7c full run would show

Running `make run` (full 2016–2025 simulation with real committee calls) would:
1. Generate 2,121,727+ events including the new `payment_received_event` and
   `bad_debt_event` types
2. Publish an updated `ANNUAL_REPORT.md` with the Phase 7b cash waterfall:
   - Revenue billed: £93,868
   - Less: bad debt written off: ~£1,100 (estimate, based on fast-run rates)
   - = Cash collected: ~£92,750
   - Net margin (cash): ~£1,900 (revenue-based net margin minus bad debt)
3. Update `ledger_latest.json` with the extended event set

## Alternative: what's already validated

The fast-run (`--fast --end-year 2020`) confirms Phase 7b events are correctly
generated and the waterfall renders cleanly. The code is correct. The only gap
is that the published report's waterfall section still shows Phase 7a figures.

## Decision for Rich

- **Option A**: Proceed to Phase 7c (trigger a full 2016–2025 run with
  `make run`) to update the published report with Phase 7b figures.
  Takes 45–90 minutes, uses GPU for committee calls.
  
- **Option B**: Move to a different backlog item while the Phase 6b+7a
  run's cached output stands. Phase 7b is fully implemented and tested;
  the next run will naturally include the extended waterfall whenever it runs.

## Gate

Proceeding with Option A in 4 hours unless redirected via staging or NTFY.
