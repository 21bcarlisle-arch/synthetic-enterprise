# Runtime dependencies for the synthetic-enterprise project.
#
# pymc-marketing pulls in pymc, arviz, pytensor, scipy and their dependency
# tree (~35 packages) — needed by saas/clv_model.py and
# saas/enterprise_value.py (Phase 4b-3/4b-5, ShiftedBetaGeoModelIndividual).
pymc-marketing==0.19.4

numpy==2.4.6
pandas==3.0.3

# background/file_api.py — file API serving docs/status for mobile checks
fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4

# sim/ data ingestion (Elexon, NBP, NESO)
requests==2.32.5

# test suite and linting (Makefile: make check)
pytest==9.0.3
ruff==0.15.16
