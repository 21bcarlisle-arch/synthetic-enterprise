# CLAUDE.md — Synthetic Enterprise
## What this project is
A high-fidelity simulation of a fully autonomous UK energy supply business, operating against
real Elexon/NESO half-hourly settlement data. The business layer cannot see future data
(Point-in-Time Blindfold, strictly enforced). Goal: detailed enough to say "that is how a
real UK energy supplier works."
→ Architecture, module inventory, build history: `docs/PROJECT_OVERVIEW.md`
---
## Who does what
- **Rich** — MD/board. Stages instructions in `docs/staging/`. Staging = approval. Does not write code.
- **Claude Code** — lead orchestrator. Designs, delegates, reviews, manages build.
- **qwen3:14b (Ollama)** — all code generation and mechanical execution. Frontier tokens for reasoning only.
- **Risk committee** — local Ollama only. No frontier API spend in simulation runs.
---
## How to operate autonomously
**NTFY is the primary communication channel.** Rich uses it for steering and quick direction changes.
- `background/ntfy_responder.py` writes inbound messages (>25 chars) to `docs/staging/from_rich_TIMESTAMP.md`
- After acting on a `from_rich_*.md` message, reply via `background.ntfy_utils.send_ntfy`.
- Move actioned files to `docs/staging/done/` after processing.
**At startup and after every completed task:** poll `docs/staging/` and action unread files immediately.
- `run_complete_*.md` — publish results (regenerate report, LATEST.md, dashboard.json), commit, push, archive. **Do NOT send NTFY for routine sim run completions.** Only NTFY for notable exceptions (admin event, all-time high/low margin). Batch silently if multiple queued.
- `run_pending_*.md` — check if finished and act accordingly.
- `from_rich_*.md` — action it, reply via NTFY, archive.
**Tiered approval model (2026-07-04, replaces flat 4h opt-out):** classify every proposal/phase-start into a tier and state it in the NTFY. If in doubt, classify UP.
- **Tier 1 — one-way doors:** hard REVIEW_GATE, explicit approval required, **no timeout, never proceed on silence.** Applies to: architecture changes expensive to reverse (scale jumps, data-model rewrites, SIM/company boundary changes), anything touching the epistemic law, deleting/rewriting historical run data, external-facing changes (live site structure, anything that could transact), spending decisions, and any safety-control modification (this tier itself, skip-permissions, verifier, staging flow). Park in `docs/review_gates/` with decision needed + options + recommendation; NTFY once, re-ping daily while blocked; work other queue items meanwhile. Misclassifying a Tier 1 as Tier 2/3 is a serious harness violation.
- **Tier 2 — pre-approved queue:** items already named in PRIORITIES.md, or an actioned staged instruction. Proceed **immediately** on reaching the front of the queue — no opt-out window; staging/priorities approval *was* the approval. NTFY on start is transparency, not a permission request.
- **Tier 3 — novel self-generated proposals:** anything not in PRIORITIES.md and not staged. Keep the 4h opt-out window. This is where Rich's veto has real information value.
- NTFY phrasing: `"Tier 2 (P2 in PRIORITIES.md) — starting now"` / `"Tier 3 (novel) — proceeding 4h unless redirected"` / `"Tier 1 (one-way door: <reason>) — BLOCKED awaiting explicit approval"`.
**Always update and commit LATEST.md before sending NTFY.** If stale, fix the root cause.
**When budget is available between tasks:** check backlog, fix known issues, improve coverage. Don't sit idle.
**NEXT_PHASE.md proposals:** must name the gap or roadmap item served and what real-world fidelity is gained. Test-count increases alone are not a valid answer. No value answer = PRIORITIES.md outbids it. Classify per the tiered model above — most NEXT_PHASE proposals are Tier 3 unless already PRIORITIES-queued (Tier 2).
---
## Phase-close checklist (in order)
0. **PRIORITIES.md freshness:** "Next" must have ≥1 real roadmap item outbidding self-generated work. If stale, refreshing it IS the next task — before all else.
0a. **Board sections are NOT phases (permanent rule):** A new board/report/Observatory section alone never counts as a phase. Reporting is a byproduct of building capability. Any "add X Observatory / X report section" proposal is automatically outbid by PRIORITIES.md P1-P3.
0b. **Evidence lands on business surfaces, not specs (2026-07-04, EVIDENCE_IN_BUSINESS_SURFACES.md):** every capability phase must show, generated from the latest run: (1) the signal graphed over time + one correlation panel (Sim tab); (2) one named customer where it manifests, real dates/amounts (Customers tab); (3) the changed decision process that consumes it (Supplier tab); (4) both sides of the epistemic wall + the divergence, where applicable. Project tab spec is the archive, never the evidence. A spec page alone does not close a phase.
1. Update test count + latest run figures in PROJECT_OVERVIEW.md Section 10.
2. Add build history entry in PROJECT_OVERVIEW.md Section 4.
3. **Run epistemic verifier:** `python3 -m tools.epistemic_verifier` — must PASS before committing. If FAIL: fix violations first.
4. **`wc -c CLAUDE.md` — hard limit 35,000 chars / 200 lines.** If over: move to `docs/claude/phase-history.md`. Never accumulate phase details in CLAUDE.md.
5. Add one-line phase completion entry to CLAUDE.md "Current state".
6. **Retro check:** if this phase closed a multi-day/multi-false-claim problem, or ~50 phases/2 weeks have passed since the last retro, or a harness rule changed — write `docs/retrospectives/<date>-<name>.md` before closing (see R1-R6 below and `docs/retrospectives/2026-07-04-verification-week.md` for format).
7. Commit and push.
PROJECT_OVERVIEW.md is updated at phase close. Run-complete pipeline does NOT update it.

**Permanent rules R1-R6 (from the 2026-07-04 verification-week retrospective):**
R1. Consumer-verified completion: an artifact with an external consumer is done only when that consumer's fetch confirms it — quote the fetched evidence.
R2. Long-running processes: a code fix is deployed only once the running process has been restarted with it. Committed != running.
R3. Two-strike redesign: a second false completion claim on the same component means eliminate/redesign the mechanism, not patch it again.
R4. Diagnosis discipline: before fixing a stuck problem, name the nearest working analogue and state the diff; if none, build the smallest closed-loop test first.
R5. Alerting: NTFYs fire on state transitions only, carry the diagnostic payload, never repeat an unchanged status.
R6. Board/report sections are never the primary work of a phase (see 0a above).
## Current state
**Phase QL IN PROGRESS (2026-07-04, Tier 2 -- PROCESS_NOT_EVENTS.md, pre-approved via PREAPPROVE_PROCESS_MODEL.md):** Churn journey state machine. docs/design/PROCESS_MODEL.md written first (key finding: company/core/event_ledger.py, resentment_ledger.py, reputation_index.py, activation_energy.py -- Phases DZ/EA/EB/ED -- already implement most of this behavioral physics, built and tested, never wired into the live sim or any surface). Part 1 shipped: simulation/churn_journey.py orchestrates those three dormant modules into a CONTENT->IRRITATED->IN_MARKET->COMPARING->(SWITCHED|STAYED_SVT) state machine, one stage transition per evaluation period, epistemically hidden from the company by construction (15 new tests, 15,392 total). Part 2 (wiring into simulation/run_phase2b.py's live per-period loop + evidence retrofit on Sim/Customers/Supplier tabs) NOT started -- QL is not closed. Epistemic: PASS.
**Phase QK COMPLETE (2026-07-04, Tier 2 -- WEEKEND_ACCELERATION.md Q4, "churn model validation loop... prove it"):** Enriched passive-renewal churn estimate -- 10 tests (15,377 total). company/crm/enriched_churn_estimate.py: enriched_passive_churn_estimate() applies the payment-behaviour/satisfaction signal to passive (SVT-roll) renewals for the first time, same max()-combination rule as active/I&C; simulation/run_phase2b.py: moved shock-count/behaviour/satisfaction tracking out of the active-only branch so passive renewals get it too. VERIFIED LIVE, full re-run: did NOT close the QJ recall=0%/precision=0% finding -- reclassified, not fixed. 3 passive renewers now correctly cross the old 0.10 cap under genuine stress and get retention offers (C1/C5/C6, all retained), but all three later churn anyway once the signal decays back down before their next renewal -- the classifier only scores the terminal event, so a correctly-caught-then-decayed risk still reads as a false negative. Root cause is signal-decay-timing, not a missing signal. Next candidates: slow the decay rate, or credit prevented-churn interventions in the accuracy metric. Epistemic: PASS.
**Phase QJ COMPLETE (2026-07-04, Tier 2 -- EVIDENCE_IN_BUSINESS_SURFACES.md retrofit queue, item 3 of 3, CLOSES the directive):** Churn-model evidence retrofit -- 7 tests (15,367 total). tools/generate_dashboard_data.py: events/retention exports gain market_signal (Phase QB switching multiplier) and realized_churn_p (SIM outcome) -- run_phase2b already computed these, never exported; retention cross-references customer_events by (customer_id, date) since retention_log only ever recorded the company side. tools/generate_shadow_html.py: _churn_model_signal() (Sim tab, real per-year TP/FP/FN/recall/precision -- honestly 0.0% everywhere, the documented PRIORITIES.md structural finding, now on-surface not just in a markdown file); _renewal_decision_case_study() (Supplier tab, picks the widest company-vs-SIM divergence automatically -- landed on C_IC1 2018-01-31, 95% estimate vs 5% SIM truth, the 0.95-ceiling case QB flagged). All 3 named retrofit items now done; docs/staging/EVIDENCE_IN_BUSINESS_SURFACES.md -> done/. Epistemic: PASS.
**Phase QI COMPLETE (2026-07-04, Tier 2 -- EVIDENCE_IN_BUSINESS_SURFACES.md retrofit queue, first item):** QD bad debt/arrears evidence retrofit -- 11 tests (15,360 total). tools/generate_shadow_html.py: _behavioral_signal_correlation() (Sim tab: income-stress population mix vs arrears-opened + bad debt by year, real cross-customer aggregation); _behavioral_case_study() (Customers tab: C7 real life event new_baby 2023-12-23 + income-stress trajectory (SIM-only) next to payment-behaviour score + 6 real arrears cases (company-observable), explicit divergence text); _collections_process() (Supplier tab: real dunning cascade totals, 20 written-off cases = GBP3,051, exactly the Phase QD figure). Reordered process_run_complete.py: billing_ledger.json now generates before shadow HTML (was after -- would have shipped one cycle stale). Found + fixed own bug during build: divergence text cited the trajectory array LAST year instead of the actual transition year -- caught by test before shipping. Epistemic: PASS.
**Phase QH COMPLETE (2026-07-04, Tier 1 fix -- QG_REOPENED_R2.md):** Dead-code regression fix, no new tests (15,349 total unchanged). background/process_run_complete.py::generate_dashboard_json() had an early `return ok` (QF's consistency-gate result) sitting before the calls to generate shadow HTML, PROJECT_STATE.txt, billing ledger, population anchoring, customers.json, supplier.json, live decisions/portfolio, scenario analysis, and the QG GitHub Pages mirror -- all dead code on every auto-processed run since QF (e3c7ea51). Root cause was a code bug, NOT an R2 daemon-restart issue as first suspected: both callers (sim_runner.py, background_worker.py) already run process_run_complete.py via subprocess.run, so no stale-import mechanism existed. Fixed by deferring the return to the end of the function. Verified live: PROJECT_STATE.txt now regenerates with the correct phase/test count on every cycle (was frozen since 8339c754); full 15,349-test suite + epistemic verifier pass. Live-fetch confirmation from the advisor's vantage point NOT done this session (no approved network tool) -- stated honestly, not claimed. Also folded EVIDENCE_IN_BUSINESS_SURFACES.md into the phase-close checklist (0b above); retrofit of QD/churn/income-stress evidence surfaces queued as next phase.
**Phase QG COMPLETE (2026-07-04, Tier 2 -- WEEKEND_ACCELERATION.md Q3):** GitHub Pages Advisor-Verification Mirror -- 7 tests (15,349 total). tools/mirror_github_pages.py mirrors site/shadow/ -> docs/shadow/ and 4 state JSONs (customer_sample/billing_ledger/population_anchoring/sim_data) -> docs/state/ every run (pure copy, same generator pass); wired into process_run_complete.py. docs/status/PROJECT_STATE.txt Key Files now lists github.io URLs as the advisor-verification channel (poesys.net proven persistently stale on the advisor's fetch path specifically, independent of any CD incident). Also fixed generate_project_state.py::_parse_phase_and_tests() picking the highest-test-count phase instead of the most recent one (fast-suite totals aren't monotonic across phases) -- was silently showing stale phase PZ instead of QF. Epistemic: PASS.
**Phase QF COMPLETE (2026-07-04, Tier 2 -- PRIORITIES.md P1/P2, Website Integrity Part C):** Permanent Consistency-Gate Wiring -- 13 tests (15,342 total). Widened the Part A net-margin-only gate to 8 headline metrics (gross margin, enterprise value, bills, committee interventions, retention, churn); generate() now propagates the gate result (was discarded) and a mismatch NTFYs Rich immediately (was silent stderr-only). Freshness stamps (git commit + phase) added to every shadow page footer. KEY FINDING: widened gate immediately caught a real gross-margin discrepancy (£6,452,603 vs £6,467,309) in tools/generate_insights.py -- same precedence bug as the QC net-margin fix, never applied to gross; fixed at root. PRIORITIES.md refreshed: P1 (shadow-live hardening) DONE via QE; Website Integrity Part B (design system + customer portal per-fuel legs) is now the sole P1. Epistemic: PASS.
**Phase QE COMPLETE (2026-07-04, Tier 2 -- PRIORITIES.md P1):** Shadow-Live Decision Log Persistence -- 7 tests (15,329 total). tools/run_live_decisions.py: append_decision_log() writes site/state/live_decisions_log.jsonl, one immutable entry per UTC calendar day (keyed on decision_run_at, not the frozen market as_of_date). Fixed gap: run_decisions() was silently overwriting the same dated snapshot file every sim_runner cycle since Frozen2025Adapter's as_of_date never changes -- no track record was ever accumulating. Verified live (two same-day production runs: 1st entry locked in INCREASE hedge rec + C9 renewal flag; 2nd call correctly did not duplicate). Epistemic: PASS.
**Phase QD COMPLETE (2026-07-04, Tier 2 -- PRIORITIES.md P2):** Emergent Bad Debt -- 10 tests (15,393 total). simulation/arrears_engine.py (new): shared payment-outcome/arrears engine moved out of tools/generate_billing_ledger.py (39 existing tests unchanged); compute_emergent_bad_debt()/apply_emergent_bad_debt() replace the flat get_bad_debt_rate() formula baked into run_phase2b records with real written-off arrears, wired into run_phase4c_on_phase2b.py. Deleted dead company/billing/arrears_book.py. KEY FINDING (full run): flat-rate bad debt was £92,550.88; real emergent figure is £3,051.07 -- net margin will rise ~£89.5k on the next production run. Epistemic: PASS.
**Phase QC COMPLETE (2026-07-04):** Website Data Integrity (Part A of staged WEBSITE_INTEGRITY_AND_DESIGN.md) -- 10 tests (15,341 total). Fixed step-ordering bug in process_run_complete.py (dashboard/site generated before run_insights.json was refreshed, baking the PREVIOUS run's exec summary next to the CURRENT run's totals -- the 'Net margin -£8,317 next to £1,445,258 on the same page' contradiction). generate_dashboard_data.py: hardcoded phase/test-count/module constants replaced by docs/observability/build_info.json (_load_build_info(), update at phase close); new _check_consistency() fails loudly (stderr) if totals vs insights net_margin diverge again. Verified live on regenerated shadow site. Parts B (design system) and C (permanent per-run consistency-gate wiring beyond this) remain open -- next candidate phase. Epistemic: PASS.
**Phase QB COMPLETE (2026-07-04):** Observable Market-Conditions Signal in Company Churn Estimate -- 12 tests (15,319 total). company/crm/market_conditions.py (new): market_conditions_multiplier(year) from public DESNZ/Ofgem switching data. Wired into estimate_passive_churn_probability + enriched_churn_estimate (renewal_year param, backward-compatible default None). Closes the P1 gap Phase QA exposed: passive estimates were capped 5-10% with no yearly variation, while SIM realized churn (post market-multiplier) legitimately ranges 3-22%. Phase QA (same day): fixed churn_estimate_error_pct comparing against the raw pre-adjustment churn_probability instead of realized_churn_probability (post-adjustment, pre-retention-offer) -- the source of the reported -80% systematic underestimate. Mean error 1.25->1.00; residual overestimate now concentrated in I&C hitting the 0.95 ceiling (next P1 item). Epistemic: PASS.
**Phase PZ COMPLETE (2026-07-04):** Scenario Stress Testing via Synthetic Market -- 22 tests (15,424 total). tools/run_live_decisions.py: run_scenario_analysis() with 4 persistent scenarios (base/bull/bear/crisis); _scenario_market_state uses CorrelatedGeneratorAdapter starting prices (not OU projection); _portfolio_exposure_delta=unhedged commodity cost delta vs base. tools/market_adapters/synthetic_generator.py: gas_start/elec_start params. saas/reporting/annual_report.py: _section_scenario_sensitivity board section (4-scenario table; exposure delta; regime-change callout). process_run_complete.py: run_scenario_analysis() wired. KEY FINDING: Crisis scenario: +£1.56M unhedged annual exposure vs base; bull: -£398K. CORRECTION (2026-07-04, advisor steer): PZ does NOT close regime-change blindness -- the 85% hedge floor closed it historically; PZ QUANTIFIES residual exposure for the board, which is visibility, not closure. Epistemic: PASS.
**Phase PY COMPLETE (2026-07-04):** Synthetic Generator Statistical Equivalence Gate -- 22 tests (15,402 total). tools/synthetic_validation.py: EquivalenceCheck/EquivalenceGate dataclasses; run_gate(seed, n_steps=5000)->EquivalenceGate; write_gate_json(). 10 checks: long-run mean (gas +/-20% of 54; elec +/-20% of 85), return vol (gas [0.30,0.80]; elec [0.35,0.95]), elec-gas correlation [0.55,0.85], crisis freq [0.04,0.14], lag-1 ACF < 0 (mean reversion), excess kurtosis > 0 (fat tails). CALIBRATION BUG FOUND AND FIXED: arithmetic OU had vol=0.009 annualised (was sigma*sqrt(dt)); fixed to proportional vol (sigma*X(t)*sqrt(dt)) giving 0.60/0.76 annualised. Gate PASS. Artifact: docs/market_research/findings/synthetic_equivalence_gate.json. Backlog gate UNLOCKED for endgame. Epistemic: PASS.
**Phase PX COMPLETE (2026-07-04):** Correlated Synthetic Market Generator -- 21 tests (15,380 total). tools/market_adapters/synthetic_generator.py: CorrelatedGeneratorAdapter; bivariate OU process (GAS kappa=0.5/mu=54; ELEC kappa=0.6/mu=85); corr=0.70; normal vol (gas 35%, elec 45%) / crisis vol (gas 120%, elec 150%); CRISIS_REGIME_PROB=8%; contango forward (2%/yr). Factory wired: MARKET_ADAPTER_SOURCE=synthetic. Reproducible (seed param); regime="crisis" forces high-vol permanently. Addresses regime-change blindness failure mode (CLAUDE.md known failure). Epistemic: PASS.
**Phase PW COMPLETE (2026-07-04):** I&C Corporate Arrears Calibration -- 24 tests (15,359 total). generate_billing_ledger.py: I&C BACS/CHAPS invoice dispute model (92% on-time, 7.3% late, 0.7% dispute); _ic_arrears_stages (INVOICE_DISPUTED->DISPUTE_NOTICE->PAYMENT_PLAN_AGREED|WRITTEN_OFF); segment stored in ledger. population_anchor.py: _arrears_check_by_year separates I&C from resi; ic_aggregate_rate_pct (10-yr) for RAG vs DESNZ <8% benchmark. KEY FINDING: IC arrears 0%->5.4% aggregate (GREEN); prior RED finding from PS was resi income-stress arrears vs I&C benchmark (category mismatch). Epistemic: PASS.
**Phase PV COMPLETE (2026-07-04):** Market Feed Swappable Adapter -- 21 tests (15,335 total). tools/market_data_port.py: MarketDataPort Protocol (get_spot_elec/get_spot_gas/get_forward_price/get_market_summary; optional as_of date). tools/market_adapters/frozen_2025.py: Frozen2025Adapter wraps live_market.py unchanged. tools/market_adapters/__init__.py: get_market_adapter(source) factory; reads MARKET_ADAPTER_SOURCE env var (default: frozen_2025); unknown source -> ValueError. tools/run_live_decisions.py: market_adapter injection param; defaults to factory. ARCHITECTURE: correlated synthetic generator drops in as CorrelatedGeneratorAdapter with zero company-layer rework. Epistemic: PASS.
**Phase PU COMPLETE (2026-07-03):** Shadow Live Operation (P4 MVP) -- 24 tests (15,314 total). tools/project_portfolio_to_2026.py: generate(run_output, out)->live_portfolio.json; extracts active customers from final sim year, last renewal dates/rates from customer_events, EAC from bills, hedge fractions. tools/live_market.py: fetch_spot_elec/fetch_spot_gas/build_live_forward_price/get_market_summary; uses Elexon SSP cache (2025-06-07 as-of) with price_feed.json fallback; gas forward = gas_spot x 1.05. tools/run_live_decisions.py: run_decisions(); renewal window 60 days; hedge INCREASE/HOLD/REDUCE vs 50-90% band; acquisition price ladder by segment; writes live_decisions_YYYYMMDD.json + live_decisions_latest.json. Wired into process_run_complete.py. KEY FINDING: C9 in renewal window (22 days), proposed 153.49 GBP/MWh vs current 210.0 (market fallen 2022->2025); hedge rec INCREASE (most pass-through I&C at 0% hf). SIM is no longer retrodiction-only. Epistemic: market data from Elexon SSP (public). PASS.
**Phase NZ COMPLETE (2026-07-03):** Ofgem FRA Regulatory Capital Ratio -- 24 tests (14,908 total). saas/reporting/fra_capital_ratio.py: FRACapitalRatio(year/equity_gbp/monthly_revenue_gbp/fra_ratio/rag/is_compliant); build_fra_ratio_series(management_accounts)->list; weakest_year/strongest_year; RAG GREEN>=6x / AMBER 3-6x / RED<1x. annual_report.py: _section_fra_capital_ratio (10-year table + weakest/strongest highlight). Live data: SIM supplier 16-32x (all GREEN); weakest 2022 at 16.8x (crisis year revenue spike vs equity growth). Context: Bulb 2021 at ~-0.01x, Igloo ~0.07x -- shows SIM is well-capitalised. Epistemic: management accounts are company-observable. PASS.
**Phase PQ COMPLETE (2026-07-03):** Population Anchoring Validation Gate -- 18 tests (15,189 total). tools/population_anchor.py: generate(run_json, out); _churn_by_year(events)->dict by year with sim_churn_rate/ofgem_benchmark/calibrated_multiplier; _bad_debt_check(years)->RAG (GREEN<0.5%, AMBER<2.5%, RED>2.5%, crisis ceiling 4%); _crisis_churn_direction->crisis_divergence_flag if 2022 normalised propensity >2x 2020; _multiplier_alignment->AMBER if Ofgem going down but SIM going up. Wired into process_run_complete.py; output to site/state/population_anchoring.json. OFGEM_SWITCHING_RATE 2016-2025 calibrated (2022: 4%). KEY FINDING (live run): RED -- 2022 normalised churn propensity 2.4x 2020, indicating crisis suppression model may be insufficient for I&C portfolio. Epistemic: company-observable metrics only. PASS.
**Phase PT COMPLETE (2026-07-03):** customers.json + supplier.json at stable fetchable paths -- 14 tests (15,290 total). tools/generate_customers_json.py: 16 customer groups with per-fuel legs (elec/gas split) and combined roll-up; dual-fuel pairs C4+C4g, C_IC3+C_IC3g explicit. tools/generate_supplier_json.py: portfolio_summary, 10-year years[], fra_ratio_series. Both wired into process_run_complete.py and git_commit_push. CLAUDE.md: verified-by-fetch rule encoded. Staging: DEPLOY_PATH_DIAGNOSE + STATE_ROOTCAUSE_COMMIT_STEP + STATE_SYNC_VERIFY_BY_FETCH actioned. Root cause confirmed: pre-NQ deploy-pages.yml overwrote state file; now fixed. Live URL: poesys.net/data/customers.json and /data/supplier.json.
**Phase PS COMPLETE (2026-07-03):** Complaints & Arrears Population Anchoring -- 22 tests (15,276 total). tools/population_anchor.py: _complaints_check(years_data)->list (avg_complaint_probability x 100 vs Ofgem QoS; GREEN 2-6% normal/2-8% crisis); _arrears_check_by_year(billing_ledger,years_data)->list (unique customers with new arrears per year / active customers vs DESNZ I&C: GREEN <8%/<12% crisis). generate() extended with billing_ledger_path; adds complaints_vs_benchmark + arrears_vs_benchmark to population_anchoring.json. annual_report.py: _section_population_anchoring per-year complaints + arrears table with RAG; fixed _section_licence_health complaints_per_100=0.0 -> actual rate. KEY FINDING: complaints GREEN all years except 2025 AMBER (6.1%); arrears RED most years (29-31%), only 2020 GREEN -- reveals arrears model calibrated to residential income-stress rates, not pure I&C. P3 Population Anchoring fully closed. Epistemic: PASS.
**Phase PR COMPLETE (2026-07-03):** Population Anchoring Robustness -- 23 tests (15,194 total). tools/population_anchor.py: _long_run_comparison(churn_by_year)->SIM 10yr avg vs Ofgem avg (ratio/rag); _crisis_churn_direction now uses 3-year rolling pre-crisis vs crisis window (not single-year comparison); crisis_divergence_flag requires BOTH rolling AND absolute divergence with N>=10; overall_rag RED only when both long_run RED AND crisis flag. KEY FINDING: SIM long-run avg 6.4% vs Ofgem 13.6% (ratio 0.47, GREEN) -- SIM is BELOW market average over full period; RED from PQ was false alarm driven by single-year small-N noise. 2022 single-year still 5.6x Ofgem but rolling criterion not met. overall_rag now AMBER (bad debt AMBER 0.5-1.6% all years; multiplier alignment AMBER 2 transitions). Epistemic: PASS.
**Phase PP COMPLETE (2026-07-03):** Per-Customer Invoice & Payment Ledger -- 23 tests (15,171 total). tools/generate_billing_ledger.py: generate(run_json, out); invoices (period/amount/due_date/status); payment events (method/outcome/income_stress_at_time); arrears cases (DD_FAILED->FIRST_NOTICE->SECOND_NOTICE->RESOLVED|WRITTEN_OFF). Payment method: I&C->BACS/CHAPS; Resi->DD. Payment outcome from income_stress_trajectory (LOW: 92% on-time; MODERATE: 50%; HIGH: 10%). Churned customers arrears written off; resolved cases cleared. Wired into process_run_complete.py; output to site/state/billing_ledger.json. bills + per_customer_behavioral added to extract_report_data. Epistemic: PASS.
**Phase OL COMPLETE (2026-07-03):** Carbon Emissions -- FuelMixRecord 2016-2025; scope 2 elec + scope 1 gas; 290->175g/kWh trend. 17 tests, 15,148 total.
**Phase OK COMPLETE (2026-07-03):** ECO Observatory -- phase rates; EXEMPT (domestic << 150k); counterfactual GBP3.20-6.80/MWh. 19 tests, 15,131 total.
**Phase OJ COMPLETE (2026-07-03):** WHD Observatory -- threshold check; I&C portfolio EXEMPT (domestic count << 150k); NIL liability; growth warning. 19 tests, 15,112 total.
**Phase OI COMPLETE (2026-07-03):** CCL Observatory -- CCLLedger wired; I&C elec+gas CCL by year; 2019 spike (+45%/+67%) flagged; pass-through note; board section. 19 tests, 15,093 total.
**Phase OH COMPLETE (2026-07-03):** FiT Levelisation Levy -- FITBook.levelisation_charge_gbp wired; per-year rate GBP 8.36-9.45/MWh (2016-2019); NIL from 2020 (scheme closed); board section. 21 tests, 15,074 total.
**Phase OG COMPLETE (2026-07-03):** Renewable Obligation Cost Observatory -- ROCLedger wired; per-year MWh from all_records x obligation_level = ROCs required; buy-out price ceiling; board section with RO cost as % revenue. 25 tests, 15,053 total.
**Phase OF COMPLETE (2026-07-03):** GSOP Obligations -- GSOPBook wired; missed_appointment (1% cust/yr) + final_bill_delay (5% churned); GBP 30/trigger; board section. 14 tests, 15,028 total.
**Phase OE COMPLETE (2026-07-03):** Ofgem Annual Supply Return -- OfgemReturnBook populated per year; customer segments / GWh / bad debt / filed status; board section; all 10 returns filed. 16 tests, 15,014 total.
**Phase OD COMPLETE (2026-07-03):** Compliance Scorecard Synthesis -- ComplianceScorecard populated (10 SLC domains per year); board section with per-year RAG table. 16 tests, 14,998 total.
**Phase OC COMPLETE (2026-07-03):** Licence Health Observatory -- _section_licence_health per-year; customer count / net assets / bad debt / cash runway checks; small-portfolio years WATCH; crisis bad debt WATCH. 16 tests, 14,982 total.
**Phase OB COMPLETE (2026-07-03):** Elexon Settlement Reconciliation Cash Flow -- settlement_reconciliation.py; ReconciliationExposure; R1/R2/R3/RF timeline; 90% HH -> GREEN RAG; crisis years net credit flagged. 25 tests, 14,966 total.
**Phase OA COMPLETE (2026-07-03):** I&C Broker/TPI Commission Model -- tpi_book.py wired; TPIBook registers Standard Energy Broker at £1.5/MWh (0.15 p/kWh); actual consumption per settled record; tpi_summary in results; board section. 21 tests, 14,941 total.
**NQ Advisor Redirect (2026-07-03):** enriched_churn_estimate 5% floor DROPPED (uniform shift, no discrimination); PRIORITIES.md refreshed (P1: observability, P2: billing infra, P3: population anchoring, P4: shadow ops); PROJECT_STATE.txt sync fixed (deploy-pages.yml overwrite step removed; process_run_complete.py now commits it). 13 tests in NQ file; 14,823 total.

→ Phases 1–HK, HO, HS–HT, IA–IE, IF–IM, FT–HZ, IJ, II, IV, IW, IN–IU, IX–IZ, JA–JE, JH–JM, JN–MU, MZ–NS, NT–NX: `docs/claude/phase-history.md` | Earlier: `CLAUDE_HISTORY.md`
---
## Architectural Laws — Epistemic Honesty: The Company Cannot See Inside the SIM
The company layer operates under the same information constraints as a real energy supplier.
It cannot see simulation internals — churn parameters, forward curve construction, weather
engine outputs, VaR internals. It discovers the world through observable interfaces: market data feeds, meter reads, customer interactions, its own bills and payments, regulatory publications.
The company's models are approximations built from observed outcomes — not reads from ground
truth. That imperfection is the point.
**Before writing any company-layer code:** ask "Could a real UK energy supplier know this?"
If the answer requires reading simulation internals, it is a violation.
The SIM/company seam (`company/interfaces/sim_interface.py`) exposes observables only, never internals.
---
## Sequencing principles
**Two-way-door filter:** don't build something that depends on an unresolved upstream question.
**Build efficiency:** tests passing + capabilities added per frontier session (hard metric).
Fidelity delta — one sentence per phase on what the sim can now do (soft metric, Rich assesses).
CLV is not a stable measuring stick — it evolves with business rules.
**Reversibility** governs data architecture and agent governance. Prefer designs that can be unwound.
**Regime-change blindness** is a known failure mode. The sim converged to near-naked hedging during
calm 2016–2020 data, directly before the crisis — mirroring what killed real suppliers. All
hedging/risk models must account for this.
**Activity-based pricing:** flat margin makes some customers net-negative. Any pricing model must
account for cost-to-serve at the customer level.
---
## Key learnings — do not repeat these mistakes
- **Local models confabulate endpoints.** Pre-load ground-truth API context before any local model touches external sources.
- **LATEST.md must be committed before NTFY**, not after. If stale, fix root cause.
- **REVIEW_GATE must only match on actual pane idleness** — not on prose mentioning the string "REVIEW_GATE".
- **Staging-watcher notifies Rich, not the agent.** Poll `docs/staging/` yourself.
- **The simulation is not the company.** Company makes decisions based on what it's allowed to see.
- **Non-blocking concurrency.** If blocked on a long run, move to the next staging item and return.
- **Session usage window is ~5 hours**, not 4. Don't under-estimate available budget.
- **CLAUDE.md hard limit: 35k chars / 200 lines.** Stop and trim before anything else if exceeded.
- **Committee cooldown must be date-based**, not record-count. With 18+ customers, 1440 records ≠ 30 days.
- **sim_runner TimeoutExpired must be caught.** Uncaught exception kills the `while True` loop.
- **"Done" = named artifact.** Every completion (NTFY/report) must cite evidence: PROJECT_OVERVIEW.md entry, passing test count, or fetchable file. No artifact -> say "in progress".
- **Observability artifacts verified by fetch.** PROJECT_STATE.txt, customer_sample.json, customers.json, supplier.json, shadow HTML: DONE only when fetched from live URL and confirmed current. "Committed" ≠ "done". NTFY must quote the fetched timestamp/phase.
---
## Technical environment
**Hardware (Skynet):** Intel i5-13400F, 32GB DDR4, RTX 3060 12GB VRAM. Windows 11 Pro + WSL2/Ubuntu.
**Network/AI:** Tailscale WSL2 `100.69.81.59` | File API `https://skynet-1.taila062fa.ts.net:8765` | Claude Code → qwen3:14b/Ollama → risk committee (Ollama)
**Key paths:** `docs/staging/` (instructions) | `docs/status/LATEST.md` | `docs/reports/ANNUAL_REPORT.md`
**Data:** Elexon `data.elexon.co.uk` (key-free; API migrated to Insights Solution — legacy wrappers partly stale) | NESO CKAN | Open-Meteo | synthetic forward curves
