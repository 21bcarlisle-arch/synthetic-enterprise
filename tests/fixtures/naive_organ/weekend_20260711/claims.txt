STATUS (weekend 2026-07-11): every daemon reads healthy/nominal.
The map is exhausted -- no drawable atoms, nothing to draw.
BUILD is inherently narrow and must be one tree at a time; it cannot be parallelised.
From first principles there is no way to run Claude Code subagents in parallel worktrees.
Net position: net margin held flat at £1,505,286 across the weekend runs.
