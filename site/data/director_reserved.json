{
  "generated_at": "2026-08-26T06:19:56.922789+00:00",
  "open_count": 1,
  "items": [
    {
      "item_id": "publish_gate_wedged",
      "what": "The run-complete PUBLISH GATE has failed 3 time(s) in a row within the last 60 min -- the site/report pipeline is WEDGED and run_complete markers are piling up unpublished. Latest cause: the publish COMMIT did not land -- the publisher's OWN scoped suite was GREEN, so this is NOT a publish-path regression: the pre-commit hook chain refused the commit (most often another lane's red on the shared tree), outran the hook deadline, or the push never reached origin. Read the 'git/hook output' tail in the log -- it names the refusing gate (rc=77, git=feaa30fed). EPISODE: wedged since 2026-08-26T00:21 UTC -- 5h58m and 8 consecutive failures in THIS episode (not a fresh hour). Markers pending: 0.",
      "how": "BLOCKING TEST (gate subject feaa30fed): FAILED tests/background/test_derived_artefact_register.py::TestStaleness::test_staleness_fires_for_EVERY_registered_artefact. Run exactly that node id against a clean checkout of HEAD. DEPTH UNKNOWN: the gate runs fail-fast and the report-only census did not run (no budget, timed out, or unavailable -- see the log), so this may be one red of many. Do NOT read it as the only one. rc=-9 is almost certainly OOM (free memory or cut test parallelism), NOT a code bug; rc>0 means run that test at HEAD to find the regression. Full output: docs/observability/sim-runner-log.md, 'Publish gate RED output tail'. The alarm clears automatically on the next clean publish. SUSPECTS (re-derived from the blocking test above -- NOT what was filed most recently): first-party modules that test imports: background/derived_artefact_register.py, background/process_run_complete.py. Commits touching those paths in the last 7 days: a4d8a7e32 publishing: twelve consecutive commits killed at a deadline set against a 2026-08-03 measurement, while the control that guards it graded against a constant and stayed green through a 12.2-hour outage; 133d9992c company: the first per-customer decision, its control, and the measurement that says it is not fit to run yet; 8f15b78c1 carbon: the number this company exists to produce now exists -- and on its hardest winter day one household emitted 108% more than the annual method reports; 7309e5dde Explore's stage 3 draws the day it has promised since it was written, and the sentence beside it stops being contradicted by its own chart; f6c465e44 The growth curve reaches the site with the reason it has that shape: six of ten years were stopped by our own engine, not by the market; 9295876ed The Capabilities door has published a two-day-old \"last run\" inside a feed rewritten every 25 minutes, because retiring the evidence PAGE unwired the evidence PAYLOAD. Filed findings whose text NAMES one of those paths (draw these first): WORKER_FINDING_THE_PUBLISH_COMMITS_HOOK_BUDGET_IS_SPENT_BY_TWO_TEST_FILES_2026-08-25.md, WORKER_FINDING_REPEATING_ALARM_BACKGROUND_COMPANY_DOCS_TESTS_AND_ELSEWHERE_LEFT_UNCOMMITTED_BY_A_2026-08-25.md, WORKER_FINDING_THE_SPLIT_INVARIANT_WEDGES_THE_TREE_ON_THE_NEXT_SUCCESSFUL_BUILD_2026-08-26.md. SUSPECT HIT RATE: not yet measured (20 closed episode(s) emitted no suspect list because the blocking test was unrecorded).",
      "why": "A silently-wedged publish gate stops the live site and report updating with NO other signal -- this is the exact ~45-min silent stall of 2026-07-14 (H15).",
      "first_asked_at": "2026-07-18T15:21:58.968619+00:00"
    }
  ]
}