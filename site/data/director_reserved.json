{
  "generated_at": "2026-08-26T10:27:02.242437+00:00",
  "open_count": 1,
  "items": [
    {
      "item_id": "publish_gate_wedged",
      "what": "The run-complete PUBLISH GATE has failed 3 time(s) in a row within the last 60 min -- the site/report pipeline is WEDGED and run_complete markers are piling up unpublished. Latest cause: the publish COMMIT did not land -- the publisher's OWN scoped suite was GREEN, so this is NOT a publish-path regression: the pre-commit hook chain refused the commit (most often another lane's red on the shared tree), outran the hook deadline, or the push never reached origin. Read the 'git/hook output' tail in the log -- it names the refusing gate (rc=77, git=18e8d215e). EPISODE: wedged since 2026-08-26T09:29 UTC -- 0h57m and 3 consecutive failures in THIS episode (not a fresh hour). Markers pending: 0.",
      "how": "BLOCKING TEST (gate subject 18e8d215e): FAILED site/test_harness_delivery_record.py::test_an_empty_WHAT_IT_GOT_WRONG_says_WHICH_kind_of_empty_it_is. Run exactly that node id against a clean checkout of HEAD. DEPTH UNKNOWN: the gate runs fail-fast and the report-only census did not run (no budget, timed out, or unavailable -- see the log), so this may be one red of many. Do NOT read it as the only one. rc=-9 is almost certainly OOM (free memory or cut test parallelism), NOT a code bug; rc>0 means run that test at HEAD to find the regression. Full output: docs/observability/sim-runner-log.md, 'Publish gate RED output tail'. The alarm clears automatically on the next clean publish. SUSPECTS (re-derived from the blocking test above -- NOT what was filed most recently): first-party modules that test imports: none resolvable. Commits touching those paths in the last 7 days: 211c97c8a the control that refused the FIRST publish after the wedge cleared was matching a WORD where it meant a STATE; 81387bad2 the delivery seat: the job that decides WHAT MATTERS NEXT was being done by hand in a chat window -- it is now a three-hourly session that writes direction, never code, and a page that answers the four questions. SUSPECT HIT RATE: 1/4 closed episodes where the repair touched a path this alarm had named (16 more emitted no list at all). Diagnostic only -- never a target (R12); if it stays at 0 the re-derivation is as useless as the recency list it replaced and should be said so.",
      "why": "A silently-wedged publish gate stops the live site and report updating with NO other signal -- this is the exact ~45-min silent stall of 2026-07-14 (H15).",
      "first_asked_at": "2026-07-18T15:21:58.968619+00:00"
    }
  ]
}