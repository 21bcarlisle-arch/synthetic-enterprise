{
  "generated_at": "2026-08-22T01:47:36.824433+00:00",
  "open_count": 1,
  "items": [
    {
      "item_id": "publish_gate_wedged",
      "what": "The run-complete PUBLISH GATE has failed 3 time(s) in a row within the last 60 min -- the site/report pipeline is WEDGED and run_complete markers are piling up unpublished. Latest cause: killed by the CALLER's deadline before the gate returned a verdict -- NOT a test failure, and the tests it was running are unjudged (rc=None, git=260aad3e2). EPISODE: wedged since 2026-08-22T00:55 UTC -- 0h51m and 3 consecutive failures in THIS episode (not a fresh hour). Markers pending: 0.",
      "how": "BLOCKING TEST: UNRECORDED -- the gate's failing node id was not captured (no red gate has run since this record was last cleared, or the record went stale). Read docs/observability/sim-runner-log.md for the last 'Publish gate RED' line; do NOT infer a cause from the backlog list below. rc=-9 is almost certainly OOM (free memory or cut test parallelism), NOT a code bug; rc>0 means run that test at HEAD to find the regression. Full output: docs/observability/sim-runner-log.md, 'Publish gate RED output tail'. The alarm clears automatically on the next clean publish. SUSPECT HIT RATE: 0/6 closed episodes where the repair touched a path this alarm had named (14 more emitted no list at all). Diagnostic only -- never a target (R12); if it stays at 0 the re-derivation is as useless as the recency list it replaced and should be said so.",
      "why": "A silently-wedged publish gate stops the live site and report updating with NO other signal -- this is the exact ~45-min silent stall of 2026-07-14 (H15).",
      "first_asked_at": "2026-07-18T15:21:58.968619+00:00"
    }
  ]
}